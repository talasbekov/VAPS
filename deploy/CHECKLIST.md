# CHECKLIST переноса релиза VAPS носителем (Story 12.5)

> **Бумажный документ.** Распечатать → заполнять ручкой ПО ХОДУ → подшить.
> ID пунктов (A1…C6) синхронизированы со скриптом `scripts/deploy-rehearsal.sh` —
> **правки только синхронно** с ним.
> Все скрипты запускаются через `bash script.sh …` — носитель FAT/exFAT теряет exec-биты.

## Шапка рейса

| Поле | Значение (ручкой) |
|---|---|
| Дата рейса | __________________ |
| Релиз-тег (`<дата>-<sha>`) | __________________ |
| Бандл собрал (ФИО, подпись) | __________________ |
| Носитель физически понёс (ФИО, подпись) | __________________ |
| Носитель (тип / инв. номер) | __________________ |
| Окно работ (с … до …) | __________________ |

---

## A. Первичная подготовка хоста (однократно)

☐ **Уже сделано в прошлый рейс** — секция A пропускается, дата первичной подготовки: ____________

- [ ] **A1.** Docker установлен на сервере контура. Версия (`docker --version`): ____________
- [ ] **A2.** БАЗОВЫЕ образы загружены с носителя: `docker load -i postgres-16.tar` и
      `docker load -i redis-7-alpine.tar`. ⚠️ Базовые образы **НЕ входят в бандл 12.2**
      (`bundle.sh` пакует только vaps-*) — на первичный рейс их кладут на носитель отдельно
      (`docker save postgres:16 -o postgres-16.tar` на DEV-стороне).
- [ ] **A3.** Install-каталог создан (например `/opt/vaps`), в нём `docker-compose.yml`,
      `install.sh`, `smoke.sh`, `backup-nightly.sh`, `restore-rehearsal.sh`.
- [ ] **A4.** `.env` доставлен **ОТДЕЛЬНО от бандла**, заполнен по `.env.example`.
      Секреты на этот лист **не выписывать**.
- [ ] **A5.** systemd-юниты бэкапа И beat установлены: `vaps-backup.{service,timer}` +
      `vaps-beat.{service,timer}` (cp в /etc/systemd/system, поправить INSTALL_DIR в обоих
      .service, `systemctl enable --now vaps-backup.timer vaps-beat.timer`).

---

## B. Сборка бандла (DEV-сторона)

- [ ] **B1.** `bash bundle.sh` отработал; тег релиза записан в шапку рейса.
- [ ] **B2.** **PINNED-SHA НА БУМАГУ.** sha256 архива `vaps-<тег>.tar` (из `sha256sums-<тег>.txt`)
      переписан сюда рукой и сверен посимвольно (это доверенный канал для 2-го аргумента
      `install.sh` — со-расположенный sums-файл подмену пары не ловит):

      sha256: ________________________________________________________________________

      Переписал/сверил (подпись): ____________
- [ ] **B3.** Состав носителя (чек по факту записи):
      ☐ `vaps-<тег>.tar` ☐ `manifest-<тег>.json` ☐ `sha256sums-<тег>.txt`
      ☐ `docker-compose.yml` ☐ `install.sh` ☐ `smoke.sh`
      ☐ `backup-nightly.sh` ☐ `restore-rehearsal.sh` ☐ systemd-юниты ☐ этот CHECKLIST
      ☐ (первичный рейс) `postgres-16.tar`, `redis-7-alpine.tar`
- [ ] **B4.** Носитель передан несущему; кто понёс — записан в шапке рейса.

---

## C. Перенос и установка (контур-сторона)

- [ ] **C1.** Версия «БЫЛО»: `cat installed-tag` → ____________ (пусто = первичная установка).
- [ ] **C2.** Целостность: `sha256sum -c sha256sums-<тег>.txt` → «ЦЕЛ» по обоим файлам.
      При провале — **СТОП до любых изменений**, рейс повторяется.
- [ ] **C3.** Установка: `bash install.sh <тег> <sha-с-бумаги-из-B2>` — до зелёного финала.
- [ ] **C4.** Смок: `bash smoke.sh` зелёный (install.sh гоняет его сам; при сомнении — повторить руками).
- [ ] **C5.** Версия «СТАЛО»: `cat installed-tag` → ____________ (совпадает с тегом рейса).
- [ ] **C6.** Утренний ритуал после первой ночи: `ls backups/ALERT-*` → пусто
      (маркер есть = ночной бэкап/репетиция провалены — разбор до закрытия рейса).
      Секция D ниже заполнена по ходу рейса.

Установку провёл (ФИО, подпись, дата): ________________________________________

---

## D. Сюрпризы и откат (заполняется ПО ХОДУ, урок RUNBOOK 1.9)

«Пришлось сделать руками сверх процедуры» — каждое такое действие записывается немедленно.
Подсказки-категории (RUNBOOK §2): антивирус (карантин .tar?), политики (допуск носителя,
право запускать .sh), firewall (порт до клиента), права/пути на сервере.

_______________________________________________________________________________
_______________________________________________________________________________
_______________________________________________________________________________
_______________________________________________________________________________

Откат выполнялся? ☐ нет ☐ да — до тега: ____________ причина: ____________________
