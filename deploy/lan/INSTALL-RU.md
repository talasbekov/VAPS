# Smart Жоспарлау — перенос в закрытую сеть

Редакция от 13 сентября 2026 года (комплект `smart-josparlau-offline-2026-09-13`, Plane №1264). Предыдущие редакции: 12 сентября 2026 года (комплект `smart-josparlau-offline-2026-09-12`, Plane №1236) и 10 сентября 2026 года (комплект `smart-josparlau-offline-2026-09-10-compose2172`, Plane №1172).

Если на сервере **уже работает** комплект от 10 или от 12 сентября — читайте сразу раздел «Обновление уже установленной системы». Остальные разделы описывают установку с нуля и ежедневную эксплуатацию; они относятся и к этому комплекту.

## Что подготовлено

Комплект рассчитан на один сервер **10.15.3.187**, Linux **x86_64**, с установленным Docker и Docker Compose. Интернет при установке и запуске приложения не нужен: образы и стартовая база находятся в комплекте.

| Адрес | Назначение |
|---|---|
| **http://10.15.3.187:3300/** | Вход пользователей в систему |
| **http://10.15.3.187:8300/admin/** | Администрирование Django |
| **http://10.15.3.187:8300/api/** | Backend; конкретный раздел API добавляется к этому адресу |
| **10.15.3.187:5300** | Подключение администратора к PostgreSQL |
| **http://10.15.3.187:8300/docs** | Интерактивная документация API, файлы оформления внутри комплекта |

Все компоненты работают в контейнерах: frontend, backend, PostgreSQL 16, Redis, Celery worker, Celery beat и nginx. Nginx принимает два HTTP-порта и передаёт запросы соответствующему приложению; он также выдаёт изображения и служебные файлы. Redis, worker и beat не требуют дополнительных открытых портов. Внутри контейнеров база доступна по `db:5432`, backend — `backend:8000`; внешние порты не нужно подставлять вместо этих внутренних адресов.

## Обновление уже установленной системы

Этот порядок — для сервера, где уже работает комплект от 10.09.2026 или от 12.09.2026 и в нём накоплены данные. **Данные сохраняются:** база PostgreSQL, загруженные файлы и фотографии, настройки `.env` остаются на месте. Обновление идёт поверх тех же томов Docker под тем же именем стека `smart-josparlau-lan`; заменяются только образы приложения, а схема базы дополняется при первом запуске новой версии. Если на сервере комплект от 10.09 — при старте применятся четыре миграции (право начальников управлений сдавать день, кадровые статусы в расходе, уведомление об отправке свода, право дежурного на статусы); если комплект от 12.09 — новых миграций нет, схема остаётся на `0126`, меняются только образы.

Что нового в комплекте от 13.09 по сравнению с 12.09: закрытие объекта ОМ без ручных оценок ставит всем неоценённым 7 автоматически (автооценка помечена как системная и отличима в реестре); диалог завершения рекогносцировки называет физнаряд и специальные группы раздельно; «Запланированные статусы» печатают день окончания участия в ОМ включительно; участник специальной группы попадает в общий пул распределения и в состав объекта; подбор сотрудника (старший объекта, замещающие) показывает полное имя и путь подразделения и ищет по «Фамилия Имя» — однофамильцы различимы; ширина левого меню модулей тянется мышью за правый край и запоминается (224–440 px, двойной клик — 256).

Что нового по сравнению с 10.09 (вошло в комплект 12.09 и сохраняется): модуль «Ежедневный расход» с двумя рабочими местами (ответственный департамента и оперативный дежурный), свод по Службе, отправка свода дежурному с уведомлением, кадровые статусы попадают в расход и в снимок сдачи, обзор по департаменту одним правом, фотографии сотрудников открываются по адресу сервера (в версии 10.09 в закрытой сети они не загружались), в меню видны только доступные роли модули.

Порядок — ровно в этой последовательности. Работайте из папки **старого** комплекта, пока не сказано иное. Ниже «старый комплект» — та папка, из которой сейчас запущен стек: `smart-josparlau-offline-2026-09-10-compose2172` или `smart-josparlau-offline-2026-09-12`.

**1. Резервная копия перед обновлением (обязательно).** Каталог должен быть новым. На время копирования вход в систему недоступен, затем сервисы поднимаются сами:

```bash
cd /путь/к/старого-комплекта
bash data.sh backup /srv/backups/smart-josparlau-2026-09-13-до-обновления
cp -p .env /srv/backups/smart-josparlau-2026-09-13-до-обновления/env.backup
chmod 600 /srv/backups/smart-josparlau-2026-09-13-до-обновления/env.backup
(cd /srv/backups/smart-josparlau-2026-09-13-до-обновления && sha256sum -c sha256sums.txt)
```

Все строки проверки — `OK`. Без этой копии не продолжайте: это единственный путь отката.

**Если вам передавали исправленный дамп базы** (`database-fixed.dump` от 12.09 — уровни подразделений, и `database-fixed-hire-2026-09-12/database.dump` — условные даты приёма и рождения) и он ещё **не** накачен на сервер, накатите его по той инструкции, с которой он передавался, **до** обновления, на работающей старой версии, и только потом снимайте резервную копию из шага 1. Накатка дампа поверх уже обновлённой базы тоже возможна, но тогда после `pg_restore` нужно перезапустить backend (`bash ctl.sh restart backend`), чтобы он заново применил миграции новой версии, — проще не смешивать эти два действия.

**2. Перенесите новый комплект рядом со старым, не поверх него**, и проверьте суммы:

```bash
sha256sum -c smart-josparlau-offline-2026-09-13.tar.sha256
tar -xf smart-josparlau-offline-2026-09-13.tar
cd smart-josparlau-offline-2026-09-13
sha256sum -c sha256sums.txt
```

**3. Перенесите настройки из старого комплекта.** `configure.py` здесь запускать **нельзя**: он создаст новые секреты, и пароль базы разойдётся с уже созданным томом.

```bash
cp -p /путь/к/старого-комплекта/.env ./.env
chmod 600 .env
grep -E '^(STACK_NAME|NEXTAUTH_URL|FRONTEND_PORT|BACKEND_PORT|POSTGRES_PORT)=' .env
```

Ожидается `STACK_NAME=smart-josparlau-lan` и прежние адрес и порты. Другое имя стека означало бы новый пустой набор томов, а не обновление.

**4. Запустите обновление** — тот же установщик, но **без** `--restore`:

```bash
bash install.sh
```

Установщик проверит суммы и настройки, загрузит новые образы, пересоздаст контейнеры на них и дождётся готовности (до 10 минут). Backend при старте применяет миграции; пока они идут, контейнер `backend` не считается здоровым — это ожидаемо. Каталог `seed-data` в этом сценарии не используется: восстанавливать стартовую очищенную базу поверх накопленных данных нельзя.

**5. Проверьте результат:**

```bash
bash ctl.sh ps
bash ctl.sh exec -T backend python manage.py showmigrations operations | tail -n 5
bash ctl.sh logs backend | grep -A1 'Проекция кадровых статусов'
```

Ожидается: семь контейнеров запущены, последние строки списка миграций — `[X] 0123…`, `[X] 0124…`, `[X] 0125…`, `[X] 0126…` (после комплекта 12.09 они уже стояли — это нормально, новых строк не появится). Если обновляетесь с комплекта 10.09, в журнале будет строка с числами переноса кадровых статусов (например, `created=N`); если среди чисел есть `conflict=…`, это статусы, которые не удалось спроецировать из-за пересечения с уже проставленным фактом раздела; система работает, а список таких сотрудников стоит проверить в «Статусах» вручную. После комплекта 12.09 этой строки не будет — проекция уже выполнена.

Затем в браузере, в новом приватном окне: вход прежним пользователем на http://10.15.3.187:3300/, раздел «Ежедневный расход» открывается, у сотрудников с фотографиями они отображаются, `admin` входит в http://10.15.3.187:8300/admin/. Подразделения, сотрудники и всё введённое до обновления на месте. Признак именно новой версии: правая граница левого меню тянется мышью; в «Реестре ОМ» при назначении старшего объекта список показывает полное имя и подразделение.

**6. Старую папку и старые образы не удаляйте** до приёмки новой версии: они нужны для отката.

**Откат.** Прямой возврат старых образов на обновлённую базу невозможен — схема уже новая. Откат — это восстановление копии из шага 1 старой версией в **новый пустой** набор томов:

```bash
cd /путь/к/smart-josparlau-offline-2026-09-13 && bash ctl.sh down      # без -v: тома остаются для разбора
cd /путь/к/старого-комплекта
sed -i 's/^STACK_NAME=.*/STACK_NAME=smart-josparlau-lan-prev/' .env
bash install.sh --restore /srv/backups/smart-josparlau-2026-09-13-до-обновления
```

Адрес и порты у пользователей остаются прежними; тома обновлённого стека `smart-josparlau-lan` сохраняются нетронутыми. Данные, введённые между копией и откатом, в восстановленной системе отсутствуют.

## Какие данные останутся при установке с нуля

Этот раздел — про установку на **новый** сервер со стартовой базой из каталога `seed-data`. При обновлении уже работающей системы (раздел выше) он не применяется. Каталог `seed-data` перенесён из комплекта 10.09.2026 без изменений; при первом запуске новая версия дополнит его схему сама.

По вашему указанию подготовлена отдельная очищенная копия базы.

**Сохранены:** все 58 пользователей с прежними паролями и признаками активности; роли и права; все справочники, включая должности, звания, типы статусов, значения справочников ОМ, страны и города, транспорт, группы рейтинга и нормативную базу; текущие общие настройки.

**Очищены:** подразделения, штат и вакансии, сотрудники, их статусы и перемещения, объекты и паспорта, охраняемые лица, мероприятия, бюллетени, заявки на силы, расходы и своды, смены дежурства, оценки, отчёты, обращения, уведомления, история действий и старые сеансы входа. Всего очищены 84 рабочие/служебные таблицы. Загруженные рабочие файлы и фотографии в комплект не перенесены.

Важные последствия:

- 19 назначений прав на удалённые подразделения сняты. После создания новой структуры назначьте эти роли заново. Они не превращены в права на всю организацию.
- Четыре значения справочника специальных групп сохранены без прежнего подразделения-владельца. Назначьте нового владельца после создания структуры.
- Пользователи пока не связаны с новыми сотрудниками: кадровые карточки нужно создать и связать заново.
- Прежние примеры из учебных инструкций с конкретными сотрудниками больше не описывают содержимое этой очищенной базы.

Исходная рабочая база не очищалась. Файл `seed-data/sanitization-report.json` в комплекте содержит проверку количества записей до и после подготовки; значений паролей в отчёте нет.

## Что нужно на сервере

Вы указали **Ubuntu 22.10 Kinetic Kudu**, Docker уже установлен, архитектура x86_64.

Ubuntu 22.10 завершила поддержку 20.07.2023. Для дальнейшей эксплуатации желательно перейти на поддерживаемый Ubuntu LTS. Комплект не обновляет ОС и не заменяет установленный Docker. [Сообщение Ubuntu об окончании поддержки](https://lists.ubuntu.com/archives/ubuntu-announce/2023-July/000293.html).

Проверьте заранее:

1. Docker запускает Linux-контейнеры, текущий пользователь имеет доступ к нему.
2. Установлен **Docker Compose не ниже 2.17.2**. Это отдельная команда `docker compose`, старый `docker-compose` не подходит.
3. Есть `bash`, `python3`, `sha256sum`, `tar`, `gzip`; они обычно уже есть в Ubuntu.
4. Серверу назначен адрес 10.15.3.187. Порты 3300,8300,5300 свободны.
5. Для тестового запуска ориентир —4 ядра,8 ГБ оперативной памяти и 40 ГБ свободного диска. С ростом файлов и пользователей потребность увеличится; это ориентир, а не результат нагрузочного испытания.
6. Клиентские компьютеры имеют доступ к трём нужным TCP-портам в вашей локальной сети. Доступ к PostgreSQL5300 предоставляйте только тем, кому он действительно нужен для администрирования.
7. Часы и дата на сервере верны. Желателен локальный сервер времени; интернет-NTP для работы не требуется. Деловая дата приложения — Asia/Almaty.
8. Есть место для резервных копий на отдельном носителе или сервере.

Команды проверки:

```bash
docker --version
docker compose version
docker info --format '{{.OSType}}/{{.Architecture}}'
ip -4 addr show
ss -ltn | grep -E ':(3300|8300|5300)\b'
df -h
date
```

Если Compose отсутствует или слишком старый, установите совместимый Compose plugin из заранее перенесённого пакета. Приложение не пытается скачать его само. Установщик проверяет версию и платформу до загрузки образов. [Порядок установки Compose plugin](https://docs.docker.com/compose/install/linux/).

## Перенос файлов

Перенесите **весь** каталог `smart-josparlau-offline-2026-09-13` или одноимённый архив вместе с файлом его контрольной суммы. Не переносите только исходный код: для запуска нужны `runtime-images.tar.gz`, настройки, скрипты и каталог `seed-data`.

Образы содержат готовое приложение. Каталог `source` нужен для прослеживаемости версии; это не отдельный комплект всех зависимостей для разработки. Пересобирать программу на закрытом сервере для установки не требуется.

Если переносите архив:

```bash
sha256sum -c smart-josparlau-offline-2026-09-13.tar.sha256
tar -xf smart-josparlau-offline-2026-09-13.tar
cd smart-josparlau-offline-2026-09-13
```

Если переносите папку, просто перейдите в неё. Затем:

```bash
sha256sum -c sha256sums.txt
```

Все строки должны заканчиваться `OK`. При ошибке повторите копирование повреждённого файла. Архив содержит учётные записи и хэши паролей, поэтому храните его как резервную копию доступа, а не как общедоступный установщик.

## Первый запуск с сохранёнными пользователями и справочниками

Работайте из папки комплекта. Если Docker доступен только через `sudo`, используйте `sudo bash ...` для скриптов, вызывающих Docker.

**1. Создайте настройки:**

```bash
python3 configure.py
```

Будет создан `.env` с адресом 10.15.3.187, вашими тремя портами и тремя новыми независимыми служебными секретами. Пароли сохранённых пользователей эта команда не меняет. Файл закрыт правами 600. Повторный запуск не перезапишет существующие секреты.

**2. Восстановите подготовленную стартовую базу и запустите систему:**

```bash
bash install.sh --restore "$PWD/seed-data"
```

Установщик проверяет файлы и настройки, загружает образы, поднимает только PostgreSQL, восстанавливает базу и файловые каталоги, затем запускает backend с обновлением схемы и остальными контейнерами. Скачиваний и сборки здесь нет.

**Не запускайте `bootstrap_contour`, `seed_operations`, `seed_demo_all` или другие команды наполнения после этого восстановления.** Они могут добавить структуру или учебные данные, которые вы попросили удалить.

Если установщик сообщает о непустой базе, остановитесь и проверьте, тот ли каталог и проект выбраны. Не удаляйте тома ради обхода проверки. Повторный обычный запуск после успешной установки:

```bash
bash ctl.sh up -d --pull never --no-build --wait
```

Команда `install.sh --restore` нужна для восстановления в **новую** базу, а не для каждого запуска.

**3. Проверьте контейнеры:**

```bash
bash ctl.sh ps
bash ctl.sh logs --tail=50 backend worker beat
```

Все семь контейнеров должны быть запущены. Для PostgreSQL, Redis, backend, frontend и worker ожидается `healthy`; у nginx и beat отдельной строки healthcheck может не быть.

**4. Откройте систему с другого компьютера:**

- Портал: http://10.15.3.187:3300/
- Администрирование: http://10.15.3.187:8300/admin/

Сохранённый `admin` входит в Django с прежним паролем. `acc_admin` — администратор портала по назначенной роли; в исходной базе у него нет права входа в Django admin. Это разные виды доступа.

После первого входа администратор создаёт нужную структуру, сотрудников и назначения ролей. Затем проверяется обычный пользователь. Пустые сотрудники/ОМ/расходы в этой поставке — ожидаемое состояние.

## Подключение к PostgreSQL

В программе администрирования базы укажите:

| Поле | Значение |
|---|---|
| Сервер | 10.15.3.187 |
| Порт | 5300 |
| База | smart_josparlau |
| Пользователь базы | smart_josparlau |
| Пароль базы | Значение `POSTGRES_PASSWORD` из закрытого файла `.env` |

Это служебный пользователь PostgreSQL, он не является пользователем портала. Менять только `POSTGRES_PASSWORD` в `.env` после создания базы недостаточно: пароль уже хранится в PostgreSQL. Не меняйте его случайно при повторном запуске.

## Проверка после переноса

- [ ] На другом компьютере открывается 3300 и видна форма входа.
- [ ] Сохранённый пользователь входит с прежним паролем.
- [ ] `admin` входит в 8300/admin/, оформление страницы загружается.
- [ ] В администрировании есть пользователи и заполненные справочники.
- [ ] Подразделения, сотрудники, ОМ, бюллетени и расходы пусты.
- [ ] После создания структуры роли назначены на новые подразделения.
- [ ] Обычный пользователь не получил доступ ко всей организации вместо прежнего ограниченного доступа.
- [ ] Backend и PostgreSQL доступны по нужным портам.
- [ ] В 8300/docs загружается документация без внешнего интернета.
- [ ] Worker и beat запущены, дата и часовой пояс верны.
- [ ] На новом учебном сотруднике проверено применение запланированного статуса.
- [ ] Выполнена и отдельно проверена резервная копия.

Если на этом адресе раньше была другая версия системы, первый вход удобно проверить в новом приватном окне браузера, чтобы прежние сохранённые настройки страницы не мешали проверке.

## Резервная копия и восстановление

Копия включает PostgreSQL, обычные файлы и закрытые файлы. На время копирования приложение недоступно: скрипт приостанавливает вход и создание фоновых задач, дожидается завершения очереди, снимает копию и запускает сервисы обратно.

```bash
bash data.sh backup /srv/backups/smart-josparlau-ГГГГ-ММ-ДД
```

Каталог результата должен быть новым. Если очередь не удалось завершить, копирование отменяется, работа приложения возобновляется. Не переносите незавершённую папку как готовую копию.

`.env` храните отдельно в защищённой резервной копии. Он не включается автоматически в копию базы. Очередь Redis не переносится: перед снимком она должна быть пустой. На новом сервере расписание запускается заново. Сервер должен быть включён к времени плановых задач; простой сервера сам по себе не является подтверждением, что пропущенные задачи выполнены.

Восстановление выполняется в новый каталог установки с новыми пустыми томами:

```bash
python3 configure.py
bash install.sh --restore /srv/backups/smart-josparlau-ГГГГ-ММ-ДД
```

Для параллельной проверки на том же сервере нужны другое имя проекта и свободные порты; их можно задать параметрами `configure.py --help`. Скрипт отклоняет восстановление в непустую базу. Если восстановление оборвалось, не запускайте его повторно поверх частично заполненных томов: сохраните журнал и разберите причину на этой новой копии.

## Остановка, повторный запуск и обновление

Остановить контейнеры с сохранением данных:

```bash
bash ctl.sh down
```

Запустить снова:

```bash
bash ctl.sh up -d --pull never --no-build --wait
```

**Не добавляйте `-v` к `down`: это удаляет тома с данными.**

Обновление на следующую версию — по разделу «Обновление уже установленной системы» в начале инструкции: проверенная резервная копия, новый комплект рядом, тот же `.env` и `STACK_NAME`, `bash install.sh` **без `--restore`**. Не восстанавливайте стартовую очищенную базу поверх накопленных данных.

Откат после изменения схемы — восстановление резервной копии в новую пустую базу вместе с соответствующей прежней версией образов. Простая смена изображения контейнера не гарантирует совместимость старого приложения с новой схемой.

## Что ещё потребуется при необходимости

- Для писем пользователям нужен внутренний SMTP-сервер и отдельная настройка. Сейчас реальная отправка email не включена; почтовый backend пишет сообщения в журнал. Внутренние уведомления и задачи используют Redis/Celery.
- Для HTTPS нужны внутренний сертификат и настройка TLS. Этот комплект использует согласованные HTTP-адреса.
- Для одинакового времени на всех машинах нужен локальный источник времени либо ручной контроль часов.
- Доступ к интернету со стороны сервера не является условием работы. Закрытость всей сети обеспечивает её администратор; контейнерная сеть приложения не заменяет правила вашей локальной сети.

## Что проверено при подготовке

**Редакция 12.09.2026 — репетиция обновления на машине сборки**, стек `sj-lan1236-rehearsal` на 127.0.0.1:13300/18300/15300, тот же порядок, что в разделе «Обновление уже установленной системы»:

1. Установлен комплект 10.09.2026 с `--restore seed-data` (42 с). В него добавлены 3 сотрудника и 5 кадровых статусов, среди них действующий больничный и запланированный отпуск.
2. `data.sh backup` — копия снята за 33 с, очередь worker пуста, контрольные суммы копии сходятся.
3. В каталог нового комплекта скопирован тот же `.env`; `bash install.sh` без `--restore` — 34 с до состояния `healthy` у всех семи контейнеров. Миграции `0123`–`0126` применены при старте; в журнале backend строка `Проекция кадровых статусов в раздел: created=2` — больничный и отпуск получили проекцию в расход, «в строю» не проецируется, так и задумано. Счётчики до/после: пользователи 58/58, сотрудники 3/3, кадровые статусы 5/5, факты раздела 0/2.
4. Браузер (Chromium через Playwright): вход `admin`, страницы `/dashboard`, «Ежедневный расход» (`/employees?view=daily` и `?view=department-summary`), свод по Службе (`/security-ops/service-summary`) и Django admin открываются; внешних адресов страницы не запрашивают (список чужих origin пуст). В консоли — известное `400 Пользователь не привязан к сотруднику` у `admin` без кадровой карточки; один раз, при первом открытии сразу после старта стека, — `403` на рукопожатии `/ws/notifications/`, три повторных прохода — без него (Plane №1244).
5. Откат по разделу выше: обновлённый стек остановлен `ctl.sh down`, старый комплект под другим `STACK_NAME` восстановил копию из п. 2 за 39 с — пользователи 58, сотрудники 3, статусы 5, фактов раздела 0, схема на `0122`, то есть состояние до обновления. Затем обновлённый стек поднят обратно `ctl.sh up -d` и продолжил работу на своих томах.

**Не проверено в этой редакции:** обновление на настоящих данных сервера 10.15.3.187 — их копии на машине сборки нет, поэтому шаг 1 раздела обновления обязателен, а числа `created/conflict` проекции нужно прочитать в журнале самого сервера; поведение под ядром Ubuntu 22.10 и версией Compose сервера; плановые задачи Celery после обновления (worker и beat поднялись `healthy`, отдельная задача не запускалась).

**Редакция 10.09.2026 (первая поставка):**

Проверены production-сборка приложения без сети на шагах сборки, загрузка образов из архива, запуск контейнеров, вход через frontend и backend, доступ к PostgreSQL через опубликованный порт, выполнение задачи Celery, сохранение пользователей и очистка рабочих данных. Отдельно проверено восстановление базы и обоих видов файлов в новую установку; повторное восстановление поверх работающей базы отклоняется.

Полная бизнес-приёмка на целевом сервере 10.15.3.187 ещё не выполнялась: сервер находится в вашей закрытой сети. Доступность с его клиентских компьютеров, установленную версию Docker и поведение на ядре Ubuntu 22.10 нужно проверить после переноса по списку выше.

Исходный код и версия сборки указаны в `source-revision.txt`, состав образов — в `images-manifest.txt`, целевая платформа — в `platform.txt`. Основная документация и история проверки находятся во втором мозге проекта; копия этой инструкции в комплекте предназначена для переноса оператору.

Исполнитель первой редакции: Кодекс Астра 6. Редакции 12.09.2026 (Plane №1236) и 13.09.2026 (Plane №1264): Claude Fable 5.1.
