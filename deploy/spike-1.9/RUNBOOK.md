# РУНБУК — контур-проба 1.9 (путь носителя hello-world)

> **Это главный продукт спайка 1.9.** Не код, а ЗНАНИЕ: список РЕАЛЬНЫХ сюрпризов пути носителя
> в закрытый контур. Заполняется **по ходу пробы, а не по памяти** — каждое «пришлось сделать
> руками сверх рунбука» дописывается немедленно. Прямой вход для E12 (особенно 12.7 «прогон по
> рунбуку 1.9»).
>
> **Легенда статусов:**
> `VERIFIED` — проверено фактически (команда/наблюдение).
> `UNVERIFIED-pending-contour` — требует реального контура, снять нельзя на dev/реплике (путь B).
> `TODO` — заполнить во время прогона.

---

## 0. Предусловия и кто несёт (гейт A5)

| Пункт | Значение |
|---|---|
| Дата прогона | `TODO` |
| Путь (A=реальный контур / B=реплика+эскалация) | **B (на 2026-06-16)** — A5 открыт, эскалация `draft-pending-send` |
| A5 разрешён? | **НЕТ** — эскалация заказчику составлена (`escalation-A5-A8-owner-2026-06-16.md`), но не отправлена/без ответа |
| Кто физически несёт носитель | `TODO` (вопрос 1.2 эскалации) |
| Носитель (USB/диск) | `TODO` |
| Docker на сервере контура + версия | `UNVERIFIED-pending-contour` (вопрос 1.3 эскалации) |
| Окно для проверки | `TODO` |

> **Решение пути (Dev Agent Record, 2026-06-16):** A5 объективно открыт — эскалация не отправлена,
> ответа заказчика нет. По правилу Task 0 «неизвестна СЕЙЧАС → путь B». Технические сюрпризы,
> не требующие реального контура, сняты на dev-машине (online) ниже; орг-сюрпризы помечены
> `UNVERIFIED-pending-contour`; реальный контур-прогон делегируется 12.7.

---

## 1. Шаги пути носителя (transfer → load → up → открыть-с-клиента)

| # | Шаг | Команда / действие | Статус | Заметка |
|---|---|---|---|---|
| 1 | Собрать бандл (DEV, online) | `./build-bundle.sh` | `VERIFIED` (2026-06-16, dev) | `.tar` 26 070 528 Б + `sha256sums.txt` |
| 2 | Перенести носителем в контур | `.tar`, `sha256sums.txt`, `install-probe.sh`, `docker-compose.yml`, `RUNBOOK.md` | `UNVERIFIED-pending-contour` | физический внос — путь A / 12.7 |
| 3 | Проверить целостность | `sha256sum -c sha256sums.txt` | `VERIFIED` (dev) | «ЦЕЛ»; битый архив → стоп до изменений |
| 4 | Загрузить образ | `docker load -i vaps-probe-<sha>.tar` | `VERIFIED` (dev, чистый контекст) | образ удалён до load → восстановлен из `.tar` |
| 5 | Поднять | `docker compose up -d` | `VERIFIED` (dev) | `spike-19-probe-1` Started |
| 6 | Открыть с КЛИЕНТСКОЙ машины | Firefox ~100 → `http://<IP>:8080/` | `UNVERIFIED-pending-contour` | на dev — только `curl localhost` (≠ LAN-доказательство) |
| 7 | Остановить | `docker compose down` | `VERIFIED` (dev) | сеть/контейнер удалены |

---

## 2. Сюрпризы по категориям

### 2.1 Антивирус
- [ ] Карантинит/сканирует `.tar` при копировании с носителя? — `UNVERIFIED-pending-contour`
- [ ] Блокирует `docker load` / запуск `install-probe.sh`? — `UNVERIFIED-pending-contour`
- Наблюдение: `TODO` (на dev-машине антивируса периметра нет — нерелевантно)

### 2.2 Политики (орг-периметр)
- [ ] Допуск носителя (USB/диск) в контур разрешён? — `UNVERIFIED-pending-contour` (= A5, вопрос 1.1 эскалации)
- [ ] Право на запуск `.sh` на сервере? — `UNVERIFIED-pending-contour`
- [ ] Firewall: порт `8080` открыт в LAN до клиента? — `UNVERIFIED-pending-contour`
- Наблюдение: `TODO`

### 2.3 Браузер (Firefox ~100)
- [ ] FF~100 дотягивается до `http://<сервер>:8080/` по LAN? — `UNVERIFIED-pending-contour`
- [ ] mixed-content / сертификат (если позже https)? — `UNVERIFIED-pending-contour` (проба по http, не https)
- Наблюдение на dev (НЕ FF~100): страница отдаётся, `curl` → HTTP 200, разметка простая (Arial, flexbox — FF~100-совместимо, без container-queries/`:has()`). `VERIFIED` (dev), но реальный FF~100 по LAN — `UNVERIFIED-pending-contour`.

### 2.4 Время / tz / NTP  → задел для спайка 3.13
- Хост (dev): `Asia/Almaty (+05)`, NTP **active**, clock synchronized **yes**. `VERIFIED` (2026-06-16)
- Контейнер: **UTC** (`Tue Jun 16 10:54 UTC` == хост `15:54 +05` — тот же момент, разные tz). `VERIFIED`
- **Сюрприз:** образ работает в UTC, хост — в локальной tz; 5 ч расхождения отображаемых часов. Для контура зафиксировать: `timedatectl` хоста, **есть ли NTP** (в контуре часто НЕТ — прямой вход 3.13), tz хоста/контейнера/БД (tzdata-канарейка деплоя).
- В контуре: `UNVERIFIED-pending-contour` (NTP/tz сервера контура неизвестны).
- **Целевой tz:** канон-tz приложения — `Asia/Qyzylorda` (architecture.md:57), НЕ dev-`Asia/Almaty` (оба +05). tzdata-канарейка (3.13 / деплой) сверяется против ЦЕЛЕВОГО `Asia/Qyzylorda`, а не dev-tz. `UNVERIFIED-pending-contour`.

### 2.5 (доп.) Offline / сеть
- [x] Образ полностью в `.tar`, при `up` ничего не тянется из сети. **`VERIFIED` (dev)**: после `docker rmi` + `docker load` контейнер поднялся, `curl` → HTTP 200 (сетевой тяги нет). Самодостаточность доказана на dev. _(Историческое dev-наблюдение делалось с `docker compose up -d --pull never`; ревью пр.5 УБРАЛ флаг из `install-probe.sh` — крашил на старом compose контура. Поставляемый скрипт теперь использует голый `docker compose up -d`; offline-самодостаточность опирается на «образ уже `docker load`-нут + в `docker-compose.yml` нет `build:` → реестр не трогается», а не на флаг.)_
- В контуре сети нет вовсе — поведение ожидается идентичным (образ не зависит от реестра).

---

## 3. Измеренные факты контура (бюджет/вход E12)

| Факт | Значение (dev, 2026-06-16) | В контуре |
|---|---|---|
| Docker | `29.4.1` | `UNVERIFIED-pending-contour` (вопрос 1.3) |
| Docker Compose | `v5.1.3` | `UNVERIFIED-pending-contour` |
| Базовый образ | `nginx:alpine @ sha256:8b1e78743a03dbb2c95171cc58639fef29abc8816598e27fb910ed2e621e589a` | то же (в `.tar`) |
| Образ пробы (digest) | `sha256:f6f03f0c613b12d646956ed4c9252e747c6f975137097daa0ef17b189cef0502` | то же |
| `.tar` sha256 | `ea6378773edd6266cd16ea95bf46cd82580508450873dfa979c455d4b0afb643` | сверять после переноса |
| Размер `.tar` | `~26 МБ` (26 070 528 Б) | носимо на любом USB |
| Порт | `8080:80` | открыть в firewall LAN |
| IP сервера (пример dev) | `10.115.70.58` | IP сервера контура — `TODO` |
| FF~100 по LAN | `UNVERIFIED-pending-contour` | штатный клиентский ПК |
| Время/tz/NTP | хост Almaty+05 / контейнер UTC / NTP active | NTP сервера контура — `TODO` |

---

## 4. Находка → стори E12 (явный вход)

| Находка пробы | Питает стори | Как именно |
|---|---|---|
| Путь носителя `save → перенос → load → up` работает (dev) | **12.2** bundle.sh, **12.3** install.sh | `build-bundle.sh`/`install-probe.sh` — черновик-механика (save+sha, sha-check+load+up) |
| Целостность через `sha256sum -c` (стоп до изменений) | **12.3** install.sh | перенести проверку sha в боевой `install.sh` ДО бэкапа/migrate |
| Offline-самодостаточность (предзагруженный образ + нет `build:`; `--pull never` снят пр.5) | **12.1** prod-compose, **12.2** | прод-образы тоже паковать в `.tar`, без сетевых тяг при `up` |
| Версия docker контура | **12.5** CHECKLIST | зафиксировать min-версию docker в чек-листе переноса |
| Порт/firewall LAN до клиента | **12.1** nginx-сервис, **12.5** CHECKLIST | проброс порта + пункт «открыть firewall» в чек-листе |
| FF~100 поведение по LAN | **12.5/12.7**, спайк **1.10** (перф-грид) | прогнать реальный FF~100; задел для перф-спайка |
| Время/tz/NTP контура | спайк **3.13** (часы без NTP), tzdata-канарейка **12.x** | зафиксировать tz хоста/контейнера/БД, наличие NTP |
| Антивирус/политики/допуск носителя | **A5** (эскалация), **12.7** | реальный прогон по рунбуку в контуре |
| Весь рунбук | **12.7** | «прогон пути носителя с РЕАЛЬНЫМ бандлом по рунбуку 1.9» |

---

## 5. Итог пробы (заполнить по завершении)

- Путь (A/B): **B** (на 2026-06-16; A после ответа на A5 / в рамках 12.7)
- Технические сюрпризы (dev/реплика): см. §2.4 (время UTC↔Almaty), §2.5 (offline ок)
- Орг-сюрпризы: `UNVERIFIED-pending-contour` — антивирус/политики/допуск носителя/FF~100/NTP контура
- A5: открыт; эскалация — находка №1 readiness-отчёта; реальный контур-прогон → 12.7
- Реальный контур-прогон выполнен? `НЕТ — отложен в 12.7 (путь B)`
