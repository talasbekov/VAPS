-r base.txt
pytest>=7.4.0
pytest-django>=4.5.0
# pypdf ПЕРЕЕХАЛ В `base.txt` (Plane №492/№639): им пользуется боевой путь
# выдачи PDF, а не только пробы. Здесь он остаётся нужен ровно затем, зачем был
# заведён изначально — читать готовый .pdf в тестах: единственный честный
# способ проверить, что кириллица ДОШЛА до документа, — прочитать его обратно.
# На встроенных шрифтах она выпадает молча, и файл при этом остаётся валидным.
# Строки своей больше нет: `-r base.txt` в начале файла его уже приносит.
# Расход сам проверяет свои равенства (Σ колонок == Список, Штат == Список +
# Вне списка + Вакансии), но срабатывают они только на придуманных примерах.
# hypothesis генерирует миры — подразделения, вакансии, приданных, статусы со
# случайными интервалами — и ищет тот, на котором равенство разойдётся.
hypothesis>=6.100.0
