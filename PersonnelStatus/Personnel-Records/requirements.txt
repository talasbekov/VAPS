amqp==5.3.1
asgiref==3.11.1
attrs==26.1.0
billiard==4.2.4
celery==5.6.3
channels==4.3.2
charset-normalizer==3.4.7
click==8.4.1
click-didyoumean==0.3.1
click-plugins==1.1.1.2
click-repl==0.3.0
cron-descriptor==1.4.5
Django==5.1.15
django-celery-beat==2.9.0
django-cors-headers==4.9.0
django-filter==24.3
django-js-asset==3.1.2
django-mptt==0.18.0
django-timezone-field==7.2.2
django_celery_results==2.6.0
djangorestframework==3.17.1
djangorestframework-recursive==0.1.2
djangorestframework_simplejwt==5.5.1
drf-spectacular==0.29.0
et_xmlfile==2.0.0
inflection==0.5.1
iniconfig==2.3.0
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
kombu==5.6.2
lxml==6.1.1
openpyxl==3.1.5
packaging==26.2
pillow==12.2.0
pluggy==1.6.0
prompt_toolkit==3.0.52
pyflakes==3.4.0
Pygments==2.20.0
PyJWT==2.13.0
pytest==9.0.3
pytest-django==4.12.0
python-crontab==3.3.0
python-dateutil==2.9.0.post0
python-docx==1.2.0
PyYAML==6.0.3
referencing==0.37.0
pypdf==6.16.1
reportlab==4.5.1
rpds-py==2026.5.1
six==1.17.0
sqlparse==0.5.5
typing_extensions==4.15.0
tzdata==2026.2
tzlocal==5.3.1
uritemplate==4.2.0
vine==5.1.0
wcwidth==0.8.1

# 🔴 ТРИ СТРОКИ, КОТОРЫХ ЗДЕСЬ НЕ БЫЛО (Plane №790). `base.txt` их называет,
# а закреплённый файл — нет; сборка по пину не поставила бы их ВОВСЕ. Это не
# «старая версия», а ModuleNotFoundError: `psycopg2` — на старте, `django_redis`
# — на первом обращении к кэшу. `psycopg2-binary` закреплён по фактически
# установленному; `redis` и `django-redis` в .venv не стоят вовсе (окружение
# разработки кэш и очередь не поднимает), поэтому взяты старшие версии,
# совместимые с закреплённым Django==5.1.15: django-redis 7.0.0 требует
# Django>=5.2 и с этим пином не сходится, 6.0.0 объявляет поддержку 5.1.
psycopg2-binary==2.9.12
redis==8.1.0
django-redis==6.0.0

# 🔴 ВЕБ-СОКЕТ И ЕГО ПОСТАВКА (Plane №804, №806; решение заказчика
# 06.09.2026). `channels-redis` — слой каналов, который настройки называли, не
# объявляя; он БОЕВОЙ и объявлен в `base.txt`. `daphne` — ТЕСТОВАЯ зависимость
# и живёт в `development.txt`: боевой код его не импортирует, а единственный
# его потребитель — `channels.testing`. Закреплён он здесь потому, что этот
# файл пинит окружение ЦЕЛИКОМ (ниже так же закреплены `pytest` и его плагины),
# а не только боевой контур. Ниже вместе с ними закреплено всё, что они
# привезли: `Twisted` и его обвязка
# (`autobahn`, `Automat`, `constantly`, `hyperlink`, `Incremental`, `txaio`,
# `zope.interface`, `service-identity`), криптография рукопожатия
# (`cryptography`, `pyOpenSSL`, `cffi`, `pycparser`, `idna`) и сериализация
# (`msgpack`, `cbor2`, `ujson`). Закреплять только два верхних имени значило бы
# оставить полтора десятка пакетов на усмотрение резолвера — ровно тот дефект,
# что разбирался в №794, только этажом ниже.
channels-redis==4.3.0
daphne==4.2.3
autobahn==26.7.1
Automat==25.4.16
cbor2==6.1.4
cffi==2.1.1
constantly==23.10.4
cryptography==50.0.1
hyperlink==21.0.0
idna==3.19
Incremental==24.11.0
msgpack==1.2.2
pycparser==3.0
pyOpenSSL==26.4.0
service-identity==26.1.0
Twisted==26.4.0
txaio==26.6.1
ujson==6.0.0
zope.interface==8.6

# 🔴 БОЕВОЙ ASGI-СЕРВЕР (Plane №806, решение заказчика 06.09.2026: `uvicorn`
# вместо `daphne`). Версии сняты разрешателем, а не придуманы:
# `pip install --dry-run --report uvicorn[standard]` 06.09.2026. `[standard]`
# привозит `websockets`, `uvloop`, `httptools`, `watchfiles`, `python-dotenv` и
# `h11`; `anyio` приезжает с `watchfiles`. Закрепляются все — по тому же
# доводу, что и у Twisted выше (№794): назвать один верхний пакет значило бы
# отдать хвост на усмотрение резолвера. `typing_extensions` своей строки здесь
# не получает: он УЖЕ закреплён выше на 4.15.0, и это ограничение uvicorn
# устраивает — поднимать его заодно значило бы менять не свой пин.
uvicorn==0.52.4
anyio==4.15.1
h11==0.16.0
httptools==0.8.0
python-dotenv==1.2.3
uvloop==0.22.1
watchfiles==1.2.0
websockets==17.1

# `pytest-asyncio` — не украшение, а условие того, что пробы веб-сокета
# ВЫПОЛНЯЮТСЯ. Без него `async def`-пробы с `@pytest.mark.asyncio` падают все
# три: «async def functions are not natively supported» (замерено 06.09.2026,
# `-p no:asyncio` → 3 failed; с плагином → 3 passed). Соседи по инструменту
# (`pytest`, `pytest-django`) закреплены здесь же.
pytest-asyncio==1.4.0

# Боевой ASGI-запуск (Plane №830): `gunicorn` держит процессы, воркер uvicorn
# даёт протокол. Версия снята разрешателем (`pip download gunicorn --no-deps`),
# а не придумана; обязательных зависимостей у пакета нет — всё прочее у него в
# `extra`, и тянуть их незачем.
gunicorn==26.2.0
# Воркер отдельным пакетом (Plane №888): `uvicorn.workers` объявлен
# устаревшим самим upstream. Обязательные зависимости пакета — `uvicorn>=0.36`
# и `gunicorn>=21` — сходятся с пинами выше.
uvicorn-worker==0.4.0
