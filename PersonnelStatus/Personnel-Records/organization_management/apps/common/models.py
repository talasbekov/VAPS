"""Моделей у `common` больше нет.

🔴 СТАРЫЙ КАТАЛОГ РОЛЕЙ СНЕСЁН (Plane №352, Ш-6).

Здесь жили `Role`, `Permission`, `RolePermission` и `UserRole` — портальная
система прав: одна роль на человека (`OneToOneField`), область одним
подразделением, признаки вроде `is_seconded` и `overview_at_department`. Она
была вторым каталогом рядом с каталогом раздела ОМ и не выражала того, что
просил заказчик: несколько ролей на человека, у каждой своя область.

Переезд шёл шестью шагами и снимал читателей по одному, а не разом:
Ш-1 — видимость меню (`entities/portal-access`), Ш-2 — область портальных
экранов, Ш-3 — права штатки и вакансий (`common/rbac.py`), Ш-4 — JWT и клиент,
Ш-5 — семь ролей заказчика получили права раздела, Ш-6 (этот) — снос моделей,
когда читателей не осталось ни одного.

Права и роли живут в `apps/operations` (`Role`, `Permission`, `RolePermission`,
`UserRole` с областью у каждого гранта); спрашиваются через
`PermissionService` и ручку `/api/operations/my-permissions/`.
"""
