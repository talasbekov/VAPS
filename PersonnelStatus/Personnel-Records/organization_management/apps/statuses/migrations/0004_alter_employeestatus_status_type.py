"""Тип статуса перестаёт быть списком в коде и становится справочником (Plane №354).

Заказчик: «в админке добавил новый статус, там она не появилась» — про окно
планирования статуса. Показать новый тип в списке, не сняв `choices`, было бы
обманом: сервер отбил бы его на сохранении валидацией поля.

Данные не трогаются: тринадцать старых кодов остаются в строках как лежали,
мост к справочнику — поле `legacy_code` у `ops_status_types`. Длина поля
20 → 100, потому что коды справочника длиннее старых.

ОТКАТ безопасен ровно до тех пор, пока никто не завёл статус НОВОГО типа:
возврат `choices` и длины 20 обрежет такие строки. Поэтому обратная операция
здесь — та же ALTER, а не «привести данные к старому словарю»: молча портить
данные при откате хуже, чем отказаться откатываться.
"""
# Generated by Django 5.1.15 on 2026-08-31 13:23

from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('statuses', '0003_alter_employeestatus_created_by'),
    ]

    operations = [
        migrations.AlterField(
            model_name='employeestatus',
            name='status_type',
            field=models.CharField(default='in_service', max_length=100, verbose_name='Тип статуса'),
        ),
    ]
