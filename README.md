Проект **VAPS** — это веб-система для автоматизации кадрового учёта, управления организационной структурой, штатным расписанием и ежедневным расходом личного состава.

Система позволяет вести сотрудников, подразделения, штатные единицы, вакансии, статусы сотрудников, прикомандирования, отчёты, уведомления и аудит действий пользователей. Основная цель проекта — заменить ручной учёт и формирование документов единой цифровой системой с ролевым доступом, автоматическими статусами, генерацией отчётов в DOCX/XLSX/PDF и контролем изменений.

Технологически проект строится на **Django REST Framework + PostgreSQL** для backend и **Next.js / React / TypeScript** для frontend. Авторизация — **JWT** (`POST /api/token/`); регламентные задачи — идемпотентные catch-up-джобы по watermark (management-команды); **Celery/Redis и WebSocket-уведомления** — в планах последующих эпиков.

Рабочий стек живёт в `Backend/PersonnelStatus/`:

* `Personnel-Records` — Django-бэк (стенд `:8100`, settings `local_postgres`);
* `PersonalRecordFront` — Next-фронт (стенд `:3106`), включая раздел
  «Охранные мероприятия» `/security-ops/*`.

12.08.2026 из репозитория выведена параллельная ветка разработки — vite-SPA
`frontend/` и бэк `Backend/VAPS`: работаем только со старым стеком. Код остаётся
в истории git, откат — `git revert` коммита удаления.

Документация — в `obsidian-vault/` (Obsidian vault, вход `00-Index.md`):
описание продукта и модулей — `Продукт/`, карта требований — `Требования/Канон.md`,
рабочие леджеры — `<Раздел>/{Status,Changelog,Decisions,Known-Issues}.md`.

