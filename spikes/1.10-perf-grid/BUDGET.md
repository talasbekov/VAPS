# Перф-бюджет E9 — спайк 1.10 (перф-грид на целевой машине)

**Дурабельный артефакт спайка 1.10.** Продукт спайка — ЭТОТ документ (измеренный перф-бюджет), а не код. Прототип в этом каталоге — одноразовый измерительный инструмент.

> **Статус замера: путь A (железо подтверждено Bratan 2026-06-17); открыт только физический прогон Task 3.** Авторитетные числа TTI / память / p95 СНИМАЮТСЯ НА ЦЕЛЕВОМ ЖЕЛЕЗЕ 4ГБ / Firefox ~100 — это человеко-исполняемый прогон (Bratan), он НЕ выполнен в dev-сессии (AI не водит GUI Firefox на целевом железе). Аппарат замера собран и верифицирован на dev (headless Chromium); числа-бюджет ниже помечены `PENDING-target`. См. раздел «Гейт A9».

---

## 1. Что измеряем и зачем

Допущение **A9** (epics.md:268): «4ГБ/FF100 потянут грид» — вероятность *средняя*, влияние *высокое*. Митигация — этот спайк. Перф-бюджет 1.10 — заявленная замена отсутствующей UX-спеки по перфу (readiness-report:252) и прямой вход:
- **9.4** грид-компонент на TanStack Table + Virtual (epics.md:1038);
- **9.8** перф-смоук — «счётчики коммиты/DOM + тренд p95, **сверенные с бюджетом спайка 1.10**» (epics.md:1071);
- DoD-гейт E1 (epics.md:165/276): «перф-числа целевой машины зафиксированы как бюджет».

## 2. Как провести авторитетный замер (путь A, человеко-исполняемо)

1. **Собрать на dev (online):** `npm install && npm run build` → каталог `dist/` (статика, без Node-рантайма).
2. **Перенести `dist/` на целевую машину** (реальный клиентский ПК контура 4ГБ/FF~100 ИЛИ эталонная замена: 4ГБ-VM + архивный **Firefox 100 ESR**). На целевой Node НЕ нужен.
3. **Поднять статикой** (любой способ): `python3 -m http.server 8080` в `dist/`, либо nginx, либо `npm run preview` если на машине есть Node. Открыть в Firefox ~100.
4. **Слепой ввод 100 символов** (без мыши): грид авто-фокусится при загрузке (`App` ставит фокус на контейнер) — клик НЕ нужен; если фокус потерян, вернуть `Tab`'ом (не мышью). Далее только клавиатура — печатаемые символы пишутся в активную ячейку, `Enter`↓, `Tab`→, `Esc` отменяет правку ячейки. Ввести 100 символов, периодически перемещаясь.
5. **Списать с HUD** (зелёный оверлей справа сверху): TTI, p50/p95/max keydown→commit, commits/last key (должно быть 1), max commits/key, DOM-строк в гриде. Либо в консоли: `__vapsPerf.exportJson()`.
6. **Память вкладки — ВРУЧНУЮ из Firefox:** открыть `about:processes` (или `about:memory` → Measure) ПОСЛЕ ввода 100 символов, записать пик памяти вкладки. *(В Firefox нет `performance.memory` — инструментацией не снимается.)*
7. **Зафиксировать**: модель/ОЗУ машины и точную сборку Firefox (версия/ESR). Заполнить таблицу §4, пометить числа `VERIFIED-on-target`.

## 3. Методология (как меряется каждая метрика и оговорки)

| Метрика | Метод | Оговорка |
|---|---|---|
| **TTI** | `performance.now()` в момент монтирования App (≈ от navigation start). На ПРОД-сборке. | Грубый прокси «готова к вводу»; для спайка достаточно. Не `vite dev`. |
| **Память вкладки** | Firefox `about:processes` / `about:memory` ПОСЛЕ 100 символов | `performance.memory` в FF отсутствует → только ручное чтение. |
| **p95 keydown→commit** | `performance.now()` на keydown → дельта в `useLayoutEffect` (после коммита, до paint); выборка ≥100, p95 (метод: nearest-rank `floor(0.95·(n−1))`) | Playwright на FF100 невозможен (architecture.md:258) → только in-page + HUD. Тайминги — тренд, не блок-гейт (architecture.md:244). Метод занижает хвост при n<~20; при n≥100 корректно. |
| **commits/keystroke** | счётчик в `useLayoutEffect` без deps (тик на каждый коммит); commits-per-key трекается в `recordCommitTick` (на каждом коммите, вне latency-гарда — иначе async-каскад виртуализатора был бы невидим, ревью пр.1 D1) | **НЕ через `<Profiler>`** — его onRender но-оп в прод-сборке (см. §6, находка). Инвариант: edit-keystroke = 1 коммит. |
| **DOM-строк в гриде** | `querySelectorAll('[data-grid-row]').length` на HUD | Инвариант виртуализации: **≪ 1000** (десятки). Число = ⌈высота вьюпорта / `ROW_HEIGHT`28px⌉ + 2·`OVERSCAN`8, ЗАВИСИТ от высоты окна — на target легитимны ~40–46 (НЕ регрессия); инвариант — «≪1000», а не фикс. «=30». |
| **bundle initial gzip** | отчёт `vite build` | Информационно; канон-бюджет ≤300 КБ gzip (architecture.md:231). |

## 4. БЮДЖЕТ — таблица чисел (вход для 9.4 / 9.8)

| Метрика | Значение | Статус | Источник |
|---|---|---|---|
| **TTI** | _________ ms | `PENDING-target` | прогон FF100/4ГБ (§2) |
| **Память вкладки (пик)** | _________ MB | `PENDING-target` | FF `about:processes` |
| **p95 keydown→commit** | _________ ms | `PENDING-target` | HUD на FF100/4ГБ |
| **max keydown→commit** | _________ ms | `PENDING-target` | HUD на FF100/4ГБ |
| **commits / edit-keystroke** | **1** | `apparatus-verified` (Chromium, см. §5) | инвариант кода |
| **DOM-строк в гриде** | **≪ 1000** (окно+overscan; ≈30 на dev-вьюпорте, ~40–46 на target — зависит от высоты окна) | `apparatus-verified` (Chromium, см. §5) | виртуализация |
| **bundle initial gzip** | **≈ 70.7 KB** (JS 69.57 + CSS 0.52 + html 0.60) | `measured-dev` | `vite build` |

> Запас по бандлу огромный: прототип грида = ~70 КБ gzip против канон-бюджета ≤300 КБ → у полного приложения E8 остаётся ~230 КБ на остальное.

## 5. Apparatus-smoke (dev, headless Chromium) — НЕ перф-бюджет

Прогон на dev-ноутбуке через headless Chromium (`google-chrome`, режим shell), 100 keystroke (30 правок in-place + 70 со смешанной навигацией). **Chromium ≠ FF100, dev-ноут ≠ 4ГБ — тайминги ниже НЕ являются бюджетом E9.** Цель прогона — доказать, что АППАРАТ замера корректен, чтобы прогон на FF100 свёлся к механическому списыванию с HUD:

- console: **чисто** (favicon подавлен `<link rel=icon data:,>`).
- DOM-строк: **30** (не меняется со вводом) → виртуализация работает.
- edit-commit-delta: **30 / 30** → ровно 1 коммит на edit-keystroke на ПРОД-сборке.
- max commits/key: **1** на dev-Chromium — НО этот прогон сделан ДО патча детектора (ревью пр.1, D1): прежний `recordCommitLatency` обнулял `pendingTs` первым коммитом, поэтому async-коммит `scrollToIndex` в max НЕ попадал → claim «навигация не каскадит» аппаратом не доказывался. **После патча** (max трекается в `recordCommitTick` на каждом коммите) детектор способен показать >1; claim требует ПЕРЕПРОГОНА на исправленном аппарате (целевой прогон Bratan / повторный dev-смоук). Инвариант edit-keystroke=1 коммит (строкой выше, 30/30) патч не затрагивает — остаётся в силе.
- samples_n: **100**, p95: **0.5 ms**, max: **1 ms**, TTI: **~137 ms** — *(Chromium/dev — не бюджет, только подтверждение, что числа вычисляются)*.

## 6. Находки спайка

1. **`build.target: 'firefox100'` — обязателен и работает.** Дефолт Vite 7 = FF≥104; явный `firefox100` в `vite.config.ts` собирается без ошибок (Vite 7.3.5, esbuild). Без него риск белого экрана на FF~100. Это центральная техническая проверка A9 — пройдена на этапе сборки.
2. **`<Profiler onRender>` — но-оп в стандартной ПРОД-сборке react-dom.** Инвариант «1 коммит/keystroke» нельзя мерить Profiler'ом на проде (нужна profiling-сборка). Здесь коммиты считаются через `useLayoutEffect`-тик (работает везде). **Вход для 9.8:** Profiler-инварианты в гейте 9.8 живут в Vitest/dev-окружении (там Profiler работает); рантайм-замер на проде — через layout-effect/иной механизм.
3. **Память — только вручную из Firefox.** `performance.memory` в FF отсутствует; не закладываться на него в 9.8.
4. **Бандл прототипа ~70 КБ gzip** — React 19 + TanStack Virtual укладываются с большим запасом под ≤300 КБ.
5. **Детектор «commits-per-key» был слеп к навигационному каскаду — ИСПРАВЛЕНО (ревью пр.1, 2026-06-18, D1).** Прежде `maxCommitsPerKey` обновлялся только внутри latency-гарда `if (perf.pendingTs != null)` в `recordCommitLatency`; пост-paint коммит виртуализатора (`scrollToIndex` в passive `useEffect`) приходил уже после очистки `pendingTs` первым коммитом → инкремент `commitsSinceKeydown` не отражался в max. Патч: трекинг `max`/`last` вынесен в `recordCommitTick` (на КАЖДОМ коммите, гейт `keystrokes>0` отсекает mount); pre-edit/scroll-эффект → `useLayoutEffect` (каскад в окне keystroke). Верифицировано `tsc --noEmit` + `vite build` (зелёные, бандл без регрессии — 69.60 КБ gzip JS). Перепрогон headless-смоука в агент-окружении не запустился (Chrome убит лимитом ресурсов песочницы) → финальное «навигация не каскадит / каскадит N» подтверждается на исправленном аппарате при целевом прогоне Bratan. **Вход для 9.8:** перф-смоук E9 должен считать commits-per-key вне latency-логики, иначе async-коммиты невидимы.

## 7. Гейт A9 — вердикт

- **Путь A (железо подтверждено Bratan 2026-06-17).** A9 организационно разрешён — целевое железо 4ГБ/FF~100 доступно; открыт ТОЛЬКО физический прогон Task 3 (слепой ввод 100 символов + снятие памяти из `about:processes`), который AI выполнить не может (не водит GUI Firefox на целевом железе). Аппарат собран и верифицирован на dev (headless Chromium) — числа §5 предварительные и НЕ являются бюджетом.
- **Авторитетный прогон за Bratan** (путь A, §2) — на реальном клиентском ПК контура (можно совместить с 12.7, «тот же класс машины», см. 1.9) ЛИБО на эталонной замене (4ГБ-VM + FF100 ESR).
- **Предварительный вывод по A9:** риск белого экрана на FF~100 снят на этапе сборки (`firefox100`-таргет ок); виртуализация и 1-коммит-инвариант кода подтверждены. Открыто — абсолютные TTI/память/p95 на слабом железе (заполнить §4).

---
_Создано спайком 1.10, 2026-06-17. Прототип одноразовый; после того как E8/E9 заберут бюджет — каталог удаляется._
_Ревью 1.10 пр.1 (bmad-code-review, 2026-06-18): патчи D1 (детектор каскада), D2 (путь A), P1 (методология: viewport-DOM, метод p95, авто-фокус), P2 (троттлинг HUD), P3 (комментарии). Числа §4 остаются `PENDING-target` — авторитетный прогон за Bratan._
