---
baseline_commit: 8d28c41
---

# Story 11.4a: Бэк — отметка прочтения

Status: done

## Story

As a **оператор с непрочитанными уведомлениями**,
I want **эндпоинт, которым бэкенд реально записывает `read_at` на моё собственное уведомление**,
so that **11.4b сможет навесить на него мутацию UI, и счётчик непрочитанных в колокольчике (11.4) наконец начнёт гаснуть, а не расти монотонно до конца сессии**.

## Acceptance Criteria

Источник: `_bmad-output/implementation-artifacts/11-4-центр-уведомлений-ui.md:251` (передача из 11.4, Решение №1): «11.4a — бэк: отметка прочтения. Эндпоинт (форму выбирает та стори: `POST /api/notifications/{id}/read/` либо bulk `POST /api/notifications/mark-read/`), самоскоуп по `recipient=actor` (чужая строка не отмечается), запись в `test_rbac_matrix.py` (матрица fail-closed — незамапленный роут валит сьют), `@extend_schema` (иначе spectacular не эмитит тело), regen `schema.yaml` + `schema.d.ts`, плюс unread-индекс под `read_at IS NULL` — прямая незакрытая половина `deferred-work.md:495`».

**Решение по форме эндпоинта (принято при create-story):** ЕДИНИЧНЫЙ, не bulk — `POST /api/notifications/{id}/read/`. Обоснование: `11-4-центр-уведомлений-ui.md:145` уже фиксирует форму будущего потребителя — «11.4b добавит в строку кнопку «прочитано»» (per-row, не «отметить всё разом»). Bulk-эндпоинт под несуществующего потребителя был бы сочинительством сверх скоупа (урок 5.7c: «не расширяй поверхность»).

1. **AC-1 (эндпоинт существует и пишет `read_at`).** `POST /api/notifications/{id}/read/` — новый `@action(detail=True, methods=["post"])` на существующем `NotificationViewSet` (`Backend/VAPS/apps/notifications/api/views.py`), либо отдельная запись в `apps/notifications/api/urls.py` тем же паттерном `path()` (НЕ router — коллизия `api-root` уже задокументирована в файле). Given уведомление с `read_at IS NULL`, принадлежащее актору, When `POST .../read/`, Then `read_at` в БД становится не-`NULL` (текущее время через `Clock`, НЕ голый `timezone.now()` — канон проекта), ответ `200`.
2. **AC-2 (самоскоуп — чужое уведомление не отмечается).** Given уведомление `recipient != request.actor_id`, When `POST .../{id}/read/`, Then `403 PERMISSION_DENIED` (уже зарегистрированный код, реестр не трогается), `read_at` целевой строки НЕ меняется (ассертить из БД). Given `id`, которого не существует вовсе, When `POST`, Then `404 ENTITY_NOT_FOUND` (уже зарегистрированный код). Порядок проверок — сначала существование (404 для фантомного `id` любому актору), потом владение (403) — зеркало `DailySubmissionViewSet.amend` (`submissions/api/views.py:277-311`: resolve pk → 404 если нет → scope-check → мутация).
3. **AC-3 (идемпотентность — повторный вызов не взрывается).** Given уже прочитанное уведомление (`read_at` не `NULL`), When повторный `POST .../{id}/read/` тем же актором, Then `200` без ошибки, `read_at` НЕ перезаписывается новым временем (первый факт прочтения — источник истины, не последний клик; иначе "когда реально прочитано" разъезжается при двойном клике/повторном фокусе вкладки).
4. **AC-4 (RBAC-матрица — fail-closed).** Новая запись в `MATRIX` (`Backend/VAPS/apps/operations/tests/test_rbac_matrix.py`) для нового имени роута — `_AnyAuthenticated()` (зеркало существующей `"notification-list": _AnyAuthenticated()`, строки 219-223: доступ — не RBAC-право, а самоскоуп в бизнес-логике). `seed_operations.py` НЕ трогается (нет нового RBAC-кода). Пропуск этой записи валит ВЕСЬ модуль `test_rbac_matrix.py` (`test_matrix_covers_every_registered_route`) — не факультатив.
5. **AC-5 (схема эмитит тело, а не пустышку).** `@extend_schema` на новом action/view с `responses` (мини-сериализатор ответа: например `{id, read_at}` — НЕ полный `NotificationSerializer`, которому не место в write-пути с `read_only_fields = fields`). `make schema` (из `Backend/VAPS/`) → `schema.yaml` содержит путь `/api/notifications/{id}/read/` с непустым `post`. `cd frontend && npm run generate:api` → `schema.d.ts` регенерируется, `schema-check.mjs` (байт-в-байт) зелёный.
6. **AC-6 (unread-индекс — закрывает `deferred-work.md:495`).** Новая миграция `0003_notification_ix_notif_recipient_unread.py` — частичный индекс под `read_at IS NULL` на `(recipient, ...)`, зеркало именования существующего `ix_notif_recipient_recency` (миграция 0002). EXPLAIN на запрос вида `WHERE recipient = ? AND read_at IS NULL` использует новый индекс, не seq scan (доказать тестом или явным EXPLAIN-выводом в Dev Notes, не декларативно).
7. **AC-7 (регресс нулевой).** `make gate` (из `Backend/VAPS/`) зелёный: существующие `test_notifications_read_api.py` (5.7c) не сломаны, `test_rbac_matrix.py` зелёный целиком (не только новая запись), `test_ws_notify`/консьюмеры (E11) не задеты — это чисто REST-мутация, WS-слой не трогается. `frontend`'s `npm run gate` тоже зелёный (только `schema.d.ts` меняется аддитивно — новый путь, существующие типы `notifications_list` не трогаются).

## Tasks / Subtasks

- [x] Task 1 — Селектор/сервис отметки прочтения (`Backend/VAPS/apps/notifications/selectors.py`, `services.py`, MOD) (AC: 1, 2, 3)
  - [x] `NotificationSelector` получает метод резолва по `id` (например `by_id(notification_id)` — зеркало `DailySubmissionSelector.by_id`, `submissions/selectors.py`) — простой `Notification.objects.filter(id=...).first()`, без самоскоупа внутри (скоуп проверяется отдельно во view, зеркало `amend`).
  - [x] Сервисная функция `mark_read(*, notification_id, actor_id)` в `services.py` (рядом с существующим `notify()`): резолвит через селектор → `DomainError("ENTITY_NOT_FOUND", 404, ...)` если `None` → `if obj.recipient != actor_id: raise DomainError("PERMISSION_DENIED", 403, detail={"notification_id": str(notification_id)})` → если `obj.read_at is not None: return obj` (AC-3, идемпотентность, ранний возврат БЕЗ записи) → иначе `obj.read_at = Clock.now(); obj.save(update_fields=["read_at"]); return obj`.
  - [x] Использовать `Clock` (проектный канон времени, НЕ `django.utils.timezone.now()` напрямую) — сверить точный импорт с соседним использованием в `apps/notifications/services.py` или `apps/core` (грепнуть `from apps.core.clock import Clock`-подобный импорт в файлах, уже пишущих временные метки в этом приложении).
- [x] Task 2 — View + маршрут + схема (`Backend/VAPS/apps/notifications/api/views.py`, `serializers.py`, `urls.py`, MOD) (AC: 1, 2, 5)
  - [x] Минимальный сериализатор ответа (`NotificationMarkReadResponseSerializer` или `inline_serializer`, поля `id`/`read_at`) — НЕ переиспользовать `NotificationSerializer` (весь `fields` там `read_only_fields`, семантически неверно для write-ответа).
  - [x] `@extend_schema(responses={200: <response-serializer>})` + `@action(detail=True, methods=["post"], url_path="read")` на `NotificationViewSet` — добавить `"post"` в `http_method_names` (сейчас `["get","head","options"]`, `views.py:37`) ТОЛЬКО для этого action; если `@action`-механизм DRF ViewSet конфликтует с ручным `path()`-роутингом (`urls.py` не использует router — см. Dev Notes), явно проверить это первым делом red-пробой, ДО написания остального Task 2, и при конфликте перейти на отдельный `path("<int:pk>/read/", NotificationViewSet.as_view({"post": "mark_read"}), name="notification-mark-read")` записью в `urls.py` (зеркало текущего единственного роута `notification-list`).
  - [x] Тело action/метода: вызывает `mark_read(notification_id=pk, actor_id=request.actor_id)` (сервис из Task 1), сериализует, `Response(..., status=200)`. Не забыть `initial()`-гард (`views.py:39-49`) уже покрывает "аноним → 403" для ЛЮБОГО метода этого ViewSet — новый action его наследует бесплатно, но подтвердить тестом (AC-4/поведенческая матрица), не декларативно.
  - [x] `urls.py`: если остался на `@action`, ничего менять не нужно (DRF сам матчит `url_name`/`url_path` через `.as_view({...})` c явным методом — свериться с фактическим DRF-механизмом для non-router `path()` + `@action`, это НЕ стандартная комбинация, проверить руками до коммита).
- [x] Task 3 — RBAC-матрица (`Backend/VAPS/apps/operations/tests/test_rbac_matrix.py`, MOD) (AC: 4)
  - [x] Новая запись `"notification-mark-read": _AnyAuthenticated()` (или фактическое имя `name=` роута из Task 2) рядом с существующей `"notification-list"` (строки 219-223), тот же стиль комментария (почему не RBAC-код).
  - [x] Если использован `_MethodGate` вместо `_AnyAuthenticated` (маловероятно — `_AnyAuthenticated` не зависит от метода), сверить, что набор `methods` точно совпадает с фактически обслуживаемыми (`test_method_gates_cover_exactly_served_methods`).
- [x] Task 4 — Unread-индекс (миграция) (`Backend/VAPS/apps/notifications/models.py`, `migrations/0003_*.py`, NEW) (AC: 6)
  - [x] `models.Index(fields=["recipient"], condition=models.Q(read_at__isnull=True), name="ix_notif_recipient_unread")` — частичный индекс (Postgres `WHERE read_at IS NULL`), зеркало существующего `ix_notif_recipient_recency` по месту объявления (`class Meta: indexes = [...]`) и по стилю именования.
  - [x] `manage.py makemigrations notifications` → `0003_notification_ix_notif_recipient_unread.py` (или ручной файл, если авто-генерация даёт неканоничное имя — сверить с 0002 стилистически).
  - [x] Подтвердить индекс используется: `EXPLAIN` на `Notification.objects.filter(recipient=X, read_at__isnull=True)` в тесте или ручном прогоне против реальной Postgres (SQLite/тестовый раннер может не поддерживать partial index EXPLAIN одинаково — задокументировать метод проверки в Dev Notes).
- [x] Task 5 — Тесты (`Backend/VAPS/apps/notifications/tests/test_notifications_mark_read_api.py`, NEW) (AC: 1, 2, 3, 7)
  - [x] `test_mark_read_sets_read_at` — `read_at` был `NULL`, после `POST` не `NULL`, значение из `Clock` (мокнутого в тесте, зеркало паттерна остальных тестов проекта).
  - [x] `test_mark_read_is_idempotent` — второй `POST` не меняет уже выставленный `read_at` (ассерт равенства снятого значения, не просто "нет ошибки").
  - [x] `test_mark_read_rejects_foreign_notification` — актор Б не может отметить уведомление актора А, ассерт `403` И `read_at` в БД не изменился у чужой строки (не только код ответа — реальный побочный эффект, урок памяти проекта про DB-персистентность).
  - [x] `test_mark_read_404_for_missing_id` — несуществующий `id` → `404`, для ЛЮБОГО актора (не только владельца — существование само по себе не течёт, но и не даёт 403 вместо 404).
  - [x] `test_mark_read_requires_authentication` — аноним → `403` (зеркало `initial()`-гарда), явный live-тест поверх RBAC-поведенческой матрицы (Task 3 уже даёт параметризованный прогон по всем ролям — этот тест может быть избыточен, если матрица его покрывает: явно решить и задокументировать, не дублировать вслепую).
- [x] Task 6 — Схема и регресс (AC: 5, 7)
  - [x] `make schema` (из `Backend/VAPS/`) — regen `schema.yaml`.
  - [x] `cd frontend && npm run generate:api` — regen `schema.d.ts`.
  - [x] `make gate` (из `Backend/VAPS/`) — зелёный целиком.
  - [x] `npm run gate` (из `frontend/`) — зелёный целиком (schema-check байт-в-байт, `notifications_list` тип не задет — только аддитивный новый путь).

## Dev Notes

- **Форма эндпоинта — единичная, не bulk (решено при create-story).** `11-4-центр-уведомлений-ui.md:145` уже фиксирует будущий UI-потребитель: «11.4b добавит в строку кнопку «прочитано»» — per-row действие. Bulk-`mark-all` эндпоинт сегодня не имеет потребителя ни в 11.4, ни в 11.4b — было бы расширением поверхности сверх текущего скоупа (урок 5.7c: «`read_at`/mark-read/unread-фильтр — вне скоупа... не расширяй поверхность»). Если продукту позже понадобится «отметить всё» — отдельная стори с явным потребителем.
- **Доступ — самоскоуп, НЕ новый RBAC-код.** Существующий `NotificationViewSet` уже задокументировал этот принцип для GET (`views.py`, докстринг: «Personal feed: any authenticated actor reads ONLY their own notifications»). Мутация наследует ту же философию на уровне объекта: `recipient == actor_id` — единственный контроль доступа, не право из `seed_operations.ROLE_PERMISSIONS`. Зеркало Решения Q1 из 5.7c.
- **Идемпотентность — первый факт прочтения побеждает, не последний.** AC-3 намеренно НЕ перезаписывает `read_at` при повторном вызове. Если бы перезаписывал — двойной клик или повторный фокус вкладки после 11.4b's мутации молча искажал бы момент "когда реально прочитано" (потенциально важно для будущей аналитики/аудита, хотя сегодня это не читается нигде явно — принцип "не удивлять" важнее отсутствия текущего потребителя).
- **Порядок 404-перед-403 — зеркало `amend`, не изобретение.** `submissions/api/views.py:277-311` (`DailySubmissionViewSet.amend`) уже установил паттерн «resolve pk → 404 если нет → scope-check → мутация» для похожей формы (единичный ресурс + владение). 11.4a копирует форму, не скоуп-механизм (там `ensure_division_scope`/RBAC-право, здесь голое равенство `recipient == actor_id` — принципиально разные проверки, тот же порядок операций).
- **`@action` на non-router `path()`-ViewSet — единственная реальная техническая неопределённость этой стори.** `apps/notifications/api/urls.py` НЕ использует `DefaultRouter` (осознанно, docstring: коллизия `api-root` с `list` на `^$`). Стандартный `@action(detail=True, methods=["post"])` на ViewSet предполагает роутер, который сам генерирует url для action по `url_path`/`url_name`. При ручном `path()`-роутинге нужно либо (а) явно замапить `path("<int:pk>/read/", NotificationViewSet.as_view({"post": "mark_read"}), name="notification-mark-read")` БЕЗ `@action`-декоратора (обычный метод ViewSet), либо (б) подтвердить, что `@action` + `.as_view({"post": "read"})` действительно резолвится DRF без роутера. **Первым шагом Task 2 — red-проба этого механизма** (написать минимальный вызов, увидеть, резолвится ли URL), прежде чем достраивать остальное — иначе риск часов на отладку несуществующей комбинации.
- **RBAC-матрица требует `callback.cls`/`callback.actions` присутствия для интроспекции** (`test_rbac_matrix.py` `_served_routes`, walks live URL resolver). И `ViewSet.as_view({...})`, и `@action`-based резолвинг ДОЛЖНЫ давать `callback.cls == NotificationViewSet` — подтвердить это для выбранного варианта Task 2 явным прогоном, не полагаться на аналогию с существующим `notification-list`.
- **Не трогать `apps/notifications/consumers.py`/`routing.py`/`groups.py` (WS-слой, Epic 11).** Эта стори — чисто REST-мутация на существующей модели; WS-доставка уведомлений (11.1-11.3) не подписана на `read_at` и не должна быть задета.
- **`Clock`, не `timezone.now()` напрямую** — проектный канон времени (см. `1-3-clock-сервис-и-watermark.md`), сверить точный импорт-путь по соседним файлам этого приложения перед написанием `mark_read`.
- **Реализация: `@action` конфликтовал с non-router `path()` — выбран вариант (б).** Red-проба (Task 2) подтвердила: `@action`-декоратор без `DefaultRouter` не собирает URL сам по себе. Решение — обычный метод `mark_read` на `NotificationViewSet` (без `@action`), замаплен явно вторым `path("<int:pk>/read/", NotificationViewSet.as_view({"post": "mark_read"}), name="notification-mark-read")` рядом с существующим `{"get": "list"}` — тот же паттерн, что уже был в файле.
- **Найденный и исправленный регресс: класс-широкий `http_method_names` vs `initial()`-гард.** Добавление `"post"` в `http_method_names` (нужно, чтобы POST вообще не 405-ился на уровне Django `dispatch`) сломало бы `initial()`'s раннюю проверку `request.method.lower() not in self.http_method_names` — она стала бы ложно пропускать auth-гейт для POST даже на LIST-роуте (который `"post"` вообще не мапит), превращая ожидаемый 405 в вводящий в заблуждение 403 для анонима. Поймано существующим тестом `test_write_verbs_not_allowed_for_anonymous[post]` (5.7c). Исправлено: гейт теперь проверяет `self.action is None` (per-URL, из фактического `{"method": "name"}`-маппинга ЭТОГО роута), а не класс-широкий список методов.

### References

- [Source: _bmad-output/implementation-artifacts/11-4-центр-уведомлений-ui.md:145, :251, :280-281] — передача скоупа из 11.4, форма будущего UI-потребителя.
- [Source: _bmad-output/implementation-artifacts/5-7c-api-уведомлений.md] — Решение Q1 (self-scope, не RBAC-код), паттерн read-API, precedent теста.
- [Source: _bmad-output/implementation-artifacts/deferred-work.md:495] — unread-индекс, точная незакрытая половина.
- [Source: Backend/VAPS/apps/notifications/models.py:29, :39-42, :47-50] — `read_at`, существующий `ix_notif_recipient_recency` индекс (миграция 0002), докстринг-предвидение этой стори.
- [Source: Backend/VAPS/apps/notifications/api/views.py:1-11, :24-57] — `NotificationViewSet`, `http_method_names`, self-scope докстринг, `initial()`-гард.
- [Source: Backend/VAPS/apps/notifications/api/serializers.py:8-22] — `read_only_fields = fields`, почему нужен отдельный write-сериализатор.
- [Source: Backend/VAPS/apps/notifications/api/urls.py] — плоский `path()`, обоснование против router (коллизия `api-root`).
- [Source: Backend/VAPS/apps/operations/submissions/api/views.py:277-311] — `DailySubmissionViewSet.amend`, структурный precedent (404→scope-check→мутация).
- [Source: Backend/VAPS/apps/operations/tests/test_rbac_matrix.py:219-223, :283-343, :357-365] — существующая запись `notification-list`, механика fail-closed, per-method полнота.
- [Source: Backend/VAPS/apps/core/exceptions.py:14-38] — `DomainError`, зарегистрированные коды `ENTITY_NOT_FOUND`/`PERMISSION_DENIED`.
- [Source: Backend/VAPS/apps/operations/submissions/selectors.py:117-131] — `DailySubmissionSelector.latest_for(lock=...)`, прецедент `select_for_update`-параметра на селекторе (перенесён в `NotificationSelector.by_id(lock=...)` при ревью).

## Dev Agent Record

### Context Reference

- Собрано research-агентом (полное чтение models/views/serializers/urls нотификаций, `test_rbac_matrix.py` целиком, `5-7c`, `deferred-work.md:495`, поиск precedent'а self-scope мутации по всему `apps/operations` — precedent не найден, `amend`'s структура — ближайший, но по division-scope, не actor-equality).

### Completion Notes

Реализовано по плану с двумя отклонениями, зафиксированными в Dev Notes: (1) `@action`-механизм не работает без роутера — перешёл на вариант (б), обычный метод + явный `path()`; (2) добавление `"post"` в `http_method_names` без сопутствующей правки `initial()`-гарда сломало бы 405→403 на LIST-роуте для анонима — поймано существующим тестом 5.7c, исправлено переходом на per-URL `self.action is None`.

**Ревью (3 агента, cross-model):**
- **Blind Hunter** (diff-only) поднял один потенциально высокоприоритетный вопрос — гонка в read-modify-write `mark_read` (нет `select_for_update`/`transaction.atomic()`: два конкурентных `POST` могли оба увидеть `read_at IS NULL` и оба записать, «последний» побеждает вместо «первого», в прямом противоречии с задокументированным в AC-3 инвариантом). Также поднял ложную тревогу про `recipient` как возможный FK (на самом деле `CharField`, подтверждено чтением `models.py:25` до начала имплементации) — отклонена как false positive.
- **Edge Case Hunter** (полный доступ к проекту) независимо подтвердил ту же гонку, но классифицировал её как «реальный, но безобидный» пробел — сравнил с `amendment_service.py` (там лок нужен из-за инварианта уникальности `is_current`, здесь пишется одно nullable поле без даунстрим-инварианта) — и явно написал «фикс не нужен». Также подтвердил через живой прогон всех write-глаголов на обоих роутах, что фикс `initial()`-гарда не открыл новую дыру.
- **Решение по гонке:** несмотря на разошедшиеся оценки серьёзности между двумя ревьюерами, фикс применён — `NotificationSelector.by_id` получил параметр `lock=False` (зеркало `DailySubmissionSelector.latest_for(lock=...)`), `mark_read` обёрнут в `transaction.atomic()` с `lock=True`. Обоснование: цена исправления тривиальна (мирроринг уже существующего в проекте паттерна, не новая абстракция), а исправление делает документированный в AC-3 инвариант «первый факт прочтения побеждает» истинным БЕЗ оговорки «кроме под конкурентной нагрузкой».
- **Acceptance Auditor** независимо перепрочитал код, прогнал полный `make gate` (2803 passed) и целевой набор (614 passed), нашёл ОДИН реальный пробел: AC-6 требовал EXPLAIN-доказательство использования partial-индекса «тестом или явным EXPLAIN-выводом в Dev Notes, не декларативно» — на момент ревью Dev Notes/Completion Notes были пустым шаблоном. Исправлено: EXPLAIN прогнан вручную против реальной Postgres (сид 5000 строк, 20 получателей) — `Bitmap Index Scan on ix_notif_recipient_unread` подтверждён планировщиком (см. ниже).

**EXPLAIN-доказательство AC-6** (порт 5434, own scratch Postgres, после `manage.py migrate`, 5000 сидированных строк / 20 получателей, `ANALYZE` перед EXPLAIN):
```
Bitmap Heap Scan on notifications  (cost=6.22..80.34 rows=250 width=8) (actual time=0.034..0.120 rows=250 loops=1)
  Recheck Cond: (((recipient)::text = 'explain-actor-3'::text) AND (read_at IS NULL))
  Heap Blocks: exact=71
  ->  Bitmap Index Scan on ix_notif_recipient_unread  (cost=0.00..6.16 rows=250 width=0) (actual time=0.017..0.017 rows=265 loops=1)
        Index Cond: ((recipient)::text = 'explain-actor-3'::text)
Planning Time: 0.179 ms
Execution Time: 0.145 ms
```
Планировщик выбирает `ix_notif_recipient_unread`, не seq scan — закрывает `deferred-work.md:495`'s unread-половину.

Финальный прогон после фиксов: `make gate` (Backend) — 2803 passed; `npm run gate` (frontend) — зелёный, size-gate 212.4 KB/300 KB.

### File List

- `Backend/VAPS/apps/notifications/models.py` (MOD) — партиальный индекс `ix_notif_recipient_unread`.
- `Backend/VAPS/apps/notifications/migrations/0003_notification_ix_notif_recipient_unread.py` (NEW).
- `Backend/VAPS/apps/notifications/selectors.py` (MOD) — `NotificationSelector.by_id(notification_id, lock=False)`.
- `Backend/VAPS/apps/notifications/services.py` (MOD) — `mark_read(*, notification_id, actor_id)`, `transaction.atomic()` + `select_for_update`.
- `Backend/VAPS/apps/notifications/api/serializers.py` (MOD) — `NotificationMarkReadResponseSerializer`.
- `Backend/VAPS/apps/notifications/api/views.py` (MOD) — `http_method_names` += post, `initial()`-гард на `self.action`, `mark_read` метод + `@extend_schema`.
- `Backend/VAPS/apps/notifications/api/urls.py` (MOD) — новый `path("<int:pk>/read/", ...)`.
- `Backend/VAPS/apps/notifications/tests/test_notifications_mark_read_api.py` (NEW) — 7 тестов.
- `Backend/VAPS/apps/operations/tests/test_rbac_matrix.py` (MOD) — `"notification-mark-read": _AnyAuthenticated()`.
- `Backend/VAPS/apps/audit/tests/test_audit_coverage.py` (MOD) — `"notification-mark-read": _DeferredAudit(_NOTIF)`.
- `Backend/VAPS/schema.yaml` (MOD, regen) — новый путь `/api/notifications/{id}/read/`.
- `frontend/src/shared/api/schema.d.ts` (MOD, regen) — аддитивно, `notifications_read_create`.

## Change Log

| Дата | Изменение |
|---|---|
| 2026-07-28 | Story создана (create-story) |
| 2026-07-28 | dev-story: реализация + фикс flat http_method_names/initial() регресса + фикс гонки mark_read (ревью) + EXPLAIN-доказательство AC-6 (ревью) → done |
