# Test Automation Summary — Story 8.7 (Роутер, routes.ts и layout)

Дата: 2026-07-07 · Скилл: bmad-qa-generate-e2e-tests · Модель: Claude Fable 5
(предыдущая сводка — 8.6 — в git-истории этого файла)

## Контекст

Стори 8.7 — лицо портала: полная карта `ROUTES` + `NAV_SECTIONS`, линт-ужесточение
ARCH-FE-012/014, Tailwind v3.4 + токены донора, 7 вендоренных shadcn-компонентов,
AppLayout (роль-фильтрованный сайдбар + шапка) вокруг `<Outlet/>`, разводка 6
разделов за `RequirePermission`. Браузерного раннера нет (Playwright на FF100
невозможен — канон L260) → E2E-уровень стека = vitest 4 + RTL + user-event +
MSW 2 (jsdom per-file, прецедент QA 8.5/8.6).

Dev-стори принесла 9 тестов (`AppLayout.test.tsx` — 8, + ассерт type=password в
LoginPage.test). QA-проход искал непокрытые связки поверх них.

## Найденные пробелы покрытия (auto-applied, 108 → 121 тестов)

Все закрыты новым файлом **`src/app/app-layout.qa.test.tsx`** (13 E2E через
РЕАЛЬНУЮ композицию `<Providers>` + `AppRoutes`; размещение в `app/` —
инцидент boundaries 8.6):

1. **Сайдбар никем не использовался как навигация** — тесты стори заходят на
   маршруты только напрямую (`initialEntries`), ни одного клика по `NavLink` и
   ни одного ассерта активной подсветки. Закрыто: клик «Расход дня» из Дашборда →
   `<Outlet/>` меняет контент, `aria-current="page"` переезжает за активным
   NavLink (семантика активности — jsdom-честно, вместо класса `sidebar-accent`).
2. **Карта гейтов покрыта на 2 маршрута из 6** (в стори — `/` и `/daily-expense`).
   Закрыто матрицей `it.each` по оставшимся 4: `/employees`, `/organization`
   (status.view), `/reports` (daily_report.generate), `/audit` (audit.view) —
   и deny-ветка направленная: «все коды пилота, КРОМЕ нужного» → «Доступ
   запрещён» (доказывает, что маршрут гейтится именно СВОИМ кодом, а не любым),
   allow-ветка «ровно один код» → заглушка раздела (8 тестов).
3. **Права зависли в загрузке** (Task 6: «сайдбар — скелет/пусто») — не покрыто.
   Закрыто: `delay('infinite')` на `['me']` → сайдбар без единого пункта, но
   «Выйти» из шапки доступен и работает (credential снят, /login) — logout
   не заблокирован состоянием прав.
4. **«При ошибке загрузки прав — не прятать шапку»** (Task 6, дословное
   требование стори) — не покрыто НИГДЕ (guards-тест 8.6 проверяет только текст
   ошибки на локальной обёртке). Закрыто: 500 на `['me']` через реальный
   Providers → после дефолтных ретраев (цена Д10-8.6: ~7 с) `role="alert"`
   `PERMISSIONS_ERROR_TEXT` в main, сайдбар пуст, шапка на месте (меню
   пользователя + колокольчик).
5. **Инициалы Avatar** (AC 5 «инициалы/фолбэк») — обе ветки без покрытия.
   Закрыто: dev `operator-1` → «OP» + userId в метке меню; JWT-вход
   (userId=null, ARCH-SEC-030) → «??» + «Вход по JWT».

## API-тесты (Step 2 workflow): не требуются

Стори чисто фронтовая (`Backend/**` не тронут, граница стори); API-слой
(client/errors/useApiMutation) не менялся и покрыт 8.4–8.6.

## Верификация (не вакуумность)

- `vitest run src/app/app-layout.qa.test.tsx` — **13/13 зелёные с первого
  прогона**, без правок прод-кода.
- **Мутационная проба А**: код права `/audit` подменён `audit.view` →
  `status.view` (App.tsx) → красные РОВНО два теста матрицы `/audit`
  (deny открылся чужим кодом, allow закрылся): гейт-карта пинуется дословно.
  Откачено.
- **Мутационная проба Б**: AppLayout прячет весь каркас при ошибке прав
  (`if (error !== null) return null`) → красный РОВНО тест «шапка НЕ спрятана»
  (1 failed | 12 passed): единственное покрытие этого требования Task 6
  в проекте. Откачено.
- Прод-код восстановлен байт-в-байт (обратные Edit; файлы стори не закоммичены —
  git restore недоступен, урок 8.4 учтён).
- `npm run gate` — **все 9 шагов зелёные**: deps-gate (530 пакетов),
  schema-check, tsc -b, eslint (boundaries чисты для нового файла в app/),
  lint-canon (16+8), schema-check.test, vitest **121/121**, vite build,
  size-gate. Бандл **149.4 KB gzip — не вырос** (тесты в бандл не утекли).

## Покрытие

- AC стори: 10/10 закрыты механикой (AC 1, 5, 6, 7 — E2E-тесты стори + QA;
  AC 2, 9 — lint-canon 16+8; AC 3, 4 — статические гарантии + gate; AC 8 —
  тесты LoginPage; AC 10 — сам gate). QA добавил E2E-срез для AC 5 (навигация,
  Avatar, шапка) и AC 6 (полная карта гейтов).
- Карта гейтов: **6/6 маршрутов в deny+allow** (было 2/6), deny — направленный
  («все коды, кроме нужного»).
- Состояния прав в каркасе: **загрузка/ошибка/отказ/allow — 4/4** (было 2/4).
- Suite: 14 файлов, 121 тест; QA-файл добавляет ~8 с к прогону (осознанно:
  тест цены Д10 ждёт реальные ретраи; hardcoded-sleep нет — только polling
  findBy*/waitFor).

## Файлы

- Создано: `frontend/src/app/app-layout.qa.test.tsx` (13 E2E).
- Обновлено: `_bmad-output/implementation-artifacts/tests/test-summary.md` (эта сводка).
- Продакшен-код, конфиги, gate-цепочка — не тронуты (пробы откачены байт-в-байт).

## Next Steps

- Error-тест шапки наследует цену Д10 (~7 с реальных ретраев): при донастройке
  retry для `['me']` (кандидат из ревью 8.6) поправить `timeout` в двух местах.
- Матрица гейтов завязана на H1-тексты заглушек `section-stubs.tsx` — при
  переезде заглушек в реальные экраны (E9/E10) тесты подскажут смену заголовков;
  сама механика гейтов останется валидной.
- Подсветка активного раздела пинуется через `aria-current` (семантика NavLink);
  визуальный класс `sidebar-accent` jsdom не проверяет — вид держится
  eslint-plugin-tailwindcss + ревью (Debug Log стори, проба «в»).
- Настоящие браузерные E2E (Playwright) — по-прежнему кандидат на конец E8/E9;
  релизный смоук Radix на архивном FF100 остаётся ручным (Ловушка 5 стори).
