---
baseline_commit: 745399f
---

# Story 10.6b: Метка протухшей сводки — UI

Status: done

## Story

As a **руководитель**,
I want **видеть на панели сдачи явную метку «сводка устарела», если пины сводки моего подразделения разошлись с текущими версиями детей**,
so that **я не принимаю решение по данным, которые молча протухли — тот же принцип, что серверный drift-маркер 10.3b, но для другой оси (5-11:97)**.

## Acceptance Criteria

Источник: `_bmad-output/implementation-artifacts/sprint-status.yaml:401-421` («10.6b — UI: метка протухшей сводки поверх 10.6a. ⚠️ свежесть пинов — ОТДЕЛЬНАЯ ось от цвета светофора (5-11:97), выводить одно из другого нельзя»). Также `10-6-amendment-флоу-ui.md` (Story 10.6, done) явно оставила эту метку НЕ реализованной: «метка протухшей сводки (третья треть epic-AC): `summary_freshness` существует сервисом БЕЗ HTTP-поверхности... Тянуть неоткуда → 10.6a (роут) + 10.6b (UI)» (`DaySubmissionPanel.tsx:19-27`, комментарий уже на месте в коде).

1. **AC-1 (метка видна ТОЛЬКО на STALE).** Given `GET /daily-submissions/freshness/` вернул `status: "STALE"`, Then на `DaySubmissionPanel` виден блок с текстом «Сводка устарела» (или эквивалент, словом — не только цветом, DESIGN.md:349,371). Given `status` — `"FRESH"` ИЛИ `"NOT_SUMMARY"`, Then блока нет ВООБЩЕ (зеркало `serverDrift`-паттерна 10.3b: обогащение, не первичный сигнал — рендерится только когда есть что показать).
2. **AC-2 (запрос — enrichment, тот же гейт, что серверный drift).** `useQuery({queryKey: ['summary-freshness', divisionId, businessDate], ..., enabled: divisionId !== null && dateValid && current !== null})` — ТОТ ЖЕ гейт, что `serverDriftQuery` (`DaySubmissionPanel.tsx:229`): на несданный день сводки нет смысла спрашивать. Загрузка/ошибка/сетевой обрыв → блока нет (тот же посыл, что у `serverDrift`: нет явного error-UI, просто нечего показать).
3. **AC-3 (три оси текстом, не JSON-дампом).** Given `superseded`/`missing`/`unpinned` непусты, Then блок называет КОЛИЧЕСТВО расхождений человеческим текстом (например, «N подразделений пересдали после сборки сводки» для `superseded`) — НЕ читать `division_id` как имя (у ответа `freshness` имён нет, в отличие от `serverDrift`, который резолвит `resolveName` из состава дня; тянуть имена подразделений сюда — вне скоупа, это read-only индикатор наличия расхождения, не детальный дифф).
4. **AC-4 (инвалидация на amend — урок 10.3b, не повторять баг).** `submitMutation.onSuccess` И `amendMutation.onSuccess` оба зовут `queryClient.invalidateQueries({queryKey: ['summary-freshness', divisionId, businessDate]})` — ОБЯЗАТЕЛЬНО на обоих, не только на одном (10.3b Blind Hunter поймал ИМЕННО этот класс бага: серверный drift не инвалидировался на amend, хотя amend меняет то самое состояние, от которого drift зависит; здесь тот же риск — амендмент ЭТОЙ сдачи не меняет её собственную свежесть напрямую, но инвалидация всё равно ставится защитно, зеркалом уже существующего паттерна на этой же панели).
5. **AC-5 (свежесть — ОТДЕЛЬНАЯ ось, не производная от светофора).** Компонент/модель этой стори НЕ читает `TrafficLightStatus`/`DivisionTrafficLight` ни в каком виде для расчёта метки — источник ИСКЛЮЧИТЕЛЬНО ответ `/freshness/`. Доменный инвариант `5-11:97` («свежесть пинов — ОТДЕЛЬНАЯ ось от цвета светофора») — нарушение здесь означало бы вывод одного из другого, что явно запрещено постановкой.
6. **AC-6 (чистая модель, типы из схемы).** Новый файл `frontend/src/features/daily-grid/freshness.ts` — чистые функции (без React/`apiClient`, тестируются в env `node`, зеркало `amendment.ts`/`daySubmission.ts`). Типы — ИЗ `schema.d.ts` (`components['schemas']['SummaryFreshnessResponse']`, добавлен 10.6a) — НЕ рукописное зеркало (в отличие от `amendment.ts`, где зеркало было вынужденным по историческим причинам этой стори; здесь схема свежая и полная, ARCH-FE-011 требует переиспользовать её без исключений).
7. **AC-7 (регресс нулевой, границы фич соблюдены).** `ExpenseJournalPanel`/`ExpensePeriodPanel`/`traffic-light`-фича — не тронуты (ARCH-FE-013: `features/A → features/B` запрещено, метка живёт строго в `daily-grid`, зеркало решения №1 10.3 «панель лежит ВНУТРИ features/daily-grid»). `npm run gate` зелёный (928+N тестов).

## Tasks / Subtasks

- [x] Task 1 — Чистая модель `freshness.ts` (AC-3, AC-6)
  - [x] `frontend/src/features/daily-grid/freshness.ts` (NEW): `export type SummaryFreshness = components['schemas']['SummaryFreshnessResponse']` (импорт `components` из `../../shared/api/schema`, зеркало паттерна `daySubmission.ts`/`trafficLight.ts`).
  - [x] `parseSummaryFreshness(raw: unknown): SummaryFreshness | null` — тотализирующий разбор (unknown → безопасный null при несовпадении формы, НЕ throw; зеркало `parseSubmissionList`/`parseTrafficLightTree`). Валидирует `status` ∈ `{FRESH, STALE, NOT_SUMMARY}`, `superseded`/`missing` — массивы объектов с нужными полями, `unpinned` — массив строк.
  - [x] `describeStaleness(freshness: SummaryFreshness): string[]` — человеческие строки по трём осям (например: `superseded.length` → `` `${n} подразделени${...} пересдали после сборки сводки` `` с корректным русским склонением количества; `missing.length` → `` `${n} подразделени${...} без действующей сдачи` ``; `unpinned.length` → `` `${n} новых подразделени${...} вне сводки` ``). Пустой массив, если все три оси пусты (защита от вызова на не-STALE — тот же принцип «not trusting the backend invariant blindly», что 10.3b Dev Notes про all-empty drift).
- [x] Task 2 — Тесты модели (`frontend/src/features/daily-grid/freshness.test.ts`, NEW) (AC: 3, 6)
  - [x] `parseSummaryFreshness`: валидный FRESH/STALE/NOT_SUMMARY проходит; мусорный `status` (не из трёх литералов) → `null`; отсутствующие/неверно типизированные оси → `null`; `unpinned` с не-строковым элементом → `null`.
  - [x] `describeStaleness`: по одному сценарию на каждую ось (только `superseded` непуст, только `missing`, только `unpinned`, все три сразу — три строки, а не одна слитная); склонение количества (1/2/5 — «подразделение»/«подразделения»/«подразделений», зеркало существующего склонятеля, если он уже есть в `shared/lib` — проверить перед копированием логики).
- [x] Task 3 — Запрос свежести на `DaySubmissionPanel.tsx` (AC: 1, 2, 4, 5)
  - [x] Импорт `parseSummaryFreshness`/`describeStaleness` из `./freshness`.
  - [x] `freshnessQuery = useQuery({queryKey: ['summary-freshness', divisionId, businessDate], queryFn: () => apiClient.get<unknown>(`/api/operations/daily-submissions/freshness/?division_id=${encodeURIComponent(divisionId as string)}&business_date=${encodeURIComponent(businessDate)}`), enabled: divisionId !== null && dateValid && current !== null})` — размещение РЯДОМ с `serverDriftQuery` (тот же гейт-паттерн, соседний блок кода, не разбросано по файлу).
  - [x] `const freshness = parseSummaryFreshness(freshnessQuery.data)` — производная от ответа, НЕ отдельный `useState`.
  - [x] `submitMutation.onSuccess` и `amendMutation.onSuccess`: добавить `queryClient.invalidateQueries({queryKey: ['summary-freshness', divisionId, businessDate]})` в ОБА (AC-4, урок 10.3b — не пропустить amend).
- [x] Task 4 — Рендер метки (AC: 1, 3, 5)
  - [x] Блок рендерится ТОЛЬКО при `freshness?.status === 'STALE'`, размещение — сразу под секцией «Версии за {businessDate}» (`data-testid="day-versions"`, `DaySubmissionPanel.tsx:~508`), стиль — амбер-карточка (`bg-amber-100 text-amber-800`, зеркало `serverDrift`, ARCH-FE-014 — только токенные Tailwind-классы).
  - [x] `data-testid="day-summary-stale"`, `role="alert"`, заголовок словом («Сводка устарела»), список строк из `describeStaleness(freshness)`.
  - [x] Пограничная фраза (зеркало стиля `serverDrift`'а «Экран видит только...»): явно назвать, что это метка НАЛИЧИЯ расхождения, не деталей — «Список подразделений и причин остаётся в API (10.6c/ручная сверка)» ИЛИ эквивалентный честный текст, НЕ обещающий несуществующую детализацию.
- [x] Task 5 — Интеграционные тесты (`DaySubmissionPanel.test.tsx`, MOD) (AC: 1, 2, 4, 5, 7)
  - [x] AC-1: MSW-хендлер `/freshness/` → `{status: "STALE", superseded: [...], missing: [], unpinned: []}` → блок `day-summary-stale` виден с текстом расхождения. `{status: "FRESH", ...}` → блока нет. `{status: "NOT_SUMMARY", ...}` → блока нет (большинство сдач — обычные, не сводки; это ОСНОВНОЙ путь, не край).
  - [x] AC-2: запрос НЕ уходит, пока `current === null` (несданный день) — capture-счётчик вызовов `/freshness/`, зеркало паттерна `capturePost`/аналогичного capture-GET, если уже есть в файле; иначе завести локально.
  - [x] AC-4: сдать день → amend → capture-счётчик подтверждает ДВА отдельных инвалидационных цикла (не один пропущенный) — красная проба: временно убрать инвалидацию из `amendMutation.onSuccess`, убедиться что тест падает, вернуть.
  - [x] AC-5: убедиться, что мутация ответа `traffic-light/division/` (серверный drift, YELLOW) НЕ включает `day-summary-stale` сама по себе — регресс-тест на «не путать оси» (сеять `division-traffic-light` YELLOW БЕЗ `freshness` STALE → блока свежести нет; и наоборот).
  - [x] AC-7: полный прогон файла + `npm run gate`.

## Dev Notes

- **Дом стори — `DaySubmissionPanel.tsx`, не `traffic-light`-фича.** Сама Story 10.6 (done) уже назвала точку врезки в коде: `DaySubmissionPanel.tsx:19-27` явно перечисляет метку протухшей сводки как «осознанно НЕ реализовано» и форвард-ссылается на 10.6a+10.6b. Alternative-гипотеза (метка на дереве светофора 10.4) отклонена: 400 узлов дерева ⇒ N+1 point-lookup запросов на каждый видимый узел сломал бы перф-бюджет AC-4 стори 10.4 (`TrafficLightNode.tsx` монтирует только развёрнутые узлы, но даже это — десятки одновременных фетчей без batch-роута, которого не существует). `DaySubmissionPanel` уже держит `divisionId`+`businessDate` в контексте одной сдачи — точечный запрос естественен.
- **Прямой прецедент — `serverDriftQuery` (10.3b), скопировать структуру буквально.** `DaySubmissionPanel.tsx:221-229` (гейт `enabled`) и `:656-708` (рендер только когда есть что показать, амбер-карточка, `role="alert"`, честная пограничная фраза). Freshness — тот же паттерн: «обогащение, не первичный сигнал», рендерится только на непустом/релевантном состоянии, никакого явного error-UI (ошибка чтения = «нечего показать», не блокирующий сбой).
- **Свежесть — ОТДЕЛЬНАЯ ось, доменный инвариант, не деталь реализации.** `sprint-status.yaml:420` и `DaySubmissionPanel.tsx:24-27` (комментарий уже в коде от Story 10.6): «Выводить свежесть из светофора НЕЛЬЗЯ: это отдельная ось — сводка, которая есть, не RED, а пины при этом могут быть протухшими». AC-5 — прямая тестируемая гарантия этого инварианта: два независимых MSW-сценария (YELLOW-drift без STALE-freshness и наоборот) должны рендерить РАЗНЫЕ, не связанные блоки.
- **Большинство сдач — `NOT_SUMMARY`, это норма, не край.** Только подразделения-родители со сводкой (сервис 5.11, `sources`-ключ в snapshot) когда-либо получат `FRESH`/`STALE`; обычная сдача листового подразделения — всегда `NOT_SUMMARY`. Тесты обязаны покрыть `NOT_SUMMARY` как ОСНОВНОЙ путь (большинство панелей в проде), а не только STALE как «интересный» случай.
- **`ExpenseJournalPanel`/`ExpensePeriodPanel` key-коллизия (10.5e) — урок, НЕ повторяется здесь.** Эта стори не добавляет новый sibling-компонент с собственным `key`-пропом на общем родителе — freshness-блок рендерится ВНУТРИ уже существующего `DaySubmissionPanel`, а не как отдельный монтируемый сосед. Урок 10.5e неприменим напрямую, но упомянут для полноты — если в будущем метка вынесется в отдельный компонент-соседа, применить префиксованный `key`.
- **Именование запроса.** `['summary-freshness', divisionId, businessDate]` — НЕ `['freshness', ...]` (слишком общее имя рискует коллизией queryKey с будущими фичами; `summary-freshness` однозначно указывает на 5.11-домен, зеркало специфичности `['division-traffic-light', ...]`/`['division-submissions', ...]`).

### References

- [Source: _bmad-output/implementation-artifacts/sprint-status.yaml:401-421] — постановка, разбивка 10.6→10.6a→10.6b, инвариант «свежесть — отдельная ось».
- [Source: _bmad-output/implementation-artifacts/10-6a-роут-свежести-сводки.md] — бэкенд этой стори (done, `ca1f543`): контракт `GET /daily-submissions/freshness/`, форма ответа, `NOT_SUMMARY`-литерал.
- [Source: frontend/src/features/daily-grid/DaySubmissionPanel.tsx:1-30] — шапка файла, явное форвард-упоминание 10.6a/10.6b как «осознанно не реализовано».
- [Source: frontend/src/features/daily-grid/DaySubmissionPanel.tsx:161-260] — `historyQuery`/`serverDriftQuery`/гейты `enabled` (прямой прецедент структуры).
- [Source: frontend/src/features/daily-grid/DaySubmissionPanel.tsx:656-708] — рендер `serverDrift` (прямой прецедент вёрстки/стиля метки).
- [Source: frontend/src/features/daily-grid/DaySubmissionPanel.tsx:505-530] — секция «Версии за {businessDate}», точка врезки нового блока.
- [Source: frontend/src/features/daily-grid/amendment.ts] — стиль чистой модели (типы/parse/describe), зеркало для `freshness.ts`.
- [Source: frontend/src/shared/api/schema.d.ts] — `SummaryFreshnessResponse`/`SummaryFreshnessSuperseded`/`SummaryFreshnessMissing` (добавлены 10.6a), `operations_daily_submissions_freshness_retrieve`.
- [Source: _bmad-output/planning-artifacts/architecture.md#L235-242] — ARCH-FE-010…015 (состояние через TanStack Query, схема — источник типов, границы фич, только токенные классы).
- [Source: _bmad-output/planning-artifacts/EXPERIENCE.md#L238] — цвет никогда не единственный сигнал.

## Project Context Reference

Смотри `10-6a-роут-свежести-сводки.md` (бэкенд, done) и `10-3b-серверный-drift-маркер.md` (прямой UI-прецедент того же класса «точечный enrichment-запрос поверх уже сданного дня, инвалидация на submit+amend»).

## Dev Agent Record

### Agent Model Used

claude-sonnet-5

### Debug Log References

### Completion Notes List

- `freshness.ts` — типы напрямую из `schema.d.ts` (`components['schemas']['SummaryFreshnessResponse']`), НЕ рукописное зеркало (AC-6 — сознательное отличие от `amendment.ts`, где зеркало было исторически вынужденным).
- `DaySubmissionPanel.tsx` — метка рендерится ТОЛЬКО на `status === 'STALE'` (`staleness`-производная от `freshness`), FRESH/NOT_SUMMARY/загрузка/ошибка не рисуют ничего (зеркало `serverDrift`-паттерна 10.3b). Запрос гейтится ТЕМ ЖЕ условием, что `serverDriftQuery` (`divisionId !== null && dateValid && current !== null`).
- Инвалидация `['summary-freshness', divisionId, businessDate]` добавлена в ОБА мутационных `onSuccess` (submit и amend) — AC-4, красная проба на amend-путь выполнена (временно убрана инвалидация → тест `AC-4: успешное исправление...` упал, ожидаемо; возвращена → зелёный).
- AC-5 (свежесть — отдельная ось) протестирована двумя независимыми MSW-сценариями в одном тесте на комбинацию (YELLOW-drift без STALE-freshness, и STALE-freshness без YELLOW-drift/GREEN) — оба блока рендерятся независимо, не гасят и не подменяют друг друга.
- Дом стори подтверждён кодом ДО начала работы: `DaySubmissionPanel.tsx:23-32` уже содержал форвард-ссылку на 10.6a/10.6b от Story 10.6 — альтернативная гипотеза (метка на дереве светофора 10.4) отклонена на этапе create-story по причине N+1 (без batch-роута фетч свежести на каждый видимый узел 400-элементного дерева сломал бы перф-бюджет AC-4 стори 10.4).
- Regressions: `npx vitest run src/features/daily-grid/` — 66 тестов в `DaySubmissionPanel.test.tsx` (56 существующих + 10 новых) + 17 новых в `freshness.test.ts`, все зелёные. `npm run gate` — 954 теста (было 928 до стори), tsc/eslint/lint-canon/schema-check/build/size-gate (211.5 KB gzip / 300 бюджет) все зелёные.

**Ревью (3-агентное: Blind Hunter / Edge Case Hunter / Acceptance Auditor) — 2 реальных находки, обе исправлены:**
- **Edge Case Hunter нашёл реальный баг (грамматика):** «1 подразделение пересдали после сборки сводки» — рассогласование числа сказуемого с подлежащим (ед.ч. ср.р. «подразделение» требует «пересдало», не «пересдали»). Тесты (`freshness.test.ts:80`, `DaySubmissionPanel.test.tsx:1450`) изначально ЗАКРЕПЛЯЛИ неверную строку как ожидаемую — исправлено в `freshness.ts` (согласование через `divisionWord(n) === 'подразделение' ? 'пересдало' : 'пересдали'`) и в ОБОИХ тестовых файлах. Остальные две оси (`missing`/`unpinned`) грамматически нейтральны/корректны, не затронуты.
- **Blind Hunter (без контекста) поднял 8 пунктов — 7 ложных срабатываний** (гипотетическая cancel/withdraw-мутация не существует в коде; race-condition спекуляции неверно трактуют жизненный цикл TanStack Query — Edge Case Hunter независимо подтвердил, что `enabled`-гейт и remount-паттерн `key={divisionId}-{businessDate}` на `DailyUpdatePage.tsx:587` работают идентично уже проверенному `serverDriftQuery`; отсутствие явного error-UI — установленная, документированная поза «enrichment, не первичный сигнал», та же, что у `serverDrift`; `divisionId as string` — тип `string | null` по интерфейсу пропсов, не `string | undefined`, каст безопасен под существующим `enabled`-гейтом; XSS — некорректная спекуляция, `encodeURIComponent` на месте). **1 пункт — валидная, хоть и структурно недостижимая находка:** рендер-гейт метки не дублировал all-empty-защиту `describeStaleness` (домен гарантирует STALE⇒непустая ось, но защита в стиле 10.3b добавлена превентивно — `staleness`-производная теперь явно суммирует длины трёх осей, зеркало `serverDrift`).
- Edge Case Hunter независимо проверил ВСЕ 7 пунктов своего чек-листа (гейт/remount, коллизия queryKey, форма ответа `apiClient.get`, соответствие полей бэку/схеме, склонение числительных на широкой выборке 11-14/21/25/100-104/111, невакуумность AC-4-теста, отсутствие MSW-коллизий) — багов, кроме грамматики выше, не нашёл.
- Acceptance Auditor независимо перепрогнал `npx vitest run src/features/daily-grid/` (66+17 тестов) и `npx tsc -b` (чист), красно-пробировал AC-4 amend-инвалидацию самостоятельно (независимое подтверждение) — все 7 AC подтверждены построчно с цитатами, ложных заявлений в Completion Notes не найдено.
- Финальный прогон после обоих фиксов: `npx vitest run src/features/daily-grid/` — 345 тестов (18 файлов), `npm run gate` — 954 теста, tsc чист.

### File List

- `frontend/src/features/daily-grid/freshness.ts` (NEW) — `SummaryFreshness` (тип из схемы), `parseSummaryFreshness`, `describeStaleness`.
- `frontend/src/features/daily-grid/freshness.test.ts` (NEW) — 17 тестов.
- `frontend/src/features/daily-grid/DaySubmissionPanel.tsx` (MOD) — `freshnessQuery`, `staleness`-производная, рендер метки, инвалидация в `mutation.onSuccess`/`amendMutation.onSuccess`.
- `frontend/src/features/daily-grid/DaySubmissionPanel.test.tsx` (MOD) — 10 новых тестов (AC-1, AC-2, AC-3, AC-4×2, AC-5×2, AC-2/AC-8).
- `frontend/src/shared/api/testing/handlers.ts` (MOD) — дефолтный MSW-хендлер `/daily-submissions/freshness/` (NOT_SUMMARY).

## Change Log
