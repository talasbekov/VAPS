# Ретроспектива Epic 4 — «Аудит: каждое решение восстановимо»

**Дата:** 2026-06-29 · **Owner:** Bratan · **Фасилитация:** Dev (bmad-retrospective, Opus 4.8, соло-режим — без оценок времени)

## Сводка эпика

- **Стори:** 7 кодовых (4.1–4.7) — **все `done`** (100%).
- **Доставлено:** FR-36 (append-only аудит: actor/action/target/before-after JSONB/request_id/IP/created_at) + AR-9 (audit-coverage).
- **Качество:** `make gate` зелёный — **1381 passed**, makemigrations пуст, ruff чист. Регрессия нулевая по всему эпику.
- **Стори-карта:** 4.1 модель `AuditLog` · 4.2 append-only на БД (REVOKE+триггер) · 4.3 `record()`+request_id-middleware · 4.4 аудит мутаций статусов (образец) · 4.5 read-API чтения аудита · 4.6 audit-coverage CI-страж (AR-9) · 4.7 аудит увольнения.

## ✅ Успехи

1. **Follow-through из ретро E3 — 4/4 action items закрыты:**
   - AI#1 механизм аудита → **синхронный-на-мутации** (гипотеза ретро подтвердилась: request_id/IP request-scoped, beat-catch-up не реконструирует) — впаян в 4.4.
   - AI#2 Clock-эншафорс времени → `record()` пишет `created_at=Clock.now()` (не `auto_now`/`timezone.now()`); тесты под `clock.override`.
   - AI#3 append-only на БД → 4.2 (REVOKE UPDATE/DELETE + триггер, ARCH-SEC-032); транзакционный тест в гейте.
   - AI#4 сверка AC↔реестр + ARCH-004-флаг на create-story → применялось (growth_rule реестра; 4.7 create-story заранее флагнул core↛operations).
2. **Единая точка записи + AST-бан** (`record()`/`record_many()`, бан `import audit.models` — `test_audit_write_boundary`): чистый сеам. 4.4 стал **образцом интеграции для будущих эпиков** — 4.7 (увольнение) переиспользовал дословно.
3. **Closed-world living-registry** (4.6 Фасет B): руками-поддерживаемый литерал `_STORY_4_4_ACTIONS` заменён source-derived AST-сканом `record()`/`record_many()` по `apps/**` → `emitted ⊆ audit-events.yaml`. Реальные зубы; зеркало RBAC-матрицы 2.9.
4. **Адверсариальные 3-слойные ревью работали В ОБЕ СТОРОНЫ:** ловили реальное (4.6 — scope creep + неверный учёт «+5»; 4.7 — `reason` не в событии, непокрытый откат частичной мутации) И корректно опровергали ложняки (4.7 Blind-High `_dismiss_core→None` и Med entity_id-mismatch — оба сняты repo-aware слоями + верификацией). Триаж-дисциплина «verify before accept» сработала.
5. **Превентивный контекст-инжиниринг:** stale-instance gotcha в 4.7 (рефетч через `select_for_update`) пойман на create-story-анализе ДО кода, не в ревью.

## ⚠️ Уроки (системные, без обвинений)

1. **[ГЛАВНЫЙ — повтор E3-урока №1] Спек-дрейф продолжается.** **4.7 НЕТ в epics.md** — определение пришло из решения 4.4 №6=B. 4.6 пришлось **реинтерпретировать** AR-9 (буквальный «каждая мутирующая вьюха → assert AuditLog» нереализуем: аудит на сервис-уровне, 0/18 роутов аудируются, REST=E10). Эпик-AC — досборочные, расходятся с нейл-даун реальностью. → **Привычка (AI#2):** стори из decision/defer → бэкфилл epics.md или явная пометка источника.
2. **[повтор E3-урока №4] Same-model review caveat.** Все E4-ревью — Opus 4.8 (имплементатор). Для 4.7 имплементатор=ревьюер В ОДНОЙ СЕССИИ. Слои всё равно нашли реальное, но фреш-LLM / `/code-review ultra` усилил бы. → AI#3.
3. **Stale-instance gotcha (новый паттерн):** при снапшоте ПОСЛЕ кросс-сервисного вызова, который рефетчит сущность (`select_for_update`), брать ВОЗВРАТ/`refresh_from_db`, не переданный инстанс.
4. **Композитное событие — переносимый паттерн:** `SECONDMENT_RETURNED`(4.4) → `EMPLOYEE_DISMISSED`(4.7): «одно событие на оркестраторе, ноль пер-row двоений (прямой `.save()`), счётчики в `new_value`» перенёсся чисто.

## 🧾 Технический долг → E5+

- **E4-ревью defer'ы** (`deferred-work.md`): 4.6 forward-протекция Фасета B (не-литеральные `action=`/алиас/raw-insert невидимы — задокументирован инвариант, не усилен); 4.6 Фасет A слеп к не-DRF/admin мутациям (`cls is None`); 4.6 анти-вакуум `>=11` нулевой запас; 4.6 `_emitted_actions` без обработки SyntaxError; 4.7 (counts>1 + спанящий-leg секондмент не покрыты; асимметрия `statuses_truncated` vs `statuses_closed`; `before` из in-memory).
- **4.4 defer'ы:** bulk-summary `created_at` микро-сдвиг; сырой целочисленный PK в `new_value` (хрупко при миграции PK→UUID); `update_status` value-diff семантика → E10/4.5.
- **Человеко-хвосты (путь A, без дедлайна):** 1.11 (реальная прод-выгрузка донора), 3.13 (реальный OS-clock контура) — рунбуки готовы.

## 🔜 E5 «Сдача дня» — зависимости и готовность

- E5 (5.1–5.11): 5.1 вход оператора (внешний Auth/JWT-middleware) · 5.2 `DailySubmission` · 5.3 сервис сдачи дня · 5.4 amendment-flow · 5.5 светофор · 5.7 notifications backend · **5.9 аудит сдач** · 5.10 property-иммутабельность снапшота · 5.11 фрактальная сводка.
- **Аудит-инфра E4 переиспользуема:** `DAILY_SUBMISSION_*` коды уже forward-seeded в `audit-events.yaml`; 5.9 = новый потребитель `record()`. Фундамента менять не нужно.
- **Новая зависимость:** 5.1 вводит внешний Auth как источник `actor`. Threading готов (X-User-Id сегодня → JWT позже, `user_context`/PermissionService не меняются).
- **Сеам catch-up (3.12 `EFFECT_MATERIALIZERS`) всё ещё пуст** — ждёт 5.7 notifications / auto-return (как предсказала ретро E3). Не блокер.
- **Значимое открытие:** нет фундаментального изменения плана E5. Аудит-сеам стабилен и переиспользуем; основной риск E5 — внешний Auth (5.1, спайк готовности уже в AC стори).

## 📝 Action Items (все подтверждены Bratan; owner — Bratan)

1. **[CRIT/критический путь] Закоммитить весь E4-фронт** — ревью 4.6 (докстринг-патч + доки) + полная 4.7 (код/тесты/реестр) + ревью 4.7 (4 патча) + bmad-трекинг. Рабочее дерево большое; до старта E5. **Решение Bratan: сделать сразу после сохранения ретро.**
2. **[ПРОЦЕСС] Стори из решения/дефера → бэкфилл эпика.** Когда стори рождается из decision/defer (как 4.7 из 4.4 №6=B), а не из epics.md — дописать её в epics.md ЛИБО явно пометить «вне эпик-AC, источник — решение X». Прямое продолжение E3-урока №1. **Критерий:** применяется с первой такой стори E5.
3. **[ПРОЦЕСС] Фреш-LLM / `/code-review ultra` для ключевых стори E5** — особенно где имплементатор=ревьюер в одной сессии. **Критерий:** хотя бы для самых рискованных E5-стори (5.3 сервис сдачи, 5.10 property).
4. **[ARCH] Проверить/поправить `ARCH-DATA-022`** — E3 AI#1 требовал сверить «аудит среди beat-материализуемых эффектов» (оказался синхронным). Если в architecture.md ещё числится beat-материализуемым — поправить. **Критерий:** ARCH-DATA-022 отражает синхронный-на-мутации аудит.

## 🚧 Критический путь (до старта E5)

1. **Коммит E4-фронта** (AI#1) — сейчас, после ретро.
2. **epic-4 → done** — флипнуть (все 7 стори done).
3. Человеко-хвосты (1.11 прод-выгрузка, 3.13 OS-clock) — путь A, НЕ блокируют E5.

## Готовность

- **Тесты/качество:** E4 зелёный (1381 passed), все стори с RED-эквивалентом и behavioral-покрытием. AST-бан + closed-world + append-only-на-БД доказаны на гейте.
- **Деплой/стейкхолдеры:** N/A на этой фазе (пилот — E12; parallel-run — с конца E6).
- **Технич. здоровье:** аудит-сеам (единая точка записи) стабилен и переиспользуем; долг каталогизирован в `deferred-work.md`, адресован в E5/E10/E12.
- **⚠️ Незакоммичено:** весь E4-фронт в рабочем дереве — критический путь #1 (AI#1).
- **Вердикт:** E4 функционально и формально завершён (все стори done); единственный хвост — коммит.

## Решения сессии

- Action items: все 4 приняты (Bratan).
- Коммит E4-фронта: сейчас, после ретро (Bratan).
- `epic-4` → `done`; `epic-4-retrospective` → `done`.
- E5 стартует без planning-review (значимых открытий, меняющих план, нет — аудит-инфра переиспользуема).
