---
baseline_commit: 030a9487ae8f63089702d69f0cf5bf1d8a98350a
---

# Story 16.3b: Конфликт-детектор — двойное назначение и Отдых (FR-25, часть 2/4)

Status: done

## Story

As a **планировщик Расстановки**,
I want **видеть, что сотрудник уже назначен в другое место или ещё отдыхает после дежурства**,
so that **черновик перед согласованием несёт видимые предупреждения, а не тихие противоречия (FR-25/FR-11)**.

## Scope Decision (найдено при create-story)

- **Часть 2/4 расщепления Story 16.3.** 16.3a (done) добавила `SecurityEvent.starts_at`/`ends_at` — пререквизит для ЛЮБОЙ интервальной проверки. Эта часть — только ДВЕ проверки из заголовка Story 16.3 (двойное назначение + Отдых), НЕ «несоответствие Посту» (16.3c) и НЕ «перегрузка» (16.3d, заблокирована открытым продуктовым вопросом FR-32 §8).
- **Прямой прецедент — `validate_duty_plan()` (`apps/operations/duties/services.py`, Story 14.11f), чей ДОКСТРИНГ ЛИЧНО называет это «territory Story 16.3»**: read-only конфликт-скан, переиспользующий `apps.operations.statuses.conflict_matrix.classify_pair()` (FR-11's гибридная hard/soft-матрица, Story 3.4) — тот же паттерн буквально переносится сюда, не изобретается заново.
- **Обе проверки — ФИКСИРОВАННО SOFT**, не через `classify_pair()`'s hard/soft-логику: `docs/registries/error-codes.yaml`'s `conflict_codes`-реестр держит `DOUBLE_ASSIGNMENT_CONFLICT`/`REST_VIOLATION_CONFLICT` ИСКЛЮЧИТЕЛЬНО под `SOFT_CONFLICT_DETECTED` — нет HARD-варианта ни для одного из двух на уровне assignment (в отличие от FR-11's status-уровневого hard-block, `OVERLAPPING_HARD_STATUS`, 422, — другой механизм, другой уровень, НЕ вызывается здесь). Обобщённая проверка «пересечение с ЛЮБЫМ hard-block статусом» (`UNAVAILABLE_STATUS_CONFLICT`/`OVERLAPPING_HARD_STATUS`) — шире заголовка этой стори, НЕ строится здесь (не изобретается без явного запроса).
- **Новое поле `PlacementAssignment.conflict_codes`** (JSONField, список строк) — `conflict_severity` (16.1) сам по себе не несёт, КАКОЙ конфликт сработал, только severity; `SOFT_CONFLICT_DETECTED`'s error-response несёт `conflicts[]` (`docs/registries/error-codes.yaml`) — нужна структура для будущей API-стори (16.8), не изобретается заново каждой последующей проверкой (16.3c/16.3d допишут в ТОТ ЖЕ список, не создадут параллельное хранилище).
- **Гранулярность — событийная** (16.3a's явно флагованное v1-допущение): двойное назначение проверяется как «тот же `employee_id` в ДРУГОМ `AssignmentVersion.is_current=True` строке, чьё `SecurityEvent`-окно пересекается с этим». Оба события ДОЛЖНЫ иметь заполненные `starts_at`/`ends_at` — если у ЛЮБОГО из двух пара не заполнена, пересечение НЕОПРЕДЕЛИМО и пропускается (не считается ни конфликтом, ни его отсутствием — не изобретается предположение без данных).
- **Отдых — против `EmployeeStatus(status_type_code="REST_AFTER_DUTY")`**, переиспользуя ТОТ ЖЕ `_to_date_range()`-хелпер, что `validate_duty_plan()` (`apps/operations/duties/services.py`) — не дублируется (подтверждённая, ревью-закалённая логика локальной таймзоны, дублирование рискует расхождением).
- **Read+write, НЕ блокирует.** Функция ПЕРЕЗАПИСЫВАЕТ `conflict_severity`/`conflict_codes` на каждой строке `PlacementAssignment` версии (полный пересчёт при каждом вызове, не инкрементальный накопитель) — не отклоняет/не блокирует утверждение (это Story 16.4's территория, «согласование с возвратом»).
- **Аудит — НЕ на каждый вызов** (это read+recompute функция, может дёргаться часто, напр. после каждого добавления строки в черновик планировщиком) — аудит-стори для конфликт-скана появится там, где он реально мутирует бизнес-состояние (16.4's согласование), не здесь (тот же принцип, что `validate_duty_plan()` — read-only, без аудита).

## Acceptance Criteria

1. **AC-1 (новое поле).** `PlacementAssignment.conflict_codes` — `JSONField(default=list)`.
2. **AC-2 (двойное назначение обнаруживается).** Сотрудник, назначенный в ДВУХ разных текущих версиях с пересекающимися `SecurityEvent`-окнами — обе строки получают `conflict_severity=SOFT`, `conflict_codes` содержит `DOUBLE_ASSIGNMENT_CONFLICT`.
3. **AC-3 (Отдых обнаруживается).** Сотрудник с активным (не отменённым) `EmployeeStatus(status_type_code="REST_AFTER_DUTY")`, чей календарный интервал пересекается с окном события назначения — строка получает `conflict_severity=SOFT`, `conflict_codes` содержит `REST_VIOLATION_CONFLICT`.
4. **AC-4 (оба конфликта на одной строке — накапливаются).** Строка с ОБОИМИ проблемами — `conflict_codes` содержит ОБА кода, `conflict_severity` остаётся `SOFT` (нет HARD-варианта у этих двух типов).
5. **AC-5 (нет данных о времени события — пропуск, не ложный конфликт).** Оба (текущее и/или candidate) события без `starts_at`/`ends_at` — пересечение НЕ проверяется, строка не помечается конфликтом ИЗ-ЗА отсутствия данных.
6. **AC-6 (без конфликтов — явный сброс).** Повторный вызов после устранения конфликта (например, другая версия перестала быть текущей) — `conflict_severity`/`conflict_codes` СБРАСЫВАЮТСЯ (полный пересчёт, не накопитель, см. Scope Decision).
7. **AC-7 (регресс нулевой).** `make gate` зелёный, миграция чистая.

## Out of Scope

- Несоответствие Посту (`POST_REQUIREMENT_MISMATCH_CONFLICT`) — Story 16.3c.
- Перегрузка (`WORKLOAD_EXCEEDED_CONFLICT`) — Story 16.3d, заблокирована открытым продуктовым вопросом (FR-32 §8, коэффициенты нагрузки).
- Обобщённая проверка «пересечение с ЛЮБЫМ hard-block статусом» на уровне assignment (`UNAVAILABLE_STATUS_CONFLICT`/`OVERLAPPING_HARD_STATUS`-эквивалент) — шире заголовка стори, не строится.
- Блокировка/утверждение на основе конфликтов — Story 16.4.
- Пересечение с `SecurityEventDirectAssignment` (физнаряд) — известный, задокументированный (15.9/16.1) коллизионный риск, НЕ решается здесь (физнаряд «система пассивна» по конструкции, разный уровень цикла).
- API/HTTP-обёртка — Story 16.8.

## Tasks / Subtasks

- [x] Task 1 — `PlacementAssignment.conflict_codes`-поле + миграция
- [x] Task 2 — `services.py`: `detect_placement_conflicts(version)` — двойное назначение + Отдых, полный пересчёт
- [x] Task 3 — Переиспользовать `duties.services._to_date_range()` для Отдых-проверки
- [x] Task 4 — Тесты (AC 2-6 по отдельности; отсутствие данных времени; сброс при устранении)
- [x] Task 5 — Гейт

## Dev Notes

- `apps/operations/duties/services.py::validate_duty_plan()` — буквальный образец (её докстринг явно называет это «Story 16.3's territory»), читать целиком.
- `apps/operations/statuses/conflict_matrix.py` — НЕ используется напрямую здесь (обе проверки фиксированно SOFT, `classify_pair()`'s hard-логика не нужна) — но читать для понимания, почему `REST_AFTER_DUTY` уже фиксирован как SOFT (14.8's решение), не переоткрывать.
- `apps/operations/duties/services.py::_to_date_range()` — переиспользовать импортом, НЕ дублировать (ревью-закалённая таймзон-логика).
- `apps/operations/events/models.py`'s `AssignmentVersion`/`PlacementAssignment` (16.1), `SecurityEvent.starts_at`/`ends_at` (16.3a) — все читать перед правкой.

### References

- [Source: _bmad-output/planning-artifacts/epics.md:1433] — Story 16.3 текст.
- [Source: Backend/VAPS/apps/operations/duties/services.py:362-442] — `validate_duty_plan()`, буквальный образец + `_to_date_range()`.
- [Source: docs/registries/error-codes.yaml] — `conflict_codes.DOUBLE_ASSIGNMENT_CONFLICT`/`REST_VIOLATION_CONFLICT`, оба под `SOFT_CONFLICT_DETECTED`.
- [Source: Backend/VAPS/apps/operations/statuses/conflict_matrix.py] — FR-11's hard/soft-модель, `REST_AFTER_DUTY` фиксированно SOFT (14.8).

## Dev Agent Record

### Context Reference

- Research (16.3 scoping): `validate_duty_plan()`'s докстринг лично называет отсутствие «assignments/workload/post-requirements machinery» как «Story 16.3's territory» — прямая, недвусмысленная цитата в коде, не интерпретация.

### Completion Notes

Реализовано по AC 1-7. `PlacementAssignment.conflict_codes` — `JSONField(default=list)`, миграция `0011`. `detect_placement_conflicts(version)` — полный пересчёт всех строк версии: двойное назначение — против ДРУГИХ `is_current=True`-версий того же `employee_id`, пересечение по `SecurityEvent.starts_at`/`ends_at` (оба события ДОЛЖНЫ иметь расписание — иначе пропуск, не ложный конфликт); Отдых — против `EmployeeStatus(status_type_code="REST_AFTER_DUTY", cancelled_at__isnull=True)`, календарный интервал через ПЕРЕИСПОЛЬЗОВАННЫЙ (импортом, не дублирован) `duties.services._to_date_range()`. Оба типа фиксированно `SOFT` (реестр не несёт HARD-вариант на уровне assignment). Полный пересчёт (не накопитель) — устранённый конфликт корректно очищается на следующем вызове. 9 новых тестов, все прошли с первого запуска. `make gate` — 3699 passed (было 3690, +9), 0 regressions, no drift.

**3-агентное ревью (Blind Hunter / Edge Case Hunter / Acceptance Auditor, оба агента ревью запускались дважды — независимые прогоны сошлись) — 1 HIGH найден и исправлен, остальное чисто:**
- **HIGH (Blind Hunter + Edge Case Hunter, оба независимо нашли и подтвердили живым пробником): двойное назначение ВНУТРИ одной версии структурно невидимо детектору, исправлено.** Первая реализация проверяла двойное назначение ТОЛЬКО против ДРУГИХ текущих версий (`.exclude(version_id=version.pk)`) — сотрудник, назначенный на ДВА разных поста ВНУТРИ ОДНОЙ версии (реальный, достижимый сценарий: `form_draft_placement()`, 16.2, копирует `SecurityEventDirectAssignment`-строки БЕЗ дедупликации по `employee_id`, и та модель сама явно документирует отсутствие уникальности), оставался незамеченным — обе строки получали пустой `conflict_codes`. Живой пробник Edge Case Hunter подтвердил на неисправленном коммите: `conflict_codes == []` для обеих строк. **Исправлено**: добавлена intra-version-проверка (`employee_ids.count(employee_id) > 1` внутри самой версии) — безусловная, БЕЗ требования расписания (два ряда в ОДНОЙ версии — заведомо одно и то же событие, интервал сравнивать не с чем). 1 новый регресс-тест.
- Blind Hunter (доп., не изменено): `_to_date_range()`-вызов был внутри цикла по назначениям, хотя зависит только от `event` (инвариант для всей версии) — тривиальная неэффективность, вынесена из цикла ПОПУТНО с основным фиксом (не отдельная правка).
- Acceptance Auditor (×2 независимых прогона, сошлись): все 7 AC PASS; `DOUBLE_ASSIGNMENT_CONFLICT`/`REST_VIOLATION_CONFLICT` — подтверждены исключительно SOFT в реестре; cross-app `_to_date_range()`-импорт (приватный хелпер из `duties.services`) — единственный такой прецедент в кодовой базе, оценён как «прагматичный, низкий риск» (fail-fast `ImportError`, не тихий разрыв) — не блокирует, но рекомендация: вынести в общее место, если появится третий потребитель (16.3c/16.3d — кандидаты).

`make gate` (после исправления) — **3700 passed** (было 3699, +1 регресс-тест), 0 regressions, no drift. Status → done.

### File List

- `Backend/VAPS/apps/operations/events/models.py` (modified — `PlacementAssignment.conflict_codes`)
- `Backend/VAPS/apps/operations/events/migrations/0011_placementassignment_conflict_codes.py` (new)
- `Backend/VAPS/apps/operations/events/services.py` (modified — `detect_placement_conflicts()`)
- `Backend/VAPS/apps/operations/events/tests/test_detect_placement_conflicts.py` (new)

## Change Log

| Дата | Изменение |
|---|---|
| 2026-08-01 | Story создана (create-story). Часть 2/4 расщепления Story 16.3 — только двойное назначение+Отдых. Переиспользует `validate_duty_plan()`'s паттерн буквально; обе проверки фиксированно SOFT (реестр не несёт HARD-вариант на уровне assignment). |
| 2026-08-01 | Dev-story: `detect_placement_conflicts()` + `conflict_codes`-поле. 9 новых тестов, прошли с первого запуска. `make gate` — 3699 passed. Status → review. |
| 2026-08-01 | 3-агентное ревью (×2 независимых прогона): 1 HIGH исправлен (двойное назначение ВНУТРИ одной версии было структурно невидимо детектору). 1 новый регресс-тест. `make gate` — 3700 passed, 0 regressions. Status → done. |
