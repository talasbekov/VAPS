---
baseline_commit: bde3481
---

# Story 14.11e: API — перепланирование смены дежурства

Status: review

## Story

As an **оператор с правом `duty.manage`**,
I want **`POST /api/operations/duty-plans/{plan_id}/shifts/{shift_id}/replan`**,
so that **смену дежурства можно перепланировать (частично изменить поля) через API, с автоматической отменой старой и созданием новой**.

`epics.md:1411` (буква, до разделения): «Story 14.11: API и экраны плана дежурств». Пятая из ~12 подсторий разделения.

## Scope Decision (найдено при create-story)

- **Тонкая HTTP-обёртка над `replan_duty_shift(shift, *, actor, reason, **new_fields)` (14.9b).** Донор молчит (как и для cancel, 14.9a/14.11d) — VAPS-оригинальная территория.
- **`replan_duty_shift()` НЕ требует собственного ревью-фикса на блокировку.** Её `transaction.atomic()` оборачивает вызов уже захарденной (14.11d) `cancel_duty_shift()`, которая делает `select_for_update()` ПЕРВЫМ действием внутри вложенного atomic-блока (вложенные `atomic()` на одном соединении — тот же уровень блокировки). Строка `values = {field: getattr(shift, field) ...}` читает поля СТАРОГО (пред-локового) объекта `shift`, но это безопасно: `cancel_duty_shift()` мутирует ТОЛЬКО `cancelled_at`/`cancelled_by`/`cancelled_reason` — ни одно из них не входит в `REPLANNABLE_FIELDS`.
- **URL — `POST .../{plan_id}/shifts/{shift_id}/replan`, зеркалит 14.11d's `cancel_shift`-паттерн буквально.** Тот же `isdigit()`-гард на `shift_id` (тот же класс бага, что 14.11d исправила).
- **Новый сериализатор `DutyShiftReplanSerializer` — ВСЕ поля кроме `reason` опциональны (`required=False`), НЕ переиспользует `DutyShiftCreateSerializer` (там все обязательны — семантика создания, не частичного изменения).** Эмпирически подтверждено (create-story): DRF's `PrimaryKeyRelatedField(required=False, allow_null=True)` корректно различает «поле отсутствует» (пропущено в `validated_data`) от «поле явно `null`» (присутствует как `None`) — соответствует `replan_duty_shift()`'s собственной семантике (14.9b: явный `None` снимает пост, отсутствие поля наследует старое значение).
- **`full_clean()`'s `django.core.exceptions.ValidationError` → DRF `ValidationError` — та же явная конвертация, что 14.11b's `_create_shift`.** `replan_duty_shift()` вызывает `new_shift.full_clean()` внутри, НЕ перехватывая `DjangoValidationError` сама — вьюха должна это сделать.
- **Коды ошибок — уже зарегистрированы, ничего нового.** `VALIDATION_ERROR`(400, неизвестное поле перепланирования ИЛИ пустая `reason`), `INVALID_LIFECYCLE_TRANSITION`(422, старая смена уже отменена/уже началась — переиспользуется `cancel_duty_shift()`'s гарды изнутри).
- **Ответ — НОВАЯ смена (`DutyShiftSerializer`), `201`.** Перепланирование создаёт РЕАЛЬНО новую строку `DutyShift` (не изменяет старую на месте) — `201`, тот же паттерн, что 14.11a/b's `create`, а не `200`, как 14.11c/d's state-transition/cancel-in-place (осознанный выбор, не диктуется существующим кодом).
- **Роут — `ops-duty-plan-replan-shift`.** Требует строку в `MATRIX`/`AUDIT_MATRIX` (существующий `_url_for()`-helper УЖЕ поддерживает 2-kwarg роуты, изменений не требует).
- **RBAC-строка/HTTP audit-логирование — 14.12.**

## Acceptance Criteria

1. **AC-1 (happy path — частичное изменение).** Требует `duty.manage`. Тело: `{"reason": "...", "starts_at": "...", "ends_at": "..."}` (только изменяемые поля). Успех — `201`, `DutyShiftSerializer` НОВОЙ смены; неуказанные поля (`employee_id`/`post`/`duty_type`/`duty_role_code`/`notes`) унаследованы от старой.
2. **AC-2 (старая смена отменена, новая спроецирована).** После вызова — старая `cancelled_at is not None`, её `EmployeeStatus`-записи сняты; новая смена имеет собственные `EmployeeStatus`-записи (`source_ref` по НОВОМУ pk).
3. **AC-3 (явный `null` снимает поле, отличается от отсутствия поля).** `{"reason": "...", "post": null, "starts_at": "...", "ends_at": "..."}` → новая смена без поста, ДАЖЕ если старая имела пост; отсутствие `post` в теле — пост наследуется.
4. **AC-4 (несуществующий план/смена → 404, оба независимо).**
5. **AC-5 (смена из ДРУГОГО плана в URL → 404).**
6. **AC-6 (нечисловой `shift_id` в URL → 404, не 500).**
7. **AC-7 (пустой `reason` → 400).**
8. **AC-8 (уже отменённая/уже начавшаяся старая смена → 422).**
9. **AC-9 (несовместимый `post`/`duty_type` (новой смены) → 400, старая смена НЕ отменена — транзакционный откат, переиспользование 14.9b's гарантии).**
10. **AC-10 (без `duty.manage` → 403).**
11. **AC-11 (регресс нулевой, `make gate` зелёный).**

## Out of Scope

- `validate`/`conflicts` — 14.11f/g.
- RBAC-строка/HTTP audit-логирование — 14.12.

## Tasks / Subtasks

- [x] Task 1 — `DutyShiftReplanSerializer` (AC: 1, 3, 7)
  - [x] `apps/operations/duties/api/serializers.py` — все поля опциональны, кроме `reason`
- [x] Task 2 — `@action` `replan_shift` на `DutyPlanViewSet` (AC: 1-10)
  - [x] `url_path=r"shifts/(?P<shift_id>[^/.]+)/replan"`, `isdigit()`-гард, `require_permission` → `get_object_or_404`×2 → `try/except DjangoValidationError` вокруг `replan_duty_shift(...)`
- [x] Task 3 — MATRIX/AUDIT_MATRIX-строка (AC: 10, 11)
  - [x] `ops-duty-plan-replan-shift` — `_Gate("duty.manage")`/`_DeferredAudit(_DUTY)`
- [x] Task 4 — `make schema` регенерация
- [x] Task 5 — Тесты (AC: 1-11)
  - [x] happy path (частичное+проекция), явный null vs отсутствие поля, 404×3 (план/смена/cross-plan), нечисловой shift_id→404, 400 (reason), 422×2, 400 (несовместимый post, откат старой), 403
  - [x] `make gate` зелёный, явно прогнан

## Dev Notes

- Читать `apps/operations/duties/services.py::replan_duty_shift` (14.9b, уже блокировка через вложенный `cancel_duty_shift`) и `apps/operations/duties/api/views.py::cancel_shift` (14.11d, буквальный образец URL/гард-паттерна) ПЕРЕД имплементацией.
- Эмпирически подтверждено при create-story: `PrimaryKeyRelatedField(required=False, allow_null=True)` — отсутствующее поле пропускается в `validated_data` (наследование), явный `null` присутствует как `None` (снятие) — ПОДТВЕРЖДЕНО прямым Python-тестом сериализатора, не предположение.

### References

- [Source: Backend/VAPS/apps/operations/duties/services.py::replan_duty_shift] — переиспользуемый сервис (14.9b, done).
- [Source: Backend/VAPS/apps/operations/duties/api/views.py::cancel_shift] — буквальный образец (14.11d).
- [Source: Backend/VAPS/apps/operations/duties/api/views.py::_create_shift] — `DjangoValidationError`→DRF-конвертация (14.11b).

## Dev Agent Record

### Context Reference

- Отдельный research-агент при create-story: подтверждено — `replan_duty_shift()` не требует собственного фикса на блокировку (композиция с уже захарденной `cancel_duty_shift()` достаточна); URL/гард-паттерн зеркалит 14.11d буквально; сериализатор — новый, все поля опциональны кроме `reason`; DRF's absent-vs-null различение эмпирически подтверждено прямым тестом.

### Completion Notes

Реализовано буквально по AC 1-11. `DutyShiftReplanSerializer` (все поля опциональны кроме `reason`), `@action(detail=True, methods=["post"], url_path=r"shifts/(?P<shift_id>[^/.]+)/replan")` `replan_shift` на `DutyPlanViewSet`: `require_permission` → `get_object_or_404`×2 (с `isdigit()`-гардом на `shift_id`, буквально из 14.11d) → `try/except DjangoValidationError` вокруг `replan_duty_shift(...)` (та же конвертация, что 14.11b's `_create_shift`) → `201` с новой сменой. 11 новых тестов (частичное изменение+наследование+проекция, explicit-null-vs-absent, 404×4, 400×2 (пустая причина, несовместимый post с проверкой отката старой смены), 422×2, 403), все зелёные под реальным Postgres с первой попытки; `make schema` регенерирован; `make gate` — 3329 passed (было 3308, +21), 0 regressions, no migration drift.

### File List

- `apps/operations/duties/api/serializers.py` (modified — `DutyShiftReplanSerializer`)
- `apps/operations/duties/api/views.py` (modified — `replan_shift`-action)
- `apps/operations/tests/test_rbac_matrix.py` (modified — `MATRIX`'s новая строка)
- `apps/audit/tests/test_audit_coverage.py` (modified — `AUDIT_MATRIX`'s новая строка)
- `apps/operations/duties/tests/test_duty_shift_replan_api.py` (new)
- `schema.yaml` (regenerated — `make schema`)

## Change Log

| Дата | Изменение |
|---|---|
| 2026-07-31 | Story создана (create-story). Пятая из ~12 подсторий разделения 14.11. `replan_duty_shift()` уже безопасна к конкурентности (композиция с 14.11d's захарденной `cancel_duty_shift()`). Новый `DutyShiftReplanSerializer` — все поля опциональны кроме `reason`, absent-vs-explicit-null различение эмпирически подтверждено. Ответ — `201` (новая смена — реально новая строка, не state-transition). |
| 2026-07-31 | Dev-story: `replan_shift`-action, `DutyShiftReplanSerializer`, MATRIX/AUDIT_MATRIX-строки, 11 новых тестов, все зелёные под реальным Postgres с первой попытки (уроки 14.11a-d применены проактивно — `isdigit()`-гард, `DjangoValidationError`-конвертация, cross-plan-проверка с рефрешем состояния). `make gate` — 3329 passed. Status → review. |
