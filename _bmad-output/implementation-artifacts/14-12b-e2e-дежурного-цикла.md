---
baseline_commit: 3309380
---

# Story 14.12b: E2E дежурного цикла

Status: review

## Story

As a **QA/оператор**,
I want **e2e-тест полного цикла плана дежурств (создать план → создать смену → утвердить → отменить смену → перепланировать)**,
so that **регресс всего фронтенд-потока 14.11i-l ловится автоматически, не только юнит-тестами компонентов**.

Вторая половина разделения исходного Story 14.12 (см. 14.12a's Scope Decision).

## Scope Decision (уточнено вторым research-проходом при dev-story)

- **`e2e-mock/` — РЕАЛЬНАЯ, но принадлежит несвязанному под-проекту "Smart Josparlau"** (её собственная MSW/demo-persona/scenario-машинерия — `VITE_DEMO_PERSONA`/`compose-seed.ts`, специфична этому демо, не переиспользуема как есть для duty-plans).
- **`e2e-live/` (10.10) — единственный «живой» слот, `architecture.md#L259` ограничивает 5 сценариями на весь проект.** Дежурный цикл НЕ занимает его.
- **Решение: буквальный образец — `notifications.tsx`/`day-submission.tsx` (общая e2e-harness-инфраструктура, `vite.e2e.config.ts`/`dist-e2e`/порт 4174, УЖЕ используется 7 сториями, не Smart-Josparlau-специфична).** Новый вход `e2e-harness/duty-plan.tsx` монтирует НАСТОЯЩИЙ `AppRoutes`+`Providers` (не отдельную страницу, как day-submission/traffic-light — цикл сам многостраничный: `/duty-plans` → `/duty-plans/:id`, нужна реальная `<Link>`-навигация). `features → app`-запрет линтера (ARCH-FE-013) действует ТОЛЬКО внутри `src/**/*` (`boundaries/include`) — `e2e-harness/` вне `src/`, импорт легален.
- **Сеть — `page.route`-перехват** (не MSW), тот же приём, что `day-submission.spec.ts` — реальный fetch/статусы/конверт §36.
- **Один сценарий, весь цикл, ДВЕ смены** (не одна дважды): `ShiftsTable` скрывает cancel/replan-кнопки для уже отменённой строки (14.11l) — естественный порядок цикла (создать план → смена A → утвердить → смена B → отменить A → перепланировать B) требует двух смен одновременно.

## Acceptance Criteria

1. **AC-1 (создание плана — реальная модалка).** `<dialog>`+`showModal()` в реальном chromium (не jsdom-полифилл).
2. **AC-2 (навигация список→деталь — реальный `<Link>`).**
3. **AC-3 (создание смены — реальный `datetime-local`).**
4. **AC-4 (утверждение плана — статус обновляется).**
5. **AC-5 (отмена смены — inline reason-toggle, причина обязательна, реальная disabled-семантика).**
6. **AC-6 (перепланирование — модалка предзаполнена, explicit-null снимает пост).**
7. **AC-7 (итоговое состояние — 2 отменённые строки, 1 активная).**
8. **AC-8 (регресс нулевой).** `npm run gate` (unit) зелёный; `npm run test:e2e` — новый тест зелёный, существующие 6 пре-существующих красных (expense.spec.ts×5, notifications.spec.ts×1) НЕ новые — подтверждено `git stash`-прогоном ДО этой стори (падают идентично).

## Out of Scope

- Backend-аудит — 14.12a.
- RBAC role-binding — закрыто без кода (14.12a's Scope Decision).
- `validate`/`conflicts`-UI — вне объёма 14.11l, соответственно вне e2e-цикла тоже.

## Tasks / Subtasks

- [x] Task 1 — `e2e-harness/duty-plan.tsx`/`.html` (AC: 1-3)
  - [x] Монтирует `Providers`+`MemoryRouter`+`AppRoutes`, `setCredential` (образец `notifications.tsx`)
- [x] Task 2 — `vite.e2e.config.ts` — новый rollup-вход (AC: 1-8)
- [x] Task 3 — `e2e/duty-plan-lifecycle.spec.ts` — полный цикл (AC: 1-7)
- [x] Task 4 — Гейт (AC: 8)
  - [x] `npm run gate` (unit), `npx playwright test` (полный e2e-набор, подтверждён pre-existing-статус 6 красных через `git stash`)

## Dev Agent Record

### Context Reference

### Completion Notes

Реализовано по AC 1-8. Второй, полный research-проход подтвердил `e2e-mock/` — реальную, но Smart-Josparlau-специфичную инфраструктуру (не переиспользуема); решение — общий, VAPS-собственный e2e-harness-паттерн (`vite.e2e.config.ts`/`dist-e2e`/порт 4174, буквальный образец `notifications.tsx`, 11.4), НЕ Smart Josparlau, НЕ живой-бэк-слот. Новый вход `duty-plan.tsx` монтирует настоящий `AppRoutes`+`Providers` (не изолированную страницу — цикл многостраничный). Один сценарий на весь цикл (create-plan→create-shift-A→approve→create-shift-B→cancel-A→replan-B), `page.route`-перехват (буквальный образец `day-submission.spec.ts`). Тест прошёл с первой попытки. Полный `npx playwright test`-прогон — 6 пре-существующих красных (expense.spec.ts×5, notifications.spec.ts×1), подтверждено НЕ регрессией через `git stash`-прогон ДО этой стори (падают идентично). `npm run gate` (unit) — 1047 passed, без изменений (e2e вне gate, канон Д4).

### File List

- `frontend/e2e-harness/duty-plan.tsx` (new)
- `frontend/e2e-harness/duty-plan.html` (new)
- `frontend/e2e/duty-plan-lifecycle.spec.ts` (new)
- `frontend/vite.e2e.config.ts` (modified — новый rollup-вход)

## Change Log

| Дата | Изменение |
|---|---|
| 2026-07-31 | Story-заглушка создана при разделении 14.12 (создание 14.12a). Требует полного create-story прохода (research был частичным, попутно с 14.12a) — `e2e-mock/security-event-lifecycle.spec.ts` оказался чужим (Smart Josparlau), `day-submission.spec.ts`'s harness-паттерн может быть избыточно тяжёлым для этой стори. |
| 2026-07-31 | Dev-story (автономно, без уточняющих вопросов): полный research подтвердил общий e2e-harness-паттерн (не Smart Josparlau, не live-слот). `duty-plan.tsx`/`.html`, новый rollup-вход, `duty-plan-lifecycle.spec.ts` — полный цикл, зелёный с первой попытки. `npx playwright test` — 6 пре-существующих красных подтверждены НЕ регрессией (`git stash`). Status → review. |
