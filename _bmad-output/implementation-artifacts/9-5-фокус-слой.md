---
baseline_commit: |
  bafab60 (feat(story-9.4)) на ветке claude/exciting-vaughan-3e478b. Грид 9.4
  (DailyGrid + грамматика 9.2) готов. 9.5 — фокус-слой поверх него.
context:
  - _bmad-output/planning-artifacts/epics.md (§Story 9.5: «предсказуемый фокус: Enter коммитит и идёт вниз, Tab вправо, фокус НИКОГДА не падает на body … RTL+userEvent сценарии (10–15) → все фокус-инварианты зелёные; после ConflictDialog фокус возвращается в ячейку». §9.6 валидация/конфликт-семантика — ГРАНИЦА)
  - docs/contracts/09-01-*.md (§3.2 фокус-переходы; §5 «после закрытия диалога фокус возвращается в ячейку грида»; §9 фокус не теряется)
  - frontend/src/features/daily-grid/DailyGrid.tsx (9.4 — фокус через data-active+querySelector; ФИО/флаг сейчас <span> НЕ фокусируемы → навигация к ним теряет фокус) + grammar.ts (9.2 CONFLICT-переходы Enter→OVERRIDE_RETRY/Esc→CLOSE_DIALOG возвращают в ту же позицию)
  - frontend/src/shared/ui/ConflictDialog.tsx (8.5 — props {conflict: ConflictError|null, onOverride(reason), onCancel}; открыт при conflict!=null) + src/shared/api/errors.ts (new ConflictError({status,errorCode,message,details,requestId}))
  - _bmad-output/planning-artifacts/architecture.md (L262 «RTL-тест keyboard path — в DoD каждой формо-стори»; L258 vitest-гейт; ARCH-FE-015 409 overridable→ConflictDialog)
---

# Story 9.5: Фокус-слой

Status: done

## Story

As a **оператор**,
I want **предсказуемый клавиатурный фокус в гриде: Enter коммитит и уходит вниз, Tab вправо, стрелки навигируют, Esc возвращает pre-edit, и фокус НИКОГДА не падает на `document.body` (в т.ч. на readonly-колонках и на краях); а после закрытия ConflictDialog фокус возвращается в исходную ячейку**,
so that **слепой ввод «с трубкой у уха» не теряет позицию; поведение закреплено 10–15 RTL+userEvent сценариями (DoD формо-стори, architecture L262)**.

## Scope

RTL+userEvent сюита фокус-инвариантов поверх грида 9.4 + фокус-упрочнение (все ячейки фокусируемы — фокус не на body) + интеграция ConflictDialog в CONFLICT-режим грида с возвратом фокуса в ячейку (через тонкий conflict-probe seam; реальная 409/422-семантика = 9.6).

## Out of Scope

- **Валидация zod по строке, конфликт-маркеры hard/soft, маппинг бэк-ошибок (409/422 details) на конкретные строки** → 9.6. 9.5 даёт seam входа в CONFLICT + фокус-возврат, НЕ бизнес-семантику конфликта.
- **Prefill/bulk-отправка** → 9.7; перф → уже 9.4; e2e → 9.9; экран/роут → 10.2. grammar.ts (9.2) не меняется.

## Acceptance Criteria

1. **Фокус на монтировании — не body.** Given грид с ≥1 строкой, Then при монтировании фокус в первой редактируемой ячейке (статус), НЕ на `document.body`. Given 0 строк, фокус в контейнере пустого состояния (9.4).

2. **Клавиатурная навигация (userEvent).** Given фокус в ячейке, Then: `Enter` открывает правку статуса; `Enter` в правке коммитит и уводит фокус ВНИЗ; `Tab` вправо; `Shift+Tab` влево; `ArrowUp/Down` по строкам; `ArrowLeft/Right` по колонкам; `Esc` в правке возвращает pre-edit и фокус в ячейку (NAVIGATE). Каждый переход проверен userEvent, `document.activeElement` — конкретная ячейка.

3. **Фокус НИКОГДА не падает на body (упрочнение).** Given любая колонка, включая readonly (ФИО) и флаг, Then ячейка ФОКУСИРУЕМА (tabIndex-элемент); навигация влево к ФИО / вправо к флагу держит фокус на ячейке, не на `document.body`. Given край грида (первая/последняя строка/колонка), навигация за границу оставляет фокус на месте (клампинг 9.2), не на body.

4. **Возврат фокуса после ConflictDialog.** Given грид входит в CONFLICT-режим (через seam `onCellCommit`, вернувший `ConflictError`), When рендерится `ConflictDialog` (shared 8.5) и пользователь жмёт «Отмена»/Escape ИЛИ «Подтвердить оверрайд», Then диалог закрывается, `focus.mode → NAVIGATE`, и **фокус возвращается в исходную ячейку** (не на body, не в диалог-остаток). Реальная 409/422-семантика и маркеры — 9.6; здесь — механика фокуса.

5. **10–15 RTL+userEvent сценариев зелёные.** Given `DailyGrid.focus.test.tsx` (jsdom + userEvent), Then 10–15 сценариев AC-1..4 зелёные; после каждой операции — явный ассерт `document.activeElement`.

6. **Гейт, границы.** `npm run gate` зелёный. grammar.ts (9.2) НЕ изменён. Изменения DailyGrid.tsx — только фокус-упрочнение (все ячейки фокусируемы) + ConflictDialog-интеграция; перф-инвариант 9.4 (1 commit/keystroke) сохранён (перф-тест зелёный). Каноны ARCH-FE (boundaries/lint-canon/react-hooks) чисты.

## Tasks / Subtasks

- [x] Task 1: Фокус-упрочнение грида (AC: 1, 3)
  - [x] Все 4 ячейки фокусируемы: ФИО и флаг — read-only фокусируемые (`<button>`/`tabIndex` + `data-active` при фокусе), не `<span>`. Фокус-эффект (querySelector `[data-active]`) наводит DOM-фокус на любую колонку → фокус не на body.
- [x] Task 2: ConflictDialog-интеграция + фокус-возврат (AC: 4)
  - [x] Seam `onCellCommit?: (change: RowChange) => ConflictError | null` (дефолт undefined → null). На `COMMIT`: если `onCellCommit` вернул конфликт → НЕ применять move, `setFocus({...current, mode: "CONFLICT"})` + сохранить conflict; иначе — обычный move.
  - [x] Рендер `<ConflictDialog conflict={conflict} onOverride onCancel>` при `mode==="CONFLICT"`. `onCancel`→CLOSE_DIALOG-эквивалент (mode NAVIGATE, conflict=null); `onOverride(reason)`→OVERRIDE_RETRY-эквивалент (mode NAVIGATE, conflict=null; фактический retry override:true = 9.6). Layout-эффект возвращает фокус в `[data-active]`-ячейку.
- [x] Task 3: RTL+userEvent сюита (AC: 2, 4, 5)
  - [x] `DailyGrid.focus.test.tsx` (jsdom, userEvent, TanStack Virtual-обвязка из 9.4-теста): 10–15 сценариев — mount-focus; Enter-open; Enter-commit-down; Tab/ShiftTab; Arrow×4; Esc-restore; край (клампинг, не body); readonly-колонка держит фокус; ConflictDialog cancel→focus-back; ConflictDialog override→focus-back. Каждый — ассерт `document.activeElement`.
- [x] Task 4: Гейт (AC: 6)
  - [x] `npm run gate` зелёный; перф-тест 9.4 зелёный (1 commit); grammar.ts не тронут (git-сверка); prettier.

## Dev Notes

### ⚠️ Ловушка №1 (ГЛАВНАЯ): фокус НИКОГДА не на body — все ячейки фокусируемы

9.4 сделал ФИО/флаг `<span>` (не фокусируемы) → навигация к ним теряет фокус на `document.body` (слепой ввод «с трубкой у уха» теряет позицию — прямой анти-паттерн AC). 9.5 упрочняет: КАЖДАЯ ячейка фокусируема (read-only — `tabIndex`-`<button>` без правки). Фокус-эффект наводит на `[data-active]` в любой колонке.

### ⚠️ Ловушка №2: grammar 9.2 ЗАМОРОЖЕНА — фокус применяет ГРИД

Грамматика (переходы/клампинг) не меняется. 9.5 — про ПРИМЕНЕНИЕ фокуса (DOM `.focus()` на правильную ячейку) и упрочнение. CONFLICT-переходы (Enter→OVERRIDE_RETRY/Esc→CLOSE_DIALOG, возврат в ту же позицию) уже в 9.2 — грид их применяет к диалогу.

### ⚠️ Ловушка №3: ConflictDialog — механика фокуса, семантика = 9.6

9.5 wired-ит ConflictDialog (shared 8.5) в CONFLICT-режим + фокус-возврат через тонкий seam `onCellCommit` (вернул `ConflictError` → CONFLICT). РЕАЛЬНОЕ решение «это 409/422?» + маркеры строк + retry override:true к бэку = 9.6 (использует тот же seam). 9.5 конструирует/прокидывает conflict для механики фокуса, не детектит его.

### ⚠️ Ловушка №4: userEvent + фокус в jsdom + TanStack Virtual

userEvent (не fireEvent) для реалистичного keyboard-path (L262). TanStack Virtual в jsdom требует ту же обвязку, что в 9.4-тесте (ResizeObserver-полифилл + offsetHeight-мок). Виртуализация может НЕ рендерить далёкие строки — сценарии навигации держать в пределах видимого окна (малый датасет). Авто-cleanup RTL + jest-dom per-file (конвенция).

### ⚠️ Ловушка №5: перф-инвариант 9.4 не сломать

Фокус-упрочнение и ConflictDialog не должны множить коммиты: перф-тест 9.4 (1 commit/keystroke) обязан остаться зелёным. Диалог рендерится только в CONFLICT (редко), фокус — imperative в layout-эффекте (без extra-setState).

### Дефолты (#YOLO)

- **Д1 (seam):** `onCellCommit?: (change) => ConflictError | null` — единственная точка входа в CONFLICT; 9.6 подключит реальный маппинг.
- **Д2 (файл теста):** `DailyGrid.focus.test.tsx` (отдельно от 9.4-тестов).
- **Д3 (readonly-фокус):** ФИО/флаг — `tabIndex={-1}` кроме фокусной (0), read-only `<button>`; навигация возможна, правка нет.

### Границы (что 9.5 НЕ делает)

- zod-валидация/конфликт-маркеры hard-soft/бэк-ошибки→строки (9.6); prefill+bulk (9.7); e2e (9.9); экран/роут (10.2). grammar.ts не меняется. Перф уже в 9.4.

### References

- [Source: epics.md §Story 9.5 (фокус-инварианты, 10-15 RTL, focus-return-after-dialog); §9.6 граница]
- [Source: docs/contracts/09-01-*.md §3.2/§5 (фокус-переходы + возврат после диалога); architecture.md L262 (RTL keyboard-path в DoD), ARCH-FE-015]
- [Source: frontend/src/features/daily-grid/DailyGrid.tsx (9.4 фокус data-active) + grammar.ts (9.2 CONFLICT-переходы); src/shared/ui/ConflictDialog.tsx (props) + src/shared/api/errors.ts (ConflictError)]

### Открытые вопросы (для Bratan — дефолты активны)

- **Q1 (readonly-навигация):** ФИО/флаг фокусируемы для «фокус-не-body» [Д3] — или грамматика/грид должны СКИПАТЬ readonly (Tab через них не останавливается)? (влияет на ритм слепого ввода; скип — возможное 9.6-уточнение).
- **Q2 (seam-имя):** `onCellCommit` [Д1] — ок как контракт для 9.6, или иное имя/форма?

### Процессный гейт

- `npm run gate` (frontend); RTL keyboard-path — ядро DoD (L262). Ревью — по фокус-инвариантам + перф-регресс-чек (9.4 зелёный).

## Review Findings

<!-- Ревью 2026-07-14 (bmad-code-review, Fable 5, CROSS-MODEL vs спека+dev Opus 4.8; дифф = коммит 0b58616, аудит против HEAD — DailyGrid менялся 9.6/9.7 и ревью-фиксами 9.4 f4b653b; Auditor доказал вакуумность мутацией: убитое движение фокуса оставляло 9/10 тестов зелёными). Слои: Blind Hunter / Edge Case Hunter / Acceptance Auditor. Вердикт AC: 1/3/6-pass; 2/5-PARTIAL (направление не ассертилось) → закрыты; 4-pass механикой, но CONFLICT-клавиатура диффа имела «тихий оверрайд» (закрыт f4b653b, дыра переходного f4b653b-роутинга закрыта здесь). 0 decision · 6 patch ПРИМЕНЕНЫ · 2 defer · 5 dismiss. После патчей: фокус-сюита 10→13 тестов (18 сценариев), 96 daily-grid, npm run gate зелёный (222, 150.4KB). -->

- [x] [Review][Patch] ✅ ИСПРАВЛЕНО 2026-07-14 (onCellFocus на всех ячейках: стейт следует за DOM-фокусом; сравнение через ref-зеркало БЕЗ setState — setState из фазы коммита не получает eager-bailout и ломал перф-инвариант [найдено красным перф-тестом при первой реализации]; тест 16 mouse+keyboard-микс) **Клик мышью по ячейке другой строки → рассинхрон DOM-фокуса и focus-стейта (edge, HIGH):** у ячеек не было фокус-синка — оператор кликает строку 5, видит там фокус-кольцо, жмёт Enter → правка открывается на строке 0, ввод статуса НЕ ТОМУ сотруднику; layout-эффект утаскивал фокус обратно. Вся сюита была keyboard-only и этого не видела. [frontend/src/features/daily-grid/DailyGrid.tsx]
- [x] [Review][Patch] ✅ ИСПРАВЛЕНО (OVERRIDE_RETRY в защитной ветке грида ГАСИТСЯ без действия; оверрайд — только кнопкой диалога с валидной причиной; тест 17) **Enter в CONFLICT, добравшийся до грида = бесплатный оверрайд без причины (edge, MEDIUM — дыра ревью-фикса f4b653b):** defense-in-depth-роутинг `OVERRIDE_RETRY→overrideConflict` снимал маркер и принимал коммит В ОБХОД причины 10–500 (BR-003) и аудита; в полифилл/деградированном окружении этот путь основной, не резервный. Esc→cancelConflict оставлен (безопасная отмена, тест 18). [frontend/src/features/daily-grid/DailyGrid.tsx]
- [x] [Review][Patch] ✅ ИСПРАВЛЕНО (хелпер activeCellAt(row,col) по aria-rowindex+индексу gridcell; тесты 3/4-5/6-9/11/12-13/14/15 переписаны на точную позицию; +нижний/правый края в 12-13) **7 из 15 сценариев вакуумны — направление фокуса не проверялось (blind+edge+auditor, MAJOR, доказано мутацией):** ассерт `activeIsCell()` («не body» + data-active) истинен при ЛЮБОЙ позиции — Auditor убил всё движение фокуса, 9/10 тестов остались зелёными; «Enter уводит вниз» (ядро AC-2) не был закреплён нигде; нижний/правый края и флаг-колонка не посещались вовсе. [frontend/src/features/daily-grid/DailyGrid.focus.test.tsx]
- [x] [Review][Patch] ✅ ИСПРАВЛЕНО (тест 10: selectOptions→Esc→ассерт «В строю»+счётчик 0; тест 14 аналогично для cancel-пути диалога) **Тест «Esc возвращает pre-edit» был вакуумен (blind MAJOR):** значение не менялось перед Esc и не ассертилось после — удаление всего RESTORE_PRE_EDIT-блока оставляло тест зелёным. [frontend/src/features/daily-grid/DailyGrid.focus.test.tsx]
- [x] [Review][Patch] ✅ ИСПРАВЛЕНО (conflict-стейт захватывает rowId на моменте коммита: {error, rowId}; overrideConflict чистит маркер по нему; синк-эффект «conflict живёт только в CONFLICT-режиме» гасит стейл при клампе rows; cancelConflict откатывает значение к pre-edit по §3.2 «значение не сохранено» — отвергнутое значение больше не уедет в bulk-дельты 9.7, маркер остаётся предупреждением) **Незахваченный rowId конфликта + отвергнутое значение оставалось dirty (edge MEDIUM + blind MINOR):** «Подтвердить оверрайд» снимал маркер через rows[focus.row] НА МОМЕНТ закрытия (rows мог обновиться под диалогом → чужая строка), а «Отмена» оставляла закоммиченное-но-отвергнутое значение в дельтах. [frontend/src/features/daily-grid/DailyGrid.tsx]
- [x] [Review][Patch] ✅ ИСПРАВЛЕНО (прунинг markers в RESYNC-эффекте: исчезнувшие id отбрасываются) **Стейл-маркеры воскресали при смене состава rows (edge, LOW-MEDIUM):** RESYNC чистил только values — строка, ушедшая из rows и вернувшаяся со сброшенным значением, получала старый soft/hard-маркер. [frontend/src/features/daily-grid/DailyGrid.tsx]
- [x] [Review][Defer] **Синхронный seam onCellCommit не выдержит реальную 409-семантику (blind MAJOR):** настоящий конфликт = HTTP-ответ → `Promise<ApiError|null>`; async-вход в CONFLICT (гонки «move применён, конфликт прилетел позже») — главная проектная точка стори 10.2, seam обязан расшириться там осознанно — deferred [frontend/src/features/daily-grid/DailyGrid.types.ts]
- [x] [Review][Defer] **jsdom-полифилл showModal не эмулирует модальность (edge/blind):** «фокус уходит В диалог при открытии» и inert-фон проверяемы только в браузере — сценарий обязателен в e2e 9.9 (проверить при create-story 9.9) — deferred [frontend/src/features/daily-grid/DailyGrid.focus.test.tsx]
- [x] [Review][Dismiss ×5] rows[focus.row] без guard в COMMIT-диффе (закрыт ревью 9.4 f4b653b: guard+кламп+фолбэк); двойной путь закрытия CONFLICT и CONFLICT-без-conflict-стейта (закрыты f4b653b-роутингом + синк-эффектом этого ревью); handleKeyDown deps values/rows (обработчик на контейнере, memo(GridRow) не задет, перф-тест зелёный — замеренный инвариант держится); фикстура conflictOverridableEnvelope в фича-тесте (единый источник формы конверта, осознанная связь); defineProperty offsetHeight без restore (vitest per-file изоляция, конвенция всей грид-обвязки 9.4-9.8); a11y пустой флаг-кнопки (наполнена маркерами 9.6, глубокий ARIA-проход = E10 с columnheader — уже в defer 9.4).

## Dev Agent Record

### Agent Model Used

claude-opus-4-8 (Opus 4.8) — create-story + dev-story (RTL+userEvent)

### Debug Log References

- **npm run gate зелёный:** tsc strict, eslint/boundaries/lint-canon, vitest (все + 10 фокус-тестов + 5 grid-тестов 9.4 без регресса, incl. перф 1-commit), build, size 150.4 КБ/300.
- ⚠️ jsdom не реализует `<dialog>` (showModal/close) → полифилл open-семантики в beforeAll (как ConflictDialog.test). TanStack Virtual jsdom-обвязка (ResizeObserver+offsetHeight) как в 9.4.
- 10 фокус-тестов (15 сценариев) зелёные с userEvent; инвариант «фокус не на body» через `activeElement.hasAttribute('data-active')`.

### Completion Notes List

- **Task 1 (фокус-упрочнение) — DONE.** ФИО(col0)/флаг(col3) — теперь фокусируемые read-only `<button>` (`data-active`+tabIndex), не `<span>`. Навигация к ним держит фокус на ячейке (не body).
- **Task 2 (ConflictDialog + фокус-возврат) — DONE.** Seam `onCellCommit?: (change) => ConflictError | null` в props. На COMMIT: конфликт → НЕ применять move, `mode=CONFLICT` на текущей ячейке + рендер `<ConflictDialog>` (shared 8.5); `onCancel`/`onOverride` → `closeConflict` (mode NAVIGATE, conflict=null) → layout-эффект возвращает фокус в `[data-active]`-ячейку. Реальный 409/422-маппинг + retry override:true = 9.6.
- **Task 3 (RTL-сюита) — DONE.** `DailyGrid.focus.test.tsx` (jsdom+userEvent): 15 сценариев в 10 тестах — mount-focus, Enter-open, Enter-commit-down, Tab/ShiftTab, Arrow×4, Esc-restore, ФИО-readonly-фокус, края-кламп (не body), ConflictDialog cancel→focus-back, override→focus-back.
- **Task 4 (гейт) — DONE.** Гейт зелёный; grammar.ts 9.2 НЕ тронут; перф-инвариант 9.4 сохранён (перф-тест зелёный).
- **Границы:** 409/422-семантика+маркеры=9.6 (через тот же seam); prefill+bulk=9.7; e2e=9.9; экран/роут=10.2.
- **Осталось:** Q1 (ФИО/флаг фокусируемы vs скип-readonly), Q2 (имя seam onCellCommit) — дефолты, подтвердить на ревью/9.6.

### File List

- `frontend/src/features/daily-grid/DailyGrid.tsx` (изменён — фокус-упрочнение ФИО/флаг + onCellCommit-seam + ConflictDialog + closeConflict; ревью 2026-07-14 — onCellFocus-синк клика, гашение OVERRIDE_RETRY, conflict={error,rowId}, cancel→откат pre-edit, прунинг markers, focusRef)
- `frontend/src/features/daily-grid/DailyGrid.types.ts` (изменён — +onCellCommit в DailyGridProps)
- `frontend/src/features/daily-grid/DailyGrid.focus.test.tsx` (создан — 15 RTL+userEvent сценариев; ревью — 18 сценариев в 13 тестах: позиционные ассерты activeCellAt, value-ассерты Esc/cancel, mouse+keyboard-микс, Enter/Esc при открытом диалоге, нижний/правый края)
