---
baseline_commit: 4433aba
---

# Story 15.8: `PATCH /force-requests/{id}/allocate` — выделение людей брокером (FR-24)

Status: review

## Story

As a **брокер**,
I want **проставить количество выделенных людей на запрос Группы**,
so that **статус запроса отражает прогресс комплектования, а ОМД видит, сколько ещё не хватает**.

## Scope Decision (найдено при create-story)

- **Story 15.8 (`epics.md:1423`, личная проверка research-агента + ре-верификация):** «Выделение людей брокером (двухуровневый брокеридж, FR-24)». Единственный текст — нет отдельного story-файла ещё, нет AC. FR-24 (`epics.md:62`): «Двухуровневый брокеридж: запросы Группам → брокеры выделяют; физнаряд — ОМД напрямую».
- **«Физнаряд — ОМД напрямую» — НЕ эта стори.** Story 15.9 (`epics.md:1424`) — «Физнаряд: прямое назначение ОМД; правила «спора за человека» и дефицита» — отдельная стори, подтверждено research-агентом.
- **Выделение — количественное, НЕ по конкретным людям.** Frontend-прототип (`frontend/src/features/security-events/api/pending-contracts.ts:86-90`, soft-сигнал) — `UpdateForceAllocationRequest {allocatedCount, comment}`, без `employeeId`. `GroupForceRequest.allocated_count` (15.7a) уже смоделирован как счётчик, не M2M на сотрудников — подтверждает выбор.
- **`status`-переход выводится из сравнения `allocated_count` vs `requested_count`** (тот же паттерн, что frontend-прототип's `updateForceAllocation`, soft-сигнал): `allocated_count == 0` → остаётся `SENT`; `0 < allocated_count < requested_count` → `PARTIALLY_ALLOCATED`; `allocated_count >= requested_count` → `ALLOCATED`. Не отдельное клиентское поле — сервер вычисляет.
- **`SecurityEvent.status_code` НЕ переходит в `BROKERAGE` этой стори.** Нет backend-свидетельства, что переход в `BROKERAGE` триггерится записью `allocated_count` (research-агент подтвердил: `BROKERAGE` — только enum-значение, ни одного raise-сайта). Frontend-прототип использует ОТДЕЛЬНОЕ поле (`stage`, не `status_code`) с ОТДЕЛЬНЫМ явным действием-завершением (`completeForces`), гейтующим на «все запросы полностью укомплектованы» — по аналогии by-inference, НЕ прямому свидетельству. Переход `SecurityEvent.status_code` в `BROKERAGE`/далее — открытый вопрос, оставлен будущей стори (или уточнению продукта), не изобретается здесь без спеки.
- **Permission — переиспользуется существующий `brokerage.manage`** (уже в `seed_operations.py`, «Брокеридж», уже назначен роли `OMD`) — НЕ frontend-прототипа's `ops.force_allocation.manage` (другая строка, не backend-авторитетная). Backend-код семантически точнее совпадает с FR-24's «брокеры выделяют».
- **`allocated_count` НЕ может превышать `requested_count`.** Не найдено явного запрета в спеке, но логически необходимо (нельзя выделить больше, чем запрошено) — консервативный валидационный гвард, не бизнес-изобретение.
- **Гейт по статусу запроса — НЕ ограничен.** Донор-спека не даёт правила «можно выделять только из SENT» — разрешено из любого статуса (переставление `allocated_count` вручную/коррекция — легитимный сценарий, тот же принцип, что везде в Epic 15 — не гейтовать без спеки).

## Acceptance Criteria

1. **AC-1 (выделение, полное).** `PATCH /security-events/{id}/force-requests/{request_id}/allocate` с `{allocated_count: N}`, `N >= requested_count` → 200, `status` становится `ALLOCATED`.
2. **AC-2 (частичное выделение).** `0 < N < requested_count` → `status` становится `PARTIALLY_ALLOCATED`.
3. **AC-3 (`N == 0`).** `status` возвращается/остаётся `SENT` (не `PARTIALLY_ALLOCATED`/`ALLOCATED`).
4. **AC-4 (валидация — не больше запрошенного).** `N > requested_count` → 400 (`VALIDATION_ERROR`).
5. **AC-5 (permission).** Требует `brokerage.manage` — без него 403.
6. **AC-6 (404).** Нечисловой/несуществующий `request_id` — 404, не 500.
7. **AC-7 (аудит).** `GROUP_FORCE_REQUEST_ALLOCATED` — на каждом реальном изменении `allocated_count` (idempotent-повтор того же значения — без дублирующего аудита).
8. **AC-8 (регресс нулевой).** `make gate` зелёный, роут — в обоих живых реестрах.

## Out of Scope

- Физнаряд (прямое назначение ОМД) — Story 15.9.
- Переход `SecurityEvent.status_code` в `BROKERAGE`/далее — открытый вопрос, нет backend-свидетельства триггера, не эта стори.
- Выделение по конкретным сотрудникам (не количество) — не запрошено, frontend-прототип тоже количественный.

## Tasks / Subtasks

- [x] Task 1 — `services.py`: `allocate_force_request(request, *, actor, allocated_count, comment=None)` — валидация+статус-вычисление+аудит
- [x] Task 2 — Сериализатор `AllocateForceRequestSerializer` ({allocated_count, comment?})
- [x] Task 3 — ViewSet `@action` на новом `GroupForceRequestViewSet` (свой роут `force-requests`, не вложен в `SecurityEventViewSet` — решено: прямая мутация по `request_id`)
- [x] Task 4 — Живые реестры (permission `brokerage.manage`)
- [x] Task 5 — Тесты (полное/частичное/ноль/превышение/403/404/аудит/идемпотентность)
- [x] Task 6 — Гейт + схема

## Dev Notes

- **Роутинг-решение:** `GroupForceRequest` уже доступен через `event.force_requests` (15.7a/b), но выделение мутирует КОНКРЕТНУЮ строку по её собственному `id`, не по event — естественнее как отдельный `GroupForceRequestViewSet` с `PATCH /api/operations/force-requests/{id}/allocate`, не вложенный action на `SecurityEventViewSet` (которая работает с event-scoped списками, а не отдельными force-request записями). Решение — на усмотрение dev-агента, задокументировать выбор.
- Читать `apps/operations/events/services.py` — паттерн гейта/аудита (хотя здесь гейт по статусу НЕ применяется, см. Scope Decision).
- `brokerage.manage` уже существует (`seed_operations.py`) — НЕ создавать новый permission-код.

### References

- [Source: _bmad-output/planning-artifacts/epics.md:1423] — Story 15.8 текст.
- [Source: _bmad-output/planning-artifacts/epics.md:62] — FR-24 текст.
- [Source: frontend/src/features/security-events/api/pending-contracts.ts:86-90] — `UpdateForceAllocationRequest`, soft-сигнал.
- [Source: Backend/VAPS/apps/operations/management/commands/seed_operations.py:15] — `brokerage.manage`, уже существует.

## Dev Agent Record

### Context Reference

- Research-агент лично подтвердил: количественное (не пер-сотрудник) выделение, `brokerage.manage` уже существует и назначен `OMD`, «физнаряд» — отдельная 15.9, `BROKERAGE`-переход не имеет backend-свидетельства триггера (только frontend-аналогия).

### Completion Notes

Реализовано по AC 1-8. `allocate_force_request()` — валидация `allocated_count <= requested_count` (400 иначе), вычисление `status` (0→`SENT`, `0<N<requested`→`PARTIALLY_ALLOCATED`, `N>=requested`→`ALLOCATED`), идемпотентность (тот же `allocated_count`+`comment` — no-op, без дублирующего аудита). `select_for_update()` на `GroupForceRequest` (не `SecurityEvent` — мутация точечная, по своему `pk`). Новый `GroupForceRequestViewSet` — отдельный ViewSet, свой роут `force-requests`, `PATCH .../allocate`, permission `brokerage.manage` (уже существующий код, уже назначен `OMD`). Аудит `GROUP_FORCE_REQUEST_ALLOCATED` — только на реальном изменении. 8 новых тестов (полное/частичное/ноль/превышение-400/403/404/аудит/идемпотентность-без-дублирования). `make gate` — 3589 passed (было 3571, +18), 0 regressions, no drift.

### File List

- `Backend/VAPS/apps/operations/events/services.py` (modified — `allocate_force_request()`)
- `Backend/VAPS/apps/operations/events/api/serializers.py` (modified — `AllocateForceRequestSerializer`)
- `Backend/VAPS/apps/operations/events/api/views.py` (modified — `GroupForceRequestViewSet`)
- `Backend/VAPS/apps/operations/api/urls.py` (modified — `force-requests`-route)
- `Backend/VAPS/apps/operations/events/tests/test_group_force_request_allocate.py` (new)
- `Backend/VAPS/apps/audit/tests/test_audit_coverage.py` (modified — `_Audited()`-запись)
- `Backend/VAPS/apps/operations/tests/test_rbac_matrix.py` (modified — `_Gate("brokerage.manage")`-запись)
- `docs/registries/audit-events.yaml` (modified — `GROUP_FORCE_REQUEST_ALLOCATED`-запись)
- `Backend/VAPS/schema.yaml` (regenerated)

## Change Log

| Дата | Изменение |
|---|---|
| 2026-07-31 | Story создана (create-story). Количественное выделение (не пер-сотрудник), переиспользован существующий `brokerage.manage`. `BROKERAGE`-переход явно НЕ триггерится здесь — нет backend-свидетельства, открытый вопрос. |
| 2026-07-31 | Dev-story: `allocate_force_request()` + новый `GroupForceRequestViewSet`. 8 новых тестов, оба живых реестра обновлены, схема регенерирована. `make gate` — 3589 passed. Status → review. |
