---
baseline_commit: 16371516e9e96338c76799188fe6d1e75c752612
---

# Story 7.1: Профилирование данных донора

Status: done

## Story

As a разработчик,
I want отчёт качества полной выгрузки (дубли ИИН/табельных, NULL, битые даты, кодировки, осиротевшие ссылки),
so that каждая категория грязи получает правило обработки до импорта.

## Acceptance Criteria

1. **Given** полная выгрузка (спайк 1.11 формат — `manage.py dumpdata` шести моделей), **When** запускаю профилировщик, **Then** отчёт по категориям с количествами и примерами; для каждой категории записано правило.

## Tasks / Subtasks

- [x] Task 1 — Чистый модуль профилировщика (AC: 1)
  - [x] `apps/migration_legacy/donor_profile.py` — БЕЗ ORM/Django (тот же канон, что `transform.py`/`donor_diff.py`)
  - [x] Категории employee-уровня переиспользуют `transform.transform_employee` (Skip-таксономия: `missing_iin`/`invalid_iin`/`unknown_employment_status`/`invalid_dates`) — паритет с реальным импортёром гарантирован вызовом той же функции, не копией regex
  - [x] Дубли ИИН/табельного — отдельно (transform не дедуплицирует, это зона 7.3)
  - [x] Осиротевшие статусы (employee FK NULL/вне множества employees) — отдельно от `transform_status`
  - [x] Битые даты статусов (start_date/end_date/actual_end_date не парсятся) — отдельная категория
  - [x] Кодировки — эвристика непечатаемых/replacement-символов в last_name/first_name/middle_name
- [x] Task 2 — Management-команда (AC: 1)
  - [x] `profile_donor_export` — Django-команда
  - [x] PII-маскирование примеров
- [x] Task 3 — Тесты на golden-фикстуре (AC: 1)
  - [x] `donor_slice.json` — известный состав (дубль ИИН pk1/pk7, NULL ИИН pk4, осиротевший статус pk110) — profile_export находит те же находки (12 тестов, включая golden-фикстуру, каждую категорию по отдельности, PII-маску, пустой вход, CLI-обвязку)

## Dev Notes

- Прямой прецедент: `spikes/1.11-donor-export/profile_export.py` (stdlib-only, PII-маска). Эта стори — канон-версия: категории + правило на каждую (спайк специально НЕ делал этого, см. его docstring "ЭТО НЕ профилировщик 7.1").
- Таблица «находка → стори» в `spikes/1.11-donor-export/EXPORT-RECIPE.md`: качество ключей → 7.1+7.3, осиротевшие → 7.1.
- `apps/migration_legacy/transform.py` — источник истины Skip-таксономии; `apps/migration_legacy/donor_diff.py` — источник паттерна "pure module, no ORM".
- Правило на категорию — текстовое поле в отчёте (не отдельная система), достаточно строки типа "skip (missing_iin) — Employee без ИИН не создаётся, см. transform.py".

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Epic 7, Story 7.1]
- [Source: spikes/1.11-donor-export/profile_export.py]
- [Source: Backend/VAPS/apps/migration_legacy/transform.py]
- [Source: Backend/VAPS/apps/migration_legacy/tests/fixtures/donor_slice.json]

## Dev Agent Record

### Agent Model Used

claude-sonnet-5

### Debug Log References

- `pytest apps/migration_legacy/tests/test_donor_profile.py -v` → 12/12 passed на первом прогоне.
- Полный гейт: `pytest -m "not property and not concurrency and not slow and not golden"` → **2523 passed** (было 2511 до стори), `ruff check` чисто (после 2 E501-правок), `makemigrations --check --dry-run` → no changes (новых моделей нет — pure-модуль + management-команда).

### Completion Notes List

- Профилировщик переиспользует `transform.transform_employee` буквально для employee-уровневых категорий (missing_iin/invalid_iin/unknown_employment_status/invalid_dates) — паритет с реальным импортёром гарантирован по построению, не отдельной копией regex/логики.
- 9 категорий грязи, у каждой — текстовое правило обработки (AC-1). Golden-фикстура `donor_slice.json` (уже используемая 1.6/1.7 тестами) подтверждает известный состав (дубль ИИН pk1/pk7, NULL ИИН pk4, осиротевший статус pk110) без создания нового файла.
- Осиротевшие статусы и битые даты статусов НЕ переиспользуют `transform_status` — та функция режет по окну слайса (для импорта конкретного диапазона дат), а профилировщик смотрит на ВСЮ выгрузку целиком; переиспользование внесло бы окно там, где его не должно быть.

### File List

- `Backend/VAPS/apps/migration_legacy/donor_profile.py` (new)
- `Backend/VAPS/apps/migration_legacy/management/commands/profile_donor_export.py` (new)
- `Backend/VAPS/apps/migration_legacy/tests/test_donor_profile.py` (new)

## Review Findings

Code review (2026-07-28, 3 параллельных слоя same-model: Blind Hunter / Edge Case Hunter / Acceptance Auditor). 0 decision-needed · 9 patch применены · 2 defer → `deferred-work.md` · 0 dismiss.

- [x] [Review][Patch] Профилировщик падал на структурно повреждённых строках (нет `fields`, `fields` не dict) вместо репортинга — новая категория `malformed_row` + `_well_formed_rows()` guard [donor_profile.py]
- [x] [Review][Patch] `transform_employee` мог кинуть непойманный `TypeError` (unhashable `employment_status`) и уронить весь профиль — обёрнуто в try/except → `malformed_row` [donor_profile.py]
- [x] [Review][Patch] `_has_encoding_suspect` падал на не-строковом текстовом поле (int/list в ФИО) — добавлена проверка типа [donor_profile.py]
- [x] [Review][Patch] Неизвестные Skip-причины от `transform_employee` терялись молча (ни счёт, ни категория) — новая категория `other_skip` [donor_profile.py]
- [x] [Review][Patch] `profile_donor_export` падал сырым traceback на строке без `model`/не-dict строке в JSON-массиве — guard с явным предупреждением, не CommandError [profile_donor_export.py]
- [x] [Review][Patch] AC-1 формально накрывает "полную выгрузку" (6 моделей рецепта), но профилировщик считал только 2 — добавлена секция "объём по модели" (все 6 моделей рецепта 1.11) [donor_profile.py, profile_donor_export.py]
- [x] [Review][Patch] Пример `encoding_suspect` терял метку поля (маска обрезала всю строку `"pk=1:last_name=..."` до последних 4 символов) — `record_labeled`/`record_plain` разделяют PII-значение и немаскируемую метку [donor_profile.py]
- [x] [Review][Patch] Собственный баг ревью-фикса: `f.examples.append(...)` в `_well_formed_rows` был снаружи `if len(...) < EXAMPLE_LIMIT` — безусловный append + `UnboundLocalError` риск на `pk` — исправлен отступ [donor_profile.py]
- [x] [Review][Patch] `@pytest.mark.django_db` на CLI-тесте не требовался (команда не трогает БД) — снят, тест зелёный без БД

## Deferred (см. `deferred-work.md`)

- `status_invalid_dates` считает по СТРОКЕ (не по полю) — строка с 2 битыми датами даёт count=1, не 2 (низкий приоритет, семантика документирована в тексте правила).
- `duplicate_personnel_number` намеренно шире по scope, чем `duplicate_iin` (считает и невалидные-по-ИИН строки) — уже задокументировано комментарием в коде, подтверждено Acceptance Auditor как совместимое со спекой, не расходится с AC.

## Change Log

- 2026-07-28: Story implemented (Tasks 1–3), gate green (2523 passed). Status → review.
- 2026-07-28: Code review (3-layer same-model) — 9 patches applied (crash-resilience for malformed donor rows, other_skip catch-all, per-model volume section, PII-mask label fix, own review-fix bug caught+fixed), 2 low-severity items deferred, re-verified green (2529 passed, +7 new tests). Status → done.
