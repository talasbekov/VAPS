---
baseline_commit: 6e66f4b
---

# Story 15.2b: `POST /api/operations/security-events/{id}/bulletin` — выпуск бюллетеня (FR-21)

Status: done

## Story

As a **оператор, управляющий охранными мероприятиями**,
I want **перевести ОМ из `DRAFT` в `BULLETIN`**,
so that **ОМ считается официально объявленным и Story 15.3 (рекогносцировка) может начаться**.

## Scope Decision (найдено при create-story)

- **Вторая половина разбитого планового `15-2`** (15.2a — создание+Старший, уже done; эта стори — переход статуса). См. 15.2a's Scope Decision для полного обоснования разбиения.
- **«Бюллетень» — единственный найденный источник: `SecurityEvent.StatusCode.BULLETIN`** (15.1's enum-значение, «Бюллетень выпущен»). Никакой отдельной спеки/документа/PDF-генерации нигде не найдено (research-агент подтвердил при 15.2a's research). Эта стори реализует ИМЕННО статус-переход `DRAFT`→`BULLETIN`, не документ-генерацию.
- **Транзит СТРОГО из `DRAFT`.** В отличие от `approve_duty_plan()` (14.11c/14.12a, permissive — любой не-`APPROVED` статус трактуется как «черновик» и переводится), здесь выбран строгий гейт: бюллетень можно выпустить ТОЛЬКО из `DRAFT`. Обоснование: `BULLETIN` — первый шаг ПОСЛЕ `DRAFT` в 10-значном линейном цикле (15.1's enum), не терминальное/повторно-достижимое состояние вроде `APPROVED` у `DutyPlan` (которое не продолжает свой цикл дальше). Повторный вызов на уже-`BULLETIN` — идемпотентный no-op (200, статус не меняется, аудит не дублируется, тот же паттерн `was_draft`-гварда, что 14.12a). Вызов из ЛЮБОГО другого статуса (`RECON`+) — `DomainError` 409 (конфликт состояния, тот же паттерн, что project's IntegrityError→422/DomainError→4xx backstop).
- **Паттерн — `services.py`-функция + тонкий view-`@action`**, буквальный образец `approve_duty_plan()`/`DutyPlanViewSet.approve()` (14.11c/14.12a): `select_for_update()`-блокировка внутри `transaction.atomic()`, `actor`-непустой-гвард, идемпотентный no-op без дублирующего аудита, `record()` внутри той же транзакции.
- **Новый `services.py`-файл в `apps/operations/events/`** (первый для этого приложения — 15.2a не завёл его, т.к. create/list не нуждались в отдельном сервисе, тот же выбор, что duty-plan create/list). Функция `issue_bulletin(event, *, actor)`.
- **Permission — переиспользуется `event.manage`** (тот же код, что 15.2a — единый гейт на весь `SecurityEventViewSet`, тот же паттерн, что `duty.manage` покрывает весь `DutyPlanViewSet`).
- **Роут — вложенный `@action(detail=True)`** на существующем `SecurityEventViewSet`, `url_path="bulletin"` → `POST /api/operations/security-events/{id}/bulletin`, зеркалит `DutyPlanViewSet.approve()`'s форму (`@action(detail=True, methods=["post"])`).

## Acceptance Criteria

1. **AC-1 (успешный переход).** `POST /api/operations/security-events/{id}/bulletin` на ОМ в `DRAFT` → 200, `status_code` становится `BULLETIN`.
2. **AC-2 (идемпотентность).** Повторный вызов на уже-`BULLETIN` ОМ → 200, статус не меняется, второй `AuditLog`-строки НЕ создаётся.
3. **AC-3 (конфликт состояния).** Вызов на ОМ в статусе, отличном от `DRAFT`/`BULLETIN` (например, искусственно выставленном `RECON`) → 422 (`INVALID_LIFECYCLE_TRANSITION`, переиспользован существующий registry-код, не новый 409 — см. Dev Agent Record), статус не меняется.
4. **AC-4 (permission).** Требует `event.manage` — без него 403.
5. **AC-5 (аудит).** Реальный переход (AC-1) пишет `AuditLog` (`action="SECURITY_EVENT_BULLETIN_ISSUED"`, `entity_type="security_event"`, `entity_id=uuid.UUID(int=event.pk)`) в той же транзакции; idempotent-повтор (AC-2) НЕ пишет вторую строку.
6. **AC-6 (регресс нулевой).** `make gate` зелёный, включая route-классификацию в обеих живых матрицах (audit + RBAC).

## Out of Scope

- Рекогносцировка (`DRAFT`/`BULLETIN`→`RECON`) — Story 15.3.
- Любая генерация документа/PDF для «бюллетеня» — не найдено требования, не эта стори (см. Scope Decision).
- Retrieve отдельного ОМ по id (для проверки текущего статуса перед вызовом) — клиент полагается на 15.2a's list-ответ; отдельный GET detail — будущая стори при необходимости.

## Tasks / Subtasks

- [x] Task 1 — `services.py` (AC: 1, 2, 3, 5)
  - [x] `issue_bulletin(event, *, actor)`: непустой-`actor`-гвард, `select_for_update()`, строгий `DRAFT`-гейт, идемпотентный no-op на `BULLETIN`, `DomainError("INVALID_LIFECYCLE_TRANSITION", 422, ...)` на прочих статусах (переиспользован существующий registry-код, не 409 как черновик планировал — см. Dev Agent Record), `record()` только на реальном переходе
- [x] Task 2 — ViewSet `@action` (AC: 1, 3, 4)
  - [x] `POST /security-events/{id}/bulletin` → `require_permission` + вызов `issue_bulletin()`
- [x] Task 3 — Аудит-реестр (AC: 5, 6)
  - [x] `SECURITY_EVENT_BULLETIN_ISSUED` в `docs/registries/audit-events.yaml`
  - [x] `AUDIT_MATRIX["ops-security-event-bulletin"] = _Audited()`
- [x] Task 4 — RBAC-реестр (AC: 4, 6)
  - [x] `test_rbac_matrix.py`'s `MATRIX["ops-security-event-bulletin"] = _Gate("event.manage")`
- [x] Task 5 — Тесты (AC: 1-6)
  - [x] HTTP-смок: успех, идемпотентный повтор (без дублирования аудита), конфликт из непустого статуса, 403 без permission
- [x] Task 6 — Гейт + схема (AC: 6)
  - [x] `make gate`, `make schema` (новый роут → `schema.yaml`)

## Dev Notes

- Читать `apps/operations/duties/services.py::approve_duty_plan()` + `apps/operations/duties/api/views.py::DutyPlanViewSet.approve()` (14.11c/14.12a) — буквальный образец идемпотентного статус-перехода + аудита ПЕРЕД имплементацией.
- `apps/core/exceptions.py::DomainError` — сигнатура `DomainError(code, http_status, message=...)`, существующий exception handler мапит её в JSON-ответ с нужным статусом.
- `apps/operations/events/models.py`'s `StatusCode` — `DRAFT`/`BULLETIN` уже существуют (15.1), новых миграций эта стори не требует.

### References

- [Source: Backend/VAPS/apps/operations/duties/services.py] — `approve_duty_plan()`, буквальный образец идемпотентного перехода (14.11c/14.12a).
- [Source: Backend/VAPS/apps/operations/duties/api/views.py] — `DutyPlanViewSet.approve()`, `@action(detail=True)`-паттерн.
- [Source: Backend/VAPS/apps/operations/events/models.py] — `SecurityEvent.StatusCode` (15.1).
- [Source: _bmad-output/implementation-artifacts/15-2a-создание-ом-и-назначение-старшего.md] — предыдущая половина разбиения, `event.manage`-permission, роутинг-контекст.

## Dev Agent Record

### Context Reference

- При имплементации найдено: черновик стори предполагал новый 409-код (`CONFLICT`) для AC-3, но `docs/registries/error-codes.yaml` уже содержит `INVALID_LIFECYCLE_TRANSITION` (422, business_hard) — тот же код, что `cancel_duty_shift()` использует для эквивалентного «неверный статус для этого перехода» конфликта (`apps/operations/duties/services.py`). Переиспользован буквально, не изобретён новый код — closed-world registry-принцип (AR-документ). AC-3 обновлён на 422.

### Completion Notes

Реализовано по AC 1-6. `issue_bulletin(event, *, actor)` — новый `apps/operations/events/services.py` (первый service-файл этого приложения), буквальный образец `approve_duty_plan()` (14.11c/14.12a): непустой-`actor`-гвард, `select_for_update()` внутри `transaction.atomic()`, идемпотентный no-op на уже-`BULLETIN` (без дублирующего аудита), `DomainError("INVALID_LIFECYCLE_TRANSITION", 422)` на любом другом статусе. В отличие от `approve_duty_plan()`'s permissive-гейта (любой не-APPROVED → APPROVED), здесь гейт СТРОГИЙ — только `DRAFT`→`BULLETIN` (обоснование в Scope Decision: линейный 10-значный цикл, не терминальный статус). `POST /security-events/{id}/bulletin` — тонкий `@action(detail=True)`-wrapper, зеркалит `DutyPlanViewSet.approve()`. Аудит `SECURITY_EVENT_BULLETIN_ISSUED` — только на реальном переходе. Оба живых реестра (`AUDIT_MATRIX`, RBAC `MATRIX`) обновлены. Схема регенерирована. 5 новых тестов (успех/идемпотентность/конфликт/403/аудит). `make gate` — 3419 passed (было 3404, +15), 0 regressions, no schema/migration drift.

**Ревью (Blind Hunter/Edge Case Hunter/Acceptance Auditor, параллельно):** Blind Hunter — 0 багов (детально проверил select_for_update-гонку, идемпотентность-порядок, INVALID_LIFECYCLE_TRANSITION-переиспользование — всё корректно). Acceptance Auditor — 6/6 AC чистый PASS, независимо подтвердил `INVALID_LIFECYCLE_TRANSITION` действительно предсуществовал (commit `edc922e`, задолго до этой стори). Edge Case Hunter нашёл РЕАЛЬНЫЙ High-баг: нечисловой `pk` в URL бюллетеня (`POST .../not-a-number/bulletin`) давал необработанный 500 вместо 404 — `get_object_or_404()` ловит только `DoesNotExist`, не `ValueError` от приведения строки к int-полю; ТОТ ЖЕ класс бага, что уже дважды чинился в этом кодбейзе (14.11d's `shift_id`-гвард, 14.11a's `?object=`-гвард, и даже в ЭТОЙ ЖЕ стори `list()`'s `?object=`-гвард уже был скопирован верно — только `bulletin()`'s `pk` пропустил гвард). Исправлено: `if not (pk or "").isdigit(): raise Http404(...)` перед `get_object_or_404()`, зеркалит `duties/api/views.py:252-253`. Также прогнал concurrency-тест (два потока на одном DRAFT) — подтвердил ровно 1 аудит-строку, гонки нет; все 8 остальных статусов корректно отвергаются 422 (тесты стори покрывали только RECON). Добавлен регресс-тест `test_bulletin_with_non_numeric_id_is_404_not_500`. `make gate` — 3420 passed (было 3419, +1). Status → done.

### File List

- `Backend/VAPS/apps/operations/events/services.py` (new)
- `Backend/VAPS/apps/operations/events/tests/test_security_event_bulletin.py` (new, +1 после ревью)
- `Backend/VAPS/apps/operations/events/api/views.py` (modified — `bulletin`-action, +ревью-фикс non-numeric-pk-гвард)
- `Backend/VAPS/apps/audit/tests/test_audit_coverage.py` (modified — AUDIT_MATRIX entry)
- `Backend/VAPS/apps/operations/tests/test_rbac_matrix.py` (modified — MATRIX entry)
- `docs/registries/audit-events.yaml` (modified — SECURITY_EVENT_BULLETIN_ISSUED entry)
- `Backend/VAPS/schema.yaml` (regenerated — `make schema`)

## Change Log

| Дата | Изменение |
|---|---|
| 2026-07-31 | Story создана (create-story) — вторая половина разбитого `15-2`. Строгий DRAFT-only транзит-гейт выбран сознательно (в отличие от 14.11c's permissive approve) — обоснование в Scope Decision. |
| 2026-07-31 | Dev-story: `issue_bulletin()` + `POST .../bulletin`-action. Переиспользован существующий `INVALID_LIFECYCLE_TRANSITION`(422) вместо изобретения нового 409-кода (AC-3 обновлён). 5 новых тестов, оба живых реестра обновлены, схема регенерирована. `make gate` — 3419 passed. Status → review. |
| 2026-07-31 | Ревью (3 агента параллельно): Blind Hunter/Acceptance Auditor — чисто. Edge Case Hunter нашёл РЕАЛЬНЫЙ High-баг — нечисловой pk → 500 вместо 404 (известный класс бага, уже чинился дважды в кодбейзе, здесь пропущен только в новом `bulletin()`-action). Исправлено + регресс-тест. `make gate` — 3420 passed. Status → done. |
