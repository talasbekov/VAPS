---
baseline_commit: 1b65f54 (HEAD на ветке spike/1.9-contour-probe). В рабочем дереве — review-residue 1.9/1.10 (deploy/spike-1.9/*, spikes/1.10-perf-grid/BUDGET.md, 1-9/1-10-стори, deferred-work.md, sprint-status.yaml). К стори 1.12 отношения не имеют — в File List не включать, не откатывать.
---

# Story 1.12: Инвентаризация-артефакт

Status: review

<!-- ЭТО ДОКУМЕНТ-АРТЕФАКТ, НЕ КОДОВАЯ СТОРИ. Продукт — РАЗЫМЕНОВЫВАЕМЫЕ ССЫЛКИ: реестры (yaml),
     сводный Decision Register и контракт-секции новых сущностей в спеке. Никаких Django-моделей/миграций/
     сервисов/тестов. App-код Backend/VAPS НЕ трогается. «Тесты» здесь — валидность YAML, непустота,
     сходимость счётчиков с источником и резолвимость перекрёстных ссылок, а НЕ pytest. -->
<!-- ВНИМАНИЕ: VAPS_7.8.2.md — «Strict-Audit Fixed Baseline». Поправки допускаются ТОЛЬКО как
     дельта-патч (§0.1/§0.2: «Любые новые изменения допускаются только как дельта-патчи»). НЕ переписывать
     §4 baseline; новые контракты — отдельной дельта-секцией в конце, по конвенции документа (как §44–81). -->
<!-- Эта стори ЗАКРЫВАЕТ три Important-пробела архитектурной валидации (architecture.md:668–672) и снимает
     условие подписи Winston «Инвентаризация/скелет — первая работа, блокирует остальное» (architecture.md:717).
     После неё правило «нет в реестре/Glossary/спеке → СТОП» (architecture.md:33–35) становится исполнимым. -->
<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a AI-агент-имплементатор будущих стори (E2+),
I want выгруженные `docs/registries/*.yaml` (коды ошибок / типы аудит-событий / типы WS-сообщений), один сводный Decision Register со ВСЕМИ ARCH-* и их статусами, и контракт-секции новых сущностей (DailySubmission, Amendment, DocumentSequence, AsyncJob, Attachment) в спеке VAPS_7.8.2,
so that правило закрытого мира «решение/код/тип не покрыт реестром, Glossary или спекой → СТОП и спроси, не выбирай сам» (architecture.md:33–35) **разыменовывается** — у агента есть конкретный файл/таблица/секция, в которые он смотрит, вместо «см. спеку» и обязательного полного перечитывания architecture.md перед каждым решением.

## Acceptance Criteria

1. **Given** репозиторий на `baseline_commit`, **When** инвентаризация завершена, **Then** существуют и НЕПУСТЫ три файла: `docs/registries/error-codes.yaml`, `docs/registries/audit-events.yaml`, `docs/registries/ws-message-types.yaml`; каждый — валидный YAML (`yaml.safe_load` без ошибок), каждый идентификатор — `UPPER_SNAKE` (`^[A-Z][A-Z0-9_]*$`), у каждой записи есть поле-ссылка на источник (`source:` — секция спеки/architecture/кода). [AC закрывает architecture.md:669 «реестры объявлены, не выгружены».]

2. **Given** `error-codes.yaml`, **When** он выгружен из спеки, **Then** он содержит как минимум коды контракта ошибок §1255 и ВСЕ коды из таблиц «Errors:» спеки + DomainError-усого из кода (инвентарь ≈35: 400 `VALIDATION_ERROR`, 403 `PERMISSION_DENIED`/`USER_INACTIVE`, 404 `*_NOT_FOUND`, 409 soft+override `SOFT_CONFLICT_DETECTED`/`STATUS_OVERLAP*`/…, 422 hard `OVERLAPPING_HARD_STATUS`/`HARD_UNAVAILABLE_STATUS`/…, 423 `ASSIGNMENT_VERSION_LOCKED`, 500 `INTERNAL_ERROR`, плюс `conflict_code`-значения внутри `SOFT_CONFLICT_DETECTED`); у каждого кода — `http_status` и `overridable` (bool). `ws-message-types.yaml` содержит 24 типа из матрицы уведомлений §33.2 (`DAILY_MARK_MISSING` … `IMPORT_FAILED`) с `priority`/`recipients`/`payload`-полями. `audit-events.yaml` непуст: явные `AUTH_LEGACY_TOKEN_ACCEPTED`/`REJECTED` + `action_code` истории статусов (`CREATED`/`APPLIED`/`EXTENDED`/`TERMINATED`/`COMPLETED`/`CANCELLED`/`MODIFIED`) + канонические мутации (создание/сдача/amendment/генерация/скачивание/…), каждая с пометкой провенанса; реестр помечен как растущий (новые — тем же PR, architecture.md:398).

3. **Given** все ARCH-решения (из кода: ARCH-002/003/004/007; из architecture.md: ARCH-DATA-020…025, ARCH-SEC-030…032, ARCH-FE-010…015) и 8 DEFERRED-механизмов (architecture.md:95–99), **When** собран Decision Register, **Then** это ОДНА таблица: строка на ARCH-ID, колонки — `ARCH-ID · Норма (MUST/MUST NOT) · Пример/анти-пример · Enforcement · SOURCE (якорь секции) · Статус (ACTIVE / DEFERRED+триггер)`; каждый из 8 DEFERRED-механизмов получил свой ARCH-ID со статусом `DEFERRED` и триггером возврата (architecture.md:99 «Каждый DEFERRED-механизм получает ARCH-ID со статусом и триггером»). [AC закрывает architecture.md:670.]

4. **Given** спека VAPS_7.8.2 как Fixed Baseline, **When** контракты дополнены, **Then** DailySubmission, Amendment, DocumentSequence, AsyncJob имеют контракт-секции (схема таблицы/инвариант/связь с существующими секциями §4), Attachment подтверждён ссылкой на существующую DB-DOC-002 (+API-DOC-002); поправки внесены **дельта-секцией** в конце документа (§0-конвенция), §4-baseline НЕ переписан; каждая секция привязана к своему ARCH-ID (DailySubmission→ARCH-DATA-021, DocumentSequence/AsyncJob/Attachment→Process Patterns architecture.md:464–466); для DailySubmission ЯВНО зафиксирована связь с уже существующими `ops_daily_personnel_reports`/`ops_daily_update_marks` (DB-OPS-013) — без второго источника истины. [AC закрывает architecture.md:671 «контракты дополнить».]

5. **And** анти-gold-plating: НЕ строится loader-проверка реестров при старте (architecture.md:574 — отдельная стори), НЕ строится CI-линтер сверки кодов с реестрами (architecture.md:479 — wiring отдельно), НЕ создаются Django-модели/миграции/сервисы для пяти сущностей (это E5/E6/E8 — здесь только КОНТРАКТ в спеке), §4-baseline спеки НЕ переписывается, app-код `Backend/VAPS/apps` НЕ изменяется, `make gate` НЕ расширяется этой стори. Регрессия по приложению — нулевая.

## Tasks / Subtasks

- [x] **Task 1 — выгрузить три реестра в `docs/registries/`** (AC: 1, 2)
  - [x] Создать каталог `docs/registries/` и `error-codes.yaml`. Источник: спека §1255 (контракт ошибок), КАЖДАЯ таблица «Errors:» (поиск `grep -n "Errors:" docs/PersonnelStatus/VAPS_7.8.2.md` + блоки `error_code`), плюс реальные `raise DomainError(...)` в `Backend/VAPS/apps` (`grep -rn "DomainError\|error_code" Backend/VAPS/apps`). Схема записи: `code` (UPPER_SNAKE) · `http_status` (int) · `overridable` (bool) · `category` (form/business/state/auth/not_found/server) · `description` · `source` (§-якорь). Отдельным разделом — `conflict_code` значения внутри `SOFT_CONFLICT_DETECTED` (`REST_VIOLATION_CONFLICT`, `DOUBLE_ASSIGNMENT_CONFLICT`, `DUTY_OVERLAP_CONFLICT`, `WORKLOAD_EXCEEDED_CONFLICT`, `POST_REQUIREMENT_MISMATCH_CONFLICT`, `OVERQUALIFICATION_DETECTED`, BR-003 architecture.md:1037–1046).
  - [x] `ws-message-types.yaml` — 24 типа из матрицы уведомлений §33.2 (`grep -n "type_code\|33.2\|Notification" docs/PersonnelStatus/VAPS_7.8.2.md`). Схема: `type` (UPPER_SNAKE) · `priority` (INFO/WARNING/CRITICAL) · `recipients` · `repeat` · `action_url` (паттерн) · `payload` (поля из §33.3). Шапкой — контракт конверта `{"type": "<UPPER_SNAKE>", "payload": {...}}` (architecture.md:457).
  - [x] `audit-events.yaml` — явные `AUTH_LEGACY_TOKEN_ACCEPTED`/`AUTH_LEGACY_TOKEN_REJECTED` + `action_code` истории статусов (CREATED/APPLIED/EXTENDED/TERMINATED/COMPLETED/CANCELLED/MODIFIED) + канонические мутации, выводимые из API/BR-секций спеки (создание/изменение/отмена статуса; сдача/amendment дня; генерация/скачивание/удаление документа; переходы события; брокеридж; дежурства; инциденты). Схема: `action` (UPPER_SNAKE) · `entity_type` · `description` · `source` · `provenance` (`spec-explicit` | `status-history-code` | `derived-from-mutation`). MUST: пометить файл шапкой «растущий реестр; код/тип не в реестре → СТОП; новые — добавлять тем же PR» (architecture.md:33–34, 398). НЕ выдумывать действия, которых нет ни в спеке, ни в architecture — где спека молчит, сидеть на выводимом из API/BR множестве и помечать `derived-from-mutation`.
  - [x] MUST: каждый идентификатор `UPPER_SNAKE`; у каждой записи непустой `source`. Сырого PII в примерах быть не может (это коды/типы, не данные) — но и не вставлять реальные ИИН/ФИО в `description`.

- [x] **Task 2 — собрать сводный Decision Register** (AC: 3)
  - [x] Одна таблица всех ARCH-ID. Колонки (architecture.md:166): `ARCH-ID · Норма (MUST/MUST NOT) · Пример / анти-пример · Enforcement (AST-тест/constraint/CI/линт) · SOURCE (якорь секции канона) · Статус`. Строки ACTIVE: из кода — ARCH-002, ARCH-003, ARCH-004, ARCH-007 (+BR-ACCOUNT-002); из architecture.md — ARCH-DATA-020…025, ARCH-SEC-030…032, ARCH-FE-010…015. **MUST: пройти `grep -oE "ARCH-[A-Z]*-?[0-9]+" architecture.md | sort -u` и сверить со списком кода (Backend/VAPS/README.md, существующие AST-тесты) — не пропустить и не выдумать ID; зафиксировать пробелы нумерации (напр. отсутствие ARCH-005/006), если есть.**
  - [x] 8 DEFERRED-механизмов (architecture.md:95–99) получают по ARCH-ID со статусом `DEFERRED` + триггер: event/outbox (→КУ), доктор-данных (→первый инцидент целостности), телеметрия раздражения (→пользователи), синт-генератор 5000 (→нагрузочные тесты), конкурентные тесты (→масштаб), сценарий «17:00» (→перед пилотом), автосейв массовой формы (→первая жалоба), делегирование/эскалации/брокеридж (→свои этапы). **Дефолт схемы нумерации — `ARCH-DEFERRED-040…047` (см. Решения при создании); менять только осознанно.**
  - [x] Локация — **новой секцией в `architecture.md`** (дефолт; см. Решения). Бюджет документа: 736/≈1500 строк — место есть. Шапку architecture.md:35 («Сводный Decision Register … появятся по итогам стори Инвентаризация») заменить ссылкой на готовую секцию (снять формулировку «появятся»).
  - [~] (ОПЦИОНАЛЬНО, НЕ DoD-гейт) Document Map «вопрос → канонический документ → секция» (architecture.md:35, 170) — **СОЗНАТЕЛЬНО НЕ СДЕЛАН** (не DoD-гейт; навигация «вопрос→документ→секция» покрыта SOURCE-колонкой Decision Register + тремя реестрами; строка 35 architecture.md актуализирована «опциональный follow-up»). Отдельный follow-up при необходимости.

- [x] **Task 3 — дополнить контракты новых сущностей в спеке (дельта-патч)** (AC: 4)
  - [x] Открыть отдельную дельта-секцию в конце `docs/PersonnelStatus/VAPS_7.8.2.md` (по конвенции §0/§44–81: «новые изменения только дельта-патчем»; с CODEGEN-style пометкой override + датой). НЕ редактировать §4 baseline.
  - [x] **DailySubmission** (→ARCH-DATA-021, architecture.md:286–295): `ops_daily_submissions` — division_id, business_date, submitted_by/at, snapshot (JSONB rows + `schema_version`), version, `is_current`; partial unique `(division, business_date) WHERE is_current` + uq `(division, business_date, version)` (architecture.md:423); события CONFIRMED_NO_CHANGES / CHANGED / AMENDED; инвариант «ретро-правка статуса на накрытую дату ОБЯЗАНА триггерить amendment». **MUST: явно описать связь с существующими DB-OPS-013 (`ops_daily_personnel_reports` = генерируемый ДОКУМЕНТ-отчёт; `ops_daily_update_marks` = отметки INITIAL/CORRECTION) — DailySubmission это АКТ сдачи подразделением (снапшот-факт), не дубль отчёта; не плодить второй источник истины.**
  - [x] **Amendment** (→ARCH-DATA-021 amendment-flow): механизм вытеснения снапшота новой версией; запись AMENDED (кто/когда/причина/санкция/ссылка на ретро-правку); санкция на пересдачу после ухода расхода наверх выше, чем до. Контракт, не реализация.
  - [x] **DocumentSequence** (→architecture.md:466): `DocumentSequence(doc_type, year, last_number)`; инкремент `select_for_update` в транзакции финализации; номер при финализации (не при черновике); монотонна, gap-tolerant. MUST NOT: Postgres SEQUENCE.
  - [x] **AsyncJob** (→architecture.md:464): generic `AsyncJob(type, payload, result_ref)` + статус `pending/running/done/failed` + `error_code`; контракт поллинга `GET /api/jobs/{id}`. Явно соотнести с существующим спека-частным `documents_report_requests` (DB-OPS-004/§4.4) — AsyncJob обобщает долгие операции (импорт/экспорт/генерация), `documents_report_requests` — спец-случай документов.
  - [x] **Attachment**: уже есть в спеке — `documents_attachments` (DB-DOC-002, строка 2218) + API-DOC-002 (3124) + BR-DOC-001/002/003. В дельте — НЕ дублировать таблицу, а ССЫЛКОЙ подтвердить контракт и отметить дельту к architecture.md:465, которой может не быть в DB-DOC-002 (`sha256`-колонка целостности, приватный каталог вне MEDIA_URL, отдача через `X-Accel-Redirect`, whitelist content-type + `VAPS_MAX_UPLOAD_MB`). Это alignment-заметка, не новая таблица.
  - [x] MUST: каждая контракт-секция привязана к ARCH-ID и помечена статусом «контракт зафиксирован, реализация — EN» (DailySubmission/Amendment→E5; DocumentSequence/Attachment→E6; AsyncJob→E6/E8). После дельты снять оговорку architecture.md:725 «расхождения см. Gap №3 — дозафиксировать в спеке».

- [x] **Task 4 — верификация артефактов и вход для следующих стори** (AC: 1, 2, 5)
  - [x] Прогон валидатора: `python3 -c "import yaml,glob,re; [yaml.safe_load(open(f)) for f in glob.glob('docs/registries/*.yaml')]"` без исключений; непустота каждого файла; все ключи-идентификаторы матчат `^[A-Z][A-Z0-9_]*$`. Зафиксировать счётчики (кодов/типов/событий) в Debug Log и сверить с инвентарём (≈35 / 24 / ≥9).
  - [x] Перекрёстная сверка (ручная, разовая — НЕ CI): каждый `error_code`, встречающийся в таблицах «Errors:» спеки, присутствует в `error-codes.yaml`; каждый `type_code` §33.2 — в `ws-message-types.yaml`. Расхождения — строкой в Debug Log (это вход для будущего CI-линтера architecture.md:479, который строит отдельная стори).
  - [x] Регрессия: `git status --porcelain` по `Backend/VAPS/apps`, `Backend/VAPS/config`, `pyproject.toml`, `Makefile`, `Backend/PersonnelStatus` — ПУСТО (стори их не трогает). `make gate` не расширялся.
  - [x] Таблица «находка → потребитель»: реестры → каждая API-стори E2+ (коды/типы дереференсятся); Decision Register → каждая стори (вместо полного перечитывания architecture.md); контракты сущностей → E5 (DailySubmission/Amendment), E6 (DocumentSequence/Attachment), E6/E8 (AsyncJob), E11 (ws-types).

## Dev Notes

### Цель (одним предложением)

Превратить три «обещанных, но не выгруженных» ссылки (architecture.md:668–671) в реальные разыменовываемые артефакты — реестры-yaml, одну таблицу Decision Register и контракт-секции пяти сущностей в спеке — чтобы СТОП-правило закрытого мира («нет в реестре/Glossary/спеке → остановись и спроси») стало исполнимым, а будущие агенты перестали быть обязаны перечитывать весь architecture.md перед каждым решением (architecture.md:35).

### Что это за стори (и чем отличается от 1.1–1.11)

- **Документ-артефакт, не код.** 1.1–1.8 — Django-код (модели/сервисы/команды/тесты/гейт); 1.9–1.11 — инфраструктурно-организационные спайки. 1.12 — чистая инвентаризация/документирование: yaml-реестры + таблица + дельта к спеке. Ноль Python-моделей, ноль миграций, ноль app-кода. Это явно зафиксировано в epics.md:164 («Инвентаризация … — сопутствующий документ-артефакт, не стори [код]»).
- **Закрывает три Important-пробела архитектурной валидации** (architecture.md:668–672) — ровно один-к-одному: Gap №1 → Task 1 (реестры), Gap №2 → Task 2 (Decision Register), Gap №3 → Task 3 (контракты в спеке). Снимает условие подписи Winston «Инвентаризация/скелет — первая работа, блокирует остальное» (architecture.md:717) и подготавливает E2+.
- **Последняя стори Epic 1.** После неё — `epic-1-retrospective` (optional). Дальше E2 (кадровое ядро) уже опирается на готовые реестры/Register.

### Три deliverable'а и их точные источники

| Deliverable | Куда | Источник (откуда выгружать) | Gap |
|---|---|---|---|
| `error-codes.yaml` | `docs/registries/` | спека §1255 + все «Errors:» таблицы + `raise DomainError` в `Backend/VAPS/apps` | architecture.md:669 |
| `audit-events.yaml` | `docs/registries/` | спека §4.6 audit + `action_code` истории статусов + явные `AUTH_LEGACY_*` + выводимые из API/BR мутации | architecture.md:669 |
| `ws-message-types.yaml` | `docs/registries/` | спека §33.2 матрица уведомлений (24 типа) + §33.3 payload + конверт architecture.md:457 | architecture.md:669 |
| Сводный Decision Register | новая секция `architecture.md` (дефолт) | все ARCH-ID кода + architecture.md + 8 DEFERRED-механизмов (95–99) | architecture.md:670 |
| Контракты 5 сущностей | дельта-секция `docs/PersonnelStatus/VAPS_7.8.2.md` | architecture.md ARCH-DATA-021 (286–295), Process Patterns (464–466), DB-DOC-002 (Attachment уже есть) | architecture.md:671 |

### Реестры: схема, СТОП-правило, что НЕ строить

- **Зачем именно yaml:** architecture.md:34 — «Код ошибки / тип события не в `docs/registries/*.yaml` → СТОП». architecture.md:395–398 описывает реестры как «разыменовываемые ссылки вместо "см. спеку"». Формат-намёк: error-codes — «UPPER_SNAKE, http_status, overridable» (395).
- **Реестр — растущий seed, не исчерпывающий справочник.** architecture.md:398: «новые — добавлять в реестр тем же PR. Линтер/CI сверяет использованные коды с реестром». Сегодня выгружаем СТАРТОВОЕ содержимое из спеки; полнота на день 1 не требуется — требуется непустота + провенанс + дисциплина роста.
- **audit-events — самый «тонкий» в спеке.** Спека перечисляет аудит-действия НЕ списком (явно только два `AUTH_LEGACY_TOKEN_*` + семь `action_code` истории статусов; architecture.md:456 требует «action UPPER_SNAKE из реестра», но самого реестра ещё нет — его и создаём). Где спека молчит — выводить минимальный канонический набор мутаций из API/BR-секций и помечать `provenance: derived-from-mutation`. НЕ выдумывать события без опоры.
- **НЕ строить здесь:** loader-проверку при старте (architecture.md:574 — отдельная стори, вероятно E8/прод-каркас); CI-линтер сверки кодов с реестрами (architecture.md:479 — wiring в gate отдельной стори). Реестр — данные; механизмы их проверки — другая ответственность.

### Decision Register: формат, состав, локация

- **Формат (architecture.md:166):** блок/строка на решение — ARCH-ID, норма (MUST/MUST NOT), пара пример/анти-пример, enforcement (AST-тест/constraint/CI/линт), SOURCE-ссылка на канон (ЯКОРЯ секций, не номера строк — строки плывут), статус ACTIVE / DEFERRED(+триггер).
- **Состав ACTIVE (19):** код — ARCH-002 (core UUID PK), ARCH-003 (operations integer PK; cross-context плоские UUIDField без FK), ARCH-004 (селекторы — единственный канал cross-context чтения, AST-тесты), ARCH-007/BR-ACCOUNT-002 (user_id — строка); architecture.md — ARCH-DATA-020…025, ARCH-SEC-030…032, ARCH-FE-010…015.
- **8 DEFERRED → 8 новых ARCH-ID** (architecture.md:99 это требует). Триггеры — из architecture.md:97. Дефолт нумерации `ARCH-DEFERRED-040…047`.
- **Локация — секция architecture.md (дефолт).** Обоснование: architecture.md:164 («Требования к самому architecture.md») перечисляет Decision Register как требование САМОГО architecture.md; строки 35 и 170 (обе в architecture.md) обещают, что он «появится по итогам стори Инвентаризация»; это индекс архитектурных НОРМ, а не рантайм-реестр идентификаторов (те — в `docs/registries/`); бюджет документа позволяет (736/≈1500). Альтернатива (отдельный `docs/registries/decision-register.md`) — если Bratan предпочтёт co-location с yaml; легко переносится (см. Открытые вопросы).

### Контракты в спеке: дельта-механизм и пять сущностей

- **Спека — Fixed Baseline; поправки ТОЛЬКО дельтой.** §0.1/§0.2 (строки 31–51): приняты только перечисленные слои-дельты; «Любые новые изменения допускаются только как дельта-патчи к этому master-документу»; полные перезаписи НЕ принимаются. Документ уже так живёт: §44–81 override-секции поверх v7.5–7.7. Значит наши контракты — НОВАЯ дельта-секция в конце, с CODEGEN-style override-пометкой и датой. **НЕ переписывать §4** — иначе ломается аудируемость baseline.
- **Прецедентность (architecture.md:32, 725):** по контрактам таблиц/API спека VAPS_7.8.2 СТАРШЕ architecture.md. Эти 5 сущностей сейчас живут только в architecture (младший документ) → их «промоушен» в спеку дельтой делает их каноном/старшими и дереференсируемыми (закрывает Gap №3 буквально: «дозафиксировать в спеке»).
- **Состояние пяти (по инвентарю спеки):**
  - **DailySubmission** — в спеке НЕТ как `ops_daily_submissions`; есть смежное DB-OPS-013 (`ops_daily_personnel_reports` отчёт + `ops_daily_update_marks` отметки). Канонический вид — ARCH-DATA-021. **Самый тонкий момент стори — соотнести, не задублировать** (см. Подводные камни).
  - **Amendment** — концепт «ретро-правка» в спеке есть, отдельной сущности нет; канон — amendment-flow ARCH-DATA-021 (событие AMENDED на DailySubmission).
  - **DocumentSequence** — в спеке нет (спека нумерует файлы таймстампом §34); канон — architecture.md:466.
  - **AsyncJob** — в спеке нет generic-модели; есть doc-частное `documents_report_requests` (§4.4); канон — architecture.md:464.
  - **Attachment** — В СПЕКЕ УЖЕ ЕСТЬ (DB-DOC-002 + API-DOC-002 + BR-DOC-001/002/003). Только ссылка + alignment-заметка к architecture.md:465 (sha256/приватный каталог/X-Accel-Redirect). НЕ дублировать таблицу.
- **Это КОНТРАКТЫ, не реализация.** Django-модели/миграции/сервисы пяти сущностей строят ПОЗЖЕ (DailySubmission/Amendment→E5; DocumentSequence/Attachment→E6; AsyncJob→E6/E8). Здесь — только спека-секция, чтобы СТОП-правило дереференсилось.

### Что НЕ строить / НЕ трогать (Out of Scope)

- **loader реестров при старте** (architecture.md:574) — отдельная стори каркаса; здесь только данные.
- **CI-линтер «сверка кодов с реестрами»** (architecture.md:479) — wiring в gate отдельной стори; здесь сверка разовая ручная (Task 4).
- **Django-модели/миграции/сервисы/API/тесты пяти сущностей** — E5/E6/E8. Здесь ноль кода.
- **Переписывание §4 baseline спеки** — запрещено (§0). Только дельта в конце.
- **App-код `Backend/VAPS/apps`, `config`, `pyproject.toml`, `Makefile`** — не трогаются. Донор `Backend/PersonnelStatus` — не трогается.
- **Полный Glossary-аудит** — Glossary уже есть в architecture.md (377–391); пополнение — правкой architecture.md по месту, не предмет этой стори (architecture.md:391). Document Map — опционально (Task 2), не DoD-гейт.

### Текущее состояние репозитория (прочитано 2026-06-19)

- `docs/registries/` — **НЕ существует** (создаётся Task 1). Никакого loader'а/CI, который её сейчас читает (проверено: реестры пока только «объявлены»).
- `docs/PersonnelStatus/VAPS_7.8.2.md` — мастер-спека, 8456 строк, Fixed Baseline (§0). Содержит §4 схему БД (DB-CORE-*, DB-OPS-013, DB-DOC-002 attachments, §4.4 documents, §4.5 notifications, §4.6 audit), §1255 контракт ошибок, §33.2 матрицу уведомлений. **Внимание двух-машинного окружения:** `Backend/VAPS/README.md:4` ссылается на `docs/VisitX/VAPS_7.8.2.md` — на ЭТОЙ машине файл живёт по `docs/PersonnelStatus/VAPS_7.8.2.md` (дубль путей VisitX→PersonnelStatus, не закоммичен). Работать по фактическому пути `docs/PersonnelStatus/`.
- `_bmad-output/planning-artifacts/architecture.md` — 736 строк (бюджет ≈1500). Содержит все ARCH-ID, требование Decision Register (164–173), Gap-анализ (664–675), Handoff (721–733).
- Рабочее дерево: review-residue 1.9/1.10 (`deploy/spike-1.9/*`, `spikes/1.10-perf-grid/BUDGET.md`, стори 1-9/1-10, `deferred-work.md`, `sprint-status.yaml`). К 1.12 отношения не имеют — не в File List.

### Решения, принятые при создании стори (дефолты; менять только осознанно)

1. **Decision Register → секция в `architecture.md`** (не отдельный файл). Обоснование выше (§164 — требование самого architecture.md; обещание строк 35/170; бюджет). Альтернатива `docs/registries/decision-register.md` — открытый вопрос Bratan, перенос тривиален.
2. **Контракты сущностей → дельта-секция в конце `VAPS_7.8.2.md`** (не правка §4). Прямое следствие §0 («только дельта-патчи»). Конвенция документа (§44–81 override).
3. **Реестры — стартовый seed из спеки, помеченный как растущий.** Полнота на день 1 не требуется (architecture.md:398); требуется непустота + провенанс + дисциплина «новые тем же PR».
4. **8 DEFERRED-механизмов → `ARCH-DEFERRED-040…047`.** Дефолт схемы нумерации (следующий свободный десяток после FE-015/SEC-032). Семейное имя `DEFERRED` явно отделяет их от ACTIVE.
5. **Document Map — опционально, не DoD-гейт.** AC его не требует (требуют architecture.md:35/170 «в целом»). Добавить, если дёшево; иначе follow-up. Анти-gold-plating.
6. **Attachment — ссылкой, не дублем.** Уже DB-DOC-002. Дельта только alignment к architecture.md:465.
7. **audit-events: где спека молчит — выводимый минимум, помеченный `derived-from-mutation`.** Не выдумывать события без опоры на API/BR.

### Подводные камни для dev-агента

- **DailySubmission vs DB-OPS-013 — главная ловушка.** Спека УЖЕ имеет `ops_daily_personnel_reports` (генерируемый отчёт-документ) и `ops_daily_update_marks` (отметки INITIAL/CORRECTION). architecture.md вводит `ops_daily_submissions` (АКТ сдачи подразделением со снапшотом и версионированием). Это РАЗНЫЕ сущности (документ ≠ акт сдачи), но соблазн — задублировать или молча противоречить DB-OPS-013. MUST: в дельте ЯВНО описать связь (отчёт строится из сданных submission'ов; отметки — следы сдачи) и не плодить второй источник истины. Прочитать architecture.md:286–295 ЦЕЛИКОМ перед написанием.
- **Не корраптить Fixed Baseline.** §4 спеки — заморожен (§0). Любая правка существующих DB-*/API-*/BR-* секций = нарушение приоритета документа. Только ДОБАВЛЯТЬ дельту в конце.
- **SOURCE — якоря секций, не номера строк** (architecture.md:166). Спека 8456 строк — номера плывут при любой дельте. Ссылаться на `§1255`, `DB-OPS-013`, `API-DOC-002`, `§33.2`, `ARCH-DATA-021`, не на «строка N».
- **Путь спеки — `docs/PersonnelStatus/`, не `docs/VisitX/`** (README врёт из-за двух-машинного дубля). Проверить `ls docs/PersonnelStatus/VAPS_7.8.2.md` перед правкой.
- **Счётчики должны сходиться с источником.** Если выгрузил 18 кодов, а в спеке «Errors:»-таблиц на 30 — недовыгрузил. Прогнать `grep` по всем «Errors:»/`error_code`/`type_code`, сверить число. Реестр-недосып молча ломает СТОП-правило (агент решит «кода нет → СТОП», хотя он есть в спеке).
- **UPPER_SNAKE строго.** Линтер (когда появится) матчит регуляркой; `Some_Code`/`someCode` сломают сверку. Все ключи — `^[A-Z][A-Z0-9_]*$`.
- **Не превратить в кодовую стори.** Соблазн — «раз дописываю AsyncJob-контракт, сразу набросаю модель». НЕТ: модели/миграции — E5/E6/E8. Здесь только текст контракта.

### Технические версии / окружение

- PyYAML 6.0.1 доступен (проверено) → валидатор `python3 -c "import yaml; ..."` работает. yaml-реестры — обычный YAML, без кастомных тегов.
- Никаких новых зависимостей, никакого Django-окружения для этой стори (документы + yaml). `make gate`/pytest не запускаются как DoD (нечего тестировать кодом) — DoD = валидность/непустота/сходимость/резолвимость + нулевая регрессия.
- Стек контекста (для контракт-секций, не реализуется здесь): Django 5.x + DRF 3.15+, PostgreSQL, Celery+Redis (architecture.md:117).

### Git-интеллидженс

- HEAD = `1b65f54`. Закоммичены 1.1–1.8 + спайки 1.9/1.10/1.11 (1.11 — review, артефакты за Bratan). В рабочем дереве — review-residue 1.9/1.10 (см. baseline_commit). **К 1.12 отношения не имеют** — не в File List, не откатывать.
- Урок ревью 1.9–1.11: File List полный и честный; Completion Notes без вранья (каждое «проверено» — с фактической командой: для 1.12 это прогон yaml-валидатора + grep-счётчики + `git status` регрессии). Прецедент 1.9/1.10/1.11: документ-/спайк-артефакты коммитятся ОТДЕЛЬНОЙ стори осознанно; статус → review/done по факту, авто-done переопределять осознанно если что-то остаётся за Bratan.
- Эта стори — чисто AI-исполняемая (нет человеко-части как в спайках): все три артефакта агент пишет сам. Коммит — по конвенции проекта (см. Открытые вопросы: коммитит агент или Bratan).

### Зависимости

- **Depends on:** architecture.md (источник ARCH-ID, Gap-анализа, канон-форм пяти сущностей) и VAPS_7.8.2.md (источник кодов/типов/событий + место для дельты). Оба готовы. От кода 1.1–1.11 почти не зависит (документ-трек); опционально сверяет `error-codes.yaml` с `raise DomainError` в `Backend/VAPS/apps`.
- **Blocks / питает:** **E2+ целиком** — каждая API-стори дереференсит коды/типы из реестров; каждая стори использует Decision Register вместо полного перечитывания architecture.md (architecture.md:35). Контракты сущностей питают: **E5** (DailySubmission/Amendment — сдача дня, amendment-flow), **E6** (DocumentSequence — нумерация; Attachment — вложения), **E6/E8** (AsyncJob — долгие операции), **E11** (ws-message-types — уведомления). Снимает условие подписи Winston (architecture.md:717).
- **Связана:** epic-1-retrospective (следующая после 1.12 — последняя в E1). Реестры — будущий вход loader-стори (architecture.md:574) и CI-линтер-стори (architecture.md:479).

### Тесты стори (для документ-артефакта — критерии валидности, не pytest)

- **YAML-валидность (обязательно):** `python3 -c "import yaml,glob; [yaml.safe_load(open(f)) for f in glob.glob('docs/registries/*.yaml')]"` → без исключений; каждый файл непуст (`len(safe_load) > 0`); каждый идентификатор-ключ матчит `^[A-Z][A-Z0-9_]*$`.
- **Сходимость счётчиков:** число кодов/типов/событий в реестрах ≥ инвентарю источника (error ≈35, ws 24 из §33.2, audit ≥9); зафиксировать в Debug Log с командой-grep.
- **Резолвимость ссылок:** каждый `error_code` из «Errors:»-таблиц спеки есть в `error-codes.yaml`; каждый `type_code` §33.2 — в `ws-message-types.yaml`; каждый ARCH-ID в Decision Register имеет рабочий SOURCE-якорь; каждая контракт-секция спеки привязана к ARCH-ID.
- **Decision Register полнота:** все 19 ACTIVE ARCH-ID присутствуют (сверка с `grep -oE "ARCH-[A-Z]*-?[0-9]+" architecture.md | sort -u` + код); все 8 DEFERRED имеют ID+триггер.
- **Дельта-дисциплина спеки:** §4 baseline НЕ изменён (`git diff` показывает только ДОБАВЛЕННУЮ секцию в конце VAPS_7.8.2.md, без правок существующих строк §4).
- **Регрессия (нулевая):** `git status --porcelain` по `Backend/VAPS/apps`, `config`, `pyproject.toml`, `Makefile`, `Backend/PersonnelStatus` — пусто. `make gate` не затронут.

### Definition of Done

- [x] `docs/registries/error-codes.yaml` / `audit-events.yaml` / `ws-message-types.yaml` существуют, непусты, валидный YAML, UPPER_SNAKE-ключи, у каждой записи `source`.
- [x] error-codes выгружены из §1255 + всех «Errors:»-таблиц + DomainError-кода (≈35, с `http_status`/`overridable`); ws — 24 типа §33.2; audit — явные + status-`action_code` + выводимые мутации с провенансом; реестры помечены как растущие.
- [x] Decision Register — одна таблица: 19 ACTIVE ARCH-ID (формат §166: норма/пример-анти/enforcement/SOURCE-якорь/статус) + 8 DEFERRED с ARCH-ID и триггером; шапка architecture.md:35 актуализирована.
- [x] Дельта-секция в `VAPS_7.8.2.md`: контракты DailySubmission/Amendment/DocumentSequence/AsyncJob + ссылка-подтверждение Attachment (DB-DOC-002); §4 baseline НЕ переписан; каждая привязана к ARCH-ID; связь DailySubmission↔DB-OPS-013 явно описана.
- [x] Анти-gold-plating соблюдён: loader/CI-линтер/Django-модели/миграции НЕ построены; app-код/донор/Makefile/pyproject не тронуты; §4 baseline не корраптнут.
- [x] Валидатор + grep-сходимость + нулевая регрессия зафиксированы в Debug Log (без вранья, с командами).
- [x] Таблица «находка → потребитель» (E2/E5/E6/E8/E11) оформлена.

### Project Structure Notes

- **Файлы создаются:** `docs/registries/error-codes.yaml`, `docs/registries/audit-events.yaml`, `docs/registries/ws-message-types.yaml`.
- **Файлы изменяются:** `_bmad-output/planning-artifacts/architecture.md` (новая секция Decision Register + актуализация строки 35); `docs/PersonnelStatus/VAPS_7.8.2.md` (добавленная дельта-секция в конце — БЕЗ правок §4).
- **Лимит «≤5 файлов» (CLAUDE.md):** относится к кодовым стори со смешением слоёв БД/API/фронт. Здесь — ОДНА ответственность (разыменовать СТОП-правило инвентаризацией), файлы документные и одноцелевые, а декомпозиция уже сделана эпиком (1.12 — лист). Дробить далее значило бы противоречить epics.md, который уже разложил E1 на 1.1–1.12. Три deliverable'а — три Task'а внутри одной стори, как и задумано AC.
- **Вне перечисленного — изменений ноль.** App-код, донор, конфиг, Makefile не трогаются.

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story-1.12 (381–387)] — AC стори: реестры непусты, Decision Register одной таблицей, контракт-секции пяти сущностей в спеке; user-statement «нет в реестре → СТОП разыменовывается».
- [Source: _bmad-output/planning-artifacts/epics.md (164)] — Инвентаризация = «сопутствующий документ-артефакт, не [код-]стори».
- [Source: _bmad-output/planning-artifacts/architecture.md (33–35)] — СТОП-правило закрытого мира; реестры выгружаются в walking skeleton недели 1; Decision Register/Document Map «появятся по итогам стори Инвентаризация».
- [Source: _bmad-output/planning-artifacts/architecture.md (164–173)] — требования к Decision Register (формат §166), Document Map, «первое действие — инвентаризация существующих ARCH-решений».
- [Source: _bmad-output/planning-artifacts/architecture.md (393–398)] — реестры: error-codes (UPPER_SNAKE/http_status/overridable), audit-events, ws-message-types; выгрузка из VAPS_7.8.2 — задача инвентаризации; «нет в реестре → СТОП; новые тем же PR».
- [Source: _bmad-output/planning-artifacts/architecture.md (664–675, 721–733)] — Gap-анализ (три Important-пробела = три Task'а), Handoff, прецедентность контрактов спеки.
- [Source: _bmad-output/planning-artifacts/architecture.md (286–295)] — ARCH-DATA-021 DailySubmission/amendment-flow (канон-форма + инвариант ретро-правки).
- [Source: _bmad-output/planning-artifacts/architecture.md (456–457, 464–466)] — audit action/ws-конверт; AsyncJob/Attachment/DocumentSequence Process Patterns.
- [Source: _bmad-output/planning-artifacts/architecture.md (95–99)] — 8 DEFERRED-механизмов + триггеры; «каждый получает ARCH-ID».
- [Source: docs/PersonnelStatus/VAPS_7.8.2.md (1–53)] — §0 правило приоритета документа (поправки только дельта-патчем; §44–81 override).
- [Source: docs/PersonnelStatus/VAPS_7.8.2.md (§1255, «Errors:»-таблицы)] — контракт ошибок (VALIDATION_ERROR/PERMISSION_DENIED/*_NOT_FOUND/…) — источник error-codes.yaml.
- [Source: docs/PersonnelStatus/VAPS_7.8.2.md (§33.2/§33.3)] — матрица 24 уведомлений + payload — источник ws-message-types.yaml.
- [Source: docs/PersonnelStatus/VAPS_7.8.2.md (DB-OPS-013, §4.4–4.6, DB-DOC-002, API-DOC-002)] — существующие смежные секции (daily reports/marks, documents, notifications, audit, attachments) — для соотнесения контрактов.
- [Source: _bmad-output/implementation-artifacts/1-11-спайк-выгрузка-данных-донора.md] — формат стори проекта (Dev Notes/Решения-дефолты/Подводные камни/честные Completion Notes/File List), конвенция отдельного коммита артефактов.

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (Opus 4.8, 1M context)

### Debug Log References

- **Авторитетная выгрузка из спеки (не угадано), 2026-06-19:**
  - error-codes: `grep "Errors:"` (2 таблицы: §1255-блок API-OPS-001 строки 1306–1315, API-OPS-018 строки 3158–3164) + прозовые `NNN CODE` (`grep -oE "(400|401|403|404|409|422|423|500) [A-Z_]{4,}"`) + `"error_code"`/`"conflict_code"` литералы. Каждый «claimed» код (DUTY_CONFLICT_DETECTED, TEMPLATE_DATA_MISSING, UNDER_MINIMUM_GROUP_MEMBERS, ENTITY_NOT_FOUND, conflict-коды BR-003) подтверждён `grep -c` в спеке (>0) — НИ ОДНОГО выдуманного. `DomainError` в `Backend/VAPS/apps` = 0 (API-поверхность ещё не построена, E2+) → коды чисто из спеки.
  - ws-message-types: §33.2 матрица уведомлений (строки 4602–4627) — ровно 24 type_code, считаны построчно (priority/recipients/repeat/action_url из таблицы), payload — §33.3.
  - audit-events: §81 (AUTH_LEGACY_TOKEN_ACCEPTED/REJECTED, строки 7019–7020) + DB-OPS-007 action_code CHECK (строка 2769, 7 значений) + выводимые мутации с основанием (API-OPS-001/002 audit-required, BR-005 «сводная запись в audit_logs», BR-016, BR-DOC-003 «скачивание фиксируется», ARCH-DATA-021). Каждая запись помечена provenance.
- **Валидатор (PASS), 2026-06-19:** `python3` + PyYAML 6.0.1: `yaml.safe_load` по всем трём — без исключений; все идентификаторы матчат `^[A-Z][A-Z0-9_]*$` (bad=[]); непустота. Счётчики: error 30 codes + 7 conflict_codes; ws 24; audit 9 actions + 7 status_codes.
- **Перекрёстная сверка (PASS):** все коды из «Errors:»-таблиц спеки (9) ∈ error-codes.yaml (missing=NONE); все 24 §33.2 type_code ∈ ws-message-types.yaml (missing=NONE).
- **Decision Register (PASS):** все 19 ACTIVE ARCH-ID присутствуют в таблице architecture.md (MISSING-чек пуст); 8 DEFERRED-строк (ARCH-DEFERRED-040…047) с триггерами. Пробелы 005/006/008/009 зафиксированы как зарезервированные.
- **Дельта спеки append-only (PASS):** section 82 в самом конце (строка 8460, после §81 строка 8443); §4 baseline не тронут (якоря `## 0.`/`## 4.4`/`## 4.6`/DB-OPS-013/DB-DOC-002 на месте, заголовки не переписаны).
- **Регрессия (нулевая, PASS):** `git status --porcelain` по `Backend/VAPS/apps`, `config`, `pyproject.toml`, `Makefile`, `Backend/PersonnelStatus` — пусто. `make gate` не расширялся.
- **🚩 РЕШЕНИЕ ЗА BRATAN — `docs/` полностью в `.gitignore` (строка 221):** 0 файлов под `docs/` трекаются git; `docs/PersonnelStatus/VAPS_7.8.2.md` — untracked. Значит выгруженные реестры и дельта спеки СУЩЕСТВУЮТ на диске (AC «существуют и непусты» выполнен), но НЕ в VCS. Архитектура же требует `docs/registries/*.yaml` как репо-артефакты под CI-сверку (architecture.md:479, 574) — противоречие с blanket-ignore. **НЕ редактировал `.gitignore` сам** (репо-политика + `docs/` держит донор-материал/PII; правка ignore — решение владельца, прецедент «коммит за Bratan»). **Рекомендация:** добавить негейт `!docs/registries/` (и `!docs/registries/**`) в `.gitignore`, чтобы реестры трекались, оставив остальной `docs/` игнорируемым. Канон-спека (VAPS_7.8.2.md) вне VCS — более широкий архитектурный вопрос (где живёт старший контракт-документ в репо), не решается этой стори.

### Completion Notes List

Документ-артефакт (продукт — РАЗЫМЕНОВЫВАЕМЫЕ ССЫЛКИ, не код). Все три Important-пробела архитектурной валидации закрыты на диске.

- ✅ **Task 1 (реестры):** `docs/registries/error-codes.yaml` (30 кодов + 7 conflict_codes, http_status/overridable/category/source), `audit-events.yaml` (9 actions + 7 status action_codes, provenance), `ws-message-types.yaml` (24 типа §33.2 + envelope). Валидный YAML, UPPER_SNAKE, непустые, помечены растущими.
- ✅ **Task 2 (Decision Register):** одна таблица в `architecture.md` (§Decision Register): 19 ACTIVE (4 кода + 6 DATA + 3 SEC + 6 FE, формат §166: норма/пример-анти/enforcement/SOURCE/статус) + 8 DEFERRED (ARCH-DEFERRED-040…047 с триггерами). Строка 35 актуализирована (ссылка на готовую секцию, «появятся» снято).
- ✅ **Task 3 (контракты в спеке):** section 82 дельта-патчем (§0-конвенция) в конце `VAPS_7.8.2.md` — DailySubmission (`ops_daily_submissions`, partial unique is_current, связь с DB-OPS-013 явно описана — отчёт≠сдача, отметка≠снапшот), Amendment (событие AMENDED + инвариант-триггер), DocumentSequence (`documents_sequences`, FOR UPDATE, MUST NOT SEQUENCE), AsyncJob (`jobs_async`, поллинг, связь с documents_report_requests). Attachment — ссылка на DB-DOC-002 + alignment к architecture.md:465 (НЕ дубль). §4 baseline не переписан, каждая секция привязана к ARCH-ID.
- ✅ **Task 4 (верификация + вход):** валидатор/cross-ref/append-only/регрессия — все PASS (Debug Log). Таблица «находка → потребитель»: реестры → каждая API-стори E2+; Decision Register → каждая стори (вместо полного перечитывания architecture.md); контракты → E5 (DailySubmission/Amendment), E6 (DocumentSequence/Attachment), E6/E8 (AsyncJob), E11 (ws-типы).
- [~] **Document Map** — сознательно НЕ сделан (опционально, не DoD-гейт; навигация покрыта SOURCE-колонкой Register + реестрами). Не раздувал стори.
- ✅ **Анти-gold-plating:** loader/CI-линтер/Django-модели/миграции НЕ строились; §4 baseline не корраптнут; app-код/донор/Makefile/pyproject не тронуты.

**Статус → review** (не auto-done): открыто **РЕШЕНИЕ ЗА BRATAN** по `.gitignore docs/` (реестры+спека вне VCS) + коммит артефактов — по прецеденту 1.9/1.10/1.11 (агент не коммитит; интеграция за Bratan). Артефакты НЕ закоммичены агентом. Review-residue 1.9/1.10 в рабочем дереве — к 1.12 не относится, в File List не включён.

### File List

Новые (артефакты стори):
- `docs/registries/error-codes.yaml`
- `docs/registries/audit-events.yaml`
- `docs/registries/ws-message-types.yaml`

Изменённые (артефакты стори):
- `_bmad-output/planning-artifacts/architecture.md` (новая секция «Decision Register (сводный)» + актуализация строки 35; tracked, +37/−1)
- `docs/PersonnelStatus/VAPS_7.8.2.md` (добавлена section 82 дельта-патчем в конце; §4 не тронут; **untracked — `docs/` в .gitignore**)

Изменённые (BMAD-трекинг):
- `_bmad-output/implementation-artifacts/1-12-инвентаризация-артефакт.md` (чекбоксы, Dev Agent Record, Change Log, Status)
- `_bmad-output/implementation-artifacts/sprint-status.yaml` (1-12 → in-progress → review)

⚠️ Примечание трекинга: `docs/registries/*.yaml` и `docs/PersonnelStatus/VAPS_7.8.2.md` под `.gitignore:221 docs/` — на диске есть, в git нет (см. Debug Log, РЕШЕНИЕ ЗА BRATAN).

### Review Findings

Проход 1 (bmad-code-review, 2026-06-19, scoped `{diff_output}` по артефактам 1.12: architecture.md Decision Register + `docs/registries/*.yaml` + дельта §82 VAPS_7.8.2.md; 3 слоя на Opus 4.8 — Blind Hunter / Edge Case Hunter / Acceptance Auditor). Итог триажа: 3 decision-needed, 3 patch, 3 defer, 10 dismiss. Конвергенция слоёв: source-по-номерам-строк (Blind+Auditor), висячая `architecture.md:1037-1046` (Edge+Blind). AC-вердикт Аудитора: AC-1 PARTIAL, AC-2 PARTIAL, AC-3 PASS, AC-4 PASS, AC-5 PASS. Самоотчёт стори сверен с фактами — расхождений нет (счётчики/append-only/регрессия честны).

- [x] [Review][Decision] ✅РЕШЕНО (вар.1, применено): негейт `docs/*` + `!docs/registries/` в `.gitignore` — реестры трекаемы (`git check-ignore` подтвердил), спека/донор остаются локальными. `.gitignore docs/` — реестры и спека вне VCS — `docs/` целиком в `.gitignore:221` → три `docs/registries/*.yaml` и `VAPS_7.8.2.md` untracked. AC-1 «существуют и непусты» выполнен НА ДИСКЕ, но архитектура требует реестры как репо-артефакт под CI-сверку (architecture.md §Enforcement «сверка кодов с реестрами»; loader-стори architecture.md:574). Untracked-файл в CI не попадёт. Это сам же флаг стори «🚩 РЕШЕНИЕ ЗА BRATAN». Опции: (a) негейт `!docs/registries/` + `!docs/registries/**` в `.gitignore` (рекомендация стори); (b) перенести реестры под трекаемый путь; (c) оставить как есть и осознанно отложить трекинг до E8-каркаса. [auditor + checkpoint]
- [x] [Review][Decision] ✅РЕШЕНО (вар.1, применено): канон = нормативный «Allowed» §6 (8 кодов) — добавлен `UNAVAILABLE_STATUS_CONFLICT`; `RATING_DATA_MISSING` помечен как feature-flag-расширение (BR-RATING-CONFLICT-002); `EMPTY_GROUP` оставлен 422-hard (§1093/AC-022) с задокументированной двойственностью; расхождение §6↔§BR-003 зафиксировано комментарием как вход E7/spec-owner. conflict_codes реестра разошёлся с нормативным списком спеки — `error-codes.yaml` `conflict_codes:` содержит `RATING_DATA_MISSING` (нет в нормативном «Allowed `conflict_code`» §774), но НЕ содержит `UNAVAILABLE_STATUS_CONFLICT` (есть в §774); `EMPTY_GROUP` заведён только как 422-error, потеряв conflict-роль (§780). Корень: спека содержит ДВА несогласованных списка — §773-780 (8 кодов, «Allowed») vs §1035-1043 (6 кодов, «Soft-warning»); реестр унаследовал §1035 молча. Нужно решение: какой список канон → доукомплектовать реестр. [edge]
- [x] [Review][Decision] ✅РЕШЕНО (вар.1, применено): metadata-исключение — в §82-интро добавлена заметка ARCH-DATA-022 (DDL-default только для infra/metadata-timestamps; `submitted_at` при реализации ставит app/Clock-слой, DB-default — страховка; business-время/статусы — только Clock). §82 DDL `DEFAULT CURRENT_TIMESTAMP` vs ARCH-DATA-022 — `ops_daily_submissions.submitted_at`, `jobs_async.created_at/updated_at` объявлены `DEFAULT CURRENT_TIMESTAMP`. ARCH-DATA-022 (architecture.md:300) запрещает «NOW()-defaults в доменной логике и business-полях» и предупреждает «freezegun не трогает Postgres» (DB-default обходит Clock-сервис и контроль времени в тестах). `submitted_at` — семантически значимый business-timestamp (момент сдачи), не чистая инфра-метка. Подтвердить: это допустимое metadata-исключение (architecture.md:429 разрешает UTC-timestamps в БД) ЛИБО контракт §82 должен указать Clock/app-источник для `submitted_at`. [blind]
- [x] [Review][Patch] ✅ПРИМЕНЕНО: добавлен `OBJECT_CHECKLIST_REQUIRED` (400, category state, source §60) в error-codes.yaml. Пропущен error_code `OBJECT_CHECKLIST_REQUIRED` (400) — нормативный каталог §60 спеки (`VAPS_7.8.2.md:6495`) содержит `OBJECT_CHECKLIST_REQUIRED`, соседи по таблице (`INVALID_SCOPE_STATUS`, `STATUS_OVERLAP_WARNING`) в реестр попали, а этот — нет. Недосып молча ломает СТОП-правило. Добавить запись (400 / category state / source §60). [docs/registries/error-codes.yaml]
- [x] [Review][Patch] ✅ПРИМЕНЕНО: `architecture.md:1037-1046` заменён на якорь `VAPS_7.8.2 §BR-003` (матрица живёт в спеке, строка 1020). Висячая ссылка `architecture.md:1037-1046` (BR-003) — комментарий над `conflict_codes:` ссылается на «матрицу BR-003 (architecture.md:1037-1046)». Факт: architecture.md = 772 строки (строк 1037-1046 нет), `BR-003` в architecture.md отсутствует (grep=0); матрица BR-003 живёт в СПЕКЕ (`VAPS_7.8.2.md:1020 ## BR-003`). Заменить на якорь `VAPS_7.8.2 §BR-003`. [docs/registries/error-codes.yaml:224]
- [x] [Review][Patch] ✅ПРИМЕНЕНО: `§1255`→`§6 REST API`/`§60`; `§4543`/`§4556`→`DB-OPS-032 action_code CHECK; BR-001 проекция`; `DB-OPS-007 CHECK`→`DB-OPS-032 action_code CHECK` (вкл. шапку-комментарий). Grep остаточных номеров-строк = пусто. source-поля цитируют номера строк вопреки собственному правилу «якоря, не строки» — реестры нарушают заявленное в стори правило провенанса: `§1255` (секции нет — это строка 1255, прозовый буллет API-конвенций) в `VALIDATION_ERROR`/`PERMISSION_DENIED`/`ENTITY_NOT_FOUND`; `§4543`/`§4556` (номера строк) в audit-events `APPLIED`/`COMPLETED`; `DB-OPS-007 CHECK` в `status_history_action_codes` — на деле CHECK в `DB-OPS-032` (`VAPS_7.8.2.md:2769`), DB-OPS-007 — другая таблица (594). Заменить на реальные секционные/таблично-ID якоря (§60 / API-OPS-001 Errors / DB-OPS-032 / BR-001). [error-codes.yaml + audit-events.yaml]
- [x] [Review][Defer] §82 контрактные краевые случаи → реализующим эпикам (E5/E6/E8) [docs/PersonnelStatus/VAPS_7.8.2.md §82] — deferred, контракт сознательно делегирует реализацию: (1) DocumentSequence year-rollover — кто/когда делает первый `INSERT` строки `(doc_type, year)` нового года + гонка двух транзакций (§82.3, E6); (2) AsyncJob набор состояний — нет `cancelled`/timeout/retry, обобщённая модель сузила состояния vs спец-случай `documents_report_requests` (CANCELLED), что считать терминальным для отменённой джобы (§82.4, E6/E8); (3) DailySubmission отзыв сдачи — partial unique `WHERE is_current` допускает 0 действующих vs текст инварианта «ровно одна»; «отозванная» неотличима от «несданной» в светофоре/блокировке расхода (§82.1, E5); (4) Amendment граница «накрытой даты» — при полуоткрытом `[start,end)` (ARCH-DATA-023) не определено, накрыта ли дата на границе `end`/multi-day-статус (§82.2, E5). [edge]
- [x] [Review][Defer] error-codes.yaml не несёт `provenance` (в отличие от audit-events) [docs/registries/error-codes.yaml] — deferred, минорная консистентность: 21 из 30 кодов выведены из прозы спеки, а не из формальных «Errors:»-таблиц (9), но запись `HASH_MISMATCH` визуально неотличима от табличной `VALIDATION_ERROR`. Будущему CI-линтеру (architecture.md:479) полезно различать table-codes vs prose-derived — добавить поле `provenance` при доукомплектовании реестра. [auditor]
- [x] [Review][Defer] AsyncJob.error_code — нет применимых кодов для import/export-джоб [docs/PersonnelStatus/VAPS_7.8.2.md §82.4] — deferred, growth-model: `jobs_async.error_code` ссылается на error-codes.yaml, но для импорт/экспорт/массовых джоб там пока только `DOCUMENT_GENERATION_FAILED` (генерация документа). Коды для остальных `type` добавятся реализующими стори E7/E8 тем же PR по правилу роста реестра — не дефект инвентаризации сейчас. [blind+edge]

## Change Log

- 2026-06-19 — Создана стори 1.12 (bmad-create-story): документ-артефакт инвентаризации, закрывающий три Important-пробела архитектурной валидации (architecture.md:668–671) — реестры `docs/registries/*.yaml`, сводный Decision Register (все ARCH-ID + 8 DEFERRED с ID/триггером), контракт-секции пяти сущностей дельта-патчем в VAPS_7.8.2 (Attachment уже есть — DB-DOC-002). Контекст собран из epics (1.12, 164), architecture (33–35, 164–173, 286–295, 393–398, 456–466, 664–733), спеки §0/§1255/§33.2/§4.4–4.6/DB-OPS-013/DB-DOC-002 + Explore-инвентарь (≈35 кодов / 24 ws-типа / тонкий audit). Ключевые дефолты: Decision Register → секция architecture.md; контракты → дельта-секция спеки (§0); реестры — растущий seed; 8 DEFERRED → ARCH-DEFERRED-040…047; Document Map опционален. Подводные камни: DailySubmission↔DB-OPS-013 (не задублировать), не корраптить Fixed Baseline, SOURCE-якоря не строки, путь docs/PersonnelStatus (не VisitX). Status → ready-for-dev. Ultimate context engine analysis completed — comprehensive developer guide created.
- 2026-06-19 — Dev (bmad-dev-story, Opus 4.8): закрыты три Important-пробела архитектуры. Task 1 — три реестра `docs/registries/*.yaml` (error 30+7 / ws 24 / audit 9+7), выгружены авторитетно из спеки (§1255+«Errors:»-таблицы+§33.2+§81/DB-OPS-007), ни одного выдуманного кода, валидатор PyYAML PASS, UPPER_SNAKE PASS, cross-ref спека⊆реестр PASS. Task 2 — Decision Register секцией в architecture.md (19 ACTIVE + 8 DEFERRED ARCH-DEFERRED-040…047 с триггерами), строка 35 актуализирована. Task 3 — section 82 дельта-патчем в VAPS_7.8.2 (§0-конвенция, §4 baseline не тронут, append-only PASS): DailySubmission/Amendment/DocumentSequence/AsyncJob контракты + Attachment ссылкой (DB-DOC-002); DailySubmission↔DB-OPS-013 явно разведены. Task 4 — верификация (валидатор/cross-ref/append-only/нулевая регрессия — все PASS) + таблица «находка→потребитель». Document Map сознательно опущен (опционально). **Открыто РЕШЕНИЕ ЗА BRATAN:** `docs/` целиком в `.gitignore:221` → реестры+спека на диске, но вне VCS; рекомендация — негейт `!docs/registries/`; правку .gitignore и коммит агент не делал (репо-политика/PII, прецедент 1.9–1.11). Status → review (auto-done осознанно переопределён).
- 2026-06-19 — Code review (bmad-code-review, Opus 4.8, 3 слоя — Blind/Edge/Acceptance). Триаж: 3 decision-needed + 3 patch + 3 defer + 10 dismiss. ВСЕ 6 decision+patch РЕШЕНЫ И ПРИМЕНЕНЫ: (D1) `.gitignore` негейт `docs/*`+`!docs/registries/` — реестры стали трекаемы (git-корректный паттерн вместо литерального `!docs/registries/` из рекомендации, который не сработал бы); (D2) conflict_codes выровнен на нормативный §6 «Allowed» (8): +`UNAVAILABLE_STATUS_CONFLICT`, `RATING_DATA_MISSING`→feature-flag-расширение, `EMPTY_GROUP`-двойственность задокументирована, §6↔§BR-003-расхождение помечено для E7; (D3) §82 заметка ARCH-DATA-022 (DDL-default = metadata-only, business-время через Clock); (P1) +`OBJECT_CHECKLIST_REQUIRED` (§60, был пропущен); (P2) висячая `architecture.md:1037-1046`→`§BR-003`; (P3) все source-якоря по номерам строк заменены секционными (`§1255`→`§6`/`§60`, `§4543`/`§4556`/`DB-OPS-007`→`DB-OPS-032 CHECK`). Верификация: yaml.safe_load PASS, UPPER_SNAKE PASS, остаточных битых якорей нет, негейт подтверждён `git check-ignore`. 3 defer → §82-контрактные краевые случаи (E5/E6/E8), provenance в error-codes, AsyncJob-коды failed-job (deferred-work.md). AC-вердикт: было AC-1/AC-2 PARTIAL → после патчей якорная/недосып-часть закрыта (untracked снят негейтом; §1255/§60 резолвятся; реестр выровнен). Status ОСТАЁТСЯ review: артефакты не закоммичены (интеграция/коммит за Bratan, прецедент 1.9–1.11; auto-done осознанно переопределён).
