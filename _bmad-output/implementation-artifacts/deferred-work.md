# Deferred Work

## Deferred from: code review of 1-1-тестовый-фундамент-на-postgresql (2026-06-11)

- Мёртвое условие в изоляционном тесте `Backend/VAPS/apps/core/tests/test_isolation.py:603-605`: `mod.startswith(f"{prefix}.models") or mod.startswith(prefix)` — первый операнд полностью поглощён вторым; `startswith(prefix)` без `.` на границе ложно сматчит гипотетический `apps.operationsutil`. Pre-existing, E501-рефакторинг закрепил без изменения семантики.
- Порт 5433 зашит в `Backend/VAPS/docker-compose.yml` и `Makefile` без параметризации (например, `VAPS_TEST_DB_PORT ?= 5433`) — конфликт при втором checkout или системном Postgres на 5433. Зафиксировано решением спеки №3, параметризация — будущее улучшение.
- `make gate` не убирает контейнер/том; изменение `POSTGRES_USER/PASSWORD/DB` в compose молча игнорируется на существующем томе (env применяется только при initdb) → будущие правки конфига дадут расхождение compose-файла и реальной БД. Teardown отсутствует by design (идемпотентность повторных прогонов); при смене кредов потребуется `docker compose down -v`.

## Deferred from: code review of 1-2-кастомная-модель-user-и-identity-контракт (2026-06-11)

- `User.is_active` нигде не енфорсится: `XUserIdAuthentication` по дизайну не ходит в БД, `PermissionService` смотрит только `UserRole.is_active` / окна temp-duty — деактивация `core_users.is_active=False` ничего не отзывает. Закрыть в Story 5.1 (login): при появлении DB-lookup идентичности учесть `is_active`.
- `X-User-Permissions` доверяется клиенту в `apps/core/api/views.py:26` (маскирование) вне `core/auth` — pre-existing дыра границы ARCH-SEC-030; новый AST-чек ловит только `X-User-Id` и создаёт ложный сигнал «граница закрыта». Закрыть при переводе маскирования на PermissionService (E2/2.9 или ранее).
- actor_id не валидируется на входе: длина/charset не ограничены, дубли заголовка `X-User-Id` WSGI склеивает в `"a, b"` (сегодня fail-closed через 403, но мусор утечёт в будущие логи/аудит). Уйдёт при переходе на JWT (Story 5.1). **Эскалация (ревью 1.4):** actor_id теперь пишется в `created_by` varchar(100); на негейченном `assign-employee` (нет 403-гейта, `objects.create()` без `full_clean()`) заголовок >100 символов даёт Postgres `DataError` → 500. Решение ревью 1.4: guard сейчас не вводить (auth-слой = 7-й файл логики вне лимита скоупа стори), закрыть вместе с JWT (5.1) или гейтом E2 — что наступит раньше.
- Поглощённый операнд в `test_core_does_not_import_other_context_models` — дубль записи из ревью 1.1 выше; повторно всплыл в ревью 1.2, по-прежнему deferred.

## Deferred from: code review of 1-3-clock-сервис-и-watermark (2026-06-11)

- `catchup_plan` неограничен при большом положительном разрыве: watermark годами позади (старый бэкап, fat-finger seed `1970-01-01`) → десятки тысяч дат одним планом без капа/предупреждения; алертится только отрицательное направление. Контракт чанкинга/капа — ответственность потребителя, определить в Story 3.12.
- Дисциплина записи Watermark: `key=""` проходит `objects.create()` (нет CheckConstraint), откат `last_materialized_date` назад молча ре-материализует дни, конкурентный первый upsert по одному key гонится до IntegrityError. Advisory lock/upsert-сервис явно отложены спекой 1.3 — закрыть в Story 3.12.
- `clock.override()` не распространяется на новые треды (ContextVar в дочернем треде пуст → читается реальное время) и падает с ValueError при reset токена из чужого контекста (override через границу asyncio-тасок). Задокументировать ограничение, актуально с приходом Celery (3.12) и тестов «контрольного часа» (5.3).

## Deferred from: code review of 1-4-поле-актора-в-базовых-моделях (2026-06-11)

- ПРОЦЕСС-ПРАВИЛО (решение ревью 1.4): работа сторей 1.1–1.4 коммитится одним блоком — `core/0015_created_by` зависит от незакоммиченного `0014_watermark` (1.3); отдельный коммит/перенос 1.4 (в т.ч. на вторую машину) даст `NodeNotFoundError` при `migrate`. Правило снимается общим коммитом блока.
- Негейченный `StaffingSlotViewSet.assign_employee` (`Backend/VAPS/apps/core/api/views.py:124`): нет `require_permission`, анонимный вызов с произвольным `X-User-Id` создаёт назначение и пишет сфабрикованную идентичность в `created_by`. Pre-existing (отсутствие гейта), стори 1.4 лишь заполнила поле честно для имеющегося контракта. Закрыть в E2 при переносе view→сервис + permission gate.
- Race в `RoleAdminService.assign_role`: `UniqueConstraint(user_id, role_code, scope_division_id)` не покрывает `scope_division_id=NULL` (Postgres считает NULLs различными) — конкурентные create глобальной роли дают дубли строк, дальше `MultipleObjectsReturned` → 500 во всех последующих assign/revoke/effective_permissions; с 1.4 дубли ещё и расходятся в `created_by`. Pre-existing. Лечение: partial unique index `condition=Q(scope_division_id=None)` или `nulls_distinct=False` (Django 5.0+/PG15) — кандидат E2/2.9.
- `created_by` экспонируется наружу в `TemporaryDutySerializer` (`Backend/VAPS/apps/operations/api/serializers.py:33`) — против правила «служебные поля наружу только явным решением спеки». Pre-existing (поле в сериализаторе с 1.2-эпохи), ревью 1.4 выявило, что Completion Note «утечки нет» была неверна. Решить при ревизии сериализаторов operations (E2) — убрать или узаконить спекой.

## Deferred from: code review of 1-5-минимальная-модель-статуса-с-exclusionconstraint (2026-06-11)

- Маппинг 3.1 «IntegrityError по имени constraint → 409» не покроет deadlock-исход реальной конкурентной гонки: при симметричной гонке двух hard-вставок Postgres может разрешить конфликт через `DeadlockDetected` (Django: `OperationalError`) — имени `excl_hard_status_overlap` в сообщении нет → будет 500 вместо 409. Воспроизведено ревью 1.5 (4 из 5 прогонов конкурентного теста). Стори 3.1 должна осознанно обработать OperationalError/deadlock как конфликтный исход.
- `date_start > date_end` бьёт не в `chk_status_dates`, а в вычисление generated column `period`: PostgreSQL кидает `DataError` (SQLSTATE 22000, «range lower bound must be less than or equal…») ДО проверки CHECK — тоже мимо маппинга 3.1. Сервисная 422-валидация — стори 3.3; маппинг 3.1 учесть DataError-путь.
- «Append-once» `cancelled_at` не охраняется БД: `UPDATE cancelled_at=NULL` (раз-отмена) и правка отменённой записи проходят свободно — комментарий в модели обещает инвариант, которого нет на уровне БД. Механика отмены (cancelled_by/reason, необратимость) — стори 3.6.
- ExclusionConstraint не DEFERRABLE: легитимный swap границ двух соседних hard-статусов одного сотрудника в одной транзакции падает или проходит в зависимости от порядка операторов — скрытый контракт для будущих сервисов редактирования статусов (3.6/E3).
- `XUserIdAuthentication` при отсутствующем/пустом заголовке не устанавливает `request.actor_id` вовсе (ветка else отсутствует) → downstream-код, читающий атрибут, получит AttributeError вместо None/403. Pre-existing (стори 1.2, `apps/core/auth/authentication.py:17-20`); закрыть вместе с JWT (5.1) или при первом downstream-потребителе.
- `status_type_code` — свободный текст до FK 2.2: опечатка/регистр/хвостовой пробел тихо выводит запись из-под exclusion-constraint (условие сравнивает точные литералы); окно мусора до 2.2 ограничено тем, что пишут только тесты и импорт 1.6. Закрывается FK + seed-тестом 2.2.
- SQLite-дефолт (без VAPS_DB=postgres) падает на `migrate`/`pytest` невнятной OperationalError внутри CreateModel — нет guard'а с понятной ошибкой «нужен Postgres» (system check / условный fail-fast). Санкционировано ARCH-DATA-020, эргономика — по желанию позже.

## Deferred from: code review of 1-6-минимальный-импорт-среза-донора (2026-06-12)

- Статусы никогда не обновляются при повторном импорте: natural-key проверка только скипает (`already_exists`), корректировка донора (изменившийся `actual_end_date` → другой `date_end`) либо уйдёт в `hard_overlap` (hard-типы), либо ляжет второй версией интервала (cancelled/не-hard). Асимметрия с `update_or_create` сотрудников не задокументирована. Полный импортёр с политикой обновления — E7 (7.3/7.6).
- `state="planned"` донора импортируется как живой интервал (state не переносится по ARCH-DATA-022) — planned hard-статус, пересекающийся с active того же типа, может вытеснить реальный факт (порядок вставки по `date_start`). Категория-гипотеза для диффа 1.8; политика planned-статусов — E7.
- `cancelled`-статус с отсутствующим/непарсящимся `updated_at` получает `cancelled_at=NULL` и становится живым (участвует в exclusion). Практически мёртвый путь (donor `updated_at` — auto_now), но семантика «best-effort иначе None» из спеки 1.6 даёт молчаливое оживление отменённого факта. Политика — E7.
- Структурно битый dumpdata (top-level не список, строки без `model`/`pk`/`fields`, не-строковые типы полей) → сырой traceback вместо CommandError/skip. Parsers/validators/golden-кейсы грязного входа явно объявлены E7 (Решение №1 стори 1.6).

## Deferred from: code review of 1-7-derived-статус-и-расчёт-расхода (2026-06-12)

- Clamp-артефакты открытых статусов между прогонами импорта: повтор с более поздним `--until` скипает clamped-строку как `already_exists` (natural key без date_end) и не продлевает её → derived-статус на новый день = ложный IN_SERVICE; а когда донор закрывает интервал реальной датой, новая строка бьётся об артефакт по exclusion и мисклассифицируется `hard_overlap` вместо clamp-кейса (`import_donor_slice.py:412-446`). Расширяет запись 1.6 «статусы никогда не обновляются при повторном импорте» — политика обновления E7 (7.3/7.6).
- Циклы parent у Division персистятся: разрыв цикла только для резолва организации, второй проход пишет `division.parent` без проверки (`import_donor_slice.py:235-249`); `test_parent_cycle_does_not_hang` не проверяет parent. Все subtree-обходы (`CoreDivisionTreeSelector`) обязаны переживать цикл — guard в импортёре или CHECK-стратегия — E7.
- Классификация IntegrityError по подстрокам английских сообщений Postgres (`"null value"`, `"iin"`, имя constraint) — ломается на lc_messages/переименованиях, всё падает в catch-all reason (`import_donor_slice.py:372-383, 452-466`). При полном импортёре E7 перейти на pgcode/constraint-name из diag.
- Несколько staff_units на сотрудника: division берётся последней строкой файла без skip/warning (`import_donor_slice.py:323-327`) — добавить skip-причину/warning в E7.
- Наивный `cancelled_at` при donor `updated_at` без UTC-offset → naive datetime в aware-поле, сдвиг на локальный offset (`transform.py:135`). Нормализация tz — E7.
- `gender` и dangling `rank_pk` без валидации: gender verbatim в мастер-данные, отсутствующий rank тихо → `("", 0)` без skip (`transform.py:199`, `import_donor_slice.py:344`) — выровнять со СТОП-семантикой в E7.
- Счётчики отчёта импорта: fallback-org инкрементит created без read; идемпотентный повтор показывает `updated N` при нулевых изменениях (update_or_create всегда пишет defaults) — числа читает 1.8; честный changed-детект — E7.
- Слот с не-полуночным `valid_from` невидим в свой стартовый день (`valid_from__lte=local_midnight(D)`, по букве BR-002) — сейчас пишет только импортёр (нормализует к полуночи); валидация/нормализация в будущем write-API слотов (`apps/core/selectors.py:106-122`).
- `compute()` с несуществующим `division_id` → пустой отчёт, неотличимый от пустого поддерева (`subtree_ids` не проверяет существование) — валидация существования division на API-слое 1.8 (`strength_report.py:265-267`).
- Makefile: `docker compose up --wait` внутри `timeout 300` гейта — медленный pull даёт ложный «gate TIMED OUT (NFR-8)»; креды БД захардкожены (`Backend/VAPS/Makefile:31-44`). Pre-existing 1.1–1.6, gate не трогали по Решению №7.

## Deferred from: code review of 1-7-derived-статус-и-расчёт-расхода (2026-06-15)

Повторное адверсариальное ревью (`wf_dbbaa59c-259`); ниже — отложенные находки (HIGH-патч C2 и low-патчи C3/C17/C19/C26 НЕ откладываются — они в Review Findings стори).

- `allocated_slots_on` недетерминирован при равных `valid_from` одного подразделения (нет `order_by`, строгое `>` в редьюсере) — переоценка прежней KO-3: тай **недостижим** текущими путями записи (`update_or_create` по `(division, valid_from)` де-факто уникален, `valid_to` никто не закрывает), поэтому понижено HIGH→low. Защитное усиление (вторичный tie-break по pk + `order_by`) — когда E7 введёт новый писатель таймлайна слотов (`apps/core/selectors.py:103-125`).
- `working_by_division` фильтрует `employment_status="WORKING"` без поддерживающего индекса (`core_employees` имеет только `idx_emp_div_active=(division,is_active)` и `idx_emp_full_name`) → seq scan для whole-DB, деградация на росте. Корректность и «один запрос» соблюдены. Индекс `(employment_status, division)` или partial `WHERE employment_status='WORKING'` — follow-up (1.7 без миграций) (`apps/core/selectors.py:85-101`).
- `_resolve_until` для незавершённых статусных строк подставляет `actual_end_date` при дефолте конца окна импорта — асимметрия с `transform_status`; новый `_import_staffing_slots` это значение не потребляет. Pre-existing 1.6, политика окна — E7 (`import_donor_slice.py`).
- Несколько staff_units на одного сотрудника → division берётся последней строкой файла без skip/warning (повтор записи 2026-06-12) — skip-причина/warning в E7 (`import_donor_slice.py:323-327`).
- Штат считает staff_units импорт-скипнутых сотрудников → фантомные вакансии (на фикстуре DEP1 = 5/1/4) — **намеренный паритет** с донор-агрегатором (Решение №5/№6) ради диффа 1.8, исключение скипнутых сломало бы требуемый паритет. Переоценить формулировку «вакансий» вместе с категориями-гипотезами 1.8 (`transform.py`).
- Наивный `cancelled_at` из donor `updated_at` без UTC-offset → naive datetime в aware-поле (повтор записи 2026-06-12); в 1.7 `cancelled_at` только IS-NULL-фильтруется, все фикстуры с Z-суффиксом. Нормализация tz — E7 (`transform.py:135`).
- (вердикт D2=1, 2026-06-15) `derive_report`/`compute` опускают строки для подразделений субдерева без WORKING-сотрудников и без слота (пустое подразделение не даёт нулевой строки Штат=0/Список=0). Контракт состава/отображения строк отчёта решает потребитель — **Story 1.8**: либо сеять все `subtree_ids` нулями, либо узаконить опущение. Вызывающего API сейчас нет; инварианты сходимости не затронуты (`apps/operations/statuses/services/strength_report.py:155,254-281`).

## Deferred from: code review of 1-8-read-only-вывод-расхода-и-дифф-с-донором (2026-06-16)

Второй (независимый) адверсариальный проход по фиксам C1–C10 (`wf_ff0c55ff-ea5`, 68 агентов, 3-линзовая верификация). Production-логика фиксов верифицирована корректной; патчи F13 (устаревший каталог Dev Notes) и F8 (ложно-зелёный тест) — в Review Findings стори, НЕ откладываются. Ниже — отложенные находки.

- ~~Guard счётчиков C4 (`donor_diff.py:165-169`) принимает отрицательные целые~~ — **ЗАКРЫТО (ревью спайка 1.9, пр.3, 2026-06-16), НЕ отложено в E7.** «Fail-safe»-обоснование опровергнуто: воспроизведён обход гейта — донор `in_service=1, vacation=-1` против VAPS-нулей даёт обе ячейки `model/aggregator_inferred` (зелёные) → `has_unclassified=False`, гейт ПРОХОДИТ вместо блока на потере данных; достижимо по shipped-пути `--diff-baseline` (`json.load`→`load_baseline` без санитизации). Исправлено: добавлен `or value < 0` в guard + регресс-тест `test_negative_cell_value_raises_value_error[-1/-3]` (147 passed, ruff clean).
- `diff_day` try/except в команде (`strength_report.py:75-82`) ловит только `ValueError`, а `diff_day` делает bracket-доступ `violation["division_id"]` (`donor_diff.py:368`) → отсутствие ключа даст `KeyError` мимо `except` → сырой трейсбек. Недостижимо сейчас (`violations` из `StrengthReportService` 1.7 всегда с `division_id`); защитить при изменении контракта violations — E7.
- Ветка `timing/half_open_end` для type-колонки (`donor_diff.py:326-336`) срабатывает на голом булеве `vaps_in_service_higher` без Σ-проверки величины — асимметрично строгому IN_SERVICE-правилу (`delta == timing_surplus and timing_surplus > 0`). Гейт остаётся sound (качество метки прото-классификатора). Полный timing-replay донорской интервальной логики — E7 7.8 (Решение №6).
- Gate-тест (`test_strength_report_command.py:92-98`) проверяет присутствие строк-категорий, а не полноту «каталог ↔ фикстура» — дрейф каталога/фикстуры (как F13) не ловится. Усиление: ассерт на исчерпываемость каталога/совпадение с фикстурой — test-hardening E1/E7.

## Deferred from: code review of 1-9-спайк-контур-проба (2026-06-16)

Проход 4 (bmad-code-review, branch diff vs main без graphify-out; 3 слоя на Opus 4.8). Из 3 defer-находок две (`diff_day`/`KeyError`, gate-тест/каталог-дрейф) УЖЕ присутствуют выше в секции 1-8 (проход 3) — дубликаты не добавляю. Ниже — единственная новая.

- `_load_baseline` (`strength_report.py`) в команде `strength_report --diff-baseline` ловит только `(OSError, ValueError)`. Гигантский или патологически глубоко-вложенный operator-JSON даёт `MemoryError`/`RecursionError` — не подклассы пойманных → сырой трейсбек мимо `CommandError`. Malformed-текстовый JSON в CPython = `ValueError` (уже ловится), поэтому утекают только классы исчерпания ресурсов. Low: файл подаётся оператором, контекст одноразового спайка. Кандидат в хардённинг импортёров E7 (валидация размера/глубины baseline-файла).

## Deferred from: code review of 1-9-спайк-контур-проба (2026-06-17)

Проход 5 (bmad-code-review, scoped diff `main..HEAD` по путям 1.9 без graphify-out; 3 слоя на Opus 4.8). 1 defer-находка (2 decision-needed + 1 patch — в Review Findings стори).

- Реальный `install.sh` (E12/12.3) должен сверять `.tar` с pinned/подписанным ожидаемым хэшем, а не с со-расположенным регенерируемым `sha256sums.txt`. `sha256sum -c` ловит транзитную порчу, но НЕ преднамеренную подмену (подменили `.tar`+`sums` вместе → проходит); комментарий `install-probe.sh` «битый/изменённый архив => СТОП» переоценивает (tampered не детектируется). Прото-скрипт спайка by design доказывает только механику; узаконить pinned/signed-хэш в E12-хардённинге деплоя/импорта (`deploy/spike-1.9/install-probe.sh:14-19`).

## Deferred from: code review of 1-10-спайк-перф-грид-на-целевой-машине (2026-06-18)

Проход 1 (bmad-code-review, scoped diff `main...HEAD` по путям `spikes/1.10-perf-grid/` без `package-lock.json`; 3 слоя на Opus 4.8). 2 defer-находки (2 decision-needed + 3 patch — в Review Findings стори).

- headless-Chromium апп-смоук не валидирует FF100-специфику: белый экран на firefox100-несовместимом синтаксисе, отсутствие `performance.memory` в Firefox, регрессия `Array.prototype.with` (FF115+). Присуще спайку (Playwright на FF100 невозможен, architecture.md:258; AI не водит GUI Firefox). Покрыто целевым прогоном Bratan (путь A, 4ГБ/FF~100) + `build.target:'firefox100'`-гардом сборки. Авто-детект FF-несовместимости (eslint-plugin-compat как гейт) — канон E8 (`spikes/1.10-perf-grid/vite.config.ts` vs `BUDGET.md §5`).
- Точные пины тулчейна прототипа (`react 19.2.7`, `react-dom 19.2.7`, `@tanstack/react-virtual 3.14.3`, `vite 7.3.5`, `@vitejs/plugin-react 5.2.0` + транзитивы) живут только в `package-lock.json`, который вне scoped-диффа ревью; ревьюенный `package.json` пинит лишь caret-диапазоны (`^19.2.0` и т.п.). Для воспроизводимости перф-замера на FF~100 свериться с lock перед merge / коммитом 1.10 (`spikes/1.10-perf-grid/package.json:166-177` vs `package-lock.json`).

## Deferred from: code review of 1-9-спайк-контур-проба (2026-06-18)

Проход 6 (bmad-code-review, scoped diff `main...HEAD` по путям `deploy/spike-1.9/`; 3 слоя на Opus 4.8). Контрольный 6-й проход (после 3/4/5). 1 defer (1 patch — `--pull never`-дрейф — в Review Findings стори; остальное повторы/by-design).

- Реальный `install.sh` (E12/12.3) после `docker load` должен ВЕРИФИЦИРОВАТЬ идентичность загруженного образа (тег/digest = ожидаемый `vaps-probe:spike-1.9`), а не только sha-чек `.tar`. `docker load` восстанавливает любой тег из `.tar`; при резидентном устаревшем образе с тем же тегом `compose up` (нет `build:`) тихо поднимет СТАРЫЙ образ → страница-маркер покажет неверный `__GIT_SHA__`, а runbook-evidence «этот бандл доехал» окажется ложным. Сегодня смягчено визуальной проверкой sha человеком-оператором (рунбук шаг 6) — не полностью молчаливо; для одноразовой пробы на чистом контуре риск low. Узаконить image-identity-verify (сравнение digest загруженного образа с pinned) в E12-install.sh (`deploy/spike-1.9/install-probe.sh:19-22`).

## Deferred from: code review of 1-11-спайк-выгрузка-данных-донора (2026-06-18)

Проход 1 (bmad-code-review, scoped diff по артефактам 1.11 `spikes/1.11-donor-export/` + BMAD-трекинг; 3 слоя на Opus 4.8). 1 defer (остальное: 1 decision-needed + 4 patch — в Review Findings стори; ~7 dismiss).

- Маскирование «последние 4 цифры ИИН» (`profile_export.py:174-176`, `mask()` → `…0101 ×2`) — это **spec-prescribed** (AC-2 буквально задаёт пример `…1234`), реализация ему соответствует, а коммиченный сейчас пример — синтетический ИИН фикстуры (не реальный PII). НО: последние 4 цифры нац. идентификатора — остаточный PII, а попадают они в **коммиченный** `EXPORT-REPORT.md` (раздел 2 для реальных прод-чисел, заполняет Bratan). `.gitignore`-allowlist защищает от утечки по ИМЕНИ файла, но не по СОДЕРЖИМОМУ (docstring «сильнейшая гарантия» слегка переоценивает). Ужесточить стандарт маскирования (меньше цифр / хэш / только счётчик без примера) в E7-профилировщике **7.1**, когда он начнёт коммитить прод-примеры. Для одноразового спайка на синтетике — риск low, spec соблюдён.

## Deferred from: code review of 1-9-спайк-контур-проба (2026-06-19)

Проход 7 (bmad-code-review, scoped diff `main..HEAD` по путям `deploy/spike-1.9/` + story; 3 слоя на Opus 4.8). 1 defer (2 decision-needed + 1 patch — в Review Findings стори; ~8 dismiss). Соседние bundle-integrity-пункты (image-identity-verify пр.6, tampered-hash пр.5) уже выше — здесь только новый.

- `docker save` на заполненном диске (или прерванное копирование на носитель) даёт усечённый `.tar`, чей `sha256sum` посчитается «зелёным» по факту записи → в контуре `sha256sum -c` ПРОЙДЁТ, а `docker load` упадёт уже после изменений. E12-`bundle.sh`/`install.sh` должен верифицировать размер архива и/или `docker load` в dry-режиме помимо sha транзит-целостности. Для одноразового прото-скрипта спайка — by design вне scope; узаконить в E12-хардённинге бандла/импорта (`deploy/spike-1.9/build-bundle.sh:26-29`).

## Deferred from: code review of 1-10-спайк-перф-грид-на-целевой-машине (2026-06-19)

Проход 2 (bmad-code-review, scoped diff `0bd68fb^..9e424eb` по путям `spikes/1.10-perf-grid/` без `package-lock.json`; 3 слоя на Opus 4.8). Контрольный проход по ПОСТ-патч состоянию (после пр.1). 2 defer (1 patch — §7-вердикт — в Review Findings стори; ~13 dismiss, большинство уже триажено пр.1). Lockfile-defer пр.1 (caret-пины vs lock) остаётся в силе выше.

- Разрешение таймера Firefox: FF по умолчанию (`privacy.reduceTimerPrecision=true`) загрубляет `performance.now()` до ~1 мс. Аппарат меряет `p95 keydown→commit` (на dev-Chromium 0.5 мс, субмиллисекундно) — на FF100 этого разрешения не будет. На слабом target латентности, вероятно, мс-масштаба (1-мс-кванты грубо, но юзабельно), но методология `BUDGET.md §3` должна зафиксировать разрешение таймера ЛИБО оператор снимает `privacy.reduceTimerPrecision` на прогон — иначе p95 с FF может оказаться квантованным артефактом. Покрыто целевым прогоном Bratan (путь A). Новое vs FF-defer пр.1 (там compat/`performance.memory`; тут — разрешение таймера). (`spikes/1.10-perf-grid/perf.ts:76` `performance.now()` / `BUDGET.md §3`).
- Спека внутренне рассогласована: AC-4 всё ещё предписывает path-B-числа метить ретированным маркером `PRELIMINARY-pending-target`, тогда как ревью 1.10 пр.1 узаконил 4-маркерную таксономию (`VERIFIED-on-target`/`apparatus-verified`/`measured-dev`/`PENDING-target`) в AC-3. При выбранном пути A пункт AC-4 моот, `BUDGET.md` запрещённый маркер не использует (артефакт чист) — рассогласование живёт только в несведённом тексте AC. Свести AC-4↔AC-3 при следующей правке стори 1.10 (`_bmad-output/implementation-artifacts/1-10-спайк-перф-грид-на-целевой-машине.md` AC-4 vs AC-3).

## Deferred from: code review of 1-11-спайк-выгрузка-данных-донора (2026-06-19)

Проход 2 (bmad-code-review, scoped diff по закоммиченным (777610a) источникам `spikes/1.11-donor-export/`; 3 слоя на Opus 4.8). Контрольный проход по ПОСТ-патч состоянию (после прохода 1). 1 defer (1 decision-needed + 1 patch — в Review Findings стори; ~13 dismiss). Defer прохода 1 (стандарт маскирования last-4 в коммиченном `EXPORT-REPORT.md` → 7.1) остаётся в силе выше — здесь смежная новая грань.

- PII-поверхность примеров bucket'а «невалидный формат ИИН». `mask()` (`profile_export.py:53-65`) печатает last-4 любого значения ≥5 симв.; именно `iin_bad` — самые вероятные free-text/контаминированные значения (реальный валидный ИИН, ставший «невалидным» лишь из-за `\n`/пробела → last-4 с реальными цифрами; имя, по ошибке записанное в поле `iin` → 4 символа фамилии). Spec-prescribed last-4 для дублей валидных ИИН (AC-2 `…1234`) к invalid-bucket'у формально не относится, но реализация маскирует всё одинаково. Ужесточить в E7-профилировщике 7.1 ЛИБО дешёвый мини-патч уже сейчас: для invalid-bucket печатать count-only (без примеров) — невалидные значения нельзя кросс-референсить (аналитической ценности ноль), а leak-риск выше дублей. Для одноразового спайка на синтетике — риск low, spec соблюдён (`spikes/1.11-donor-export/profile_export.py:53-65, 128-132`).

## Deferred from: code review of 1-11-спайк-выгрузка-данных-донора (2026-06-19)

Проход 3 (bmad-code-review, scoped diff `main` по путям `spikes/1.11-donor-export/` — полное текущее рабочее состояние; 3 слоя на Opus 4.8). Контрольный проход №3. 1 defer (1 patch — F541 — в Review Findings стори; ~9 dismiss). Defer'ы проходов 1/2 (стандарт маскирования last-4 + invalid-bucket → 7.1) остаются в силе выше.

- Дедуп ИИН/табельных рапортует счётчик РАЗЛИЧНЫХ дублирующихся значений (`len(duplicates())`, метка «(значений >1)» — честна), а не число ЗАТРОНУТЫХ строк. Для сайзинга merge-стратегии 7.3 чаще нужно «сколько строк слить» (значение, встреченное ×4 = 3 лишних строки); это число прямо не выводится при >5 дублирующихся значениях (cap примеров `EXAMPLE_LIMIT=5`). Не баг (метка честная, AC-2 «дубли числами» соблюдён) — обогащение для реального профилировщика 7.1/7.3: дополнительно печатать сумму затронутых строк. Рецепт уже маршрутизирует «качество ключей → 7.1/7.3» (`spikes/1.11-donor-export/profile_export.py:94-99, 139-147, 158-160`).

## Deferred from: code review of 1-12-инвентаризация-артефакт (2026-06-19)

Проход 1 (bmad-code-review, scoped `{diff_output}` по артефактам 1.12: architecture.md Decision Register + `docs/registries/*.yaml` + дельта §82 VAPS_7.8.2.md; 3 слоя на Opus 4.8). 3 defer (3 decision-needed + 3 patch — в Review Findings стори; 10 dismiss). Документ-артефакт: «баги» = пропуски/висячие ссылки/несогласованность схем, не код.

- §82 контрактные краевые случаи, сознательно делегированы реализующим эпикам (контракт сам пишет «E5/E6/E8 решает»): (1) DocumentSequence year-rollover — первый `INSERT` строки `(doc_type, year)` нового года + гонка двух транзакций не определены (§82.3 → E6); (2) AsyncJob — нет состояния `cancelled`/timeout/retry, обобщённая `jobs_async` сузила множество состояний vs спец-случай `documents_report_requests` (CANCELLED); что терминально для отменённой джобы — не сказано (§82.4 → E6/E8); (3) DailySubmission отзыв сдачи — partial unique `WHERE is_current` допускает 0 действующих, тогда как текст инварианта «ровно одна»; «отозванная» неотличима от «несданной» в светофоре и в derived-блокировке расхода (§82.1 → E5); (4) Amendment граница «накрытой даты» — при полуоткрытом интервале `[start,end)` (ARCH-DATA-023) не специфицировано, накрыта ли дата на границе `end` и как трактуется multi-day-статус, лишь частично пересекающий business_date — прямой риск «двух правд», против которого инвариант вводился (§82.2 → E5). (`docs/PersonnelStatus/VAPS_7.8.2.md §82`).
- error-codes.yaml не несёт поля `provenance` (audit-events несёт): 21 из 30 кодов выведены из прозы спеки, а не из формальных «Errors:»-таблиц (9 уникальных), но `HASH_MISMATCH`(prose) визуально неотличим от `VALIDATION_ERROR`(table). Будущему CI-линтеру сверки кодов (architecture.md:479) полезно различать — добавить `provenance: spec-table | derived-from-prose` при доукомплектовании реестра (`docs/registries/error-codes.yaml`).
- `jobs_async.error_code` ссылается на error-codes.yaml, но для импорт/экспорт/массовых джоб там пока только `DOCUMENT_GENERATION_FAILED` (генерация документа). Коды для остальных `type` (`IMPORT_FAILED`-аналоги и пр.) добавятся реализующими стори E7-миграция/E8 тем же PR по правилу роста реестра — не дефект инвентаризации, а ожидаемая работа модели роста (`docs/PersonnelStatus/VAPS_7.8.2.md §82.4`).

## Deferred from: code review of 1-11-спайк-выгрузка-данных-донора (2026-06-22)

Проход 4 (bmad-code-review, scoped diff `9e424eb..HEAD` по путям `spikes/1.11-donor-export/`; 3 слоя на Opus 4.8). Контрольный проход №4 (по запросу Bratan). 1 defer (1 patch — валидация типа `model` — в Review Findings стори; 8 dismiss). Defer'ы проходов 1/2/3 (last-4 в коммиченном отчёте, invalid-bucket PII, affected-rows-обогащение) остаются в силе выше.

- Natural-key FK (`dumpdata --natural-foreign`) → тихий orphan-storm. `print_statuses` (`profile_export.py:177-183`) сравнивает `str(r["fields"]["employee"])` со множеством `{str(pk)}`. Если выгрузка сделана с `--natural-foreign`, employee-FK = список natural-ключа (`["…"]`), `str(["…"])` никогда не совпадёт со `str(pk)` → ВСЕ статусы помечаются осиротевшими (ложный orphan-storm — инверсия метрики, ради которой спайк существует). НЕ срабатывает под прописанным рецептом (`dumpdata --format json` БЕЗ `--natural-foreign` → FK = скалярный pk), поэтому severity low. Надёжная обработка natural-key FK (резолв natural-ключа employee) — это E7-профилировщик 7.1; рецепт уже маршрутизирует «формат не совпал → 7.1». Дешёвая страховка-альтернатива на будущее: детектить non-scalar FK и репортить как аномалию формата, а не молча считать orphan.

## Deferred from: code review of 2-4-списочный-состав-на-дату (2026-06-22)

Проход 1 (bmad-code-review, scoped diff по File List 2.4; 3 слоя Opus 4.8). 2 defer (3 patch — в Review Findings стори; 3 dismiss). AC-1..5 — MET.

- **`roster_reconciliation` не атомарен** (`apps/core/selectors.py:241-259`, Blind): `expected` и `actual` берутся из двух независимых запросов без общего snapshot → конкурентный приём/увольнение между ними даёт ложный `missing`/фантомное расхождение. Low: диагностика без потребителя (в `compute` не вшита; с fallback холов нет by construction). Закрыть при вшивании reconciliation в отчёт: `transaction.atomic` ЛИБО `active` из того же `working`-источника, что строит roster.
- **Scoped-отчёт грузит весь штат + всю покрывающую историю в Python** (`apps/core/selectors.py:203-235`, Blind+Edge): `roster_on` не проталкивает `division_ids` в SQL (нужно для версионирования — интервал out-of-scope может переместить сотрудника in-scope), фильтрует после резолва. Не N+1 (бюджет=5 константа), но объём строк не ограничен скоупом → на большой БД отчёт по одному подразделению материализует весь workforce. На пилоте дормант (история пуста, импорт 1.6 = 0 строк). Perf-hardening при E7-бэкафилле истории: индекс `(division, starts_at, ends_at)` + точечный fetch in-scope ∪ covering-history-in-scope. (Уже в Out of Scope стори 2.4 / Решение №6.)
- **(связано, не из 2.4-кода) `allocated_slots_on` тот же недетерминированный tie-break при равных `valid_from`** — уже отложен в E7 (ревью 1.7, deferred-work выше); 2.4 чинит свой `roster_on` детерминированно (патч прохода 1), но аналог слотов остаётся за E7 для единообразия (вне скоупа 2.4).

## Deferred from: code review of 2-5-сервис-увольнения (2026-06-22)

Проход 1 (bmad-code-review, scoped diff по File List 2.5; 3 слоя Opus 4.8). 1 decision (archive-view fate — в Review Findings стори) · 5 defer · 5 dismiss. AC-1..5 MET (spec, Auditor).

- **Основание теряется при увольнении без ставки** (`apps/core/services.py` `dismiss_employee`, Blind): `reason` пишется только в `Vacancy.reason` в цикле по open-assignment; сотрудник без открытого assignment → вакансия не создаётся → основание нигде (нет поля на Employee, Решение №3). No-slot — edge; most-dismissals освобождают ставку. Закрыть при подключении аудита (E4) ЛИБО полем `Employee.dismissal_reason` (миграция).
- **Нет верхней границы даты D (future-dismiss)** (Blind): сервис валидирует `D` против старта интервала/ассайнмента, но не против «сегодня» (Clock в домене запрещён ARCH-DATA-022). Валидация `D ≤ today` — ответственность вызывающего/будущего dismissal-API (2.9).
- **Два представления вакантности** (Blind): освобождённая ставка вакантна и через `compute_free_slots` (derived), и явной `Vacancy(OPEN)`-строкой (Решение №4). Двойного счёта в расходе нет (Штат = `DivisionHistoricalSlot`), но две истины — согласовать при появлении потребителя `Vacancy`-строк.
- **Жизненный цикл `Vacancy` не построен** (Blind): 2.5 только ОТКРЫВАЕТ вакансию; нет close-on-fill, нет dedup → слот с уже-OPEN-вакансией может получить вторую при будущем увольнении. `Vacancy` была «мёртвой» до 2.5; полный lifecycle — follow-up (E14/штатка).
- **Multi-assignment loop validate-then-apply + тест** (`dismiss_employee`, Edge): закрытие нескольких open-assignment мутирует до полной валидации; при `starts_at >= t` на N+1 полагается на `@transaction.atomic` rollback (корректно, но не оттестировано). Усиление: пре-скан + регресс-тест «несколько assignment, один невалидный».

## Deferred from: code review of 2-2-справочник-типов-статусов-с-seed (2026-06-21)

Проход 1 (bmad-code-review, scoped diff 282 строки по `apps/operations/statuses`; 3 слоя Opus 4.8). 5 patch применяются в стори; 3 defer (ниже); ~10 dismiss. AC-1..4 — SATISFIED.

- **PENDING_CLARIFICATION → report_column_code="IN_SERVICE"** (Blind major): provisional-маппинг считает «уточняется» как в-строю и полностью включает в расход (`counts_in_staff`/`counts_in_list`=True). Сегодня НЕ потребляется — текущий `strength_report.REPORT_COLUMN_BY_CODE` несёт свои 16 кодов без PENDING, поэтому live-инфляции headcount нет. Семантика по AR-11 (своя строка расхода + жёлтый светофор) и потребление `StatusType.report_column_code` — за статусным движком E3. Bratan подтвердил provisional (AskUserQuestion 2026-06-21).
- **Seed не реконсилирует удалённые коды** (Blind+Edge): `seed_statuses.handle()` только `update_or_create` по списку — строка с кодом, выпавшим из `STATUS_TYPES`, остаётся `is_active=True` (advertised soft-delete не задействован). Каталог сейчас grow-only (тот же паттерн у `seed_operations`). Когда понадобится ретайр типа — добавить проход `StatusType.objects.exclude(code__in=[...]).update(is_active=False)` + потребители, фильтрующие `is_active`.
- **Re-seed перетирает оператор-правки** (Blind+Edge): `update_or_create(defaults={...})` на каждом прогоне форсит `is_active=True` и `color=""`, затирая ручные изменения. Безвредно до прихода Django Admin-редактирования справочников (стори 2.8). Тогда перейти на `create_defaults` (Django 5.0+) для `is_active`/`color`, оставив канон-поля в `defaults`.

## Deferred from: code review of 2-3-настройки-контроля-сдачи (2026-06-21)

Проход 1 (bmad-code-review, scoped diff 254 строки по `apps/operations/submissions` + `config/settings.py`; 3 слоя Opus 4.8). 1 decision + 1 patch в стори; 3 defer (ниже); ~7 dismiss. AC-1..4 — SATISFIED.

- **required_division_ids без валидации существования** (Blind+Edge): массив flat-UUID → `core_divisions.id` без FK (ARCH-003) и без проверки, что дивизии существуют/не soft-deleted и относятся к типу `management`. Случайный UUID сохраняется. По дизайну целостность flat-UUID компенсируется отложенным on-demand integrity-check (ARCH-DATA-025); валидация типа уже out-of-scope 2.3. Добавить валидацию на write-boundary при приходе Admin (2.8) или сервиса записи настроек.
- **control_hour tz-combine конвенция** (Blind+Edge): `TimeField` хранит naive `17:00`; `USE_TZ=True`, `Clock.now()` — aware-UTC. Консьюмеры (светофор 5.5, блокировка 5.6, beat 5.7) ОБЯЗАНЫ комбинировать час с датой в `Asia/Qyzylorda` (`Clock.today_local()`), иначе порог сдвинут на +5ч молча. Поле само корректно (config, не wall-clock). Заэнфорсить конвенцию + тест в реализующих E5-стори.
- **AST-изоляция не покрывает selectors.py** (Edge, верифицировано): `apps/core/tests/test_isolation.py::_domain_layer_files()` сканирует `models.py`/`services/`, но НЕ `selectors.py` — проект-wide пробел (любой селектор может протащить wall-clock или `import apps.core.models` мимо ARCH-DATA-022/ARCH-004). Не дефект 2.3 (пробел пре-существует). Тех-тикет: расширить `_domain_layer_files()` на файлы `selectors.py`/пакеты `selectors/`, прогнать на всех app (легаси `operations/selectors.py` импортирует `apps.core.selectors` — разрешено, не модели — должно пройти).

## Deferred from: code review of 2-1-перенос-rbac-во-вложенный-app-operations-rbac (2026-06-22)

Проход 2 (bmad-code-review, scoped diff `1b65f54..HEAD` по путям 2.1 + untracked рецепт; 3 слоя Opus 4.8). Контрольный проход по ПОСТ-harden состоянию (после прохода 1). 2 defer (1 patch — DAG-зависимость `ops_rbac/0001`→`operations/0005` — в Review Findings стори; 7 dismiss). AC-1/2/3 — SATISFIED.

- **`elidable=True` на data-правке ГЛОБАЛЬНОЙ `django_content_type` — риск при будущем squash** (Blind+Edge): `RunPython(..., elidable=True)` (`0002_rename_content_types.py:66`, патч прохода 1) → `squashmigrations` может выбросить relabel. На свежей БД no-op, но БД домигрированная до `operations/0005` и НЕ дошедшая до ops_rbac при squashed-наборе потеряет UPDATE → осиротевшие `operations`-CT. В контексте VAPS (single closed-circuit pilot, fresh-БД) почти невозможно → low; оставляем `elidable=True` (стандарт одноразовой data-миграции). Усиление: оговорка в `docs/recipes/separate-database-and-state-app-port.md` «при squash сохранять relabel non-elidable ЛИБО гарантировать свежесть целевых БД».
- **Путь UPDATE relabel не покрыт автотестами** (Blind+Edge): `test_app.py:34-37` проверяет `ContentType.app_label=="ops_rbac"`, но на свежей тест-БД это даёт `post_migrate` (создаёт CT напрямую), а НЕ ветвь UPDATE из `0002` (`_relabel`, стр.32-47). Collision-safe relabel / round-trip forward→reverse→forward имеют нулевое автопокрытие — лишь ручной `migrate` на populated-БД (Task 8, dev-attested) + round-trip за Bratan. Migration-state-тест (pre-state `operations`-CT → apply → проверить relabel + сохранённый `id`) — test-hardening, ближе к E7/тест-канону.

## Deferred from: code review of story-2.6 (2026-06-22)

- **`Position.level` без `MinValueValidator`; семантика отрицательных уровней не специфицирована** (`Backend/VAPS/apps/core/models.py:102`): поле `level = IntegerField(default=0)` допускает отрицательные → они сортируются «старше» level 0. Не введено стори 2.6 (канон лишь читает `level`); «меньше=старше» обрабатывает отрицательные консистентно. Узаконить неотрицательность (валидатор/Admin-форма) при наполнении справочника — стори 2.8 (Admin справочников) или отдельная модель-валидация. (edge MED, review 2.6)
- **`active_in_division` = 2 константных запроса (Position-map + employees), не N+1** (`Backend/VAPS/apps/core/selectors.py`): перевод на канон добавил один запрос карты `{code: level}` к запросу сотрудников. Не N+1 (docstring точен), Position — крошечный справочник, цикл-потребителя сегодня нет. Если per-person списки расхода (E6/E9/E10) начнут звать `active_in_division` в цикле — кешировать карту уровней / поднять её в вызывающего. (edge MED, review 2.6)

## Deferred from: code review of 2-7-импорт-должностей-и-званий-из-csv (2026-06-23)

Проход 1 (bmad-code-review, scoped diff 387 строк по 2 новым файлам `apps/core/management/commands/import_references.py` + тесты; 3 слоя Opus 4.8). Acceptance Auditor: ACCEPT — AC-1..6 SATISFIED. 1 decision + 4 patch в стори; 3 defer (ниже); 8 dismiss.

- **Сверхдлинные значения справочника без pre-валидации длины** (`import_references.py:176-189`, edge MED): `code`>50 / `name`>255 / `category`>50 без проверки длины перед upsert. На гейтовом Postgres превышение `max_length` → `DataError` → gracefully `skip("invalid_value")` (поведение корректно: строка отклонена, остальные импортируются), НО метка причины не длино-специфична. На SQLite (динамическая типизация) значение сохраняется без усечения → дивергенция, если pytest гонять на SQLite вместо гейтового Postgres. Полировка: pre-валидация длины с явной причиной (`code_too_long` и т.п.) или переименование метки — вместе с Admin/валидаторами справочников (стори 2.8).
- **Ветка savepoint DB-reject (`invalid_value`/`integrity_error`) без автотеста** (`import_references.py:184-189`, auditor): dev-гарантия «битая строка не роняет остальные» оттестирована только для валидационных skip (pre-DB), а DB-уровневый per-row savepoint-recovery — нет. Тест engine-зависим (`DataError` поднимается лишь на Postgres → Postgres-only маркер), а ветка `integrity_error` практически недостижима через нормальный CSV: in-file `seen` дедупит одинаковый `code` ДО БД, а другого unique-констрейнта у `Position`/`Rank` нет (PK=code). Migration/engine-aware тест на DataError-skip — test-hardening, при случае.
- **`EXAMPLE_LIMIT=5` усечение примеров без теста** (`import_references.py:204`, auditor): `examples[:5]` не покрыто — нет кейса «>5 skip одной причины → в отчёте ровно 5 примеров». Дёшево и детерминированно (без engine-зависимости), но низкий приоритет / анти-gold-plating per spec.

## Deferred from: code review of 2-8-admin-для-справочников (2026-06-23)

Проход 1 (bmad-code-review, scoped diff ~129 строк по 4 файлам `apps/core` — User Django-auth; 3 слоя Opus 4.8, same-model caveat). Acceptance Auditor: ACCEPT — AC-1..4 SATISFIED. 2 patch в стори; 2 defer (ниже); 9 dismiss.

- **Migration-state автотест 0016 (reverse + backfill существующих строк) отсутствует** (`apps/core/migrations/0016_…py`, blind+edge+auditor): AC-2 (reverse чисто откатывает добавленные поля + существующие `core_users`-строки получают `is_staff=False`) верифицировано ВРУЧНУЮ — round-trip forward→reverse→forward на одноразовой БД `vaps_rt28` (три `exit=0`, `is_staff`-колонка дропается на reverse, строка уцелела, re-forward backfill False; в Debug Log стори). Коммит-тест отсутствует: миграция `AddField`-only (обратима по построению), а migration-state тест требует фреймворка (`django_test_migrations`/`MigratorTestCase`) — нет в dev-deps. Test-hardening, отложено (прецедент 2.1: «путь UPDATE-relabel без автотеста» → defer; round-trip как принятое доказательство для рисковых миграций).
- **PermissionsMixin design-hazard: Django `has_perm` теперь живой на `User` app-wide** (`apps/core/models.py`, blind LOW design / edge): после реанимации Django-auth любой `User`-инстанс отвечает на `has_perm`/`has_module_perms`, а суперюзер ШУНТИРУЕТ все Django-проверки. Сегодня утечки нет — Edge верифицировал: `PermissionService.has_permission` ключуется на строке `actor_id`, `XUserIdAuthentication` возвращает `None` и не читает `is_staff`, DRF-permissions не зовут Django `has_perm`. Риск forward-looking: будущий код, ошибочно вызвавший `request.user.has_perm(...)`, молча консультирует Django вместо in-house RBAC, а «catalogs-only» суперюзер обходит ВСЁ. Guard-тест «бизнес-эндпоинты не консультируют Django `has_perm`/`is_staff`» + явная фиксация границы — естественный дом стори 2.9 (RBAC-матрица роль×операция, AR-9).

## Deferred from: code review of story-2.9 (2026-06-23)

Проход 1 (bmad-code-review, scoped diff 384 строки по 2 новым тест-файлам `apps/operations/tests/test_rbac_matrix.py` + `test_authz_boundary.py`; 3 слоя Opus 4.8, same-model caveat). Acceptance Auditor: ACCEPT — AC-1..6 SATISFIED. 3 patch применены (в стори); 3 defer (здесь); 4 dismiss.

- **completeness-gate сверяет имена роутов, а не пары (name, method)** (`apps/operations/tests/test_rbac_matrix.py::test_matrix_covers_every_registered_route`, blind MED): AC-1 буквально про «(роут, метод)», но gate сверяет `set(SERVED)==set(MATRIX)` на уровне имён. Новый HTTP-метод на существующем имени роута не флагается completeness'ом. Митигировано: поведенческий тест итерирует (name × served-методы × акторы) и ловит незагейченный новый метод DENY-ячейками (роль без права → ≠403 → красный) — метод-гранулярность есть на поведенческом уровне, пока все методы роута делят один guard-kind. Refinement: ключевать MATRIX по (name, method), когда появятся роуты со смешанной по-методной политикой. Отложено (поведение уже method-granular).
- **`_url_for` угадывает pk через NoReverseMatch-fallback** (`test_rbac_matrix.py::_url_for`, blind+edge LOW): `reverse(name)` → при NoReverseMatch `reverse(name, kwargs={"pk":"0"})`. Хрупко к кастомным роутерам / `lookup_url_kwarg`≠`pk` и к гипотетическому совпадению list/detail имён (тогда detail-метод уйдёт на коллекционный URL → 405, ложно зелёный для ALLOW). Сейчас все 25 роутов резолвятся корректно (Edge верифицировал, pk="0" подходит UUID/CharField-PK через `[^/.]+`). Отложено (латентно). Лечение: пробрасывать наличие `<pk>` из `_walk`-паттерна в `_url_for`.
- **boundary AST-скан слеп к алиасам `request` и `getattr`-строкам** (`test_authz_boundary.py::_violations`, blind+edge LOW): ловит прямой `request.user`/`*.request.user` + 4 forbidden-attr (`has_perm`/`has_module_perms`/`is_staff`/`is_superuser`), но не `r = request; r.user` или `getattr(u, "is_staff")`. Прецедент — эвристичные guard'ы `test_isolation.py` (тоже не покрывают алиасы). Код сегодня чист (0 совпадений). Отложено (эвристика; документировать границу гарантии в докстринге).

## Deferred from: code review of story-2.10 (2026-06-23)

Проход 1 (bmad-code-review, scoped diff 76 строк по 3 файлам config + smoke; 3 слоя Opus 4.8, same-model caveat). Acceptance Auditor: ACCEPT — AC-1..5 SATISFIED (проверено реальным прогоном). 2 patch применены (в стори); 2 defer (здесь); 3 dismiss.

- **Прод-security-hardening материализован монтированием `/admin/`** (`Backend/VAPS/config/settings.py`, blind LOW + edge LOW): `DEBUG`-default-`1` + `ALLOWED_HOSTS=["*"]` + нет `SecurityMiddleware` + нет `SESSION_COOKIE_SECURE`/`CSRF_COOKIE_SECURE`/`HSTS` (`manage.py check --deploy` → W001/W009/W012/W016/W018). До 2.10 admin не был смонтирован → риск теоретический; 2.10 ввёл cookie-сессию (sessionid/csrftoken) и login-поверхность `/admin/` → варнинги стали материальны (DEBUG=1 отдаёт трейсбэки на /admin/login/; cookie без Secure уходят по HTTP). Прод-конфиг, не код стори. → **E12 prod-deploy / prod-hardening эпик** (ARCH#L319 «HTTPS/IP-фильтрация/rate limiting — эпик production hardening; не блокирует первый релиз: закрытый LAN»). Лечение: prod-settings с `DEBUG=False`, реальный `ALLOWED_HOSTS`, `SecurityMiddleware`, `SESSION_COOKIE_SECURE`/`CSRF_COOKIE_SECURE = not DEBUG`.
- **STATIC_ROOT/collectstatic — admin-ассеты под не-DEBUG/прод** (`Backend/VAPS/config/settings.py`, blind LOW): задан `STATIC_URL="static/"`, но нет `STATIC_ROOT`. Под `DEBUG=False` без `collectstatic`+nginx-alias admin-CSS/JS не отдаются (HTML-логин рендерится 200, но «голый»); smoke-тест `status==200` это не ловит. → **E12** (ARCH#L335 «static/media — nginx alias (ASGI не отдаёт)»; не whitenoise). Лечение: `STATIC_ROOT` + `collectstatic` в bundle + nginx alias в 12.1.

## Deferred from: code review of 2-12-edit-safety-справочников-валидаторы-create-defaults (2026-06-23)

Проход 1 (bmad-code-review, scoped diff ~250 строк по 7 файлам; 3 слоя Opus 4.8, same-model caveat). Acceptance Auditor: ACCEPT — AC-1..5 SATISFIED. 1 decision (CSV-импорт пишет отрицательные ordinal'ы мимо валидатора — в Review Findings стори, ждёт решения Bratan A/B) · 0 patch · 2 defer (ниже) · 8 dismiss.

- **Существующие отрицательные ordinal-строки в БД не детектятся/не чинятся миграцией 0017** (`Backend/VAPS/apps/core/migrations/0017_*.py` + `apps/core/sorting.py`, edge+blind LOW): 0017 = 4×AlterField (Python-валидатор `MinValueValidator(0)`), без DB `CheckConstraint` и без data-migration; `sorting.py` читает `Position.level` без floor. Валидатор закрывает только будущие `full_clean`/serializer-записи, не data-at-rest: если отрицательная строка уже попала в БД (напр. через `import_references` до фикса decision D1), seniority-hazard «отрицательный level сортируется старше 0» остаётся живым. На пилоте сейчас не материально (seed/import дают неотрицательные конст.). Лечение: парно с разрешением D1 — `CheckConstraint(level__gte=0)` (DDL-миграция, surfaces существующие негативы) ЛИБО разовый audit/clamp `RunPython`.
- **Ре-сид больше не реактивирует оператор-деактивированный системный StatusType** (`Backend/VAPS/apps/operations/statuses/management/commands/seed_statuses.py`, edge LOW): перенос `is_active` в `create_defaults` (by-design #L174) означает асимметрию — канон-поля форс-синкаются каждый ре-сид, а `is_active`/`color` нет. Системно-критичный тип (напр. `IN_SERVICE`, priority 999) можно через Admin выставить `is_active=False`, и ре-сид его НЕ восстановит → тихий foot-gun без safety-net. Гард «системно-критичные типы нельзя деактивировать через Admin» (напр. `has_delete`/`is_active`-lock в ModelAdmin или проверка консьюмерами) — естественный дом статус-движка E3.

## Deferred from: code review of 2-13-гейт-прав-на-core-api-эндпоинты (2026-06-23)

Проход 1 (bmad-code-review, scoped diff 332 строки по 9 файлам; 3 слоя Opus 4.8, same-model caveat). Acceptance Auditor: ACCEPT — AC-1..5 SATISFIED. Edge эмпирически подтвердил корректность механизма (DRF-цепочка, boundary, fail-closed). 0 decision · 2 patch (в стори: ordering-guard тест + L10 doc-fix) · 3 defer (ниже) · 6 dismiss.

- **Seam резолвит effective_permissions на КАЖДЫЙ аутентифицированный запрос, вкл. незагейченные эндпоинты** (`Backend/VAPS/apps/operations/api/authz.py`, blind+edge MED): resolver в `DEFAULT_AUTHENTICATION_CLASSES` + DRF `initial()` всегда дёргает `request.user` → seam бежит на каждой вью. Edge эмпирически: ungated `employee-list` платит 3 RBAC-запроса (`ops_user_roles`/`ops_temporary_duty_permissions`/`ops_role_permissions`), которые не потребляет. ARCH-SEC-031 предписывает per-request без кэша (резолюция by-design), расточительна лишь на роутах БЕЗ гейта. Лечение: ленивая резолюция (резолвить при первом `require_permission`) ИЛИ per-request memo. Пересмотреть при горячих list/detail-эндпоинтах расхода (E6/E9/E10) или в 2.14.
- **`seed_operations` grow-only — не отзывает устаревшие RolePermission** (`Backend/VAPS/apps/operations/management/commands/seed_operations.py`, edge MED): `update_or_create` по карте, ничего не удаляет (тот же паттерн, что seed_statuses #L173/#L174). Edge эмпирически: грант `VIEWER→admin.roles` выживает ре-сид. КРИТИЧНО в контексте 2.13: core role-map PROVISIONAL и БУДЕТ ревизирован Bratan — когда право убирают из карты, старый грант в уже-посеянной БД НЕ отзывается (idempotent-тест считает только добавления). При канонизации провизорной карты: prune-проход (`RolePermission.objects.exclude(<по карте>).delete()`) ЛИБО ручной cleanup посеянной БД. Security-sensitive (незаслуженный грант выживает).
- **`*`-wildcard продублирован: core-гейт vs PermissionService.has_permission — риск дрейфа** (`Backend/VAPS/apps/core/api/permissions.py:17` vs `apps/operations/services.py:52-56`, blind+edge LOW): core↛operations вынуждает дублировать `"*" in perms`-короткозамыкание. Сегодня семантика идентична (Edge сверил). Если PermissionService введёт вторую супер-привилегию / scoped-wildcard — core-гейт молча разойдётся. Лечение: parity-тест «core require_permission ≡ PermissionService.has_permission на матрице кейсов» (в тестах импорт обоих легален) — в 2.14 при раскатке гейта. **[ЗАКРЫТО в 2.14: `apps/operations/tests/test_gate_parity.py`.]**

## Deferred from: code review of 2-14-раскатка-гейта-core-api-на-остальные-viewsets (2026-06-23)

Проход 1 (bmad-code-review, scoped diff 555 строк по 9 файлам; 3 слоя Opus 4.8, same-model caveat). Acceptance Auditor: ACCEPT — AC-1..5 SATISFIED. Edge эмпирически подтвердил корректность ядра. 0 decision · 3 patch (в стори: OPTIONS/405-фикс mixin + map==served-тест + strengthen anon-assert) · 2 defer (ниже) · 7 dismiss.

- **HTTP-тесты эндпоинтов авторизуются ADMIN(`*`) → too-weak-code/map-ошибка ловится только rbac-matrix'ем (нет независимого оракула)** (`Backend/VAPS/apps/core/tests/conftest.py` `grant`, blind): endpoint-behavior тесты с ADMIN не различают, требует ли write-action `.view` или `.edit`-tier. Митигировано: rbac-matrix гоняет реальные роли (VIEWER и т.д.) через живой DRF-стек → ловит гейт, не энфорсящий заявленную политику (Edge верифицировал VIEWER→403 на write). Остаточный зазор: ДВЕ согласованные ошибки (slack-код одновременно в prod-`permission_map` И в MATRIX) не ловятся. Независимый semantic-оракул «write-action обязан требовать edit/manage-tier, не view» — возможное усиление; при текущем matrix+parity-покрытии граничит с over-engineering. Пересмотреть, если permission-карта core усложнится (новые ресурсы/тиры).
- **`*` — безусловный мастер-ключ; нет ассерта «только ADMIN держит `*`»** (`Backend/VAPS/apps/operations/management/commands/seed_operations.py` ROLE_PERMISSIONS, blind MED): `require_permission`/`PermissionService` открывают всё при `* in perms`. Если не-ADMIN роль когда-нибудь получит `*` (баг/ошибочная правка provisional-карты) — откроются ВСЕ гейты, а rbac-matrix закодирует это как «корректно» (держатели деривятся из той же карты). Security-hardening: гард-тест «ни одна роль кроме ADMIN не содержит `*` в `ROLE_PERMISSIONS`». Естественный дом — канонизация provisional role-map (открытый вопрос Bratan, вместе с seed-prune устаревших грантов).

## Deferred from: code review of 3-1-domainerror-и-единый-exception-handler (2026-06-24)

Проход 1 (bmad-code-review, scoped diff ~483 строки по 8 файлам; 3 слоя Opus 4.8, same-model caveat). Acceptance Auditor: ACCEPT — AC-1..8 SATISFIED (проверено вживую на Postgres). 0 decision · 2 patch ПРИМЕНЕНЫ (OperationalError-сужение до SQLSTATE 40P01/40001 + `_reshape_drf` страж `detail`-поля) · 2 defer (ниже) · 2 dismiss.

- **429/406/415 остаются DRF-native (нет `error_code`/`request_id`/`timestamp` §36-конверта)** (`Backend/VAPS/apps/core/api/exception_handler.py` `_reshape_drf`, edge MED): статусы без кода в `error-codes.yaml` НЕ переоформляются в §36 — осознанная граница AC-7↔AC-8 (закрытый мир побеждает: фабриковать код вне реестра нельзя). Auditor подтвердил защитимость. Клиент против §36-конверта не распарсит throttle/406/415. Лечение: завести коды 405/429 (и нужные) в `error-codes.yaml` тем же PR, когда фронт (8.4/8.5) реально начнёт их потреблять, затем расширить `_DRF_STATUS_TO_CODE`. Сегодня этих путей в проде нет.
- **IntegrityError→422 без end-to-end покрытия; no-savepoint broken-connection для БУДУЩИХ write-view** (`Backend/VAPS/apps/core/api/exception_handler.py` + отсутствует write-эндпоинт EmployeeStatus, edge MED): проводка DRF→handler верифицирована (source-inspection), но IntegrityError→422 гоняется только на handler-unit уровне — нет write-API, создающего статус. `ATOMIC_REQUESTS` не задан → когда будущий сервис поднимет IntegrityError БЕЗ окружающего `transaction.atomic()` savepoint, соединение останется aborted (`InFailedSqlTransaction` при доп. запросах после catch). Владеют стори, вводящие write-эндпоинты статусов (3.3+): обязаны заворачивать запись в savepoint и добавить end-to-end тест IntegrityError→422 через реальный dispatch.

## Deferred from: code review of 3-2-полная-модель-статуса (2026-06-24)

Проход 1 (bmad-code-review, scoped diff ~361 строк по 5 файлам; 3 слоя Opus 4.8, same-model caveat). Acceptance Auditor: ACCEPT — AC-1..5 SATISFIED (mutation-тест доказал не-вакуумность AC-3). Edge: 0 HIGH/0 MED (эмпирически проверил границы + manager-регресс). 0 decision · 3 patch ПРИМЕНЕНЫ (@example boundary-пины на equivalence-тест + registry-тест ассертит http_status 422 + чистка коммент-коллизии) · 1 defer (ниже) · 5 dismiss.

- **`assert_user_editable()` реализован, но НЕ подключён к edit-пути (dead-код до 3.3)** (`Backend/VAPS/apps/operations/statuses/models/employee_status.py` guard, blind+edge LOW): по Решению №1=A guard-метод живёт в 3.2 (AC-2 удовлетворён на контракт-уровне модели + протестирован прямым вызовом), но operator-edit endpoint строит 3.3. Латентный риск: если 3.3 забудет вызвать `assert_user_editable()` ПЕРЕД мутацией, OM_AUTO/KU_SYNC станут редактируемыми оператором (нарушение AR-8 single-writer). Стори 3.3 (сервис создания/правки статуса) ОБЯЗАНА: (а) вызвать `assert_user_editable()` в operator-edit/patch-пути; (б) добавить end-to-end тест «оператор PATCH-ит OM_AUTO → 422» через реальный dispatch.

## Deferred from: code review of 3-3-сервис-создания-статуса-с-валидациями (2026-06-24)

Проход 1 (bmad-code-review, scoped diff ~754 строки по 6 файлам; 3 слоя Opus 4.8, same-model caveat). Acceptance Auditor: ACCEPT — AC-1..10 SATISFIED вживую. 0 decision · 2 patch · 3 defer (ниже) · 7 dismiss.

- **`None` в `date_start`/`date_end` → 500, не 4xx** (`Backend/VAPS/apps/operations/statuses/services/status_service.py:156`, edge MED): `date_start >= date_end` на `None` кидает `TypeError` → handler даёт `INTERNAL_ERROR` 500 (без утечки). Сервис — typed-kwargs контракт (`date`), форму/типы (null, коэрция) валидирует слой сериализатора/bulk; в 3.3 прод-API нет (Решение №3). Владеет стори, вводящая сериализатор создания статуса (или 3.8 bulk): добавить form-валидацию null/типов до сервиса.
- **`max_duration_days=0` делает тип несоздаваемым** (`Backend/VAPS/apps/operations/statuses/models/status_type.py:31`, edge LOW): минимальная полуоткрытая длительность = 1 > 0 → любой статус такого типа → `MAX_DURATION_EXCEEDED`. `NULL` — задокументированный сентинел «без лимита», `0` — тихий foot-gun (битый seed/canon). seed сейчас 0 не ставит. При вводе числовых лимитов из спеки: значения ≥1 + либо запретить 0 на уровне валидатора/seed, либо явно трактовать 0 как «без лимита».
- **hard-ность завязана на tuple `HARD_STATUS_TYPE_CODES`, не на `StatusType.is_hard_block`** (`Backend/VAPS/apps/operations/statuses/models/employee_status.py:13`, edge LOW, PRE-EXISTING 1.5/3.2): `StatusType(is_hard_block=True)` с кодом вне tuple получает НОЛЬ overlap-защиты и в пред-проверке (3.3), и в GiST-констрейнте. Оба слоя взаимно консистентны (оба читают tuple), но слепы к флагу. Сейчас tuple и seed-флаги синхронны (есть страж-тест 2.2). Риск материализуется, если справочник станет полностью Admin-управляемым (новый hard-тип через Admin без правки tuple). Владеет стори динамического справочника hard-типов / 3.4 (конфликт-детектор на матрице).

## Deferred from: code review of 3-4-конфликт-детектор-на-матрице-данных (2026-06-24)

Проход 1 (bmad-code-review, scoped ~1219 строк / 5 файлов; 3 слоя Opus 4.8, same-model caveat). Acceptance Auditor: ACCEPT — AC-1..9 SATISFIED вживую. 0 decision · 2 patch · 5 defer (ниже) · 8 dismiss.

- **COMPLETED (прошлый) soft-overlap → блокирующий 409, не warning** (`Backend/VAPS/apps/operations/statuses/conflict_matrix.py:95`, edge MED): `detect_conflicts` даунгрейдит в warning ТОЛЬКО `date_start > business_date` (PLANNED); soft-статус, который уже закончился (COMPLETED, целиком в прошлом), но интервально пересекает новый (бэкдейтинг), блокирует как ACTIVE (409). Спека формулирует «soft(ACTIVE)→409, soft vs PLANNED→warning» и молчит про COMPLETED. 409-overridable защитимо, но lifecycle-таксономия honored лишь наполовину. Уточнить с заказчиком; владеет 3.5 (override) или follow-up по семантике COMPLETED.
- **Ветка COMPATIBLE мертва/непокрыта до 3.10** (`Backend/VAPS/apps/operations/statuses/conflict_matrix.py` COMPATIBLE_PAIRS, edge LOW): `COMPATIBLE_PAIRS=frozenset()` пуст → `classify_pair` никогда не вернёт COMPATIBLE, `continue`-skip в detect_conflicts не исполняется, ноль тестов. Намеренно (секондмент-пары). Стори 3.10 заполняет набор И обязана оттестировать COMPATIBLE-путь (+ обновить exhaustive property-тест, который сейчас ассертит `HARD ⟺ either_hard` — сломается на COMPATIBLE среди soft-пар).
- **FR-10 «warning в ответе» не surfac'ится** (`Backend/VAPS/apps/operations/statuses/services/status_service.py`, blind MED): PLANNED-warnings вычисляются `detect_conflicts`, но `_assert_no_conflict` их отбрасывает (нет канала возврата в service-only, Решение №3). Оператор не получает сигнала про пересечение с PLANNED. Владеет API-стори создания статуса / 3.5: вернуть `warnings[]` в HTTP-ответе (200 + warnings, или заголовок/поле).
- **classify_pair не тотальна по неизвестным/None кодам → тихо SOFT** (`Backend/VAPS/apps/operations/statuses/conflict_matrix.py:50`, blind LOW): код вне hard-набора и вне COMPATIBLE_PAIRS → SOFT, без валидации существования. В сервисе тип валидируется `_resolve_status_type` (422) ДО детектора, а existing_rows приходят из БД (валидны) — недостижимо сейчас. Латентно для будущих E14/E16-адаптеров, переиспользующих детектор. Задокументировать контракт «вызывающий передаёт валидные коды» или добавить guard, когда появятся адаптеры.
- **Нет drift-guard теста (констрейнт-снимок миграции ≡ живой tuple)** (`Backend/VAPS/apps/operations/statuses/models/employee_status.py:13` / migration 0001, edge+blind LOW, связано с D3): GiST `excl_hard_status_overlap` — снимок миграции; смена `HARD_STATUS_TYPE_CODES` без новой миграции расходит Python-детектор и БД. Seed-тест 2.2 сверяет лишь `StatusType.is_hard_block` ≡ tuple, не замороженное выражение констрейнта. Добавить поведенческий drift-guard (для каждого hard-кода overlap БД-блокируется; для soft — нет), особенно когда справочник hard-типов станет Admin-управляемым.

## Deferred from: code review of 3-5-override-сущность (2026-06-24)

Проход 1 (bmad-code-review, scoped ~644 строки / 5 файлов; 3 слоя Opus 4.8, same-model caveat). Acceptance Auditor: ACCEPT — AC-1..9 SATISFIED вживую. Edge: 0 HIGH/MED. 0 decision · 3 patch · 4 defer (ниже) · 6 dismiss.

- **Денорм `division_id`-снимок для point-in-time per-division SM-C1** (`Backend/VAPS/apps/operations/statuses/models/override.py`, blind+edge LOW): Override хранит `employee_id`+`created_by`(оператор), но НЕ `division_id`. Per-подразделение SM-C1-счёт достижим только join'ом Override→Employee.division, отражающим ТЕКУЩУЮ дивизию сотрудника, не на момент override (искажается при переводах). Снимок division-на-момент-обхода → стори метрик/дашбордов SM-C1 (E20) или когда SM-C1 реально вычисляется. docstring 3.5 уже исправлен (честно: per-division через join).
- **`reason` — TextField без верхней границы** (`Backend/VAPS/apps/operations/statuses/models/override.py`, edge LOW): Решение №1=A зафиксировало только непустую причину; верхняя граница длины (и донор-BR-003 «10–500») не вводилась. Добавить max-length-валидацию, когда появится HTTP-сериализатор создания статуса (API-стори), либо подтвердить 10–500 с заказчиком.
- **FK PROTECT: нет теста/пути очистки при hard-delete статуса** (`Backend/VAPS/apps/operations/statuses/models/override.py`, blind LOW): `status` FK `on_delete=PROTECT` навсегда блокирует hard-удаление EmployeeStatus с Override; каскад Employee/Division→статус даст ProtectedError. By-design (статусы soft-cancel'ятся, не удаляются). Пересмотреть, если введётся каскадное удаление статусов/сотрудников.
- **edit-override отсутствует** (`Backend/VAPS/apps/operations/statuses/services/status_service.py`, blind LOW, by-design): `update_status` не принимает/не прокидывает override и не пишет Override-запись. Override-правка существующего статуса (изменить интервал в обход soft-конфликта) → стори 3.6 (операции жизненного цикла). — ✅ **ЗАКРЫТО стори 3.6 (2026-06-25):** `extend_status(..., override=True, override_reason=...)` обходит soft-409 при продлении и пишет `Override`-запись в том же savepoint (AC-8).

## Deferred from: dev-story 3.6 (2026-06-25)

- **Append-once `cancelled_at/by/reason` не охраняется БД** (`Backend/VAPS/apps/operations/statuses/models/employee_status.py`, story 3.6): необратимость отмены гарантируется на СЕРВИС-уровне (`cancel_status` отбивает любую операцию над CANCELLED через state-guard → 422 `INVALID_LIFECYCLE_TRANSITION`; запись только через `update_fields`). Прямой `UPDATE cancelled_at=NULL`/правка отменённой строки в обход сервиса БД не запрещены. DB-level REVOKE UPDATE/DELETE + триггер append-only → **Epic 4** (audit append-only, ARCH-SEC-032). Закрывает сервис-часть deferred-work#L33; DB-часть остаётся E4.

## Deferred from: dev-story 3.8 (2026-06-25)

- **p95-бюджет `bulk_create_statuses` на 300 строк — `PENDING-measure`** (`Backend/VAPS/apps/operations/statuses/tests/test_bulk_status_service.py`, story 3.8 AC-7): субстантивный no-N+1 контракт ЗАКРЫТ (константа 8 запросов, 5 vs 50 строк, доказано load-bearing инжектом N+1). Но wall-clock p95-бюджет на 300 строк НЕ замерен: двух-машинное окружение (dev-ноут ≠ целевая ВПС), dev-замер нерепрезентативен (прецедент спайка 1.10 путь B). Делегировано Bratan: прогнать 300-строчный bulk на ЦЕЛЕВОЙ машине, записать p95-константу, добавить охранный тест (`@pytest.mark.slow`, бюджет из замера). Число НЕ выдумано. Триггер: перед нагрузочными тестами E19/пилотом (architecture.md#L97 «нагрузочный сценарий дедлайн-17:00 → перед пилотом»).

## Deferred from: code-review 3.6 (2026-06-25)

- **Статус, начинающийся СЕГОДНЯ (ACTIVE), не имеет пути аннулирования** (`Backend/VAPS/apps/operations/statuses/services/status_service.py`, edge LOW): `[today, end)` → ACTIVE, поэтому `cancel_status` (только PLANNED) → 422, а `complete_status_early` не может дать нулевую длину (`actual_end ≤ date_start=today` → `INVALID_DATE_RANGE`; полуоткрытый интервал требует ≥1 день). Ошибочно заведённый статус с началом «сегодня» нельзя ни отменить, ни схлопнуть. Вне явных AC 3.6 — нужно продуктовое решение: разрешить ли cancel для ACTIVE-same-day (никаких фактических дней не прошло), или минимум 1 день приемлем. Пересмотреть при появлении операторского UI массового обновления (E10) / при жалобе оператора.
- **`extend_status` ре-проверяет конфликты по ВСЕМУ интервалу, не только по удлинённому хвосту** (`Backend/VAPS/apps/operations/statuses/services/status_service.py:extend_status`, edge — flagged HIGH, по факту inherited): `_assert_no_conflict` бежит по полному `[date_start, new_end)`; ранее принятый (как PLANNED-warning) soft-overlap на неизменном префиксе `[date_start, old_end)` переподнимается 409, заставляя оператора повторить override для конфликта, который он уже принял. **Консистентно с `update_status` (3.3): целый-интервал ре-чек на любой date-edit — установленный паттерн, override доступен**, hard×soft на префиксе недостижим (блок на создании). Возможное будущее уточнение: проверять только хвост `[old_end, new_end)` на extend (и симметрично для update). Не баг 3.6, а унаследованная семантика edit-пути; пересмотреть, если операторы жалуются на «лишние override» при продлении.

## Deferred from: code-review of story-3.8 (2026-06-25)

Проход 1 (bmad-code-review, scoped ~484 строки / 4 файла; 3 слоя Opus 4.8, same-model caveat). Acceptance Auditor: ACCEPT — AC-1..10 SATISFIED вживую. 0 decision · 3 patch · 5 defer (ниже) · 8 dismiss.

- **Агрегат soft-409 envelope роняет `overridable`** (`Backend/VAPS/apps/operations/statuses/services/bulk_status_service.py:219`, auditor LOW): при чисто-soft bulk-отказе агрегат-`DomainError` не передаёт `overridable=` → дефолт `False` (exceptions.py:32), хотя реестр `STATUS_OVERLAP_WARNING.overridable: true` и single-row `_assert_no_conflict` передаёт `True` (status_service.py:182). Per-row записи в `detail.rows[]` тоже без `overridable`. LOW для 3.8 — bulk-override OUT по AC-9, консьюмера (HTTP) нет. Решить семантику envelope-`overridable` (и нужен ли retry-with-override для bulk) при E10 HTTP-слое + если/когда вводится bulk-override.
- **Нет cap на размер payload + `bulk_create` без `batch_size`** (`Backend/VAPS/apps/operations/statuses/services/bulk_status_service.py`, blind LOW/MED): `IN (...)` и `bulk_create` не ограничены по числу строк. Масштаб «утреннего обновления» ~40-300 (p95-таргет 300); Postgres param-limit достижим только на ~8k+ строк. Лимит размера запроса (400 при превышении) — задача DRF-сериализатора E10 (payload-валидация, AC-9 out-of-scope). `batch_size=` для `bulk_create` — при появлении реально больших payload'ов.
- **docstring `lock_employees` переобещает анти-deadlock** (`Backend/VAPS/apps/core/selectors.py:209`, blind+edge LOW): «two operators … cannot deadlock» строго верно для двух BULK-writer'ов (оба `ORDER BY id`); single-row `lock_employee` лочит ровно одну строку и цикла образовать не может, lifecycle-операции лочат ровно одного сотрудника. Реального deadlock между bulk и single-row на практике нет, но формулировка шире проверенного. Уточнить docstring до области «среди bulk-вызовов» (doc-honesty, не поведение).
- **Перф-тест на одном типе/одном дивизионе** (`Backend/VAPS/apps/operations/statuses/tests/test_bulk_status_service.py:462`, blind LOW): `test_query_count_constant_no_n_plus_one` гоняет 5 vs 50 строк одного типа (STUDY) в одном дивизионе. Гипотетический per-distinct-type / per-distinct-division N+1 невидим (но структурно отсутствует: типы префетчатся одним `code__in`, per-division запросов нет). Опц. усиление: вариант с несколькими типами/дивизионами.
- **`_overlaps` дублирует half-open предикат в Python** (`Backend/VAPS/apps/operations/statuses/services/bulk_status_service.py:83`, blind+auditor LOW): half-open overlap `row["date_start"] < date_end and row["date_end"] > date_start` повторяет ORM-предикат single-row пути (`date_start__lt=date_end, date_end__gt=date_start`, status_service.py:152-153). Дублирование неизбежно (ORM-фильтр не исполнить in-memory; спека требует in-memory для no-N+1) и предикаты идентичны. При смене half-open семантики обновлять оба места — вынести общий предикат-функцию, если когда-нибудь поменяется.

## Deferred from: code-review of story-3.9 (2026-06-25)

Проход 1 (bmad-code-review, scoped ~560 строк / 8 файлов; 3 слоя Opus 4.8, same-model caveat). Acceptance Auditor: ACCEPT — AC-1..7 SATISFIED вживую. 0 decision · 4 patch · 2 defer (ниже) · 7 dismiss.

- **amendment-seam `min/max` union над разрозненными интервалами** (`Backend/VAPS/apps/operations/statuses/services/status_service.py:resolve_pending_clarification` + `apps/operations/statuses/amendment_hook.py`, edge+blind MED): `mark_days_for_amendment(employee_id, min(pending.date_start, date_start), max(pending.date_end, date_end))` — если разрешающий интервал НЕ пересекает PENDING (pending `[A,B)`, resolved `[C,D)` непересекающиеся), union накрывает gap незатронутых дней → E5 пере-пометит лишние сданные дни к amendment. Сегодня безвредно (seam = no-op, `DailySubmission` не существует). E5 (стори 5.4) владеет ТОЧНЫМ набором затронутых дней и границей «накрытой даты» `[start,end)` (VAPS_7.8.2 §82.2 → уже deferred выше). При реализации E5: передавать два интервала отдельно ЛИБО ограничить разрешающий интервал пересечением с pending; добавить `date_start <= date_end` ассерт в хук. Типичный кейс (resolved внутри/пересекает pending) union корректен.
- **`source` жёстко USER в `resolve_pending_clarification`** (`Backend/VAPS/apps/operations/statuses/services/status_service.py`, blind LOW): resolved-статус всегда `source=EmployeeStatus.Source.USER` (операторское разрешение, by-design — как create_status). Если появится не-операторский путь разрешения «уточняется» (импорт донора с ретро-уточнением / проекционный авто-резолв), параметризовать `source` (default USER). Пересмотреть при не-USER потребителе resolve.
