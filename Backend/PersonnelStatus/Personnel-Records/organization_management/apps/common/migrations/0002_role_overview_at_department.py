"""Признак «Обзор на уровне департамента» у портальной роли (Plane №348).

Заказчик потребовал у начальника управления «остальные модули на уровне своего
управления ЗА ИСКЛЮЧЕНИЕМ Обзор: Обзор на уровне департамента». Область у
портальной роли ОДНА, поэтому исключение приходится назвать признаком: без него
пришлось бы либо показать Обзор управлением (против прямого требования), либо
расширить до департамента ВСЁ, включая статусы.

Читатель у признака РОВНО ОДИН — `DivisionStatisticsViewSet._statistics_scope`.

САМИХ РОЛЕЙ ЗДЕСЬ НЕТ, и это не забывчивость: портальные роли в этом проекте —
данные, а не миграция (`ROLE_1…ROLE_6` заводятся стендом, и проба
`test_the_empty_role_book_is_a_loud_refusal` держит справочник пустым на чистой
базе). Роли матрицы доступа заводит `manage.py seed_access_matrix` вместе с
учётками — там же, где решается, какая персона какую роль получает.
"""
from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [("common", "0001_initial")]

    operations = [
        migrations.AddField(
            model_name="role",
            name="overview_at_department",
            field=models.BooleanField(
                default=False,
                verbose_name="Обзор на уровне департамента",
                help_text=(
                    "Экран «Обзор» показывает департамент целиком, даже если "
                    "область роли — управление внутри него"
                ),
            ),
        ),
    ]
