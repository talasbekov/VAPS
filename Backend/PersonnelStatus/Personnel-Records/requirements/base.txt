Django>=5.0,<6.1
djangorestframework>=3.14.0
django-cors-headers>=4.3.0
django-mptt>=0.14.0
psycopg2-binary>=2.9.9
celery>=5.3.0
redis>=5.0.0
django-celery-beat>=2.5.0
django-celery-results>=2.5.0
channels>=4.0.0
djangorestframework-simplejwt>=5.3.0
drf-spectacular>=0.26.0
openpyxl>=3.1.2
django-redis>=5.4.0
python-docx>=1.1.0
reportlab>=4.0.0
# 🔴 БОЕВАЯ, А НЕ ТЕСТОВАЯ (Plane №492/№639). Водяной знак «ПРОЕКТ» на
# несогласованной расстановке (`[СОГ-03]`, Plane №430) накладывается на ГОТОВЫЙ
# PDF, и `stamp_draft` читает его через pypdf. Пакет был объявлен только в
# `development.txt` с пометкой «чтение .pdf в тестах»: на стенде и в гейте всё
# зелено (там ставится development поверх base), а сборка по одному base дала бы
# 500 ModuleNotFoundError на первом же «Скачать PDF». Такое находит не гейт, а
# пользователь.
pypdf>=4.0.0
djangorestframework-recursive>=0.1.2

# 🔴 ЧЕТЫРЕ ПАКЕТА, КОТОРЫЕ БОЕВОЙ КОД ИМПОРТИРОВАЛ, НЕ ОБЪЯВЛЯЯ (Plane №794).
# Их находил не гейт, а проба зависимостей: до сегодняшнего дня они приезжали
# ТРАНЗИТИВНО — `asgiref` с Django, `lxml` с `python-docx`, `pillow` с
# `reportlab`, а `django-filter` не приезжал ни с чем гарантированно. То есть
# контракт держался не объявлением, а чужой поставкой: смена версии
# `python-docx`, уронившая `lxml` из своих зависимостей, дала бы в бою
# ImportError, которого окружение разработки не видит вовсе.
# Нижние границы взяты по фактически установленному и по тому, что требует сам
# код: Django 5.1 просит `asgiref>=3.8`, `django-filter` 24.x — минор, на
# котором написаны фильтры разделов аудита и статусов.
asgiref>=3.8.0
django-filter>=24.0
lxml>=5.0.0
pillow>=10.0.0

# 🔴 ВЕБ-СОКЕТ УВЕДОМЛЕНИЙ ЖИВЁТ В БОЮ — решение заказчика 06.09.2026
# (Plane №804 и №806, одна вилка на две карточки).
#
# `channels_redis` называла строкой `base.py` и `production.py`
# (`CHANNEL_LAYERS.default.BACKEND = channels_redis.core.RedisChannelLayer`), а
# объявлен он не был нигде и не стоял ни в одном окружении: настройки обещали
# слой каналов, которого нет. В бою это ImportError на первом же уведомлении, и
# найти его мог только пользователь — проба зависимостей держала эту строку в
# списке известного долга (№805), потому что чинить её кодом было нельзя:
# «живы ли WebSocket-уведомления» — вопрос о боевом контуре, а не о пакете.
#
# `daphne` — ASGI-сервер, и он же единственный вход в `channels.testing`: её
# `__init__` безусловно тянет `channels.testing.live` → `daphne.testing`, так
# что импорт ЛЮБОГО имени оттуда без него падает (замерено 06.09.2026 —
# импорт подмодуля `channels.testing.websocket` тоже падает, Python сперва
# исполняет `__init__` пакета). Без него три пробы раздела уведомлений
# пропускались навсегда, а `ModuleNotFoundError` на сборе рвал весь прогон.
channels-redis>=4.2.0
daphne>=4.1.0
