-r base.txt
pytest>=7.4.0
pytest-django>=4.5.0
# Чтение готового .pdf в тестах: единственный честный способ проверить, что
# кириллица ДОШЛА до документа, — прочитать его обратно. На встроенных шрифтах
# она выпадает молча, и файл при этом остаётся валидным.
pypdf>=4.0.0
# Расход сам проверяет свои равенства (Σ колонок == Список, Штат == Список +
# Вне списка + Вакансии), но срабатывают они только на придуманных примерах.
# hypothesis генерирует миры — подразделения, вакансии, приданных, статусы со
# случайными интервалами — и ищет тот, на котором равенство разойдётся.
hypothesis>=6.100.0
