// «Настройки» из сайдбара вели в никуда (404): своей страницы у хоста не
// было. Раздел живёт на модуле настроек политик поверх живого
// /api/ops/settings/ и рендерится ЗДЕСЬ, на своём адресе, — реализация
// одна, у страницы два входа (второй — /security-ops/settings в разделе ОМ),
// тот же приём, что у /feedback.
export { default } from "@/app/security-ops/settings/page";
