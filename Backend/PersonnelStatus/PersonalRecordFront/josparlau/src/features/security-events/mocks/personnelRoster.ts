// Синтетические ДАННЫЕ read-only кадрового snapshot (тип — model/types.ts).
// Feature-owned: единственный потребитель сейчас — security-events; при
// появлении второго потребителя вынести в shared (не раньше). Доступ из UI —
// ТОЛЬКО через usePersonnelRoster() (api/queries.ts), а не прямым импортом
// этого файла из pages/ (страница не должна знать, что источник — mock).
import type { PersonnelSummarySnapshot } from '../model/types'

/**
 * ⚠️ ИДЕНТИФИКАТОР ЧЕЛОВЕКА ОБЩИЙ, ПОКА ЭТО ОДИН ЧЕЛОВЕК (§19.24).
 *
 * Рейтинг при расстановке связывается с сотрудником по `employeeId`, и до
 * §19.24 связывать было НЕЧЕМ: этот снимок жил в собственном пространстве
 * `emp-*`, а оцениваемый набор — в `employee-*`. Совпадение подписей («Ерланов
 * Д.» есть в обоих) при разных идентификаторах означало бы, что сводка
 * рейтинга просто не находится ни для кого, и мягкое предупреждение §19.24
 * было бы недостижимо ни на одном экране.
 *
 * Поэтому те, кого demo-снапшот знает как ОДНОГО человека, носят один
 * идентификатор; остальные оставлены в своём пространстве — они в оцениваемый
 * набор не входят, и отсутствие рейтинга у них ЧЕСТНОЕ, а не следствие
 * несостыковки ключей. Оба исхода нужны демонстрации: §19.24 требует показать
 * и соответствие, и состояние «данных нет».
 */
export const PERSONNEL_ROSTER: readonly PersonnelSummarySnapshot[] = [
  { id: 'emp-1', name: 'Ахметов Б.', rankLabel: 'Ст. сержант', unit: 'Группа досмотра' },
  { id: 'emp-2', name: 'Бекова А.', rankLabel: 'Сержант', unit: 'Группа досмотра' },
  { id: 'employee-1', name: 'Ерланов Д.', rankLabel: 'Капитан', unit: 'Управление ОМ' },
  { id: 'emp-4', name: 'Жаксыбеков Т.', rankLabel: 'Прапорщик', unit: 'Физическая охрана' },
  { id: 'emp-5', name: 'Каримова С.', rankLabel: 'Сержант', unit: 'Физическая охрана' },
  { id: 'emp-6', name: 'Нуртаев Е.', rankLabel: 'Майор', unit: 'Управление ОМ' },
  { id: 'employee-7', name: 'Оспанов Р.', rankLabel: 'Ст. сержант', unit: 'Резерв' },
  { id: 'emp-8', name: 'Тулегенова Ж.', rankLabel: 'Сержант', unit: 'Резерв' },
  // Двое добавлены ради §19.24: без них ни «рейтинг ниже требования поста»
  // (у «Абишева Н.» последний закрытый период даёт 7,9), ни «агрегата за
  // период нет» (у «Сейтказы М.» точка есть, а значения в ней нет) не
  // встретились бы в живом сценарии — оба состояния проверялись бы только
  // подставленным ответом.
  { id: 'employee-2', name: 'Абишев Н.', rankLabel: 'Ст. лейтенант', unit: 'Управление ОМ' },
  { id: 'employee-3', name: 'Сейтказы М.', rankLabel: 'Сержант', unit: 'Группа досмотра' },
]

export function findPersonnel(id: string): PersonnelSummarySnapshot | undefined {
  return PERSONNEL_ROSTER.find((p) => p.id === id)
}
