---
title: Frontend — Changelog
module: frontend
updated: 2026-08-20
tags: [frontend, changelog]
---

# Frontend — Changelog

_Одна строка на коммит: дата, короткий хэш, сообщение. Сгенерировано скриптом из git log, дополняется вручную по ходу будущей работы._

## История (git log)

- 2026-07-04 `6392562b` feat(E8): 8.1 scaffold Vite react-ts — каркас фронтенда с контурным гейтом
- 2026-07-06 `75519a00` fix(E8): 8.1 code-review проход 1 — даунгрейд до спеки + ужесточение гейта
- 2026-07-07 `026f6044` feat(E8): 8.2 канон-линтеры — boundaries/баны/deps-gate/самотест + code-review проход 1
- 2026-07-07 `4d9ad410` feat(story-8.3): Кодоген типов из схемы
- 2026-07-07 `6d747804` feat(story-8.4): apiClient и DomainError-парсинг
- 2026-07-07 `1ef7009d` feat(story-8.5): useApiMutation и ConflictDialog
- 2026-07-07 `dcf5ec42` feat(story-8.6): Auth-подключение
- 2026-07-07 `fd136f41` feat(story-8.7): Роутер, routes.ts и layout
- 2026-07-07 `454aacbc` feat(story-8.8): Печатный каркас
- 2026-07-07 `0938c430` chore(E8): ретроспектива эпика 8 + doc-синхронизация
- 2026-07-08 `9ad9d62c` feat(story-6.1): App documents и Attachment
- 2026-07-09 `4e0172e3` feat(story-9.2): чистая state machine клавиатурной грамматики грида
- 2026-07-09 `0cc64ad5` feat(story-9.3): property-based свойства грамматики (fast-check)
- 2026-07-09 `bafab604` feat(story-9.4): грид-компонент (TanStack Virtual + грамматика 9.2)
- 2026-07-09 `0b586161` feat(story-9.5): фокус-слой грида (RTL+userEvent + ConflictDialog focus-return)
- 2026-07-09 `3a1a5c10` feat(story-9.6): валидация в гриде (zod + конфликт-маркеры soft/hard)
- 2026-07-09 `6ba10217` feat(story-9.7): prefill «вчера» + отправка только дельт (bulk-3.8)
- 2026-07-09 `7530b19d` feat(story-9.8): перф-смоук грида — блокирующие детерминированные счётчики
- 2026-07-13 `148833a0` fix(story-6.10a): правки cross-model ревью — реальный горизонт данных вместо вакуумной пробы
- 2026-07-14 `a56608d4` fix(story-6.10b): правки cross-model ревью — блок по живым laggards, 409 на дубль override, горизонт 31д
- 2026-07-14 `d13fed85` fix(story-9.2): правки cross-model ревью — guard-тест сверяет с ожидаемой таблицей §3.2, самоисцеление входа
- 2026-07-14 `f4b653be` fix(story-9.4): правки cross-model ревью — type-ahead seed живой, самоисцеление rows, фокус не падает при скролле
- 2026-07-14 `6d8f9c59` fix(story-9.3): правки cross-model ревью — инвариант «нажатия не теряются» перестал быть вакуумным
- 2026-07-14 `d2aec070` fix(story-9.5): правки cross-model ревью — клик-синк фокуса, оверрайд только с причиной, позиционные ассерты
- 2026-07-14 `1d858e5e` fix(story-9.6): правки cross-model ревью — блокируются данные, а не только фокус
- 2026-07-14 `30de448e` fix(story-9.7): правки cross-model ревью — bulk-shape выровнен с живым 3.8
- 2026-07-14 `8535e275` fix(story-9.8): правки cross-model ревью — перф-смоук перестал быть вакуумным
- 2026-07-14 `f8f10873` feat(story-9.9): e2e слепого ввода — реальный браузер против прод-харнеса
- 2026-07-14 `ea748c8c` fix(story-9.9): правки cross-model ревью — вакуумные ассерты закрыты
- 2026-07-15 `f0868806` feat(story-10.1a): REST bulk-роут статусов — backfill AI-4 (bulk-POST + regen схемы)
- 2026-07-19 `d7bb85ed` feat(story-10.2): экран массового обновления
- 2026-07-19 `9e013d2e` feat(story-10.3): экран сдачи дня
- 2026-07-19 `a56ad92b` feat(story-10.3a): роут каскадного дерева светофора
- 2026-07-19 `1353291c` feat(story-11.3): WS-клиент с reconnect, дочиткой ?since= и индикатором связи
- 2026-07-19 `15b9268f` feat(story-10.4): светофор-дерево
- 2026-07-19 `8573b329` feat(story-11.4): центр уведомлений — колокольчик, панель и WS-обновление кэша
- 2026-07-19 `ad59582a` feat(story-10.5): экран расхода
- 2026-07-19 `53b30a4e` feat(story-10.6): amendment-флоу UI
- 2026-07-19 `c5a6ee44` feat(story-11.6): e2e-уведомления — живой стек с браузером
- 2026-07-19 `84515f6c` feat(story-10.7): печатная форма расхода
- 2026-07-19 `a5cfcb38` feat(story-10.8): личный экспорт оператора («щит»)
- 2026-07-20 `c18d992e` feat(story-10.9): журнал «сообщено → исправлено» (каркас)
- 2026-07-20 `21617863` feat(story-10.10): e2e сдачи целиком — живая цепочка через все экраны
- 2026-07-24 `3891e7ba` feat(smart-josparlau): frontend-only mock-driven implementation, Этапы 0-7
- 2026-07-24 `22715ed6` test(smart-josparlau): add Playwright E2E for mock-mode build
- 2026-07-24 `eef4e2cb` feat(smart-josparlau): add Service Analytics screen (§22)
- 2026-07-24 `4a5a6bcf` test(smart-josparlau): extend E2E to Approval→Closure, fix real a11y bug
- 2026-07-24 `832d19fc` feat(smart-josparlau): add Duty Plan screen (§21/§24)
- 2026-07-24 `01491212` feat(smart-josparlau): add Dictionaries screen (§30)
- 2026-07-24 `a0f1a394` feat(smart-josparlau): add unified shift calendar (§25, day view)
- 2026-07-24 `b4ef1c15` feat(smart-josparlau): add remaining §30 dictionaries (journal entry types, post requirement groups)
- 2026-07-24 `92430180` test(smart-josparlau): extend e2e-mock to personnel/objects/dictionaries/calendar/placement
- 2026-07-24 `a24cd27c` test(smart-josparlau): close the last e2e-mock gap — RECON→DEMAND→FORCES→PLACEMENT
- 2026-07-24 `3f3fb1c5` test(smart-josparlau): e2e-mock coverage for audit, analytics, duties
- 2026-07-24 `b4f26ea8` feat(smart-josparlau): operational profile tab structure (§20.5)
- 2026-07-24 `59e4f58a` feat(smart-josparlau): combat duty groups on Routes (§24.5-24.10)
- 2026-07-24 `642aa70b` feat(smart-josparlau): combat duty execution lifecycle (§24.19-24.23)
- 2026-07-24 `82b08550` feat(smart-josparlau): combat duty roster replacement (§24.21)
- 2026-07-24 `8003d3f3` style(smart-josparlau): readiness progress bar on Command Center (per-stage fill)
- 2026-07-24 `7db8fffd` feat(smart-josparlau): form combat duty requirement for a period (§24.1)
- 2026-07-24 `b6f0f673` feat(smart-josparlau): combat duty groups on the day calendar (§25.12)
- 2026-07-24 `15f528b6` feat(smart-josparlau): duty-shift breakdown on Service Analytics (§22)
- 2026-07-24 `475eb177` feat(smart-josparlau): combat-group breakdown on Service Analytics (§22/§24)
- 2026-07-25 `447b7515` fix(smart-josparlau): accessibility — unlabeled inputs and icon-only button
- 2026-07-25 `1209af55` feat(smart-josparlau): combat duty shift handover checkpoint (§24.22)
- 2026-07-25 `d4ea1354` feat(smart-josparlau): cascading replacement of departed staff (§9.11)
- 2026-07-25 `1633d9bc` feat(smart-josparlau): keyboard navigation for operational-profile tabs
- 2026-07-25 `7870b6db` feat(smart-josparlau): axe-core accessibility audit across all screens
- 2026-07-25 `fe088a33` fix(ui): darken default Button hover to meet WCAG AA contrast
- 2026-07-25 `6a3a23c2` fix(smart-josparlau): close moderate/minor axe findings, fix real contrast bug
- 2026-07-25 `abacc30a` feat(a11y): add skip-to-content link to AppLayout (WCAG 2.4.1)
- 2026-07-27 `7a2ce30a` feat(smart-josparlau): placement print form (§9.15) as marked demo preview
- 2026-07-27 `40141290` feat(smart-josparlau): read-only case archive of a closed security event
- 2026-07-27 `3c90bf00` feat(smart-josparlau): immutable passport versions with deep-linked read-only view
- 2026-07-27 `f26da1ee` feat(smart-josparlau): bind security events to a published passport version (§9.6)
- 2026-07-27 `e4b56442` feat(smart-josparlau): bind duty shifts to a published passport version (§9.6)
- 2026-07-27 `d7520ea2` feat(smart-josparlau): monthly duty plan with server-side KPI and conflicts (§21.27-21.30)
- 2026-07-27 `d0a45704` feat(smart-josparlau): create individual duty shifts from the UI (§21.31/§21.33/§21.34)
- 2026-07-27 `90bf1ff0` feat(smart-josparlau): duty shift card with server-side conflicts and named gaps (§21.32)
- 2026-07-27 `87bc906f` feat(smart-josparlau): edit and cancel a planned duty shift (§21.31)
- 2026-07-27 `a393d0ca` feat(smart-josparlau): employee availability matrix with named missing layers (§21.30)
- 2026-07-28 `9d4538de` feat(smart-josparlau): duty shift list and history with named missing columns (§21.30)
- 2026-07-28 `1c3e60b6` feat(smart-josparlau): objects registry KPI with policy-driven passport freshness (§21.7)
- 2026-07-28 `5bb22e34` feat(smart-josparlau): monthly plan lifecycle with server-side action policy (§21.27-21.28)
- 2026-07-28 `ab6b24ae` feat(smart-josparlau): server-side safe projection and sensitive identity disclosure (§20.27-20.33)
- 2026-07-28 `05a0d480` feat(smart-josparlau): service report registry with async jobs and server-side masking (§22.18-22.25)
- 2026-07-28 `328ec0fd` feat(smart-josparlau): report history with artifact revisions and permission-scoped visibility (§22.25)
- 2026-07-28 `82015f21` feat(smart-josparlau): report job card with server-side redaction of foreign parameters (§22.26-22.28)
- 2026-07-28 `98a3155d` feat(smart-josparlau): server-owned analytics snapshot and stable-ID drill-down (§22.3-22.12)
- 2026-07-28 `37e4fef3` feat(smart-josparlau): server-owned attention detectors with own policy (§22.11)
- 2026-07-28 `3c5b4633` feat(smart-josparlau): operations analytics hierarchy by stable IDs (§22.13/§22.15)
- 2026-07-28 `d7797d2d` feat(smart-josparlau): append-only transition journal and §22.14 funnel
- 2026-07-28 `e420ef42` feat(smart-josparlau): feedback registry with server-owned confidentiality (§28)
- 2026-07-28 `40ac55e0` feat(smart-josparlau): feedback card with diff-written timeline (§28 detail)
- 2026-07-28 `e1a57169` chore(frontend): allow ngrok-free.dev hosts in vite dev server
- 2026-07-28 `c2625f2d` feat: add Sentry error monitoring (backend + frontend)
- 2026-07-28 `ffa19f5d` feat(smart-josparlau): settings section owns the attention policy (§29)
- 2026-07-28 `00c4f2e7` Merge origin/main (Sentry, ngrok hosts, docker restart) into main
- 2026-07-28 `ef2a4655` feat(smart-josparlau): conflict rules own the rest-after-duty mode (§29/§21.35)
- 2026-07-28 `ce4a9107` feat(smart-josparlau): settings own the passport freshness policy (§29/§21.7)
- 2026-07-29 `faeb0af1` feat(smart-josparlau): dictionary usage becomes a live server-side count (§30)
- 2026-07-29 `4dd580e3` feat(smart-josparlau): пределы периодов и хранения переезжают в «Настройки» (§22.5/§29)
- 2026-07-29 `a6025d3a` feat(smart-josparlau): оперативный рейтинг — read model и сводка агрегатов (§19)
- 2026-07-29 `0a98ee9f` feat(smart-josparlau): динамика оперативного рейтинга — ряд закрытых периодов и граница смены методики (§19.20)
- 2026-07-29 `9d481a84` feat(smart-josparlau): аналитика рейтинга — отчёт с подавлением малых групп (§22.16-22.17)
- 2026-07-31 `0f385b11` feat(smart-josparlau): рабочее пространство оценивания — задания, форма, отправка (§19.7-19.10, §19.14)
- 2026-07-31 `7d29faf6` feat(smart-josparlau): исправление оценки — новая запись, цепочка и diff (§19.17-19.18)
- 2026-07-31 `accb0858` feat(smart-josparlau): реестр итоговых оценок и карточка агрегата (§19.15-19.17)
- 2026-07-31 `5af40845` feat(smart-josparlau): конфликты редакций и идемпотентность отправки (§19.25-19.26)
- 2026-07-31 `c6b51a25` feat(smart-josparlau): журнал оценивания — события, отказы и ссылки (§19.27)
- 2026-07-31 `50616da7` feat(smart-josparlau): уведомления оценивания — код вместо текста, deep link с перепроверкой прав (§19.28)
- 2026-07-31 `52089489` feat(smart-josparlau): экспорт рейтинга — агрегированная выгрузка, состояния и отказ sensitive (§19.29)
- 2026-07-31 `649da3c0` feat(smart-josparlau): рейтинг при расстановке — min_rating поста, soft warning с override и краткая сводка (§19.24)
- 2026-07-31 `bb212b33` feat(smart-josparlau): канонические empty states оценивания §19.31 — дословные формулировки без потери причины (§19.30)
- 2026-07-31 `5291155a` feat(smart-josparlau): замок закрытого мероприятия для поздней оценки (§19.23)
- 2026-07-31 `cd10db88` feat(smart-josparlau): гард фактического участия — ревизия перечня §19.33
- 2026-07-31 `f3d965c2` test(smart-josparlau): экраны оценивания в axe-аудите + heading-order сводки (§19.32)
- 2026-08-01 `c0145c1a` feat(smart-josparlau): устойчивый ID сотрудника и подразделения на смене дежурства (§22.9)
- 2026-08-01 `8363320a` feat(smart-josparlau): аналитика нагрузки — раздел LOAD_POLICY и серверный LoadMetric (§22.9)
- 2026-08-01 `d49f9708` fix(smart-josparlau): разделы «Настроек» выводятся из SECTION_TITLE — LOAD_POLICY был молча спрятан списком
- 2026-08-01 `74a4dad8` fix(smart-josparlau): живое первое впечатление — донор-дефолты demo-режима и посадочная «/» (Этап 71)
- 2026-08-01 `3efff53b` fix(smart-josparlau): переход Бюллетень→Рекогносцировка + сквозная e2e полного цикла ОМ (Этап 72)
- 2026-08-01 `487e1931` refactor(smart-josparlau): фиксы код-ревью — 15 замечаний пяти углов (Этап 73)
- 2026-08-02 `6dcf2072` feat(host-перенос): эквивалентность доказана — 125/125 общих e2e против PersonalRecordFront (Этап M2)
- 2026-08-02 `3603748f` chore(host-перенос): npm run test:e2e:host
- 2026-08-03 `f2acd119` feat(host-перенос): Этап M4 — SPA в каркасе хоста + sidebar-секции ОМ (/ops и натив)
- 2026-08-07 `2bd2c70c` fix: три дефекта, найденные подъёмом стенда и чтением логов обеих сторон
- 2026-08-08 `c0666e28` fix(expense): печатная форма — та же вкладка вместо target=_blank (implicit noopener)
- 2026-08-10 `7b9ccaa7` chore(front): демонтаж копии josparlau и host-e2e — SPA-встройка выведена целиком
- 2026-08-12 `1a8c34e7` test(e2e): смоук-обход портала по живому стенду — 39 маршрутов × 3 персоны
- 2026-08-12 `c3fdc293` chore: вывести vite-SPA frontend/ и бэк Backend/VAPS — работаем на старом стеке
- 2026-08-20 `—` chore(прототип): восстановлен `Прототип/` в главном чекауте — 13 файлов были удалены из рабочего дерева, но не проиндексированы и не закоммичены, так что истории удаление не коснулось (хэша нет: `git checkout` вернул дерево к HEAD, коммитить было нечего). Заодно из устаревшей копии `Smart Josparlau (Прототип HTML)/` вычищены проверенные по md5 дубли — 6,2 → 2,8 МБ; `screenshots/` и `uploads/` (11 HTML-заготовок этапов, два `.docx` заказчика) оставлены как ЕДИНСТВЕННЫЙ локальный носитель этих исходников
- 2026-08-22 `57f86b77` fix(traffic-light): один разбор сдачи дня на три экрана — `classifyLeaf`/`countSubmissions`/`buildDivisionForest` вынесены в `entities/daily-grid` (слово «Сдано» значило три разных числа на карточке командного центра, в аналитике и на борде); листья считаются по лесу, а не наивным `!parents.has(id)`; «+N сдавших» → «+N подразделений» (под зелёной веткой каскад допускает NEUTRAL-потомков — строка утверждала неправду)
- 2026-08-22 `c8879533` fix(daily-expense): гейт `status.view` у «Ежедневного расхода» (403 печатался как «расход не ответил» — вина сервера вместо права), перечитка состояния после 409 «день уже сдан» (панель инвалидировала ключ без читателей, board-side вариант проверен и оказался пустышкой — восстановленный `day-submission-panel.tsx` БОЛЬШЕ НЕ байт-в-байт равен `4d83f361^`), честная подпись о сером цвете пилюль
- 2026-08-22 `f7962258` fix(ops-ui): заголовок «Мероприятия с участием» не пропадает в загрузке/отказе, подписи «бэк-этапом» приведены в один голос, докстринг `statusCode` в `status-table` — к коду
- 2026-08-22 `737c460c` fix(layout): атрибуты расширений браузера в `<body>` (`data-gptw`, Grammarly, ColorZilla) больше не поднимают шум о гидратации — `suppressHydrationWarning` на `<body>`. Диагноз доказан A/B: сервер отдаёт голый `<body>` (curl), в исходниках `gptw` нет, headless-профиль молчит, а врезка атрибута до гидратации даёт ровно ту же ошибку. Щит действует на ОДИН уровень — расхождения внутри дерева (свёрнутый сайдбар, `4ea89bc2`) по-прежнему видны: проверено врезкой того же атрибута на вложенный `<div>`. Сторож в `e2e/hydration.spec.ts` 4/4, красная проба со снятым атрибутом красная
- 2026-08-22 feat(nav): навигация разделена на шесть НЕРАСКРЫВАЕМЫХ категорий (решение Bratan) — «Личный кабинет», «Ежедневный расход», «Объекты», «Охранные мероприятия», «Администрирование», «Обратная связь». Портальный плоский список и группы раздела ОМ («Оперативная работа», «Оценка и отчётность») расформированы и слиты в один список; аккордеон (`aria-expanded` + `ChevronDown` + `openGroups`) убран целиком. **«Аналитика ОМ» (`/security-ops/analytics/operations`) впервые попала в меню** — экран жил с 17.08.2026 и открывался только по ссылкам. Убраны ИЗ МЕНЮ (код и адреса живы, входящие ссылки не тронуты): «Структура организации» `/organization`, «Настройки» `/settings`, «Оперативный рейтинг» `/security-ops/ratings`; дубль «Обратная связь ОМ» схлопнут в один пункт (у экрана два входа на одну реализацию). Надзаголовки 17 страниц (`data-slot="page-eyebrow"`) переименованы под новые категории — имя категории на странице и в меню это одна сущность, расходиться им нельзя; надзаголовки выведенных из меню экранов оставлены прежними намеренно. Подсветка «вы здесь» переписана на САМЫЙ ДЛИННЫЙ подошедший адрес: наивный `startsWith` зажигал «Аналитику службы» заодно с вложенной «Аналитикой ОМ». Задержка stagger-анимации обрезана восемью шагами — на 19 развёрнутых пунктах нижние приезжали через 1.3 с. Сторож: устаревшая проба плотности аккордеона снята (контрола больше нет), вместо неё две новые в `e2e/prototype-skin.spec.ts` — «категории нераскрываемые: все пункты видны сразу» (пин порядка и состава, ноль `aria-expanded`, ненулевая высота каждого списка, 19 ссылок) и «подсвечен один пункт — самый длинный подошедший адрес»; обе прошли красную пробу (сортировка по возрастанию → горит «Аналитика службы» вместо «Аналитики ОМ»; свёрнутый список → «список свёрнут»). prototype-skin 58/58, плюс 70/71 по 14 живым спекам (единственное падение — `object-passport › баннер неготовности`, пред-существующее: A/B с откаченным надзаголовком даёт тот же отказ фикстуры «нет готового объекта»), tsc чист. **Цена решения замерена:** меню 982px, при 1366×768 семь пунктов из 19 уходят под сгиб (первым — «Аналитика ОМ»), при 1600×900 — пять.
- 2026-08-22 fix(nav): строка меню уплотнена — все 19 пунктов и 6 заголовков влезают в 1366×768 (было: семь под сгибом). Резался ВОЗДУХ, не кегль: `py-2`→`py-[3px]`, добавлен `leading-4`, иконка 20→16px (при 13px тексте она одна задавала высоту строки), `space-y-1`→`space-y-0.5`, отступ категорий `mt-6`→`mt-2`, поле заголовка `mb-1.5`→`mb-0.5`. Меню 982→586px, запас при 1366×768 — 55px, всё видно вплоть до вьюпорта 720px. **Пол достигнут:** пункт 22px + зазор 2px = шаг ровно 24px, граница исключения по интервалу WCAG 2.2 AA (2.5.8 Target Size) — дальше жать строку нельзя, остаток воздуха брался на неинтерактивной обвязке. Сторож «меню целиком влезает в 1366×768 и не жмётся ниже шага 24px» пинит обе половины (содержимое меряется низом ПОСЛЕДНЕЙ ссылки, а не `scrollHeight`: у невыходящего блока он равен `clientHeight` и ассерт был бы вакуумным; шаг берётся у соседей ВНУТРИ категории, иначе в него попадал бы заголовок). Красные пробы: возврат `py-2` → 5 пунктов под сгибом; снятие `space-y-0.5` → шаг 22px. prototype-skin 59/59, tsc чист. **Остаток:** на физическом ноутбуке 1366×768 (вьюпорт ~648px после хрома браузера) два пункта по-прежнему под сгибом — упирается в арифметику пола, лечится только сжатием шапки/карточки роли или возвратом раскрытия.
- 2026-08-22 `51da7f47` fix(nav): все 19 пунктов видны на РЕАЛЬНОМ ноутбуке 1366×768 — сторож влезания мерил 768px, то есть высоту ЭКРАНА, а не вьюпорта (после хрома браузера ≈648px), и был зелёным, пока два нижних пункта на живом ноутбуке оставались под сгибом (замер: 586px содержимого против 521px доступных). Резалась только НЕинтерактивная обвязка — строка меню стоит ровно на полу WCAG 2.2 AA 2.5.8 (шаг 24px), кегль прототипа 13px не тронут: шапка-бренд 64→48px (плитка 36→32), карточка роли из двух этажей в одну строку 55→31px (описание и отдел остались в `title`), поля навигации/категорий `mt-2`→`mt-1`, поле заголовка категории снято в пользу `leading-[12px]`. Итог 536px против 565px — запас 29px, переживает открытую панель закладок. Сторож переведён на вьюпорт 1366×648 и получил второй ассерт «запас ≥24px» (без него «влезает» держалось бы на волоске). Красные пробы: откат обвязки → два пункта под сгибом; `py-4` карточке роли → запас 9px. prototype-skin 59/59, tsc чист
- 2026-08-22 `ba332839` fix(nav): меню вернулось к ЧИТАЕМОЙ плотности, курс на влезание отменён — погоня за «19 пунктов без прокрутки» довела строку до пола WCAG 2.2 AA 2.5.8 (кегль 13px, пункт 22px, шаг 24px), а 24px это граница СООТВЕТСТВИЯ, а не цель проектирования: заказчик прочитал результат как «мелкое и некрасивое», и прочтение верное. Новое правило — размер строки НЕ разменивается на число видимых пунктов. Пункт 36px, кегль 14px, шаг 40px; кегли сведены на одну шкалу 12/14/16 (россыпь 10/10.5/12/12.5/13/14 с ручными leading — это «random font sizes» из базы ui-ux-pro-max); шапка-бренд назад на 64px, заголовки категорий 10→12px, карточка роли снова двухэтажная. Прокрутка стала штатной и получила признаки: градиент снизу, градиент сверху (список упирался в шапку и резал заголовок категории пополам — обрывок читался как брак вёрстки) и подводка активного пункта. **Подводка вскрыла свой дефект:** карточка роли рисуется только когда доехал `user`, меню становится ниже на ~57px, и разовая подводка промахивалась ровно на неё (замер: активный пункт на 49px ниже края) — теперь повторяется по ResizeObserver, держит зазор больше высоты градиента, право на прокрутку снимается после первого ручного скролла. Заодно добавлен `prefers-reduced-motion` для stagger-выезда (его не было вовсе). Сторож «влезает в 1366×648» снят вместе с требованием, вместо него три: читаемость (кегль ≥14, строка ≥36, шаг ≥40, заголовок ≥12), достижимость нижних пунктов с обеими подсказками, подводка активного. 🔴 Первая версия последнего была ВАКУУМНОЙ — `toBeInViewport` меряет пересечение с ОКНОМ и слеп к обрезке прокручиваемым предком, он был зелёным на том самом промахе в 49px; переписан на замер по коробке меню. Красные пробы: откат строки → «кегль 13px»; глушение каждой подсказки → своя ошибка; отключение подводки → «меню не прокручивалось»; BOTTOM_INSET=0 → «на 32px заехал под градиент». prototype-skin 61/61, hydration 4/4, tsc чист
- 2026-08-22 `0f98f309` feat(profile): «Мой профиль» приведён к раскладке прототипа Smart Жоспарлау — расхождение было каркасным, не косметическим (снято скриншотом живого стенда и сверено с эталоном заказчика). Шапка получила ПРАВУЮ половину прототипа: кольцо рейтинга и полосу готовности к службе. Обе величины система не считает (рейтинг ведёт своё пространство идентификаторов, правила процента готовности нет ни в справочниках, ни в настройках), и внутри форм стоит прочерк с причиной — решение заказчика «форма прототипа + честная пустота»: выдуманная оценка живого человека хуже пустого места. Вкладка мероприятий разложена на две колонки (2fr/1fr), справа — «Закреплённое имущество» с той же причиной, которой блок помечает сам прототип («после введения цифрового склада»). Карточка назначения переверстана по эталону: код ОМ пилюлей, пост крупным кеглем, врезка «Краткая инструкция» из ЗАДАЧИ поста расчёта (`ReconSectorPost.task` — своего документа-инструкции в модели нет, но задача это то, что человеку велено делать) и вход «Инструкция по посту» на карточку ОМ. Добавлена четвёртая вкладка прототипа «Инструкции» — задачи и требования по моим постам, данные РЕАЛЬНЫЕ. Список «чего в профиле нет» ужат с 8 до 4 пунктов: блоки, занявшие своё место в раскладке, несут причину там, где их ждут. 🔴 Яма shadcn-v4: `CardHeader` это `grid`, и класс `flex-row` без `flex` дисплей не меняет — бейджи-счётчики уехали на отдельную строку под заголовок (видно на промежуточном скриншоте). Проба `my-profile` обновлена вместе с подписью счётчика, ассерт ТОЧНЫЙ (`^N назначений$`) — подстрока прошла бы и на 129. my-profile 3/3 живые, tsc чист (eslint в чекауте не установлен — гейт линта не прогонялся). **Остаток:** сайдбар и топбар всё ещё портальные («Проект Расход», иконки, состав меню), а эталон несёт бренд «Smart Жоспарлау · Силы и мероприятия», селект роли, фильтр подразделения и подвал «Закрытый контур» — это ОБЩИЙ каркас всех страниц, отдельная задача
- 2026-08-23 `805c04a4` feat(shell): каркас приложения приведён к прототипу — бренд, подвал сайдбара, топбар. Менялся ВИД; состав и имена категорий остались вчерашние (решение заказчика от 22.08), надзаголовки 17 страниц не тронуты — заказчик выбрал «только вид» из двух предложенных прочтений. Бренд «SЖ / Smart Жоспарлау / Силы и мероприятия» в сайдбаре, мобильном меню и `<title>`; крошки начинаются с бренда. Появился бейдж-счётчик у пункта меню: число — `count` СЕРВЕРА при `page_size=1` (строки не нужны, тащить ради бейджа страницу мероприятий на каждом экране расточительно), без права `event.view` бейдж гаснет целиком — пустое место честнее нуля, который читался бы как «мероприятий нет». Подвал: «Закрытый контур · связь есть» и карточка человека со входом в профиль; точка индикатора НЕ константа — читает состояние самого свежего запроса приложения (react-query cache) и офлайн-признак браузера, иначе «связь есть» горело бы ровно тогда, когда её нет; пульс гаснет под `prefers-reduced-motion`. Топбар: роль пилюлей (в прототипе это СЕЛЕКТ — там демостенд, где ролью переключают показ; здесь роль назначает сервер, и меняющий ничего список был бы мёртвым контролом), бейдж уведомлений красный и с «99+». 🔴 Сторож состава меню считал ссылки по всему `<aside>` и поймал карточку подвала (20 против 19) — переведён на `<nav>`, добавлен ассерт статуса связи, красная проба со снятым `data-slot` красная. 🔴 Вторая яма: `app/layout.tsx` был в CRLF, и скриптовая правка схлопнула его целиком (55/55 строк в диффе) — восстановлено до 2/2 ещё до пуша. prototype-skin 61/61, my-profile 3/3, hydration 4/4, tsc чист
- 2026-08-23 `8bd3af47` feat(profile): «Мой профиль» доведён до эталона по РАЗМЕТКЕ прототипа (handoff, строки 1980–2075), а не на глаз. Карточка назначения переложена на сетку эталона `60px | 1fr | auto`: бейдж состояния и кнопка «Инструкция по посту» стоят в своём столбце (бейдж вверху, кнопка внизу) — в первой версии переноса бейдж жил в строке заголовка, и правый край карточки разъезжался тем сильнее, чем длиннее название мероприятия. Правая колонка стала ТРЕМЯ карточками, как в прототипе: «Закреплённое имущество», «Допуски и подготовка», «Требует внимания». Последняя — единственная из трёх, у которой источник ЕСТЬ: назначения без подтверждённого ознакомления это и есть личные действия со сроком (срок = дата мероприятия, закрытые ОМ не входят — подтверждать задним числом нечем). «Допуски и подготовка» стоит пустой с причиной: квалификаций и их сроков модель не хранит, а перечислить «Огневую подготовку до 30.09» значило бы выдать человеку допуск, которого система не выдавала. Вкладка «Инструкции» получила верхнюю карточку эталона «Инструкции перед выходом на пост» на РЕАЛЬНОМ реестре нормативки (`useLegalDocuments`: приказы, регламенты, инструкции; законы отфильтрованы — право описывают, а не инструктируют), ниже осталась своя карточка задач и требований по постам. Сторож счётчика «Требует внимания» сверяется с ответом сервера, а не сам с собой; красная проба (снять фильтр по `acknowledgedAt`) даёт 29 вместо 3. my-profile 3/3, prototype-skin 61/61, tsc чист. 🔴 По дороге умер `next dev` (ERR_EMPTY_RESPONSE на `/dictionaries/`, порт перестал слушаться) — перезапущен, сюита зелёная
- 2026-08-23 `e6494aa8` feat(events): реестр ОМ приведён к прототипу (сверено с разметкой handoff, строки 262–340). Колонки: ОМ | Даты | Локация | Этап и готовность | Потребность | Ответственный. «Этап и готовность» слиты в одну колонку — это один ответ на вопрос «где мероприятие сейчас»; колонка «Конфликты» убрана, но сигнал сохранён красным бейджем рядом со стадией И ТОЛЬКО когда конфликты есть (столбец нулей был шумом во всю таблицу). «Даты» и «Локация» получили вторую строку эталона: продолжительность и версию паспорта (в прототипе там время смены и подпись объекта — времени у ОМ нет, продолжительность отвечает на тот же вопрос). Появился ВТОРОЙ РЕЖИМ экрана — «Календарь мероприятий»: месяц с отметками и список выбранного дня, режим живёт в URL рядом с фильтрами. Якорь месяца — текущий, если в отборе есть его мероприятия, иначе ближайший вперёд: первая версия брала самое раннее и открывала июль, когда всё живое в августе. Кнопка названа «+ Создать бюллетень» по эталону. Колонок «Охраняемое лицо» и «Старший» нет — сноской объяснено почему (лица ведутся отдельным каталогом и с ОМ не связаны, старшего модель не хранит); пустые столбцы были бы шумом в каждой строке. 🔴 Сторож колонок написан по разметке (`thead th`), а не по роли: Playwright читает эти `<th>` как `cell`, и `getByRole('columnheader')` не находит ничего — ассерт был бы вечнозелёным на пустом массиве. 🔴 Ассерт счётчика дня в первой версии был ВАКУУМНЫМ (искал число подстрокой во всей карточке; красная проба с константой 999 осталась зелёной — «1» нашлась в коде ОМ и в дате) — переписан на сам бейдж `data-slot="events-day-count"` + `toHaveText`, красная проба красная. events-registry 5/5, prototype-skin 61/61
- 2026-08-23 `e143b916` feat(events): этапы карточки ОМ — заголовок этапа, шапка расчёта постов, человеческие даты. У карточки появился ЗАГОЛОВОК ЭТАПА прототипа («Этап N из 6» + название + строка о том, что на этапе делают): до этого экран не называл этап вовсе, форму приходилось опознавать по содержимому. Номер берётся от ШАГА, а не от стадии — три стадии модели (DEMAND/FORCES/PLACEMENT) это один шаг эталона, и нумерация подряд обещала бы девять шагов там, где степпер показывает шесть. У расчёта постов рекогносцировки появилась шапка колонок эталона (Сектор | Пост | Задача | Кол-во | Требования | Рейтинг | Тип | Вооружение | Форма одежды | Примечание): до неё подписи жили только в `aria-label`, и глазом «Мин. рейтинг» от «Требований» не отличался — плейсхолдер исчезает, как только в поле что-то введено. Метки времени и даты перестали печататься сырым ISO (`2026-08-17T12:00:27.039319+00:00` в маршруте согласования, журнале штаба, архиве, отметках ознакомления) — добавлен `formatIsoDateTime` в `shared/lib/date` (разбор часового пояса здесь НУЖЕН: сервер шлёт момент, а не дату). 🔴 Индикатор связи в подвале сайдбара переписан на `useSyncExternalStore`: подписка на кэш react-query дёргала `setState` во время рендера ДРУГОГО компонента (панель закрытия), React ругался, и e2e закрытия ловил это как ошибку консоли. 🔴 Проба бюллетеня искала свою фикстуру на ПЕРВОЙ СТРАНИЦЕ реестра — тот перевалил за `page_size=50`, и только что созданное ОМ в неё не попадало (та же яма, что уже записана в память про растущий реестр); теперь ищет запросом по названию
- 2026-08-23 `0d2de65f` feat(bulletin): блок «Документы к подготовке» из эталона — таблицы Документ/Ответственный/Срок в системе нет (перечня документов с ответственными и сроками модель не хранит), поэтому блок стоит на своём месте с причиной и ссылкой в «Законы об ОМ». **Остаток по этапам:** действия этапа в прототипе стоят В ЗАГОЛОВКЕ этапа справа («← Назад» и «Завершить этап и перейти далее»), у нас остались внизу карточки — перенос затрагивает гварды семи форм и отложен сознательно; у прототипа в рекогносцировке есть вкладки карточки объекта («Информация» / «Замещающий») и блок «Материалы рекогносцировки» с загрузкой файлов — источника (хранилища файлов ОМ) в системе нет
- 2026-08-23 `0f3ff935` feat(events): строка реестра ОМ кликабельна целиком, как в эталоне — обработчик на строке избавляет мышь от прицеливания в текст, но ссылки в ячейках ОСТАВЛЕНЫ (их видит скринридер, они открываются средней кнопкой; клик по самой ссылке в `router.push` не уходит). Проба архива ждала сырую ISO-дату и покраснела после форматирования — ожидаемое теперь собирается из ТОГО ЖЕ ответа сервера, а не пинится литералом: иначе ассерт сторожил бы формат вместо содержимого. Этапные сюиты 12/12, tsc чист
- 2026-08-23 `968e4cd2` fix(ops-ui): тёмные варианты бейджей профиля и реестра — плашки кода ОМ, конфликтов и отметок ознакомления стояли на светлых заливках без dark-варианта и в тёмной теме читались вырезанными белыми пятнами. Проверено скриншотом в `colorScheme: dark`
- 2026-08-23 `b2a34665` fix(ops-ui): даты и метки времени по всему разделу — не сырым ISO. Тот же дефект вида, что нашёлся на согласовании: аудит, настройки ОМ, карточка версии паспорта и панель версий печатали серверные строки как есть (`2026-08-21T17:50:06.992336+00:00`); даты идут через `formatIsoDate`, моменты — через `formatIsoDateTime`. object-passport 6/7, падение «нет готового объекта» пред-существующее (израсходованная фикстура стенда, см. [[Known-Issues]])
- 2026-08-23 `a0417983` fix(shell): индикатор связи не гаснет на 403 — только на обрыве сети. Первая версия считала отказом ЛЮБУЮ ошибку запроса, и у персоны без права `event.view` подвал писал «связи нет» при живой связи: 403 — это ответ сервера, а не обрыв; признак берётся у клиента ОМ (`kind === "network"`). Сторож — в своём describe с `serviceWorkers: 'block'`: без блокировки `page.route` не перехватывает запросы, ушедшие через service worker MSW, и третий шаг (обрыв сети) оставался зелёным на живых данных. Проба ведёт через три состояния: живой стенд → персона без права → обрыв
- 2026-08-23 `157deeb8` fix(audit): идентификатор автора подписан «ID», а не выдан за имя — в колонке «Пользователь» стояла голая «1»; журнал несёт только идентификатор учётной записи, имени в нём нет, и без подписи число читалось как обрезанное имя
- 2026-08-23 `1ef317fb`, `c652efce`, `7274f216` feat/fix(events): **полная проходка «Реестра ОМ» кликами по живому стенду** — от окна создания до архива (создано ОМ-2026-101 «Проходка цикла (walk)», проведено через все шесть шагов: бюллетень → рекогносцировка → потребность → выделение сил → расстановка → согласование → ознакомление → проведение → закрытие). Ни одного 5xx на всём цикле. Найдено и починено пять дефектов, все — кликами, не чтением кода: **(1)** в окне создания НЕ БЫЛО поля «Дата окончания», хотя бэк принимает `business_date_end`, а реестр считает по нему продолжительность — каждое созданное вручную ОМ выходило однодневным; заодно окно названо «Создать бюллетень» (совпало с кнопкой реестра, было «Создать охранное мероприятие»), добавлены надзаголовок эталона и пример в поле названия; **(2)** карточка ОМ показывала только дату начала — трёхдневное мероприятие читалось как однодневное; то же нашлось в шапке архива, в «Карточке, бюллетене, программе» и в карточке своего назначения на ознакомлении; **(3)** у таблицы потребности не было шапки колонок — «Смена» от «Группы» глазом не отличались (плейсхолдер исчезает после ввода); **(4)** ошибки полей печатались машинным путём сервера — «rows.0.group: Выберите группу», три одинаковые строки в никуда; путь переводится в «Строка 1 · Группа» (номер строки человек считает с единицы), словарь покрывает и ключи закрытия; **(5)** пустое закрытие давало «directionSummaries.1.summary» — теперь «Строка 2 · Итог». 🔴 Сторож клиентской проверки периода писался ТРИЖДЫ: ассерт на текст ошибки вакуумен (тот же текст возвращает сервер), счёт POST через `page.route` вакуумен (перехват маршрутом не видит запросов service worker и считает ноль) — рабочий вариант только `page.on('request')`, красная проба со снятым `refine` падает. events-registry 6/6, closure 4/4, tsc чист
- 2026-08-23 `aa83fc3c` feat(events): **окно «Создать бюллетень» приведено к скриншотам прототипа** — заказчик прислал два снимка эталона (внутреннее / с участием иностранцев) с требованием полного совпадения. Окно знало только название, объект и две даты; добавлены подзаголовок и разделитель шапки, «Тип мероприятия *» двумя кнопками с подсказкой по выбору, три поля периода в ряд (начало / окончание / **время**), строка-сводка периода с днями недели и числом дней, «Охраняемое лицо» из живого справочника ОЛ, «Локация» с умолчанием «г. Астана», «Старший наряда» / «Старший ГВО» (подпись и подсказка меняются по типу), кнопка «Создать бюллетень» вместо «Создать». Осознанное отклонение по решению заказчика: поле «Объект» ОСТАЁТСЯ (в эталоне его нет — там объект выбирает старший наряда позже; здесь по нему привязывается версия паспорта). Введённое видно: тип, локация, время и старший вышли в «Сведения об ОМ» карточки, лицо бюллетеня попало в сводку ГВО, факт сводки переименован в «Старший ГРУППЫ ГВО» (иначе у визита иностранного лица две строки назывались бы одинаково). **Два дефекта поймал СНИМОК окна, не чтение кода:** `setValue` по незарегистрированному имени клал значение в форму, но не будил `watch` (первый выбор типа не подсвечивал кнопку — поле теперь регистрируется скрытым инпутом); автофокус Radix вставал в «Название ОМ», первый клик мимо уводил фокус с пустого обязательного поля, строка ошибки раздвигала форму и кнопка «Внутреннее» уезжала из-под курсора между нажатием и отпусканием — клик не срабатывал вовсе (фокус ставится на само окно). Бэкенд-половина — см. [[../Personnel-Records/Changelog]]
- 2026-08-23 `9a3b8ca9` fix(auth): **«Выйти» уводил на мёртвый порт** — кнопка снимала сессию и отправляла браузер на `http://localhost:3000/`, где ничего не слушает: вместо экрана входа человек получал страницу ошибки соединения. Причина не в кнопке: относительный `callbackUrl` NextAuth резолвит от `NEXTAUTH_URL`, а не от адреса, по которому открыто приложение, — в dev переменная стоит на `:3000` (`.env.local`), стенд живёт на `:3106`. Починено переносимо (`signOut({redirect:false})` + переход по `window.location`), а не правкой переменной: `NEXTAUTH_URL` у каждого стенда своя (dev :3106, docker :3100, прод — хост) и разойдётся с портом снова. **Смоук-обход дефект не ловил и не мог**: он сравнивал только `pathname`, а у мёртвого адреса путь ровно «/» — вердикт выходил «✅ увело на экран входа» поверх страницы ошибки; судейство переведено на полный адрес. Сторож — `e2e/auth-logout.spec.ts` (полный адрес + живой экран входа + пустая сессия), красная проба роняет его на `chrome-error://chromewebdata/`
- 2026-08-24 `7ab2f1a6`, `c38c82a0` feat(events): **строка реестра ОМ = бюллетень, раскрытие = объекты посещения** — первая задача бэклога ClickUp ([86eyqf5dc](https://app.clickup.com/t/86eyqf5dc)). Кнопка-раскрыватель в первой колонке (`aria-expanded`/`aria-controls`) открывает врезку с объектами посещения мероприятия: объект со ссылкой на паспорт, охраняемое лицо ЭТОГО объекта, готовность расстановки. Раскрыватель — кнопка, а не клик по строке (строка уже ведёт в карточку — одно нажатие не может значить два действия), детали — вторая `<tr>` с `colSpan`, а не Accordion (внутрь `<tbody>` кладётся только `<tr>`). Бэкенд-половина — [[../Personnel-Records/Changelog]] (`1fb64208`). **Вид поправил СНИМОК, не код:** врезка сперва пыталась выровняться по колонкам реестра (её поля другие — читалось как сбитая вёрстка) и рисовала полосу готовности с нулём там, где постов ещё нет вовсе. Готовность не выдумывается: пока расчёт постов не разнесён по объектам, экран пишет причину вместо доли. Сноска под таблицей переписана — колонка «Охраняемое лицо» эталона закрыта раскрытием, отсутствует только «Старший». 🔴 Проба адресует раскрытие по `aria-controls`, а не по совпадению текста (имя объекта и так стоит в колонке локации — ассерт был бы вакуумным), проверяет, что кнопка не уводит со страницы, и что подпись меняется на «Свернуть»; красная проба с пустым списком объектов её роняет. Заодно починены фикстуры `recon`/`acknowledgement`/`approval`: они создавали ОМ без обязательного с 23.08 `kind`, получали 400 и падали на «не удалось подготовить фикстуру» — см. [[Known-Issues#Фикстуры трёх этапных проб создавали ОМ без обязательного `kind` (найдено и починено 24.08.2026)]]
- 2026-08-24 `7e7f275b` feat(events): **кнопка «Добавить объекты» у строки бюллетеня + снятие объекта** — задача ClickUp [86eyqf7y1](https://app.clickup.com/t/86eyqf7y1). Рядом с раскрывателем в первой колонке кнопка «+» открывает окно выбора объектов (поиск по названию и коду, мультивыбор чекбоксами, уже добавленные выключены с подписью «уже добавлен», отсутствие опубликованного паспорта названо прямо в строке) и сразу раскрывает строку — добавленное видно на месте. Во врезке у каждого объекта крестик снятия; отказ сервера («у объекта есть посты в расчёте») показывается текстом сервера, а не общим «не получилось». Кнопки скрыты без `event.manage` и на закрытом ОМ. Мультивыбор шлёт объекты ПООЧЕРЕДНО: эндпоинт добавляет по одному, и частичный успех честнее общего отката — добавленные остаются, неудачные остаются отмеченными в окне. Бэкенд-половина — [[../Personnel-Records/Changelog]] (`7009e501`). 🔴 e2e ведёт СВОЁ мероприятие и возвращает стенд в исходное состояние (добавил → сверил врезку И ответ сервера → снял); две красные пробы роняют её. **Урок прогона:** ассерт по «ОБЪЕКТЫ ПОСЕЩЕНИЯ · 2» был вечно красным — капс делает CSS, в DOM текст обычный; и после правки экшенов Django с `--noreload` не подхватил новые маршруты (404 на живом стенде при зелёных pytest) — стенд надо перезапускать
- 2026-08-24 `59ce778a` feat(events): **объект в окне «Создать бюллетень» — поиск, заведение на месте, необязательность** — задача ClickUp [86eyqf7a7](https://app.clickup.com/t/86eyqf7a7), отменяет решение 23.08 об обязательном объекте. `<select>` заменён поповером с поиском (в родном списке искать можно было только первой буквой); пункт «— объект не выбран —» такой же, как остальные — иначе снять выбранный объект было бы нечем; «Объекта нет в списке — завести» заводит минимальную карточку прямо в поповере и сразу выбирает её, а отказ сервера («объект с таким названием уже есть») показывается У ПОЛЯ названия. Пустой объект НАЗВАН везде, где раньше печаталось имя (реестр, карточка ОМ, бюллетень, ознакомление, архив) — через общий `objectLabel()`. Поле `objectId` зарегистрировано скрытым инпутом: `setValue` по незарегистрированному имени не будит `watch` — **та же яма, что стоила дефекта «Типа мероприятия» 23.08, встретилась на том же экране второй раз**. Бэкенд-половина — [[../Personnel-Records/Changelog]] (`2ca28992`). 🔴 Новая проба ведёт оба пути (создание без объекта + выбор поиском) и следит, что пустой объект назван словами; заведение объекта проверяется ОТКАЗОМ на тёзку — успешное создание засорило бы реестр объектов, чистить который нечем. Две соседние пробы переведены с `<select>` на поповер, третья теперь берёт первое ОМ С ОБЪЕКТАМИ: первым в реестре теперь может стоять ОМ без объекта, и проба падала бы на состоянии, а не на предмете. events-registry 10/10, смежные спеки 90 passed
- 2026-08-24 `ccd7c09d` feat(events): бюллетень — над этапами, цепочка на пять шагов, объект посещения ведёт в этапы (Plane «Реестр ОМ-4»)
- 2026-08-24 (проверка, без правок кода) test(events): **задача очереди №1 «строка реестра = бюллетень, раскрытие в объекты посещения» проверена на живом стенде и закрыта** (Plane `42decf8f` → Done). `events-registry.spec.ts` 10/10 под `SMOKE_LIVE=1` — БЕЗ переменной все десять молча пропускаются, и это первое, во что упирается проверка: скип читается как зелень. Снимок раскрытой строки подтвердил то, чего ассерты не видят: панель объектов даёт имя объекта, ссылку на карточку, версию паспорта, охраняемое лицо и готовность СЛОВАМИ («посты не рассчитаны»), а не выдуманным процентом; ошибок консоли на экране нет. Побочная находка со снимка — реестр забит пробными ОМ: 188 из 194 строк это `Проба… (e2e)`, живых мероприятий шесть; заведена карточка Plane на чистку и на то, чтобы спеки перестали копить строки
- 2026-08-24 (проверка, без правок кода) test(events): **задачи очереди №2, №3, №4 проверены на живом стенде и закрыты** (Plane `b75d9a30`, `649ee3d9`, `ddae7e46` → Done). Прогоны: `events-registry` 10/10, `stage-chain` + `bulletin-stage` 5/5 (все под `SMOKE_LIVE=1`). Снимки трёх окон подтвердили форму, а не только наличие текста: (2) «+» у строки открывает «Добавить объекты посещения» с поиском, уже добавленный объект помечен «уже добавлен» и не выбирается; (3) в «Создать бюллетень» объект — поповер с поиском, значение по умолчанию «— объект не выбран —» (поле необязательно), внизу «+ Объекта нет в списке — завести»; (4) клик по объекту посещения ведёт на `/security-ops/events/<id>/?visit=<visitId>`, цепочка начинается с «1. Рекогносцировка» и несёт пять шагов, «Бюллетень» шагом не стоит — его сведения панелью НАД этапами с подписью «Шаг 1 из 5 · Бюллетень заполняется над этапами», плюс врезка «ОБЪЕКТ ПОСЕЩЕНИЯ» с выбранным объектом. Ошибок консоли ни на одном экране нет
- 2026-08-24 (правила, без кода) chore(process): **колонка Plane «Предложено Claude» + обязательная пересортировка трекера после каждой задачи + сторож памяти `next dev`** (Plane `c0fac845` → Done). Требования заказчика: (1) штатная колонка `Todo` переименована через API в «Предложено Claude» (группа `unstarted`, id `df2dfb0f`) — туда идут ВСЕ задачи, которые завёл не заказчик (побочные находки, техдолг, идеи), заказчик их читает и управляет комментариями внутри карточек; брать оттуда в работу самовольно нельзя. (2) После каждой законченной задачи — не «когда накопится» — прогонять `list --open`, сверять состояния с фактом, сводить дубли, отрабатывать комментарии заказчика и раскладывать находки по колонкам, а итог писать в vault. (3) Заведено обязательство мерить раздувание `next dev` **после КАЖДОЙ законченной задачи** — замер входит в закрытие карточки наравне с тестами, цифра RSS идёт в запись vault: `ps -o rss`, порог 2 ГБ, гасить по PID (не `pkill -f`), обрывы соединений сначала списывать на память, один dev-сервер на машину, не оставлять стенд поднятым после работы. Всё записано в корневой `CLAUDE.md` (§ Plane пп. 8-9, § «Стенд фронтенда: следить за памятью `next dev`»). Замер по закрытии этой карточки: `next dev` не поднят вовсе (в `ps -C node` только контейнеры Plane, максимум 148 МБ) — мерить нечего, стенд чист
- 2026-08-25 (правила, без кода) chore(process): **непрерывный цикл задач + полный прогон работоспособности каждые два часа** (Plane `e72c89fb` → Done). Требования заказчика, записаны в корневой `CLAUDE.md` двумя разделами. **(1) «Непрерывный цикл»:** закрытая задача — точка перехода, а не конец работы; закрыл → сразу `list --open` → взял следующую. Порядок выбора — не сверху вниз, а по смыслу: блокирующее (модель/контракт) → дефекты живого стенда → задачи заказчика по логике сценария → разрешённые находки из `Предложено Claude`; смежное по файлу/экрану делать подряд, ранжирование объяснять строкой в бэклог-заметке. Останов допустим ровно в четырёх случаях (очередь пуста / нужна развилка от заказчика / потерян доступ к инструменту / прервал сам), и каждый называется прямо. **(2) «Полный прогон работоспособности»:** решение о прогоне принимается **по глубине правки, а не по таймеру** (уточнение заказчика от 25.08: «перекрасить кнопку — нет необходимости гонять весь функционал»; двухчасовой интервал остался ориентиром, а не законом). Полный прогон — если задача тронула модели/миграции/схему БД, контракт API, бизнес-логику, архитектуру или общий компонент дизайн-системы; точечная правка вида «цвет, отступ, подпись в одном месте» обходится целевыми пробами и `tsc`; правка доки не гоняется вовсе. Сомнение трактуется в пользу полного прогона. Безусловные поводы: перед отчётом о пустой очереди, после пяти-семи мелких задач подряд без единой полной проверки, после перезапуска/пересборки стенда, при странном поведении без причины. Состав прогона: весь `pytest` по всем приложениям + обход КАЖДОГО эндпоинта по схеме (список берётся из `schema.yaml`, не из памяти); `tsc` + полный `playwright.smoke.config.ts` под `SMOKE_LIVE=1` (без переменной живые спеки молча пропускаются — скип читается как зелень) + обход экранов кликами с чтением консоли; связка фронт↔бэк на ЖИВЫХ данных (мок зелен и при 500 от сервера); замер памяти `next dev`. Инструмент — любой (Playwright, pytest, curl по схеме, `webapp-testing`, `project-sentinel`, `/code-review`), требование одно: результат виден выводом команды. Каждая находка — отдельной карточкой в `Предложено Claude` (что сломано, где, как воспроизвести, чем грозит), затем пересортировка очереди и следующая задача. Зелёный прогон записывается в леджер тоже — «ничего не найдено» это факт о состоянии системы
- 2026-08-25 `e8e7e25a` feat(events): **карточка ОМ листается по этапам** — админ кликает любой шаг цепочки и видит его панель, не двигая мероприятие. До правки `ActiveStage` переключался только по `event.stage`, а степпер был неинтерактивной лентой. Просматриваемый шаг — в адресе (`?step=N`): «покажи мне этап N вот этого ОМ» это ссылка, которую пересылают на разборе; без права параметр не действует вовсе (иначе — дыра в обход гварда), номер из адреса чинится. Шаг становится кнопкой ТОЛЬКО при праве `event.stage_override`, иначе остаётся статичной меткой — кликабельная на вид, но мёртвая пилюля обещает действие, которого нет. Состояние «просмотр» отличается словом («Просмотр шага N из 5 · мероприятие стоит на шаге M»), а не только кольцом: цвет один информацию не несёт. Панель чужого шага выключается `inert` и поля НЕ прячет — предмет просмотра как раз содержимое этапа; `inert` снимает и клик, и обход по Tab. «Перевести ОМ сюда» не разблокирует форму на клиенте, а меняет стадию на сервере — панель оживает сама. Мок-слой получил ту же ручку. 🔴 `e2e/stage-override.spec.ts` 4/4 (включая персону БЕЗ права: у стендового admin права приходят звёздочкой, и на нём закрытое состояние недостижимо), четыре красные пробы. **Яма пробы:** ассерт «клик внутрь не проходит» через `control.click()` из JS КРАСЕН на рабочем коде — программный клик диспетчеризуется и по `inert`-поддереву; судить надо фокусом. Гейт смоука 303/307, четыре падения разобраны в [[../Personnel-Records/Known-Issues#Три спека смоука красны на пустых фикстурах стенда (подтверждено A/B 25.08.2026)]]. Решения — [[Decisions#Просмотр чужого этапа: адрес, `inert` и слово вместо оттенка (24.08.2026)]]
- 2026-08-25 (замер и обслуживание) chore(stand): **`next dev` дважды перешагнул порог за сессию** — 5,6 ГБ к моменту первого прогона гейта (гейт из-за этого полз и был остановлен), после перезапуска 1,9 ГБ, к концу задачи снова **8,2 ГБ**. Оба раза снят по PID и поднят заново. Замечено сверх правила: СВЕЖИЙ `next dev` держит 2,9 ГБ уже после компиляции одной страницы, то есть порог в 2 ГБ перешагивается не «за долгую сессию», а сразу — это факт для задачи заказчика #42 («фронт раздувается и вешает машину»), поставленной первой в очередь. Полный прогон смоука разогревает процесс до 6+ ГБ
- 2026-08-25 `5a73ee2a` perf(dev): **раздувание фронта — причина измерена и обуздана** (задача заказчика #42). Память копится на пересборках HMR и не отдаётся: 2,67 → 3,15 ГБ за десять правок, ~48 МБ за цикл — отсюда 6-8 ГБ за сессию на машине с 15 ГБ ОЗУ и зависания. Отвергнуто замером: Turbopack (2105 против 1932 МБ — тяжелее), `--max-old-space-size` (RSS 2890 МБ при лимите кучи 2048 — лимит V8 не про RSS), `preloadEntriesOnStart: false` (нет выигрыша), `onDemandEntries` 2/15с (хуже под нагрузкой). Оставлено подтверждённое: `experimental.webpackMemoryOptimizations` — старт 1802 → 781 МБ. Главное средство — `scripts/dev-guard.sh` (`npm run dev:guard`): мягкий порог 2500 МБ ждёт затишья, чтобы не рвать e2e, жёсткий потолок считается долей ОЗУ машины (40% = 6037 МБ здесь) и бьёт сразу. 🔴 Первая редакция сторожа МОЛЧА не срабатывала: цепочка процессов в четыре звена, память у последнего, а считались прямые дети — 90 МБ вместо двух гигабайт. Стенд стартует с 987 МБ вместо 1,8-2,9 ГБ; гейт 302/307 (три известных сторожа фикстур + две «сдачи дня», вызванные моим же `seed_expense_chain` и вылеченные снятием его строки — см. [[../Personnel-Records/Known-Issues#`seed_expense_chain` СДАЁТ ДЕНЬ и этим краснит спеки «сдачи дня»]])
- 2026-08-25 `17335530` feat(events): **ОМ с объектом открывается СРАЗУ рекогносцировкой** (задача заказчика Plane «Реестр ОМ-5»). Косметикой не делалось: форма рекогносцировки на стадии `BULLETIN` обещала бы действие, которого нет — `PATCH /recon/` сервер там отклоняет, — поэтому менялось СОСТОЯНИЕ ЗАВЕДЕНИЯ. `create_event` ставит ОМ с объектом сразу в `RECON` (`readiness 15`), вход в цепочку журнал пишет как `None → RECON`; без объекта осматривать нечего, и ОМ остаётся на «Бюллетене». Гейт `complete_bulletin` держит теперь ОБЪЕКТ, а не текст бюллетеня: ОМ, заведённые до правила, открывают рекогносцировку без заполненного бюллетеня, а `BULLETIN_INCOMPLETE` живёт ровно там, где объекта нет. **Следствие, без которого правка была бы потерей:** панель бюллетеня правится на ЛЮБОЙ стадии, кроме `CLOSED` — ОМ с объектом заводится с пустым бюллетенем и минует стадию, к которой правка была привязана НА КЛИЕНТЕ (сервер её принимал всегда, `update_bulletin` гварда стадии не имеет); иначе описание и задачи такому ОМ было бы уже не вписать. Строка «можно открывать рекогносцировку» осталась только на стадии `BULLETIN` — дальше это обещание уже случившегося. Из эталона добавлен счётчик чек-листа «Выполнено: N из M» (по ЧЕРНОВИКУ формы: галочка ставится до сохранения, и отставший счётчик читался бы как «не засчиталось»; живым регионом не объявлен — счёт меняет сам человек своей же галочкой). 🔴 Четыре красные пробы (снятие `RECON` у заведения, снятие гейта по объекту, счётчик по сохранённому, `editable` только на `BULLETIN`) — все красные. 🔴 **Проба «несохранённый бюллетень» была ВАКУУМНОЙ на переиспользуемой фикстуре:** прошлый прогон сохранил в неё свой же черновик, повтор той же строки поля не менял, `dirty` не вставал — в одиночку проба зеленела (доставалась свежая фикстура), в полном прогоне краснела; текст черновика сделан уникальным на прогон. 🔴 Фикстуры `bulletin-stage.spec.ts` создавались БЕЗ обязательного с 23.08 поля `kind` — сервер отвечал 400, `call` статус не смотрит, подготовка молча не работала, и пробы держались на случайных строках стенда. Решения и границы — [[Decisions#Мероприятие с объектом стартует с рекогносцировки; гейт держит объект, а не текст (25.08.2026, Plane «Реестр ОМ-5»)]]
- 2026-08-25 (полный прогон) test: **гейт целиком после модельной правки** — `pytest` по всем `organization_management/apps` **3139 зелёных** (девять пинов стадии заведения поправлены ПО СМЫСЛУ, а не подгоном: распределение и воронка аналитики ждут вход в `RECON`, сквозной проход ждёт от `bulletin/complete/` отказ `INVALID_STAGE_TRANSITION`, две пробы обхода этапов сверяют «стадия не изменилась» с `RECON`, покрытие аудита перестало завершать несуществующий шаг); `tsc` чист; полный `playwright.smoke.config.ts` под `SMOKE_LIVE=1` — **254 прошло, 9 упало за 47 минут**, из них три известных на пустых фикстурах стенда (`forces-gathering` ×2, `object-passport`; карточка Plane «Стенд: нет фикстур под сторожа смоука»), одна своя (вакуумная проба выше) и **четыре оборваны перезапуском `dev-guard`**: `next-server` дорос до 6237 МБ, жёсткий потолок 6037 сработал НЕ ДОЖИДАЯСЬ затишья и убил сервер посреди прогона (`ECONNREFUSED 127.0.0.1:3106` у `stage-chain`, трёх `stage-override` и обхода каркаса observer). Все пять перепрогнаны зелёными. **Замер:** свежий `next-server` 631 МБ; за 47 минут смоука дорос до 6,2 ГБ — обход разогревает процесс сильнее любой правки
- 2026-08-25 (находка замера) chore(stand): **предписанная команда замера меряет НЕ ТОТ процесс.** `ps -o pid,rss,etime,cmd -C node` из корневого `CLAUDE.md` сервер разработки не показывает вовсе: его `comm` — `next-server (v15.2.4)`, а не `node`, и в выводе видны только npm-обёртка (77 МБ) и запускатор (81 МБ), тогда как сам `next-server` держал в тот момент **2,1 ГБ**. То есть порог «2 ГБ — перезапуск» по этой команде не срабатывает никогда. Заведено карточкой в «Предложено Claude»; мерить до правки правила — `ps -eo pid,rss,etime,comm,args --sort=-rss | grep next-server`
- 2026-08-25 `7a2e07b6` feat(events): **запрос личного состава с рекогносцировки доходит до штаба 2-го департамента** (задача заказчика Plane «Реестр ОМ-23»). У этапа появился ИТОГ — блок эталона «Требуемое число сотрудников \*» с подсказкой расчёта по постам и словами о том, куда число уйдёт; кнопка завершения переименована в «Завершить рекогносцировку → запрос штабу»: она и есть отправка. На `/employees` появился блок **«Запросы с рекогносцировки — ждут распределения»** ВЫШЕ «Запроса сил по мероприятиям» — сверху то, что пришло и не разложено (ОМ на стадии «Потребность» с проставленным моментом отправки), снизу то, что уже в работе (утверждённые заявки по группам); в строке — число, номер ОМ, дата, объект и ПОДПИСЬ старшего наряда: запрос это оценка человека, а не выход расчёта. Расчёт по постам назван рядом, но в поле НЕ подставляется — осмотр даёт поправку на местность, и подстановка выдавала бы расчёт за оценку. 🔴 Красная проба: подмена `reconForceRequest` на `forceNeed` в блоке штаба краснит e2e, который ведёт число через весь путь (ввод → завершение этапа → экран штаба), а не ассертит своё же поле. **Снимки сверены глазами** (`.shot-tmp2/recon-summary.png`, `staff-inbox.png`, временные): блок стоит на месте, «запрос ещё не направлен» до завершения, «всего запрошено N чел.» в шапке входящих. Пин ссылки «сводка ГВО» в `events-registry` сделан ТОЧНЫМ (`'Сводка ГВО →'`): панель бюллетеня раскрывается теперь и после стадии «Бюллетень», когда он пуст, и внутри неё вторая ссылка на ту же сводку — подстрочный матчер ловил обе и падал строгим режимом (падение зависело от данных: в первом прогоне ему досталась строка с заполненным бюллетенем). Решения — [[Decisions#Запрос личного состава — своё поле, а не `forceNeed`; штаб получает его завершением рекогносцировки (25.08.2026, Plane «Реестр ОМ-23»)]]
- 2026-08-25 (решение заказчика) test: **полные прогоны отложены до конца очереди.** Заказчик: «пока тесты можешь не делать, сделаешь только после окончания всех задач». Правило корневого `CLAUDE.md` о полном прогоне на каждую глубокую правку приостановлено ЯВНЫМ указанием — задачи закрываются на целевых пробах (`pytest` по затронутым приложениям, `tsc`, спеки затронутых экранов, снимки). Полный гейт (`pytest` целиком + весь `playwright.smoke.config.ts`) прогоняется одним заходом, когда очередь Plane опустеет. Последний ПОЛНЫЙ смоук этой сессии успел дойти до 258 из 263 до остановки: четыре падения — пин ссылки ГВО (починен) и три известных на пустых фикстурах стенда (`forces-gathering` ×2, `object-passport`)

- 2026-08-25 (подъём стенда, без правок кода) chore(stand): **стенд поднят и проверен целиком** (запрос заказчика «проект подними»). Ничего поднимать не пришлось — все четыре звена уже слушали свои порты и отвечали живьём: Postgres `127.0.0.1:5434`, Django `runserver 0.0.0.0:8100 --noreload --settings=…local_postgres` (pid 56261, RSS 156 МБ, аптайм 1ч04м) — `GET /api/schema/` **200**; `next-server (v15.2.4)` на `:3106` (pid 97849, аптайм 6 мин) — **200**; Plane на `:8090` — **200**. **Замер фронта: 1 550 МБ за 6 минут работы** — порог 2 ГБ не перешагнут, гасить не стал, но темп подтверждает находку от 25.08: свежий сервер набирает полтора гигабайта на компиляции первых страниц, а не «за долгую сессию». Мерил `ps -o pid,rss,etime -p <pid>` по pid, найденному через `ss -ltnp`: предписанная в `CLAUDE.md` команда `ps -C node` этот процесс НЕ показывает (его `comm` — `next-server`), карточка на правку правила уже висит в «Предложено Claude» (`8f0001e5`). Очередь Plane на момент подъёма — 12 незакрытых, из них одна в `In Progress` (№24, замещающие на объекте посещения)
- 2026-08-25 (трекер, без правок кода) chore(plane): **очередь сверена и пересортирована** (запрос заказчика «зайди в Plane, обнови и отсортируй»). 12 незакрытых из 42; **состояния менять не пришлось** — сверка с рабочим деревом подтвердила `In Progress` у #24 (бэк замещающих готов: `OpsVisitObjectDeputy`, миграция `0037`, права, аудит, API, тесты; фронт не начат), дублей в очереди нет. Комментариев внутри карточек нет, но разрешение заказчика вписано прямо в описания четырёх находок — «Исправляй» (#30, #34) и «Можешь делать» (#43, #44, #45), то есть запрет на самовольный вынос из «Предложено Claude» с них снят. Проставлены `priority` и `sort_order` через `PATCH …/issues/<id>/` (обёртка `plane_task.py` ни того, ни другого не умеет — только `list/set/add/plan`). Порядок: #24 → #44 → #30 → #34 → #45 → #37 → #35 → #22 → #38 → #40 → #36 → #43; обоснование каждой позиции таблицей в [[../WIKI/Бэклог-Smart-Josparlau#Пересортировка очереди 25.08.2026]]. Спорное решение записано явно: #36 «Роли и права» по правилу «блокирующее вперёд» просилась бы наверх, но оставлена одиннадцатой — она надстраивает группировку над существующими кодами прав, гварды #24/#35 переделывать не заставит, а подъём наверх остановил бы весь сценарный ряд ради инфраструктуры
- 2026-08-25 `d19935b9` feat(events): **замещающие на объекте посещения** (задача заказчика Plane «Реестр ОМ-24», фронт + бэк). В раскрытии строки реестра у каждого объекта появилась строка «Замещающие» с кнопкой «+ Замещающий» и окном назначения (поиск по ФИО/подразделению, выбор ОДНОГО человека, флажок «может править расстановку» с названным последствием обоих состояний). Право показано СЛОВОМ («правит расстановку» / «только просмотр»): по одному имени в строке наблюдателя от правящего не отличить. Строка — вторая линия объекта, а не колонка: замещающих может не быть, а может быть трое, и колонка переменной длины ломала бы выравнивание врезки. Уже назначенные в окне не предлагаются — сервер отобьёт повтор ошибкой поля, и строка обещала бы действие, которое не пройдёт. 🔴 Красная проба: подмена подписи права на константу краснит e2e, который идёт ЧЕРЕЗ ЭКРАН (раскрытие → окно → выбор → строка → сверка с сервером), а не дёргает ручку. **Ямы пробы:** `tr` с именем объекта находится у каждого ОМ на этом объекте (их на стенде десятки) — искать надо `li` во врезке; ассерт капса `ЗАМЕЩАЮЩИЕ:` красен, потому что капс делает `uppercase`, а `toContainText` читает `textContent` (та же яма, что уже записана в памяти). **Снимок сверен глазами.** Решения — [[Decisions#Замещающий на объекте посещения: право живёт в данных, гейт получает явный хук (25.08.2026, Plane «Реестр ОМ-24»)]]
- 2026-08-25 `585c1743` feat(events): **календарь реестра ОМ доведён до эталона** (задача заказчика Plane «Реестр ОМ-22»). Режим был с 23.08, но врал: **отмечался только ДЕНЬ НАЧАЛА** — трёхдневное ОМ ставило точку на первый день, а на второй и третий календарь показывал пустой день, то есть ошибался ровно там, где его открывают («что у нас в этот день»); теперь мероприятие раскладывается по всем суткам периода, шаг — календарные сутки UTC (даты деловые, без часа, и локальная полночь на минусовых зонах сдвинула бы период на день). **День нельзя было НЕ выбрать:** панель всегда отвечала про какой-то день, вернуться к обзору было нечем — повторный клик по выбранному дню снимает отбор (эталонное `sel?null:d`), справа встаёт «Все мероприятия месяца», смена месяца выбор сбрасывает. **Легенды не было** — добавлена строка эталона «● Есть мероприятие»: смысл отметки назван словом, а не цветом. Дата в строке списка печатается только в обзоре месяца (при выбранном дне она уже в заголовке панели и повторялась бы). 🔴 Красная проба: возврат к «только день начала» краснит новый e2e, который листает календарь до месяца фикстуры и жмёт ПОСЛЕДНИЙ день периода — у пустого дня кнопка выключена, и «отметка есть» проверяется этим, а не наличием точки в разметке; в пробе стоит и ассерт «фикстура однодневная — проба вакуумна». `events-registry` 12/12, `tsc` чист, снимок сверен глазами. Осознанное расхождение: в эталоне календарь показывает один зашитый месяц и не листается — наше листание оставлено. Решения — [[Decisions#Календарь реестра ОМ: период целиком, снимаемый отбор дня, обзор месяца (25.08.2026, Plane «Реестр ОМ-22»)]]

- 2026-08-25 `a2ca6d67` docs(process): **правило «коммит и пуш после КАЖДОЙ закрытой задачи»** (Plane «Правило: коммит и пуш…» → Done). Требование заказчика внесено разделом корневого `CLAUDE.md`: закрытие карточки включает `git commit` + `git push` наравне с записью в vault, замером памяти и пересортировкой трекера. Порядок шагов зафиксирован (код → vault → add → commit → push → `Done` → хэш в леджер ПОСЛЕДНИМ шагом, иначе он устаревает при `--amend`); `git add -A`/`commit -a` запрещены — в дереве постоянно лежат тяжёлые untracked-каталоги (`frontend/` — 367 МБ с зависимостями, две прототипные выгрузки); один коммит на задачу; пуш подтверждается выводом `git status -sb`, а не словом; прямые коммиты в `main` запрещены; отказ пуша — блокер карточки, а не мелочь. Ветка `feat/frontend-battle-modules` на `origin` не существовала — запушена впервые (`-u`), после пуша ветка в ноль. 🔴 **Обнаружено при попытке коммита: в ЭТОМ ЖЕ рабочем дереве параллельно работает вторая сессия Claude** (pid 6459, аптайм 3 часа против моих 39 минут). За время моей пересортировки она закрыла #24 (`d19935b9` замещающие), #23 и #22 (`585c1743` календарь), завела #46 и взяла #34 в работу — то есть очередь, которую я ранжировал, разбирается с другого конца. Состав `git status` менялся между двумя соседними командами. Мои правки уцелели, но продолжать реализацию в общем дереве нельзя: это яма [[../Archive/.claude-memory/feedback_parallel_story_commit_sweep|«параллельная стори крадёт правку»]]. Работа остановлена до решения заказчика. **Замер:** `next-server` 1 393 МБ за 7 минут (pid 119792 — процесс сменился, прежний 97849 перезапущен чужой сессией)
- 2026-08-25 `a2ca6d67` feat(events): **удаление ОМ, чистка реестра и номер как счётчик выданных** (задача заказчика Plane «Реестр ОМ-34»: 237 пробных строк из 242). Появился `DELETE /api/ops/security-events/<id>/` и кнопка у строки реестра под СВОИМ правом `event.delete` — это не только уборка после проб: в реестре лежали «ААААппрр», «ccc», «EEE», заведённые человеком по ошибке, и убрать их было НЕЧЕМ. Два запрета с разными причинами (закрытое ОМ — история; ОМ с расстановкой или журналом штаба — работа людей). Подтверждение спрашивает ИМЕНЕМ того, что исчезнет, и окно закрывается ОТВЕТОМ сервера, а не кликом — отказ человек должен увидеть здесь же. Команда `manage.py purge_probe_events` (сухой прогон по умолчанию, `--yes`, `--force`) чистит через тот же сервис, значит пишет журнал и соблюдает запреты. Две пробы, обязанные заводить строку каждый прогон, теперь убирают за собой. **Стенд: 242 строки → 8**, после прогона проб реестра — 11 вместо роста на два десятка. 🔴 **Дефект, вскрытый чисткой:** номер ОМ считался как `count + 1`, и после удаления 230 строк счётчик вернулся на занятые номера — КАЖДОЕ создание падало 500 на уникальности кода, то есть чистка ломала реестр. Номер — счётчик ВЫДАННЫХ (следующий за наибольшим в году), освободившийся не переиспользуется: код уходит в бумагу. Заодно `ops_security_events_code_key` заведён в `CONSTRAINT_ERROR_MAP` — комментарий в коде обещал конверт, а карты не было. `ops`+`operations` 2968 зелёных, `tsc` чист. **Яма пробы удаления:** `getByText('ОМ-2026-12')` подстрокой входит в «ОМ-2026-123» — нужен `exact`; и ждать закрытия окна надо ДО счёта строк, иначе код находится в заголовке ещё открытого окна. Решения — [[Decisions#Удаление ОМ: своё право, два запрета и команда чистки; номер ОМ — счётчик выданных (25.08.2026, Plane «Реестр ОМ-34»)]]
- 2026-08-25 (замечено) chore(git): **параллельный коммит увёл правки задачи в чужое сообщение.** Пока файлы «Реестра ОМ-34» лежали в индексе, снаружи сессии прошёл коммит `a2ca6d67 docs(process): правило «коммит и пуш после каждой закрытой задачи»` — и забрал ВСЕ 18 файлов задачи вместе с собой (яма уже записана в памяти: «Параллельная стори крадёт правку»). История не переписывалась: ветка уже на `origin`, и разрезать опубликованный коммит ради красоты сообщения дороже, чем назвать расхождение здесь. Работа задачи 34 лежит в `a2ca6d67`, а не в отдельном `feat(events)`
- 2026-08-25 `994138fe` feat(nav): **«Настройки ОМ» → «Администрирование»** (задача заказчика Plane «Реестр ОМ-37»). Переименованы пункт меню, крошка и заголовок страницы `/security-ops/settings`; пин `prototype-skin` поправлен осознанно. Историческая строка журнала изменений («Настройки ОМ: правка правила не сохранялась…») оставлена как есть — она описывает событие прошлого. **Расхождение, названное заказчику:** категория меню, в которой пункт живёт, уже называется «Администрирование», и теперь категория, пункт и надзаголовок страницы носят одно имя. Выполнено буквально, вариант с переименованием КАТЕГОРИИ заведён в «Предложено Claude» — это решение заказчика. `prototype-skin` 62/62, `tsc` чист
- 2026-08-25 `19cd75ad` feat(profile): **история заступлений на ОМ — своя вкладка «История»** (задача заказчика Plane «Реестр ОМ-40»). Бокс уехал из вкладки «Охранные мероприятия» в четвёртую вкладку ПОСЛЕ «Моей статистики» и получил колонки эталона: мероприятие, пост, дата, форма одежды, вооружение, **балл**, ознакомление. Оснащение берётся из СТРОКИ РАСЧЁТА поста (назначение — это связь «человек ↔ пост», а чем пост оснащён, решает рекогносцировка); пустые графы говорят «не указана»/«не указано», а пропавшая строка расчёта — «—». Балл приходит из реестра оценок участников, суженного ПО СЕБЕ, и у ячейки ЧЕТЫРЕ разных ответа: «…», «нет доступа» (рейтинг обезличен и открывается по праву `ratings.*`), «не оценивалось» и число — прочерк на все случаи читался бы как «ноль баллов». 🔴 Красные пробы: перестановка вкладок и оставление блока на старом месте краснят e2e (проба ассертит и ПОРЯДОК вкладок, и отсутствие блока на прежней). **Пин поправлен осознанно:** `my-profile` сверял бейдж «Предстоящие назначения» с числом ВСЕХ назначений admin, включая закрытые ОМ, которых бейдж не считает — до чистки реестра разница не проявлялась. `my-profile` 4/4, `tsc` чист, снимок сверен глазами. Решения — [[Decisions#«Мой профиль»: история заступлений — своя вкладка, балл берётся из реестра оценок (25.08.2026, Plane «Реестр ОМ-40»)]]

- 2026-08-25 (обслуживание стенда, без правок кода) chore(stand): **проект переподнят целиком** (запрос заказчика «переподними весь проект»; Plane «Стенд: полный перезапуск проекта» → Done). Гашение — строго по PID, не паттерном: у фронта цепочка в шесть звеньев (`npm run dev:guard` 9477 → `sh` → `bash scripts/dev-guard.sh` → `npm exec next dev` → `node` → `next-server` 129351, державший **1,98 ГБ**), и снимать надо КОРЕНЬ — сторож перезапускает `next-server` сам, убийство только его ничего не даёт. Порядок подъёма: контейнер `vaps-db-5434` (`docker restart`, `pg_isready` с первой попытки) → Django `runserver :8100 --noreload --settings=…local_postgres` → фронт `npm run dev:guard` (лимит сторожа 2500 МБ, лог `/tmp/next-dev-3106.log`). **Проверено, а не объявлено:** `/api/schema/` **200** и несёт **226 эндпоинтов**; неприменённых миграций нет; корень фронта **200**, семь разделов (`/dashboard/`, `/employees/`, `/reports/`, `/organization/`, `/statuses/`, `/settings/`) дают **307 на `/api/auth/signin?callbackUrl=…`** — гвард анонима цел, `/security-ops/events/` отдаёт **200**; связка фронт↔бэк живая (`/api/auth/csrf/` 200), `/api/ops/security-events/` без сессии — **403**, то есть права работают; Plane `:8090` 200; ошибок в логе `next` ноль. **Две ямы обхода, обе НЕ дефекты:** маршрута `/login` в приложении не существует вовсе (вход через NextAuth), а 308 на всех разделах — редирект `trailingSlash`, проверять надо со слэшем; путь реестра — `/api/ops/…`, а не `/api/v1/ops/…` (первый мой запрос дал 404 по своей же ошибке). **Замер: `next-server` 2 071 МБ через 1 мин 12 с после старта** — порог правила (2 ГБ) взят компиляцией восьми маршрутов, ещё до первой правки кода; перезапускать бессмысленно (свежий наберёт столько же), мягкий порог сторожа 2500 МБ не достигнут. Это третье подтверждение, что «2 ГБ за долгую сессию» — неверная модель: гигабайты набираются на первой же компиляции. ⚠️ Перезапуск оборвал стенд у параллельной сессии Claude (pid 6459, аптайм 3ч33м), которая продолжает работать в этом же дереве
- 2026-08-25 (обслуживание стенда, без правок кода) chore(stand): **стенд погашен по запросу заказчика «можешь тушить»** — правило «не оставлять `next dev` после работы» исполнено полностью. Сняты по PID: цепочка фронта целиком (корень `npm run dev:guard` 142011 → … → `next-server` 142052, **1 993 МБ** на момент гашения) и Django `:8100` (141800, 114 МБ), затем остановлен контейнер `vaps-db-5434` (`Exited (0)`). Порты 3106, 8100 и 5434 свободны, остаточных процессов нет. Plane `:8090` НЕ трогался — трекер живёт отдельно от стенда и остаётся доступен (200). **Цифра к находке о раздувании:** сервер набрал 1 993 МБ за 10 минут, из которых работой были только восемь HTTP-проб обхода — то есть почти два гигабайта уходит на компиляцию маршрутов, а не на «долгую сессию»; это уже третий замер подряд с тем же выводом. ⚠️ Гашение выполнено при ДВУХ живых параллельных сессиях Claude в этом же дереве (pid 6459 — аптайм 3ч44м, pid 141641 — 10 мин): если у них шли прогоны, они оборваны. В рабочем дереве осталась их незакоммиченная работа (ГВО: `security-ops/gvo/[id]/page.tsx`, `entities/gvo-summary/*`, `features/gvo-section-edit/*`, миграции `0038`/`0039`, тест бэкфилла) — я её не трогал и не коммитил
- 2026-08-25 `2947c1dc` feat(gvo): **«Объекты посещения» сводки ГВО сведены с таблицей объектов мероприятия** (Plane «Реестр ОМ-35.1», первый шаг плана [[../WIKI/План-визит-иностранного-ОЛ]]). Было два списка одних и тех же объектов — таблица `ops_security_event_visit_objects` (по ней расстановка и готовность) и секция `visits` патча сводки свободным текстом; расходились молча. Теперь источник один: сводка выводит объекты из `event.visitObjects`, ключ `visits` снят с патча на сервере и в типе клиента. Таблице добавлены `visit_day` (NULL = «в день ОМ») и `note`, ручка `PATCH .../visit-objects/{id}/`, миграции `0038` (поля) + `0039` (бэкфилл из патчей с поиском строки по имени и снятием ключа). Окно правки переписано со свободного текста на список объектов с полями «День посещения» и «Примечание» (`features/gvo-section-edit/ui/GvoVisitsDialog.tsx`), у секции появилось пустое состояние. Гейт: `pytest` 3171 passed (весь `organization_management/apps`), `tsc` чисто, полный `playwright.smoke.config.ts` с `SMOKE_LIVE=1` — 173 passed, 7 красных все pre-existing (фикстуры стенда — карточка Plane №43; `smoke-buttons › обход портала` — [[Known-Issues#Три e2e-пробы упираются в ИЗРАСХОДОВАННЫЕ фикстуры стенда (23.08.2026)]] по памяти машины, роняет 128 хвостовых проб персоны). Снимки: секция с днём и примечанием, окно правки, пустое состояние. Память `next-server` после полного смоука — **4,8 ГБ**, перезапущен по PID, свежий — **712 МБ**, на закрытии карточки — **1,1 ГБ** (под порогом, перезапуск не нужен). Замер `ps -C node` процесса не видит: он называется `next-server`, карточка Plane №44
- 2026-08-25 `e9913eab` feat(events): **старший объекта посещения — поле, ручки назначения/снятия и журнал** (Plane «Реестр ОМ-35.2», второй шаг плана [[../WIKI/План-визит-иностранного-ОЛ]]). `chief_employee_id` + `chief_name` у объекта посещения (плоская ссылка + снимок подписи, миграция `0040`), ручка `POST/DELETE /api/ops/security-events/{id}/visit-objects/{visitId}/chief/` — POST и заменяет, DELETE снимает; журнал мутаций `VISIT_OBJECT_CHIEF_ASSIGNED`/`VISIT_OBJECT_CHIEF_REVOKED` (архитектурный сторож `test_every_declared_action_is_actually_written` покраснел на объявленном-но-не-пишущемся событии — покрытие дописано). В раскрытии строки реестра ОМ появилась строка «Старший объекта: …» со снятием крестиком; кнопка назначения ждёт постранички кадрового списка (`ОМ-35.3`/`ОМ-35.7`). Гейт: `pytest` 3175 passed (весь `organization_management/apps`), `tsc` чисто, `events-registry.spec.ts` + `gvo-sections.spec.ts` — 18 passed; красная проба (снятие не чистит id) роняет новую пробу. 🔴 Первый прогон новой пробы был ВАКУУМНЫМ: «не назначен» — подстрока «Замещающие: не назначены»; строка старшего получила `role="group"` с именем объекта. 🔴 Второй красный оказался не своим: `runserver --noreload` остался с мутированным кодом красной пробы — «падает после отката» = не перезапущенный стенд. Память `next-server` на закрытии — **1,65 ГБ**, под порогом
- 2026-08-25 `a9229473` feat(personnel): **поиск и постраничка кадрового списка на сервере** (Plane «Реестр ОМ-35.3», третий шаг плана [[../WIKI/План-визит-иностранного-ОЛ]]). `/api/ops/personnel/` принимает `search` (ФИО, звание, подразделение, табельный номер), `page`, `page_size` (потолок 100) и отдаёт `count`/`next`/`previous`; постраничка включается ТОЛЬКО по запросу — без параметров ответ прежний, иначе четырём экранам, читающим снимок целиком, молча обрезало бы выбор людей (долг назван карточкой в «Предложено Claude»). Новый общий компонент `features/personnel-picker` (задержка запроса 250 мс, `placeholderData` вместо мигания в пустоту, «Найдено N» от сервера); окно «Замещающий на объекте» переведено на него с клиентского фильтра. Гейт: `pytest` 3177 passed (весь `organization_management/apps`), `tsc` чисто, `events-registry` + `gvo-sections` + `placement-stage` + `acknowledgement-stage` — 18 passed, 1 красный pre-existing (израсходованная фикстура, карточка Plane №43). 🔴 Красная проба: `search`, не уходящий на сервер, роняет пробу (14 вместо 1). 🔴 `replace(/\D+/g,'')` склеил «Найдено 1 · страница 1» в 11 — из строки берётся первое число. Память `next-server` на закрытии — **1,74 ГБ**, под порогом
- 2026-08-25 `b9a140e7` feat(events): **«Назначить старшего объекта» рядом со строкой объекта** (Plane «Реестр ОМ-35.7», четвёртый закрытый шаг плана [[../WIKI/План-визит-иностранного-ОЛ]]). Кнопка «+ Старший» / «Заменить» в строке старшего объекта раскрывает окно с общим `PersonnelPicker` (поиск и страницы на сервере из `ОМ-35.3`); замена идёт одним вызовом и называет прежнего по имени. Проба `events-registry` переписана с ручки на ЭКРАН: раскрытие строки → кнопка → выбор человека → подпись строки и ответ сервера. Гейт: `tsc` чисто, полный `playwright.smoke.config.ts` с `SMOKE_LIVE=1` — 173 passed, 8 красных: семь pre-existing (фикстуры стенда — карточка №43, `smoke-buttons` по памяти машины) и один — **мусор стенда**: `prototype-skin › «Сбросить фильтры»` не сужался, потому что из 49 мероприятий 41 было пробным (32 на этапе `RECON`, больше страницы в 20 строк). Убрано 41 пробное ОМ ручкой удаления, проба зелёная; причина названа карточкой в «Предложено Claude» (пробы не убирают за собой). Память `next-server` после полного смоука — **4,94 ГБ**, перезапущен по PID, свежий — **702 МБ**
- 2026-08-25 `72e11c56` feat(gvo): **сводка ГВО раскрывается панелью в карточке мероприятия** (Plane «Реестр ОМ-35.4», пятый закрытый шаг плана [[../WIKI/План-визит-иностранного-ОЛ]]). Разметка сводки (620 строк, одиннадцать правимых секций) вынесена в `widgets/gvo-summary`; экран `/security-ops/gvo/[id]` стал оболочкой на 100 строк, карточка ОМ получила кнопку «Информация по ГВО» справа в шапке и раскрывает ту же панель НА МЕСТЕ, без ухода со страницы. Два режима шапки: `page` (код ОМ, этап, H1 «Сводные данные») и `embedded` (без них — они уже стоят в карточке). Гейт: `tsc` чисто, полный `playwright.smoke.config.ts` с `SMOKE_LIVE=1` — **175 passed**, 7 красных все pre-existing (фикстуры стенда — карточка №43, `smoke-buttons` по памяти машины); `prototype-skin` после уборки мусора стенда зелёный. 🔴 Проба правки дня объекта была одноразовой: «Сохранить» заперта, пока значения не изменились, — теперь берёт значения от текущих и снимает их в конце. 🔴 Прерванные прогоны оставили на стенде патч сводки — «Черновик» не сходился; патчи убраны, проба прогнана ДВА раза подряд зелёной. Память `next-server` после полного смоука — **4,67 ГБ**, перезапущен по PID, свежий — **704 МБ**
- 2026-08-25 `HASH6` feat(gvo): **кнопка «Информация по ГВО» — только там, где сводке есть чем быть** (Plane «Реестр ОМ-35.5», шестой закрытый шаг плана [[../WIKI/План-визит-иностранного-ОЛ]]). Условие `kind !== "INTERNAL"`: у внутреннего ОМ выездной охраны нет и кнопка обещала бы пустоту, а `kind === null` (ОМ заведено до появления типа) кнопку СОХРАНЯЕТ — скрывать по незнанию значило бы отрезать доступ к заполненной сводке после снятия модуля. Проба заводит внутреннее ОМ сама и снимает в конце, плюс контроль на ОМ не внутреннего типа. Гейт: `tsc` чисто, `gvo-sections` + `events-registry` + `prototype-skin` — **82 passed, 1 skipped, красных нет**; красная проба (снятый гейт) роняет пробу. Память `next-server` — **4,66 ГБ** после прогонов, перезапущен по PID, свежий — **700 МБ**

