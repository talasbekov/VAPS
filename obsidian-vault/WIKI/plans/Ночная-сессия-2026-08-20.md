---
title: Ночная сессия 20→21.08.2026 — план и решения
module: frontend
updated: 2026-08-20
tags: [plan, night-session]
---

# Ночная сессия 20→21.08.2026

Bratan ушёл до утра (~08:00), задача: работать без остановки, решения принимать
самостоятельно и фиксировать здесь. Старт 20:00. Ветка `feat/prototype-skin`
(= main + docs), параллельная сессия работает на main (ГВО) — её файлы не трогаю,
перед каждым мержем/коммитом сверяю HEAD.

## Бэклог на ночь (порядок = приоритет)

1. **«Расход и светофор»** — следующий экран обхода прототипа (нав-пункт
   `traffic`, группа «Личный состав»). Хостовый экран, к прототипу не сводился.
   Приём стандартный: живой источник (`strength-report`, `/traffic-light/tree/`,
   модели) → на экран; чему источника нет — строка на экране + леджер.
2. **Группа «Контроль»** — Отчёты (`service-reports`), Аудит и настройки
   (`audit`, `settings`): сверка с прототипом, доводка расхождений.
3. **«Поддержка» — Обратная связь** (`feedback`): сверка (переписана срезом J,
   ожидаю малый объём).
4. **Хвост скина**: 12 контрастных пар тёмной темы на сырых Tailwind-цветах
   (из отчёта 19.08 §7-9), по возможности — пограничные переполнения.
5. **Финал**: vault (Frontend/Status, Changelog), LOG.md, память, сводка для
   Bratan.

## Решения, принятые без Bratan

- (20:00) Бэклог собран сам из [[../../Frontend/Status|Frontend/Status]] и
  памяти обхода прототипа; приоритет — продолжение обхода, а не хвост скина,
  потому что обход — заявленная «следующая» работа, а хвост помечен «сознательно
  отложенным».
- Записи о ходе — в этом файле по мере работы; итог утром — в LOG.md и
  Changelog.

## Журнал

- 20:00 — план зафиксирован, начинаю разведку «Расход и светофор».
- 20:10 — разведка закрыта. Прототип (строки 941–1010 экспорта): шапка с ролевым
  селектором и «Напомнить отстающим», 4 KPI сдачи, дерево подразделений со
  светофором, свод расхода, история версий, панель напоминаний. Живые источники
  найдены ПОЧТИ НА ВСЁ: `daily-submissions` (+export XLSX, history), `traffic-light`
  (tree + точечный drift), `tomorrow-block` (blocked/overridden/laggards + override),
  `documents`/`attachments` (выпуски с номерами), `strength-report` (+submitted).
  Напоминания отстающим — ЖИВАЯ АВТОМАТИКА бэка (`lagging_check.py`: контрольный
  час, водяной знак, эскалация через OpsNotification), ручной ручки нет.

### Решения по экрану «Расход и светофор» (принял сам, обсудить утром)

1. **Новый маршрут `/security-ops/traffic`** — по прецеденту profile/forces
   (новые экраны идут в раздел, легаси `/reports` не переделываю: он про
   живую строевую записку, а этот экран — про сдачу/контроль).
2. **Ролевой селектор прототипа НЕ переношу** — зона видимости в живой системе
   реальная (RBAC-область актора), симулировать роль нечем и незачем.
3. **Плитка «Черновик» прототипа → «С опозданием»** — черновиков сдач в домене
   не существует (сдача иммутабельна), а `late` живой; отклонение от прототипа.
4. **Кнопки «Напомнить» не рисую** — ручной отправки нет; на экране строка о
   живой автоматике напоминаний. Панель «Напоминания» прототипа → панель
   «Блокировка расхода на завтра» (laggards поимённо + законный обход
   `override` под правом `daily_report.override_block`).
5. **Сдачу дня (create/amend) на экран НЕ выношу** — прототипный экран
   контрольный, не операторский; запись — отдельная стори.
6. KPI считаю по ЛИСТЬЯМ дерева (родитель несёт худший цвет поддерева —
   считать по всем узлам значило бы двойной счёт).
7. Drift выбранного узла — счётчиками (added/removed/changed), поимённая
   расшифровка с именами сотрудников — следующий срез (нужен джойн id→имя).

- 20:50 — экран ГОТОВ (`6eae7142`): страница + hooks/use-daily-control +
  opsApiClient.download + сайдбар + карта смоука + findApiGap-исключение
  (снят ложный баннер «на бэке нет /api/ops/*»). e2e 4/4 зелёные по живому
  стенду с собственного dev :3107 (worktree), красная проба KPI==листья
  прошла (3≠6). Спека читающая: обход блокировки и сдача НЕОТЗЫВНЫ — не
  дёргаю. Заметка экрана: Продукт/Охранные-мероприятия/Расход-и-светофор.
- 20:55 — merge main `9e1dc8e2`: подтянуты 9+ ГВО-коммитов параллельной
  сессии; конфликты только в vault (5 файлов, взял сторону main как канон).
  main продолжает двигаться — ПЕРЕД утренним мержем свериться ещё раз.
- 21:20 — группа «Контроль» сведена (`d543886d`). Аудит: +селекты
  действия/актора по фактической ленте (адаптер /api/ops/audit-logs
  отдаёт 200 без фильтров — серверные фильтры /api/operations ждут своего
  среза, сказано на экране), +сброс фильтров, вслух про отсутствие
  экспорта журнала. Отчёты: +«Расход за период» (живой CSV period-export,
  проверен: 200, строка на дату), «Журнал действий» из мёртвой заглушки —
  в ссылку на живой аудит, подвал про «светофор сдачи за период» (ручки
  нет) и «последние выгрузки» (хранилища нет). Прототипную пару
  «Аудит и настройки» хост осознанно держит двумя страницами — не менял.
  Решение: карточку «Расход на завтра» не дублирую — выпуски/версии уже
  на /security-ops/traffic, из подвала ведёт ссылка.
- 21:25 — «Обратная связь»: в актуальном локальном экспорте прототипа
  (20.08) экрана НЕТ вовсе (grep по «feedback/Обратная связь» пуст — есть
  только generic «Служебный раздел»). Сверять не с чем; хостовый экран
  среза J (632 строки, на PageHeader) остаётся каноном. Ролевое ограничение
  пункта (7 ролей из старого снимка Claude Design) — вопрос будущей сверки
  RBAC-навигации, не этого экрана. Задача закрыта без правок кода.

- ~00:00 — сессия прервалась/сжалась между задачами 3 и 4 (потерян процесс
  dev-сервера на :3107, файловые правки уцелели). Продолжаю тем же планом.
- 00:10 — хвост скина закрыт (`62d00d4a`). Headless-скан computed-стилей
  по 16 маршрутам в `prefers-color-scheme: dark` нашёл 7 уникальных мест
  (не 12 — часть пар отчёта 19.08, видимо, уже перекрывалась PageHeader/
  StatCard рефакторингом днём позже); все закрыты `dark:`-вариантом.
  Красная проба (откат одного класса) подтвердила чувствительность скана.
  Побочно задет мой же `/security-ops/traffic` — «с опозданием» тоже не
  имел `dark:`-варианта.
  Известный нюанс: `forces-screen.spec.ts` красный на прогоне —
  предсуществующий дрейф фикстуры (см. память), к правкам не относится.
