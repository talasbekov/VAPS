---
title: Infrastructure — Status
module: infrastructure
updated: 2026-08-24
tags: [infra, status]
---

# Infrastructure — Status

## Трекер задач: self-hosted Plane (24.08.2026)

Заказчик отказался от ClickUp; вместо него на рабочей машине поднят [Plane](https://github.com/makeplane/plane) community edition.

- Каталог стека: `/home/erda/plane` (вне репозитория VAPS), проект compose — `plane-selfhost`, 13 контейнеров.
- Адрес: **http://localhost:8090** (HTTP-порт уведён с 80, HTTPS — 8453): на машине уже живут стенд VAPS `:3106`/`:8100`, Postgres `:5434` и чужие контейнеры.
- Рабочее пространство `VAPS` (slug `vaps`), 21 проект. Задачи Smart Josparlau (17 штук) перенесены снимком ClickUp; остальные проекты созданы пустыми — данных ClickUp на день переезда достать не удалось.
- Доступы — `/home/erda/plane/CREDENTIALS.txt` (права 600, вне git). Пароль сгенерирован автоматически и подлежит смене.
- Справка по стеку и командам — `/home/erda/plane/README-vaps.md`, карта переноса — `/home/erda/plane/migration/PLAN.md`.

**Яма установки, стоившая часа.** Сменить пароли Postgres/RabbitMQ в `variables.env` НЕ достаточно: `docker-compose.yml` строит подключения из `DATABASE_URL`/`AMQP_URL`, а они в поставке ПУСТЫЕ — и подставляется дефолт `postgresql://plane:plane@plane-db/plane`. Базы поднялись с новым паролем, приложение ходило со старым: `migrator` падал с `password authentication failed`, **при этом главная страница отдавала 200**, и только `/api/instances/` выдавал 502. Меняешь пароль — заполняй и строку подключения.

**Вторая яма — регистрация первого администратора.** `POST /api/instances/admins/sign-up/` отвечает 500 (не 4xx), и причина по ответу не видна. Администратор и рабочее пространство заведены напрямую через `manage.py shell` (`User` + `InstanceAdmin` + `Instance.is_setup_done`), после чего вход по паролю и API заработали штатно.

**Память.** Plane + стенд VAPS вместе держат ~10 из 14 ГБ. При тесноте гасить то, что сейчас не нужно: `docker compose -p plane-selfhost --env-file .env down` (данные остаются в томах). Отдельно: Docker на машине держит ~64 ГБ образов, из них ~53 ГБ не используются.


_Обновляется по ходу работы. См. также [[Changelog]], [[Decisions]], [[Known-Issues]]._

Архивные справки удалённого стека (тест-инфраструктура `Backend/VAPS`, целевая архитектура G1, проектные заметки SPA) вынесены в [[../Archive/spa/Справки-удалённого-стека|Archive/spa]] (21.08.2026); действующие домен-константы из них — в [[../Продукт/История|Продукт/История]].

## Окружение и машины

С 11.08.2026 разработка ведётся только локально, на одном ноутбуке (`/home/erda/Музыка/VAPS`) — решение Bratan «локально будем разрабатывать». Второй машины (ВПС `root@81.17.99.96`) больше нет в контуре, её чекаут отстаёт от origin и не синхронизируется, ssh туда не тратить. «Прод» для операционных задач (например ротации секретов) уточнять у Bratan отдельно, а не считать ВПС продом по умолчанию. Исторический след двух машин ещё может попадаться: graphify-граф, собранный на ВПС, нёс узлы с абсолютным путём `/root/projects/VAPS/...` (лечится полной пересборкой на одной машине); документы когда-то жили в `docs/VisitX/`, потом переехали в `docs/PersonnelStatus/` без коммита — при сверке путей нельзя доверять только HEAD или только рабочему дереву.

Каталог auto-memory физически живёт в `/home/erda/Музыка/VAPS/.claude/memory/` (свой git-репозиторий, путь `.claude/` в `.gitignore` — в историю VAPS не входит), харнесс-путь `/home/erda/.claude/projects/-home-erda--------VAPS/memory` — симлинк на него. Перенесено 11.08.2026 по решению Bratan, чтобы память не зависела от переключения веток/сноса worktree в основном чекауте.

## Тулинг и скиллы

Personal-скиллы ставятся `npx skills add` в `/home/erda/.agents/skills/<name>`, симлинкуются в `/home/erda/.claude/skills/<name>`. Список слэш-команд строится при старте сессии — скилл, поставленный в уже открытую сессию, невидим до перезапуска; «isn't a recognized command here» не значит «не установлен», проверять `ls`/`head SKILL.md` напрямую. У personal-скилла (в отличие от плагина) `CLAUDE_PLUGIN_ROOT` пустой — пути к скриптам в SKILL.md писать абсолютными, не через переменную.

`npx skills find` в этой песочнице виснет насмерть (реестр недоступен через прокси), но `npx skills add <owner/repo>` работает нормально (другой путь). Искать скиллы вместо CLI через `WebFetch` по `skills.sh` (лидерборд, `/topic/<slug>` в единственном числе, карточка `skills.sh/<owner>/<repo>` с install count).

## Полный аудит фронтенда PersonalRecordFront 17.08.2026 — сводка

Резюме структурного вывода и статуса спринтов перенесено в [[../Frontend/Status]] (раздел «Аудит фронтенда 17.08.2026»). Здесь фиксируется только источник: полный отчёт по 8 направлениям — `docs/frontend-audit-2026-08-17.md` (local-only, `.gitignore` глушит `docs/*` с точечными негейтами — см. [[Decisions#Проектные доки и BMAD-кастомизация untracked с точечными исключениями]]).
