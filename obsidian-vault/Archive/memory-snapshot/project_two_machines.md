---
name: project-two-machines
description: "ВПС ВЫВЕДЕН (11.08.2026, решение Bratan): разработка только на ноутбуке; следы ВПС в графе/истории — артефакты"
metadata: 
  node_type: memory
  type: project
  originSessionId: f107bfae-f7dd-4367-8388-c4246a96a468
---

**11.08.2026: ВПС больше не используется — решение Bratan «локально будем
разрабатывать».** Единственное окружение — ноутбук (`/home/erda/Музыка/VAPS`).
ВПС (`root@81.17.99.96`, `/root/projects/VAPS`) на тот момент был недоступен
(TCP-таймаут на 22 и 80) и осознанно оставлен позади; его чекаут отстаёт от
origin и НЕ синхронизируется. Не тратить время на ssh туда. «Прод» для
ротации NEXTAUTH_SECRET из [[project-full-review-2026-08-08]] — уточнять у
Bratan, а не считать ВПС продом по умолчанию.

Исторический контекст (когда машин было две — следы могут попадаться):

Последствия (обнаружено 2026-06-10):
- graphify-граф, собранный на ВПС, содержит узлы с абсолютными путями `/root/projects/VAPS/...`; после локального `graphify update` появляются дубли узлов с двумя префиксами путей. Лечится полной пересборкой графа на одной машине.
- В git-истории документы жили в `docs/VisitX/`; локально их переместили в `docs/PersonnelStatus/` без коммита — рабочее дерево и HEAD долго расходились (локальные файлы untracked, старые — deleted unstaged). При сверке путей нельзя доверять только HEAD или только дереву — проверять оба.
- `graphify-out/cache/` (тысячи файлов AST-кэша) не должен коммититься — кэш машинно-специфичен.

Связано: [[vaps-architecture-decisions]]
