---
name: project-native-port-security-ops
description: "Нативный порт Smart Josparlau в PersonalRecordFront (/security-ops/*) — план, состояние фаз, договорённости"
metadata: 
  node_type: memory
  type: project
  originSessionId: b33594a3-9107-4224-a980-05e1a24b9c99
---

Ветка `claude/smart-josparlau-e55`, worktree busy-almeida-b520b8. Решения Bratan (03.08.2026): **нативный порт** в стек OLD (Next.js 15, ApiClient-стиль, shadcn OLD), очередь **по прототипу** (`Прототип/*.dc.html`): Объекты → Реестр ОМ → Карточка ОМ → Командный центр → Дежурства → Календарь; данные — **host-MSW-мок** (реального бэка `/api/ops|operations/*` нет). Полный план: `/home/erda/.claude/plans/dynamic-painting-corbato.md`.

Состояние: **HEAD переезда — срез 150 (23fe9e72), ops-сюита 2252 зелёная.**

**Срезы 143–150 — «утечки живых данных в подписанный день» доведены до конца.**
Каждый следующий дефект находился ПРОВЕРКОЙ ПОСЛЕДСТВИЙ предыдущего, не сканом.

- **143 (fix):** печатный документ брал название живым, копия — замороженным;
  два артефакта одного снимка звали подразделение по-разному. Три места
  (день, строка ребёнка в сводке, шапка сводки), у каждого своя проба.
- **144 (fix):** СХЕМА 6 — штат и вакансии. Живыми они делали подписанный
  документ ВНУТРЕННЕ ПРОТИВОРЕЧИВЫМ: после удаления штатной единицы штат
  оказывался МЕНЬШЕ, чем список+вакансии.
- **145:** щит на оба артефакта разом. Сначала сравнивал только документ —
  проба показала, что тест на переименование ТИПА вакуумен (подписи типов
  документ не печатает вовсе); расширен на личную копию.
- **146 (fix):** СХЕМА 7 — «+N». Первый тест был вакуумным: подтверждение
  возврата вступает в силу СО СЛЕДУЮЩЕГО дня, живое число не двигалось.
  Двигает его отмена ноги ATTACHED. Плюс отдельный тест «замороженного нуля»
  (признак — наличие ключа, не истинность).
- **147 (fix):** щит сводки покраснел сразу — шапка брала ПОРЯДОК КОЛОНОК у
  живого справочника, хотя все строки заморожены.
- **148:** договор раскладки снимка (набор ключей каждой версии руками +
  версии подряд + поля только добавляются + все прежние поддержаны).
- **149 (fix):** эталоны печатной формы несли снимок формы схемы 1, т.е.
  охраняли ЗАПАСНУЮ ветку, а прод ходит по замороженной. Новый случай вскрыл
  АСИММЕТРИЮ БИЛДЕРА: справочник он морозил сам, а шапку/знаменатель/«+N»
  принимал разрешёнными → всякий вызывающий мимо expense_release получал
  живое. Владелец сведён в `build_expense_document`.
- **150:** гвард на СЛЕДУЮЩУЮ утечку — все читатели снимка (28) отнесены к 4
  группам, списки проверяются в обе стороны. Сработал на себе на первом прогоне.

**Итог заморозки:** схема снимка 2→7; пять помощников чтения в strength_report
(`catalog_of`, `names_of`, `title_of`, `denominator_of`, `attached_of`);
единственный владелец разрешения — `build_expense_document`.

Приёмы: **проверять последствия своей же правки** — так найдены 139, 140, 143,
147, 149. **Эталон/щит, покрывающий только запасную ветку, бесполезен.**
**Список-договор руками сильнее любого автовывода**, если проверять его в обе
стороны.

**Срезы 136–142 — две жилы, обе очень урожайные (7 дефектов из 7 срезов).**

ЖИЛА 1 — «фикстура описывает мир, которого прод не производит». Общие фикстуры
тестов расходились с `seed_status_types`, и расхождение делало проверки слепыми
ровно к тому, что они проверяют:
- **136:** seed_types и types(bulk) не заводили IN_SERVICE. Дополнение не
  уронило ни теста и позволило включить отказ на СБОРКЕ снимка при непригодном
  справочнике (хвост 135; запасной путь на чтении снят как ветка на
  несуществующее состояние).
- **137 (fix):** сид не ставил `is_placeholder` — ретро-замена заглушки была
  МЕРТВА в проде, хотя покрыта 12 тестами: их фикстура ставила признак руками.
- **138 (fix):** гвард знал DETACHED наизусть, `restricts_editing` читал только
  сериализатор (API обещал ручку, которой нет). Фикстура заводила DETACHED без
  флага. Попутно снята ранняя ветка «список пуст → выйти»: наблюдаемого
  поведения нет, зато ПРЯТАЛА литерал от пробы.

ЖИЛА 2 — досканы после заморозки справочника (срез 135). Каждый следующий
дефект находился проверкой последствий предыдущего:
- **139 (fix):** сводный документ ПАДАЛ KeyError, если дети сданы при разных
  каталогах (переименование колонки между сдачами). Лечение — объединение
  колонок в шапке + достройка ячеек нулями; порядок закреплён отдельно (первые
  пробы его не задевали).
- **140 (fix):** светофор пересчитывал снимок СЕГОДНЯШНИМИ приоритетами и
  МОЛЧАЛ о настоящем расхождении. Довод среза 135 («одна мерка на обе стороны»)
  оказался неверен и снят. Свод по дереву покрыт отдельно — это своя цепочка.
- **141 (fix):** СХЕМА 4 — подписи типов в снимок; переименование типа меняло
  уже выданную копию.
- **142 (fix):** СХЕМА 5 — название подразделения в снимок; побочно копия о
  расформированном подразделении теперь называет его, а не печатает id.

Приёмы: **три повышения схемы за партию безопасны** только с явным списком
SUPPORTED_SCHEMA_VERSIONS (`{1,SCHEMA_VERSION}` роняет среднюю) + assert на
импорте. **Ветка без наблюдаемого поведения прячет дефекты** — снимать.
**Запасной путь на состояние, которого больше не бывает, снимать вместе с
причиной.**

**Срезы 130–135 — «что означает половина пары» и заморозка подписанного дня.**
Три скана перед 130 оказались ЧИСТЫМИ (коды отказа 25 — все с raise-сайтом и
все в тестах; события журнала 20 и типы сущностей — закрыты в обе стороны;
маршруты ops 115 — непокрытых нет). Дефекты нашлись не сканом, а вопросами.

- **130 (fix):** `entity_id` принимался БЕЗ `entity_type`, а нумерация у семи
  сущностей журнала своя — «лента объекта» сливала истории семи разных
  объектов. Отличить нечем: события настоящие. Теперь 400.
- **131 (fix):** `?actor=` уезжал в фильтр пустой строкой → 200 с нулём строк.
  Журнал пустого актора НЕ ПИШЕТ, значит совпасть не могло никогда.
- **132 (fix):** у пагинации не было `max_limit` — `?limit=1000000` отдавал
  таблицу целиком (проверено: 120 из 120). Поставлен 1000 (мерка MAX_BULK_ROWS).
- **133:** заморозка сданного дня проверена ЦЕПОЧКОЙ (сдача→снимок→расход) на
  семи правках живого мира. Дефектов нет.
- **134 (fix):** использованный тип статуса удалялся, и подписанный день
  становился НЕВЫВОДИМЫМ навсегда (ValueError на незнакомом коде). Правило
  «деактивация, не удаление» было записано в модели и не держалось ничем; FK
  здесь нет → приёмник pre_delete (накрывает и queryset.delete() админки).
- **135:** СХЕМА СНИМКА 3 — справочник замерзает в снимке (`catalog`).
  До неё правка каталога переписывала подписанный день: перенос колонки и
  `counts_in_staff=false` (последнее меняет ЗНАМЕНАТЕЛЬ). Старые снимки (1,2)
  читаются живым каталогом — закреплено отдельным тестом, честно.

Приёмы: **у запрета два владельца** — селектор (единственный канал чтения) и
маршрут (перевод в 400); без второго ValueError вышел бы 500-м, у каждого свой
тест. **`SUPPORTED_SCHEMA_VERSIONS` перечислять явно**, `{1, SCHEMA_VERSION}`
роняет среднюю версию при повышении. **Отказ на СБОРКЕ снимка при непригодном
каталоге пробовал — уронил 153 теста** (фикстуры сдают дни при неполном
справочнике); это смена условия СДАЧИ, отдельная работа. Осталось открытым.
**Untracked-файл не откатить `git checkout`** — бэкап до мутации обязателен.

**Срезы 126–129 — свойства и вторые владельцы порядка.** 126–127 портированы два
непортированных набора property-тестов источника (третий, `test_roster_merge_properties.py`,
портировать НЕЛЬЗЯ: `roster_on`/`_resolve_roster` в цель не переехали вовсе;
четвёртый, snapshot-immutability, в источнике остался только `.pyc`).
`hypothesis` добавлен в requirements/development.txt.

- **126:** расход проверяет свои равенства сам, но встроенные утверждения не
  связывают ИТОГ со строками и слепы к слоту, уехавшему в чужое подразделение —
  оба случая держат только новые тесты. Дефектов не найдено.
- **127:** состояние выводится ДВАЖДЫ (@property на Python и Case/When в SQL);
  сверка была на двух строках, одной дате, без границ и без отменённых, а у
  Secondment её не было вовсе. Границы полуинтервала прибиты через `@example` —
  случайная дата попадает в них ~1/4000, то есть никогда. Расхождений нет.
- **128 (fix):** личная копия дня шла в порядке снимка (employee_id), расход — по
  канону FR-5. Два файла из ОДНОГО снимка перечисляли людей по-разному, а копию
  берут именно чтобы положить рядом с расходом. Прежнее объяснение («своей
  сортировки нет, две копии обязаны совпасть») верно против СВОЕЙ сортировки, но
  канон — не второй порядок. `test_the_order_repeats_the_snapshot` заменён на
  проверку независимости от порядка входа.
- **129:** рендерер периода на страницах с РАЗНЫМ набором колонок — случай,
  недостижимый через HTTP (справочник один на запрос), а в коде под него два
  необслуженных решения (порядок от первой страницы, недостающая колонка = 0).

Приёмы цикла: **фикстура обязана различать два порядка** (одинаковые приоритеты
у разных колонок — иначе проба «убрать тай-брейк» зелёная; id растёт, а
должность падает — иначе канон неотличим от порядка базы); **кросс-проверку
брать из построенного артефакта, а не пересчитывать своей копией правила** —
иначе тест сверяет правило с самим собой (проба B среза 128 это и показала).

**Срезы 121–125 — конкурентность несущих путей записи.** Разведка перед циклом:
N+1 у списков НЕТ (статусы 8/8, журнал 5/5, уведомления 2/2); индексы в целом
есть; настоящих проб конкурентности не было у submit/amend/bulk/dismissal/
secondment. Найдено ДВА настоящих дефекта и ОДИН тупик:

- **122 (fix):** у amend замок брался на ГОЛОВЕ цепочки версий, а голова
  ДВИЖЕТСЯ — взаимного исключения не было, и номер версии считался от строки,
  прочитанной ДО ожидания на замке. Вторая поправка получала отказ ни за что.
  Лечение: замок на строке ПЕРВОЙ версии (не движется) + перечитка головы свежим
  запросом под замком. Урок общий: под READ COMMITTED замок на строке не
  защищает от ФАНТОМА (вставки соседа).
- **123:** `create_status` не имел маршрута, а пачка обхода мягкого пересечения
  не умеет и сама отправляет «разводить поштучно через create_status» — оператор
  был в тупике. Маршрут заведён (POST /api/operations/statuses/).
- **124 (fix):** уволенному можно было завести статус: границы найма смотрят на
  dismissal_date, а уволить можно и не проставив её. Заведён код
  EMPLOYEE_NOT_EMPLOYED; гвард берёт УЖЕ ЗАГРУЖЕННУЮ карточку (запрос по id дал
  бы N+1 у пачки — два теста на число запросов покраснели сразу).

**Дважды чуть не сломал ВЕРНЫЕ решения — оба раза остановили существующие тесты
с объяснением:** (1) агрегированный отказ пачки НЕ несёт `overridable` намеренно
— у пачки обхода нет, и кнопка обещала бы несуществующее; (2) пересечение ещё не
начавшихся статусов — неблокирующее предупреждение (FR-10), а не дефект.
Правило: перед «починкой» искать тест/докстринг, объясняющий текущее поведение.

**Скан «сервисы без вызывающего» из среза 106 был ДЫРЯВ:** искал подстрокой, и
`create_status` утонул внутри `bulk_create_statuses`. Правильный скан — по точным
именам через ast (см. срез 123).

**Приём для гонок:** просто два потока НЕ сталкиваются (успевают разойтись) —
нужен `threading.Barrier`, и ставить его надо ПЕРЕД взятием замка (после —
первый займёт замок и встанет, второй до барьера не дойдёт → таймаут). Проба на
снятие ОДНОГО из двух ограничений вакуумна, если оба ведут к одному коду
(submit: `unique_ops_submission_current` + `unique_ops_submission_version`).
Уборки руками под transaction=True не нужно, а по журналу она и невозможна —
он append-only на триггере.

**Срезы 106–120.** A (106–107) два сервиса БЕЗ маршрута, найдены сканом
«сервисы без вызывающего» (таких во всём разделе ровно два):
`complete_status_early` и `extend_status` → POST `.../statuses/{id}/complete/` и
`/extend/`. B (108–109) закрытый словарь кодов отказа `error_codes.py` + проверка
в конструкторе DomainError + покрытие в обе стороны; ВТОРОЙ путь выдачи кода —
`CONSTRAINT_ERROR_MAP` в обработчике — идёт мимо DomainError, и его стережёт
только тест покрытия. C (110–113) канон порядка личного состава FR-5:
`roster_order.py`, `position_level` в снимке (СХЕМА 2), врезка в
`expense_document`, кросс-форматный тест. D (114–117) гварды: анонимный обход
всех маршрутов роутера, полнота схемы, конверт отказа по всем кодам, дисциплина
часов. E (118–120) полный порядок страничных списков, авто-покрытие запрета
Admin, сид цепочки расхода `seed_expense_chain`.

**Найдено и починено по ходу (не планировалось):**
- `strength-report/period/` отдавал в схеме «200 без тела»: при добавлении
  выгрузки ОБА `@extend_schema` съехали на `period_export`. Фронт не получал типа.
- PDF ПАДАЛ (LayoutError) на ячейке выше листа A4 — 20 имён с хвостом. Расход с
  людным нарядом не выпустился бы. Лечение: `splitByRow=1, splitInRow=1`.
- Два страничных списка без разрыва ничьей (`user-roles` по user_id,
  `temporary-duty` по -starts_at) — строки могли повторяться/пропадать.
- Запрет Admin перечислял ЗАПРЕЩЁННЫЕ модели руками: три модели выпуска
  документа туда не попали. Список перевёрнут на разрешающий.

**Вакуумные пробы, вскрытые в этом цикле:** момент в конверте отказа сверялся с
датой, совпадавшей с СЕГОДНЯШНЕЙ датой машины (подмена Clock→datetime.now не
краснила) → фикстура уведена в 2019; нормализация `details` в {} имела двух
владельцев, второй мёртв → снят; тест fail-closed гейта считал решение СВОЕЙ
копией кода → переписан на настоящий `initial()`; ожидаемый порядок в
кросс-форматном тесте был мой неверный (Ёлкин перед Дроздовым) — три формы
дружно дали правильный.

**Границы проб, записанные честно:** аноним НЕ ловит забытое право у @action
(миксин fail-closed — закреплено отдельным тестом); удаление маршрута целиком
гвард схемы не ловит; из двух списков без tie-break воспроизводится только один
(`temporary-duty`), `user-roles` Postgres на этом объёме отдаёт в физическом
порядке.

**Срезы 91–105 — четыре блока.** A (91–94) прочтение уведомлений: mark_read /
POST `.../notifications/{id}/read/` / read-all с границей `until` / `?unread` +
`unread-count`. Это ДОСТРОЙКА, не порт: писателя у `read_at` не было ни здесь, ни
в источнике (11.4 была только UI). B (95–97) расход за ПЕРИОД: `expense_period.py`
(страница на дату, MAX_PERIOD_DAYS=62, гвард будущего В СЕРВИСЕ), маршруты
`strength-report/period/` и `period-export/` (свой .csv «строка на дату», без
«ИТОГО» — сложить расходы разных дней не во что). C (98–99) четвёртый формат
.pdf на **reportlab** (не fpdf2 как в источнике — reportlab уже в зависимостях);
шрифты Liberation Serif вендорены в `apps/operations/fonts/` вместе с лицензией;
pypdf добавлен в requirements/development.txt. D (100–103) эталон печатной формы:
`docx_fingerprint.py`, `golden.py`, 3 случая в `tests/golden/`, команда
`update_golden [--case] [--check]`, плюс конкурентный выпуск на потоках.
E (104–105) команды `seed_lagging_probe` и `check_document_integrity`.

**Пять раз пробы опровергли то, что я написал в комментариях** — это главный урок
цикла, и все пять исправлены В КОДЕ, а не замолчаны:
1. reportlab + core-шрифт: кириллица НЕ выпадает целиком, отказ ПОЛОВИНЧАТЫЙ —
   текстовый слой цел (`extract_text` даёт «Управление кадров»), а рисуется
   «■■■■■». Следствие: тесты чтения текста подмену шрифта НЕ ловят, ловят только
   проверки вложенного шрифта (`/FontFile2` в дескрипторе).
2. Повторная регистрация TTF у reportlab безвредна — «сломала бы каждый второй
   документ» было неправдой.
3. C14N на выходе python-docx недоказуема (снятие ничего не краснит) → нужен тест
   МИМО генератора на двух записях одной разметки.
4. Замок головы сдачи НЕ предотвращает дубль выпуска (это делает частичная
   уникальность); он отвечает за то, что проигравший получает 409, а не
   IntegrityError→500.
5. `seed_lagging_probe` с целью «вчера» (как в источнике) даёт ДВА уведомления:
   горизонт эмиттера от его «сегодня», план (знак, горизонт]. Цель = сегодня,
   настраиваемый `--day` убран вовсе — ручка не могла удержать обещание.

Прочее: два теста «неизвестный формат» использовали в качестве примера именно
`pdf` и сломались от его появления — сюита сама показала оба места.
`test_audit_coverage` краснеет на каждое новое событие словаря (это его работа).
Окно сдачи не принимает прошлое: сеять прошлые дни только со сдвинутыми часами
(`at=MORNING - timedelta(days=N)`).

**Прогон теперь на СОБСТВЕННОМ .venv проекта**: `Backend/PersonnelStatus/
Personnel-Records/.venv/bin/python` (django 5.1.15, channels 4.3.2, pytest 9.0.3,
reportlab 4.5.1, pypdf) — он и был тем интерпретатором, чьи .pyc лежали в дереве;
`find -maxdepth 7` его не находил. `/home/erda/Музыка/PR/.venv` тоже гоняет сюиту
зелёной (django 5.2.4), но проектный вернее.

**Срезы 76–90 — подсистема выпуска официального документа расхода** (порт
`Backend/VAPS/apps/documents/*` + `document_release_service.py`; в приёмнике всё
живёт ВНУТРИ `apps/operations`, отдельного приложения не заводили — писатель у
таблиц один). 76 модель `OpsAttachment` (`ops_attachments`), 77 приватное
хранилище (`OPS_PRIVATE_STORAGE_ROOT` вне MEDIA_ROOT, `OPS_XACCEL_*`), 78
`create_attachment`, 79 `OpsAttachmentSelector`, 80 `OpsDocumentSequence`, 81
`allocate_number`, 82 `OpsIssuedDocument`, 83 `OpsIssuedDocumentSelector.current`,
84 `issue_expense_document`, 85 `reissue_expense_document`, 86 маршруты
release/reissue, 87 `verify_integrity`, 88 `prepare_download`, 89 маршрут
скачивания `/attachments/{id}/download/`, 90 реестр `/documents/`.

Ключевые решения, не выводимые из кода: имя файла на диске — отдельное поле
`storage_key` (UUID, default в питоне), потому что байты обязаны лечь ДО вставки
строки, а целый pk выдаёт база; нумерация исходящих — обычное целое под
`select_for_update`, а НЕ SEQUENCE (nextval не откатывается → дырка = утрата
документа); порядок замков «голова сдачи → счётчик»; при замене отзыв прежнего
идёт ПЕРВЫМ, иначе частичная уникальность немедленно отвергает вставку; область
скачивания выводится из ВЫПУСКА (вложение не знает подразделения), вложение без
выпуска — 404; `storage_key` наружу не отдаётся ни одним маршрутом.

Вакуумные пробы, вскрытые красными пробами в этом цикле (все закрыты): порядок
«байты→строка» неотличим по результату — нужен ассерт из сигнала `post_save`;
год документа под часами того же года неотличим от года выпуска — часы сдвинуты
в следующий год; версия сдачи, равная 1, неотличима от константы — день сначала
поправлен; `bool` в разбор идентификатора уже отсекает `str()`, а в разбор года
— диапазон, оба лишних гварда сняты (дублирующий владелец = вакуумная проба);
тест на pk=1 требует ЯВНОГО pk=1 в фикстуре.

`test_audit_coverage.py::test_every_declared_action_is_actually_written` краснеет
на КАЖДОЕ новое событие словаря — это его работа; врезка вызова его зеленит
(потребовал досеять `IN_SERVICE`, которого нет в `seed_types`).

Окружение прогона приёмника: интерпретатор `/home/erda/Музыка/PR/.venv/bin/python`
(django 5.2.4, channels 4.3.1; в `Backend/VAPS/.venv` каналов НЕТ), из
`Backend/PersonnelStatus/Personnel-Records`, Postgres на 5434. ruff — только из
`Backend/VAPS/.venv/bin/ruff`; в `api/serializers.py:295` и `api/views.py:1553`
E501 ПРЕД-СУЩЕСТВУЮЩИЕ, в `tests/` F811 — идиома фикстур pytest, не регресс. Срез 75 — выключатель WS: `OPS_WS_ENABLED` (имя под OPS_, не VAPS_) в config/settings/base.py, дефолт '1' со сравнением == '1' (без него в настройке строка, а '0' истинна — выключатель молча не выключает). Гвард первой строкой _publish ДО get_channel_layer (соединение к Redis открывается на ПОЛУЧЕНИИ слоя → проба считает получения, не отправки). В потребителе ИДЕНТИЧНОСТЬ первой, флаг вторым (иначе выключенный WS принимает анонимов = дыра доступа); закрытие 4503 ПОСЛЕ accept(), в отличие от 4403 — close до accept даёт HTTP 403 и браузеру 1006/не чисто, приватный код до провода не доезжает. Срез 74 — публикация из notify() в сокет: _publish() в notify_service.py, регистрируется transaction.on_commit(partial(...)) ТОЛЬКО при created. Свой try/except у _publish обязателен обеими ветками (в транзакции обработчик вне стека notify(); в autocommit — внутри его try, и проглоченный там сбой вернул бы None, который догон читает как «не легло» → откат дня). Конверт печатает моменты в поясе проекта (astimezone, не голый isoformat) и полностью равен проекции OpsNotificationSerializer (сериализатор в сервис НЕ импортируется, договор держит тест). Группа — от ХРАНИМОГО recipient. Оснастка тестов: sync_to_async(thread_sensitive) островок под django_db(transaction=True) — atomic там настоящая, on_commit исполняется по-настоящему, захват обработчиков не нужен; островок сам прибирает строки.  ОСТАТОК ПЕРЕЕЗДА (сверено грепом по цели 06.08.2026, ~15–21 срез, после среза 75): (1) apps/documents ЦЕЛИКОМ — Attachment/DocumentSequence/IssuedDocument, create_attachment/allocate_number/verify_integrity/prepare_download, AttachmentViewSet, плюс submissions/services/document_release_service.issue_expense_document (в цели expense_release.py умеет только СОБРАТЬ и отрисовать документ, не ВЫДАТЬ его с номером, вложением и аудитом) — ~10–14; (2) expense_read_service — derive_period/report_data_horizon/assert_report_date_has_data (период расхода date_from..date_to) — 2–3; (4) scope_gate.ensure_division_scope — проверить, покрыт ли RequirePermissionMixin'ом, 0–1. НЕ ПЕРЕЕЗЖАЮТ: apps/parallel_run, apps/migration_legacy, submissions/golden.py + golden_update, seed_e2e_*, apps/core (в старом проекте своё родное). ОСТОРОЖНО с инвентаризацией сабагентом: агент 06.08 записал в остаток уже переехавшее (ws_auth/ws_groups/ws_consumers/asgi/ленту/audit-logs) и насчитал 26–36 — сверять каждую позицию грепом по цели. Срез 73 — WS-транспорт ленты: /ws/operations/notifications/ (ws_groups.py + ws_auth.py + ws_consumers.py + ws_routing.py, врезка в config/asgi.py). ГРУППА ТОЛЬКО ИЗ ТОКЕНА (инвариант «своя лента» на WS; проба: клиент кричит чужой id во все параметры при своём токене — код закрытия тут не улика, отвечает только доставка). Правила токена НЕ свои: JWTAuthentication.get_validated_token + get_user — вторая половина ОБЯЗАТЕЛЬНА, иначе сокет уволенного живёт 8 часов (жизнь access-токена), а HTTP отказал бы сразу. Токен в СТРОКЕ ЗАПРОСА (браузерный WebSocket не ставит заголовки). Отказ 4403 ДО accept(). Имя группы — sha256-хэш (channels валидирует ^[a-zA-Z\d\-_.]+$, потолок 100). NOTIFY_MESSAGE_TYPE='ops.notify.message' ↔ метод ops_notify_message (точки→подчёркивания; разъезд = падение в проде на первом уведомлении, поймано красной пробой). Своё пространство /ws/operations/, старый /ws/notifications/ НЕ тронут; вскрыт пред-существующий дефект старого потребителя: отказанное рукопожатие роняет его disconnect (AttributeError user_group, notifications/consumers.py:20) — не чинилось. ⚠️ РЕЦЕПТ WS-ТЕСТОВ В ЭТОМ ЧЕКАУТЕ: pytest-asyncio и daphne НЕТ; корутина через async_to_sync + свой WsCommunicator поверх asgiref.testing; посев ТОЛЬКО фикстурами (ORM из корутины → SynchronousOnlyOperation); тесты рукопожатия ТРАНЗАКЦИОННЫЕ (django_db(transaction=True)) — под обычным django_db close_old_connections внутри database_sync_to_async закрывает соединение тестовой транзакции («the connection is closed»); autouse-фикстура сбрасывает channel_layers.backends. Kill-switch WS (11.5 источника) НЕ портирован — отдельный срез. Публикация из notify() (WS-сигнал on_commit, 11.2 источника) — СЛЕДУЮЩИЙ срез.  Срез 72 — GET /api/operations/notifications/ (NotificationViewSet): гейт = АУТЕНТИФИКАЦИЯ, не код права (как my-permissions; вопрос не «кому можно», а ЧЬИ — отвечает фильтр получателя в селекторе); чужое уведомление → пустая СТРАНИЦА, не 403; только list и только GET, держит ОТСУТСТВИЕ действий (405 раньше 403); since разбирает общий _parse_datetime_param (отвергает НАИВНОЕ время, в отличие от DateTimeField сериализатора в источнике). Старые /api/notifications/ старого проекта НЕ тронуты. НАХОДКА ПРО СХЕМУ: у списков ОМ страничная обёртка объявлена ДВАЖДЫ — ручной paginated_response_schema() И автовывод spectacular из pagination_class; проба (в т.ч. на audit-logs) даёт байт-в-байт ту же схему при снятии ручного вызова → ни один из двух не удержан тестами. limit/offset в схему даёт ТОЛЬКО pagination_class. У ленты ручного вызова нет (один владелец), схемный тест пинит limit/offset. Уборка у соседей вынесена в отдельную задачу (chip task_d1ab6499). Срез 71 — OpsNotificationSelector.list(actor, since=): ЛИЧНАЯ лента, фильтр по получателю БЕЗУСЛОВЕН (он и есть разграничение доступа — в отличие от плоского журнала, где audit.view видит всё); since — СТРОГАЯ нижняя граница created_at (нестрогая возвращала бы последнюю виденную при каждом опросе); порядок (-created_at, id) — id ВОЗРАСТАЮЩИЙ (не -id как у журнала), потому что так лежит индекс ленты из среза 67. ВАЖНО: поведенческий ассерт разрыва ничьей ВАКУУМЕН — со снятым вторым ключом остаётся зелёным, EXPLAIN даёт Index Scan по idx_ops_notif_recency, порядок по id обеспечивает ИНДЕКС, а не запрос; единственный гвард — буквальный пин query.order_by. Отклонение от источника: actor.strip() при чтении (notify() обрезает при записи; иначе писали одному, а читает другой, и пустая лента неотличима от «нет уведомлений»). Пустой/не-str actor → ValueError. API ленты (viewset+сериализаторы+маршрут, self-scope, LimitOffset 50/200, GET-only) — СЛЕДУЮЩИЙ срез. NB: Celery-обёртки (12.6) в источнике НЕ существует — портировать нечего. Ещё не портировано из источника (список ВЫВЕРЕН на срезе 73): golden/golden_update, публикация в WS из notify() + kill-switch, expense_pdf, parallel_run, migration_legacy (donor_diff/import_donor_slice), seed_e2e_lagging. ОТМЕНА прежней записи: scope_gate, amendment_service и amendment_hook портированы давно под другими именами — _assert_division_in_scope в api/views.py, day_submission_service.amend_day, amendment_enforcement.py; сверять по ИМЕНИ ФУНКЦИИ, не файла. WS-транспорт (consumers/routing/groups) закрыт срезом 73. Срез 70 — команда `check_lagging_submissions` (зеркало команды среза 66): Celery не импортируется; занятый замок и ХОЛОСТОЙ ПРОХОД — успех, не ошибка (у этой работы контрольный час, поэтому утренний прогон штатно ничего не находит — самый частый запуск); остановка — CommandError (ненулевой выход, планировщик смотрит на код возврата); --today из будущего отсекается НА ВХОДЕ (отравленный знак чинится только правкой БД), «сегодня» законно; неразбираемая дата → CommandError с форматом; в успешной строке печатается «получателей охвачено» — notified_count считает ПОЛУЧАТЕЛЕЙ и засчитывает повторно пройденный день, «разослано» было бы враньём. +12 тестов, 7 красных проб. Следующий срез — обёртка Celery-задачи и расписание (12.6 в источнике). Срез 69 — lagging_check.check_lagging_submissions: свой знак «lagging_submissions» и свой замок 0x5641474C (НЕ ключи догона эффектов); КОНТРОЛЬНЫЙ ЧАС (день проверяется только после дедлайна, граница строгая; до него горизонт = вчера — иначе знак уедет за сегодня и вечерний прогон день не переспросит); часы назад ловятся по НАСТОЯЩЕЙ дате, не по горизонту (тот отстаёт по построению → ложная тревога каждое утро), плюс ранний выход ДО catchup_plan (он записал бы ту же тревогу сам — ассертится МОЛЧАНИЕ журнала); None от notify() → LaggingNotifyError (здесь уведомление и ЕСТЬ операция, знак не должен уехать за несообщённый день); группировка отставших по получателю (ключ «одно на день» по получателю — иначе второе уведомление тому же человеку просто не запишется); отставший без получателя → warning+пропуск; обход блокировки завтра уведомление НЕ отменяет; заведение знака со ВЧЕРА и по настоящим часам (не по параметру today). Попутно Clock.local_now() (три прежних копии _local_tz() в разделе НЕ тронуты) и id в payload ЦЕЛЫЕ, не строки. Команда запуска — следующий срез (как 66 после 65). Срез 68 — справочник получателей уведомлений: модель OpsDivisionNotifyRecipient (ops_division_notify_recipients, миграция 0013, division_id ЦЕЛОЕ+unique — один получатель на подразделение, иначе «одно на день» держится по получателю, а не по факту) + поле default_notify_recipient на OpsSubmissionControlSettings (общий дежурный; CHECK: пусто законно, «   » нет) + NotifyRecipientSelector.resolve_many (свой → дежурный → ОТСУТСТВИЕ КЛЮЧА; пачкой: 1 запрос справочника + 1 чтение настроек; вход материализуется, None отбрасываются, получатель обрезается) + регистрация справочника в Admin (поиск ТОЛЬКО по recipient: LIKE по int Postgres не умеет). Приведения ключей к строке из источника НЕТ — id целые. Следующий срез — lagging_check (детект отставших + рассылка). Срез 67 — модель OpsNotification (ops_notifications, миграция 0012; имя `notifications` в старом проекте ЗАНЯТО) + notify_service.notify: «одно на день» unique(получатель,вид,дата), побеждает ПЕРВЫЙ payload, получатель строкой и .strip(), запись СИНХРОННАЯ в транзакции вызывающего, инфра-сбой глотается (None), пустой получатель — ValueError. WS-доставки нет (отдельный срез). ⚠️ Вызывающему, у кого уведомление и ЕСТЬ операция (догон отставших), None надо превращать в ошибку. Срезы 64–66 положили ОПОРУ ДОГОНА: 64 (7c0cadc) — модель OpsWatermark (ops_watermarks, миграция 0011, CHECK на непустой ключ — отличие от источника) + шлюз watermark.py (get_or_bootstrap / advance РАЗДЕЛЬНО: общий update_or_create съедал бы пропущенные дни) + locks.py (СЕАНСОВЫЙ pg_advisory_lock — транзакционный отпустил бы на первом подённом коммите); 65 (be45926) — catch_up.materialize_status_effects (реестр EFFECT_MATERIALIZERS ПУСТ намеренно: знак должен двигаться сейчас, иначе первый эффект получит годовой разрыв); 66 (637b7dc) — команда materialize_status_effects (--today только в прошлое, остановка = ненулевой выход, занятый замок = НЕ ошибка). Ещё НЕ портировано из Backend/VAPS: lagging_check + apps.notifications (уведомления отстающим), golden/parallel-run, apps.documents (реестр выпуска, expense_pdf), Celery-обёртки. Прогон: из Backend/PersonnelStatus/Personnel-Records: `/home/erda/Музыка/PR/.venv/bin/python -m pytest organization_management/apps/operations -q` (СВОЕГО .venv у Personnel-Records нет; venv из Backend/VAPS не годится — там нет corsheaders) (psycopg3, ruff/линтера нет — только pyflakes). Прежнее состояние: **ПЕРЕЕЗД НОВОГО БЭКА В СТАРЫЙ НАЧАТ (58a2d83, срез 1)** — стратегия Bratan (04.08.2026): Backend/VAPS переписывается в Personnel-Records по кускам под идиомы старого, старую логику не ломать, НО авторизацию МОЖНО менять — роли/права целевые из нового. Срез 1: organization_management/apps/operations = ops-RBAC (таблицы ops_* тождественны источнику; PermissionService/RoleAdminService дословно; Clock+override; DivisionTreeSelector по СТАРОМУ mptt-дереву int-pk), /api/operations/my-permissions/ отдаёт новый RBAC (superuser-шортката НЕТ — admin через роль ADMIN), seed_operations --assign user:ROLE[:div] (сеяно: admin→ADMIN, erda→DIVISION_OPERATOR). Отличия от источника: user_id=str(User.pk), плоские int-ссылки scope/employee (без FK намеренно). 17 тестов зелёные; в старой сюите 6 failed+1 error ПРЕД-существующие (проверено чистым деревом). **Срез 4 (4f0d6a3) — СТЕНД ТЕПЕРЬ POSTGRES**: решения Bratan — новая таблица статусов РЯДОМ со старой (strangler) + стенд на PG. `settings/local_postgres.py` (контейнер vaps-db-5434, база personnel_records, драйвер psycopg3 доставлен в .venv; данные старого sqlite перенесены dumpdata/loaddata), `settings/test.py` тоже PG и МИГРАЦИИ ВКЛЮЧЕНЫ (DisableMigrations убран — иначе нет BtreeGistExtension). Запуск: `DJANGO_SETTINGS_MODULE=organization_management.config.settings.local_postgres .venv/bin/python manage.py runserver 0.0.0.0:8100 --noreload`. Портированы: OpsEmployeeStatus (ops_employee_statuses, выводимое состояние, полуинтервалы, ExclusionConstraint+GiST+GeneratedField), conflict_matrix дословно, status_service.create_status/cancel_status, ops_exception_handler (конверт ТОЛЬКО для ошибок раздела — чужие ответы DRF не переформатируются, иначе сломались бы старые экраны). Не портировано: update/complete_early/extend, bulk, догон, увольнение, прикомандирование, расход, аудит мутаций. 82→117 тестов; старая сюита на PG даёт те же 6 failed+1 error, что и на SQLite. **Срез 5 (30384f0)** — bulk_status_service.bulk_create_statuses: массовое создание статусов, всё-или-ничего, контракт «нет N+1» сохранён (create_status в цикле НЕ зовётся; 4 запроса + bulk_create, тест сравнивает 5 vs 50 строк, красная проба даёт 13 vs 58). Отличие от источника: подразделение сотрудника берётся из StaffUnit.employee (у старого Employee нет division_id!), сотрудник БЕЗ штатной единицы → 403 fail-closed. Аудит и override в пачке не портированы. +22 теста, ops-сюита 109. **Срез 6 (777ff27)** — POST /api/operations/statuses/bulk/: StatusViewSet(RequirePermissionMixin) под status.manage, только POST (GET→405), область = PermissionService.visible_division_ids, None(wildcard)→новый DivisionTreeSelector.all_ids(), актор из resolve_actor_id (не из тела), BulkStatusCreateSerializer с MAX_BULK_ROWS=1000. Конверт 409 пачки НЕ overridable намеренно (обхода в массовом пути нет). ⚠️ 403 гейта и 403 области различаются ФОРМОЙ: гейт → DRF {detail}, сервис → конверт {error_code} — тест гейта без этого различения вакуумен. Живьём 201/422/409/400/404/405 прожиты. +14 тестов, ops-сюита 123. **Срез 7 (2c8267f)** — расход/строевая записка + GET /api/operations/strength-report/ (status.view, only GET). Bratan 04.08.2026: «внутри раздела меняй под себя, старую логику не трогай» → два отличия от источника: (1) приоритеты/колонки/counts_in_staff читаются из САМОГО справочника ops_status_types (StatusCatalog), литеральных дублей источника нет; порядок колонок выводится по мин. приоритету, колонка без типов исчезает; (2) знаменатель — одна таблица staff_units: Штат=слоты, Список=слоты с РАБОТАЮЩИМ минус прикомандированные, Вакансии=пустые+занятые уволенным (warning dismissed_in_slot), инвариант Штат=Список+Прикоманд.+Вакансии по построению. Историчности штата НЕТ (статусы на дату, штат сегодняшний) — записано в докстринге. Сотрудник без слота в расход не входит. Селекторы: catalog_rows/overlapping_on/slots_with_working_occupants/names_map, ≤6 запросов. +30 тестов, ops-сюита 153. **Срез 8 (730d4aa)** — update_status (правка интервала и метаданных) + общая преамбула `_lock_for_edit`: блокировка сотрудника → SELECT FOR UPDATE строки + перечитка → гард source=USER → гард «отменённая терминальна» (единственный владелец). Этим ЗАКРЫТА дыра ретро E3: в источнике update_status без перечитки и без cancelled_at-гарда. cancel_status переведён на ту же преамбулу (её PLANNED-проверка теперь владеет ровно ACTIVE/COMPLETED; попутно отмена строки проекции → 422, пропавшая строка → 404). Перепроверка интервала навешана на СМЕНУ ДАТЫ, не на факт правки; себя исключаем из периметра (exclude_pk). Возвращается ПЕРЕЧИТАННАЯ строка — переданный объект не мутируется. ⚠️ мягкое пересечение с НЕ НАЧАВШИМСЯ соседом матрица понижает до предупреждения — тест 409 с planned-соседом вакуумен. +16 тестов, ops-сюита 169. **Срез 9 (c91a1ee)** — PATCH /api/operations/statuses/{id}/: поверхность StatusViewSet = POST bulk + PATCH detail (PUT/DELETE/GET → 405 намеренно). Две добавки против молчаливого успеха DRF: пустое тело → 400 и ЯВНЫЙ отказ на неизменяемых полях (employee_id/status_type_code/source/source_ref/cancelled_*) → 400 с указанием поля (DRF молча глотает неизвестные ключи → клиент получил бы 200 на несделанную смену типа). Область — новый общий `StaffUnitSelector.divisions_of`, на него переведён и `bulk_status_service._divisions_of` (одно правило «без слота = вне области» на оба пути). pk коэрцится в int до запроса (мусор → 404, не 500). Ответ — `OpsEmployeeStatusSerializer` с выводимым state. Тип параметра пути объявлен явно, иначе spectacular даёт 4-е предупреждение (базовая линия 3/3). +23 теста, ops-сюита 192. Живая проба велась на ОТДЕЛЬНОМ процессе :8101 — стенд Bratan на :8100 не трогался, пробная строка удалена. **Срез 10 (8fb483d)** — POST /api/operations/statuses/{id}/cancel/: отдельное действие, а НЕ DELETE (строка не удаляется — отмена это факт, после неё период свободен). Причина обязательна, владелец проверки — сериализатор; ⚠️ у cancel_status свой такой же гард, и с allow_blank=True ответ ВСЁ РАВНО 400 (конвертом сервиса) — тест только по коду 400 вакуумен, нужен ассерт на ключ `reason` в теле. cancelled_by из токена (тело с cancelled_by=999 игнорируется). Общий `_get_status_or_404` (коэрция pk → 404 вместо 500) переиспользован правкой. +19 тестов, ops-сюита 211, схема 3/3. **Срез 11 (3b3ecb9)** — чтение статусов GET /api/operations/statuses/ и /{id}/: право чтения = **status.view**, НЕ status.manage (гейт расхода; проверено с обеих сторон — manage-only списка не получает, view-only получает). Резолв области вынесен в общий `_resolve_division_scope`, расход переведён на него же (чужой division_id → 403 {detail}, не пустой список). Область СПИСКА — фильтр по новому `StaffUnitSelector.employee_ids_in(scope)`; область ОДИНОЧНОГО чтения — `_assert_status_in_scope` с правом-ПАРАМЕТРОМ (зашитое status.manage дало бы читателю 403 на всё). Фильтры: business_date (period__contains, полуинтервал), division_id, employee_id, status_type_code, include_cancelled (строгий разбор: "yes" → 400). Отменённые скрыты в списке, но по прямому адресу отдаются. Порядок пинится во вьюхе (-date_start, employee_id, id) — у модели ordering нет, страничная выдача без него теряет строки. ⚠️ **Схема страницы у plain ViewSet врёт по умолчанию**: spectacular описывает paginated-ответ голым массивом → добавлен `paginated_response_schema(...)` с many=False на КЛАССЕ; у UserRoleViewSet/TemporaryDutyViewSet этот дефект ОСТАЛСЯ (пред-существующий). Тест поверхности правки перестал ждать 405 на GET detail. +29 тестов, ops-сюита 239, схема 3/3 (база). **Срез 12 (b561031)** — откомандирование: модель `Secondment` (ops_status_secondments, миграция 0004) + `secondment_service.initiate_secondment` — пара DETACHED (дома «по списку») + ATTACHED («+N» у принимающего) + связь, ОДНОЙ транзакцией. ⚠️ **Урок владения замком**: с вложенным `with transaction.atomic()` вокруг вставок снятие внешнего декоратора НЕ роняло ни одного теста (дублирующий гард → вакуумная проба) — savepoint убран, владелец атомарности один, проба красная. Вторая нога проверяется ПОСЛЕ записи первой — несущая проба COMPATIBLE_PAIRS (обнуление роняет 9 тестов). Отличия от источника: штатное подразделение из StaffUnit и пишется СНИМКОМ (перевод по штату не переписывает «откуда»), без слота → 422. Инварианты на DB: CHECK divisions_differ + legs_differ, ноги PROTECT. Мягкое пересечение → 409 БЕЗ обхода (откомандирование не продавливают). НЕ портировано: возврат (request/confirm), эндпоинт, проекция «+N» в расход (отложена и в источнике). +20 тестов, ops-сюита 259. **Срез 13 (3f0483b)** — `complete_status_early`: закрытие ИДУЩЕЙ строки фактической датой (не начавшаяся → отмена, завершённая → уже закрыта, факт не в будущем, интервал непустой). Исправлен дефект источника: там гарды с блокированной строки, а save() на ПЕРЕДАННОМ объекте (устаревшая копия переписала бы свежие факты). Гард FR-16 СУЖЕН (в источнике снят вовсе): не действует только на ноги DETACHED/ATTACHED. +12 тестов. **Срез 14 (237b0c3)** — возврат из прикомандирования: `request_return`/`confirm_return` + 4 колонки фактов (миграция 0005). ⚠️ **Расхождение с источником**: возврат вступает в силу СО СЛЕДУЮЩЕГО дня (`_end_leg_tomorrow`), а не закрывает ноги сегодняшним — иначе переписывается сданный за сегодня расход, а ногу, начавшуюся сегодня, вообще нельзя закрыть (пустой полуинтервал; в источнике это 422-тупик). Следствие: замок FR-16 снимает РЕШЕНИЕ, а не календарь — `assert_employee_status_editable` исключает ноги с `return_confirmed_at` (иначе до полуночи вернувшемуся нельзя поставить наряд). Закрытие ноги — своя запись поверх `_lock_for_edit`: не complete_status_early (гард «факт не в будущем») и не update_status (его гард откомандированного заблокировал бы снятие ограничивающей строки; ослаблять операторский PATCH ради внутренней нужды нельзя). CHECK на DB: факт целиком (пустая строка закрыта явно), подтверждение только после запроса. +22 теста, ops-сюита 292. **Срез 15 (8516595)** — эндпоинты прикомандирования: POST /api/operations/secondments/ + /{id}/request-return/ + /{id}/confirm-return/ под status.manage (новых кодов прав не заводил — каталог закрытый мир). ⚠️ **Область видимости у трёх действий РАЗНАЯ**: откомандирование и запрос возврата гейтятся стороной «откуда» (from_division_id), подтверждение — стороной «куда» (to_division_id); общая проверка сделала бы обе стороны одним лицом. Сотрудник без слота: скоупованному 403 областью, безскоуповому 422 сервисом. Маршрута /secondments/{id}/ НЕТ (чтение пар — отдельный срез) → прямой GET даёт 404 маршрутизации, а не 405; корень существует и на прочие методы отвечает 405. +36 тестов, ops-сюита 328, схема 3/3 (база). **Срез 16 (0019ef2)** — чтение пар: GET /api/operations/secondments/ и /{id}/ под status.view. Область чтения — ОБЕ стороны (Q(from∈scope)|Q(to∈scope)): пара общее дело двух подразделений. Стадия рукопожатия (INITIATED/RETURN_REQUESTED/RETURNED) выводится: один канон для @property и аннотации `with_state()`, ею же и фильтруют (второй набор условий разошёлся бы с полем в строке). Фильтры: employee_id, state (закрытый список, мусор → 400), division_id по любой стороне. PUT/DELETE → 405 (пару не переписывают; «правка» = возврат). +15 тестов, ops-сюита 339. ⚠️ Параллельно шла задача-чип «paginated schema lie» (UserRole/TemporaryDuty list) в ОТДЕЛЬНОМ worktree — она тоже трогает api/views.py, при слиянии смотреть конфликт. **Срез 17 (5ebde4e)** — проекция «+N» в расход (в источнике была ОТЛОЖЕНА, здесь закрыта): `SecondmentSelector.attached_counts_on` по стороне «куда», один запрос, сужается тем же множеством, что и слоты. ⚠️ **Поле `attached` сменило смысл — разведены два числа**: `off_list` (наш слот занят нашим человеком вне списка, В знаменателе) и `attached` («+N» чужого штата, СВЕРХ знаменателя). Инвариант теперь `Штат == Список + Вне списка + Вакансии`, прикомандированные — добавка; в колонки не попадают. Считается ЖИВАЯ НОГА ATTACHED на дату, НЕ return_confirmed_at (иначе второе правило о том же разошлось бы с срезом 14 на день возврата). Подразделение только с приданными теперь появляется строкой. Ответ /strength-report/ несёт оба поля. +8 тестов, ops-сюита 348. **ЛИНИЯ ПРИКОМАНДИРОВАНИЯ ЗАКРЫТА** (срезы 12–17). **Срез 18 (a029fec)** — увольнение, сторона раздела: `dismissal.close_statuses_on_dismissal(employee_id, dismissal_date, actor)`. Накрывающий статус усекается до D (D — верхняя граница найма в том же смысле, что и у `_validate_interval`), не начавшийся отменяется с причиной, законченный не трогается; пара штампуется закрытой системно — ОБА факта рукопожатия (одинокое подтверждение не примет CHECK базы). ⚠️ Порт НЕ вызывается из старого пути увольнения (employees/api/views.py `dismiss` — тонкий экшен, хука нет): wiring — отдельное решение Bratan. ⚠️ Урок: первая версия шла ДВУМЯ выборками с select_for_update по одной таблице → проба блокировки была вакуумной (зеленела от второй); сведено в ОДИН проход, теперь мутация краснеет. +16 тестов, ops-сюита 364. **Срез 19 (4abf80d)** — врезка раздела в увольнение СИГНАЛОМ (решение Bratan). ⚠️ **Старый `dismiss`-экшен МЁРТВ**: `api/employees/` в config/urls.py закомментирован, EmployeeViewSet больше нигде не регистрируется — врезка туда не исполнилась бы ни разу. Увольнение идёт через сохранение карточки, и у старого раздела на это уже есть свой pre_save-приёмник (apps/statuses/signals.py). Свой приёмник в apps/operations/signals.py: pre_save запоминает переход (FIRED или появление dismissal_date), post_save зовёт `close_statuses_on_dismissal`; пустая дата → Clock.today_local() раздела; актор — нечисловая метка `"system:dismissal"`. Подключён через `OperationsConfig.ready()`. ATOMIC_REQUESTS в проекте НЕТ → атомарность только когда вызывающий сам обернул save в транзакцию; порядок (карточка → закрытие) выбран осознанно. Убраны 2 дублирующих гарда (`created`, пустой pk) — владелец один: «старой строки нет». +9 тестов, ops-сюита 373; старая сюита поимённо та же (7 failed + 1 error). **Срез 20 (58536bc)** — журнал раздела: `OpsAuditLog` (ops_audit_logs, миграция 0006) + `audit_service.record()` как ЕДИНСТВЕННАЯ точка записи. Решение Bratan: свой журнал, не старый `audit.AuditLog` (там актор = FK на User, HTTP-образный список действий, и его уже наполняет middleware на каждый /api/ — было бы две записи об одной мутации). Закрытый словарь событий+типов сущностей проверяется НА ЗАПИСИ (отличие от источника). Время — Clock раздела, без auto_now_add. Append-only держит ТРИГГЕР БД (BEFORE UPDATE OR DELETE, строчный — TRUNCATE не задевает). Полей request_id/ip/user_agent нет (request-контекста в старом проекте не существует). ⚠️ Две ловушки, пойманные пробами: ассерт времени по `.date()` краснеет в +05 (сравнивать замороженный МОМЕНТ); правило «record не открывает свою транзакцию» НЕДОКАЗУЕМО в Django (вложенный atomic откатывается с внешним) — заменено на проверяемое «запись синхронна, не через on_commit». Врезка в мутации — следующий срез. +15 тестов, ops-сюита 388. **Срез 21 (fd0bd6d)** — врезка `record(...)` в мутации: создание/правка/досрочное закрытие/отмена статуса, пачка, обе ноги и пара прикомандирования, увольнение. **ПРАВИЛО ПОКРЫТИЯ: событие на каждую записанную СТРОКУ, а не на «операцию»** — главный разрез журнала это entity_type+entity_id, и при «одной строке на операцию» лента конкретного статуса пуста у всего, что создано не поштучно (ноги пары, вся утренняя пачка). Пара и увольнение пишут свою строку СВЕРХ построчных. **СНЯТ `STATUSES_BULK_CREATED`**: entity_id NOT NULL int, а у «пачки» своего идентификатора нет; пачка видна как N строк одного актора с ОДНИМ временем. Добавлен `record_many` (одна вставка — иначе рушится контракт постоянного числа запросов массового пути; проба сравнивает ДВА размера, не абсолют). ⚠️ **Две вакуумные пробы, пойманные и переписанные**: «пачка легла одним временем» зеленела под `clock.override` (замороженные часы отдают один момент на КАЖДЫЙ вызов — построчная отметка прошла бы незамеченной) → разведено на замороженные (источник времени) и ЖИВЫЕ часы (единственность момента); «откат уносит строку» зеленел бы и у отложенной записи (on_commit под django_db не выполняется вовсе) → добавлена половина «событие видно ДО коммита». ⚠️ **Расположение record() внутри savepoint create_status НЕ закреплено тестом и закрепить нечем** — под django_db всё в одной транзакции, вынос за savepoint остаётся зелёным (проверено); держится доводом об автокоммите, помечено в коде. Изоляция тестов — отметкой последнего pk, НЕ очисткой: append-only триггер отвергает DELETE (первая версия тестов на нём и упала). +19 тестов, ops-сюита 409. **Срез 22 (48c92c1)** — чтение журнала: `GET /api/operations/audit-logs/` и `/{id}/` под правом **`audit.view`** (уже было в каталоге seed_operations — ORGD и админ; у DIVISION_OPERATOR его НЕТ, отсюда честная persona-без-права). Область **ПЛОСКАЯ** (как в источнике): сузить по подразделению нечем одинаково для всех типов событий — у статуса через сотрудника+слот, у пары через две стороны, у сотрудника нет вовсе; довод и точка входа в `OpsAuditLogSelector`. Порядок `-created_at, -id` — второй ключ НЕСУЩИЙ, т.к. record_many даёт равный created_at; пробы: порядок на ТРЁХ строках одного момента + сквозной проход по страницам «каждая ровно один раз». Окно `[from, to)` полуоткрытое; наивный datetime → 400 (тот же довод, что у clock.override); неизвестный action/entity_type → 400, не пустая страница. ⚠️ **`http_method_names` снят как декорация** — его снятие не роняло ни одного теста; 405 держит ОТСУТСТВИЕ пишущих действий у ViewSet, и 405 приходит даже БЕЗ права (проверено). Схема закреплена тестом по СОБРАННОЙ схеме (страница объектом, только get, id целым; красная проба — снятие many=False с класса); базовая линия spectacular не сдвинулась (3 warning + пред-существующий Error у UserRoleViewSet). +25 тестов, ops-сюита 436. **Срез 23 (ad0c090)** — продление `extend_status`: монотонная операция поверх общей преамбулы `_lock_for_edit` (дата конца строго позже; укорачивание — это complete_status_early, «продление» на ту же дату 422). Продлевается любая НЕ отменённая строка, включая ЗАВЕРШЁННУЮ (отпуск, длившийся дольше, — правка правды о прошлом). Интервал перепроверяется целиком, конфликты — с exclude_pk; мягкий обходится override+причина с записью StatusOverride в том же savepoint, жёсткий никогда. Новое событие STATUS_EXTENDED в закрытом словаре. ⚠️ **Тест `test_every_declared_action_is_actually_written` поймал незаписанное событие сразу** — объявить действие в ACTIONS и не позвать его нельзя. Отличия от источника: (1) гард отменённой снят — владелец один, `_lock_for_edit`; (2) ДОБАВЛЕН гард откомандированного (в источнике у extend его нет) — продление такая же операторская правка, что и update_status; (3) порог сравнивается со СВЕЖЕЙ строкой, а не с переданным объектом (устаревшая копия объявила бы продлением откат уже записанного); (4) отдельного OVERRIDE_APPLIED нет — причина обхода едет полем reason самого события, как у create_status. 7 красных проб (монотонность, порог с устаревшего, снятие FR-16, потеря exclude_pk, безусловный обход, подмена события, снятие перепроверки интервала). +23 теста, ops-сюита 459. ⚠️ ruff в этом чекауте НЕТ (ни в .venv, ни в PATH) — гейт раздела = pytest. Осталось непортированным: догон материализации (catch_up.py) и вся линия submissions (сдача дня, amendment, светофор, выгрузки). **Срез 24 (c8658c4)** — ЛИНИЯ SUBMISSIONS НАЧАТА: модель `OpsDailySubmission` (ops_daily_submissions, миграция 0007). Версионируемое иммутабельное заявление о дне; снимок самодостаточен (roster-знаменатель + интервалы-ФАКТЫ статусов, не выведенные состояния). Инварианты на БД: partial-unique «не более одной текущей» на (подразделение, день), unique номеров версий, CHECK словаря событий (event БЕЗ дефолта), version≥1, «поправка объяснена» (~AMENDED | reason~\S & sanction~\S; NULL-щели нет — оба поля не-null). Инвариант «РОВНО одна текущая» осознанно оставлен прикладным. Отличия: division_id/employee_id/status_id целые плоские, submitted_by строкой. Красные пробы — снятие каждого из 5 ограничений ИЗ МИГРАЦИИ (мутировать модель бесполезно: вставка идёт в БД) + новое значение Event без правки CHECK краснит пробу зеркальности. +16 тестов, ops-сюита 475. Дальше по линии: билдер снимка, сервис сдачи дня, amendment, светофор. ⚠️ catch_up.py источника НЕ портирован намеренно — его реестр эффектов EMPTY, машина ничего не запускает; портировать имеет смысл вместе с первым реальным эффектом. **Срез 25 (46264fe)** — билдер снимка `build_division_snapshot(подразделение, дата)` → {schema_version, roster, rows}. Знаменатель — ТОТ ЖЕ селектор, что у расхода (slots_with_working_occupants): двух разных списков одного подразделения быть не может; факты сужаются множеством знаменателя → rows ⊆ roster по построению. Предикат «живой факт на дату» сведён в один `EmployeeStatusSelector._live_overlapping` с ДВУМЯ проекциями (overlapping_on для расхода, snapshot_facts_on для снимка) — два похожих набора условий разошлись бы. Новый `EmployeeSelector.denorm_for` (ФИО+звание значениями). Часы не читаются, дата — параметр. ⚠️ **Своя коэрция `int(division_id)` СНЯТА**: её снятие не роняло ни одного теста — приведение и ValueError на мусоре уже держит поле модели ([[feedback-redundant-guards-vacuous-probe]]). ⚠️ **ИНЦИДЕНТ: `bash probe.sh | head -12` убил скрипт SIGPIPE до восстановления файла** — правка легла поверх мутированного файла, бэкап снялся с испорченного. Не пайпить пробы в head/tail-с-обрывом; untracked файл git checkout не спасёт. +24 теста, ops-сюита 499. Дальше: сервис сдачи дня (5.3b), amendment, светофор. **Срез 26 (f90602e)** — сервис сдачи `submit_day`: существование подразделения (404 ДО сборки снимка — иначе «сдача» призрака с пустым списком) → окно дат (дефолт сегодня+завтра, параметром переопределяемо) → повтор → снимок → событие → опоздание → версия 1. Событие сравнивает ЗНАЧИМОЕ содержимое снимка (знаменатель + интервалы), НЕ снимок целиком: переименование и пересоздание идентичного факта не изменение. База сравнения — ближайшая предыдущая сдача (`previous_for`), не «вчера». Новые: `DailySubmissionSelector.{exists_for,previous_for}`, `DivisionTreeSelector.exists`, событие `DAILY_SUBMISSION_SUBMITTED` + `ENTITY_SUBMISSION`, `audit_service.submission_snapshot` (снимок ДНЯ в журнал не кладётся). Отличия от источника: (1) предпроверка повтора по ЛЮБОЙ версии дня, не только текущей — иначе день со снятой с текущих v1 давал бы IntegrityError/500 вместо 409 (нашлось тестом); (2) контрольный час ПАРАМЕТРОМ с дефолтом-константой 17:00 (справочника настроек контроля ещё нет — отдельный срез); (3) entity_id журнала = pk строки сдачи, не подразделение. ⚠️ Проба «сравнение опоздания по UTC вместо локальной зоны» краснеет только потому, что фикстура EVENING=13:30Z взята ПО ТУ СТОРОНУ границы в +05 (18:30 местного) — на +05 такие тесты вакуумны без специально подобранного момента. +25 тестов, ops-сюита 524. **Срез 27 (c0cce80)** — маршрут сдачи: `POST /api/operations/daily-submissions/` под **`daily_report.mark_update`** (каталог закрытый; поправка пойдёт под `daily_report.correct` — сдать и переписать сданное разные полномочия, тест на CORRECTOR-persona это пинит). Тонкая вьюха: форма → область → сервис. Область проверяется ДО сервиса (иначе 404 vs 403 работает оракулом существования) общим модульным `_assert_division_in_scope(request, division_id, permission_code, field=)` — метод SecondmentViewSet переведён на него, владелец правила один. Ответ БЕЗ снимка (`OpsDailySubmissionSerializer`: 9 полей, ни snapshot, ни полей поправки). Обе уникальности дня добавлены в `CONSTRAINT_ERROR_MAP` одним 409 DAY_ALREADY_SUBMITTED. Чтения нет намеренно — у снимка своя область видимости, отдельный срез. ⚠️ **Проба «открыть GET через http_method_names» осталась зелёной**: у роутера GET на list-URL и так 405, пока у ViewSet нет `list()` — проба доведена до красного добавлением `list()`, тест переписан на то, что реально пинит (отсутствие чтения/правки), в докстринге записано. 5 из 6 остальных проб краснеют. +26 тестов, ops-сюита 550, схема spectacular не сдвинулась (3 warn + пред-существующий Error UserRoleViewSet). **Срез 28 (529be05)** — поправка `amend_day`, положена В day_submission_service рядом со сдачей (в источнике отдельный amendment_service; здесь идиома раздела — весь цикл статуса тоже в одном status_service). Вытеснение, не правка: новая версия AMENDED со СВЕЖИМ снимком, прежняя цела, уходит только флаг. Гашение ДО вставки (обратный порядок роняет 14 тестов — частичная уникальность немедленная). Новый `DailySubmissionSelector.latest_for(lock=True)` ищет СТАРШУЮ версию, не текущую: «ровно одна текущая» инвариант прикладной, и в состоянии «ноль текущих» поиск по is_current объявил бы день несданным → поправка пошла бы писать v1 поверх истории. Отличия от сдачи: окна нет, событие всегда AMENDED, `late=False` всегда. Отличия от источника: entity_id = pk строки; **`submission_snapshot` теперь БЕЗУСЛОВНО несёт reason/sanction/triggered_by_status_id** (в источнике надстройка только над v≥2 — разный набор ключей у событий одного типа делает отсутствие ключа неотличимым от «причины не было»); пины сводки `sources` не портированы (сводки в разделе нет). Все 10 красных проб краснеют; тест покрытия словаря снова поймал незаписанное событие сразу. +28 тестов, ops-сюита 578. **Срез 29 (8ce46e5)** — маршрут поправки `POST /api/operations/daily-submissions/{id}/amend/` под СВОИМ правом `daily_report.correct` (право отмечать правки не открывает и наоборот — проверено с обеих сторон). {id} адресует ЦЕПОЧКУ: голову перерезолвит сервис, ссылка на вытесненную версию продолжает тот же день. Область — по подразделению НАЙДЕННОЙ строки, тело игнорируется. Отличие от источника: ответ = проекция ПОПРАВКИ (+reason/sanction/triggered_by_status_id) — в источнике клиент не видит, что записано, хотя сервис текст обрезает. ⚠️ **Проба «актор из тела» сперва показалась зелёной**: скриптовая замена по ПЕРВОМУ вхождению легла в чужой метод (шаблон `actor=resolve_actor_id(request),` встречается многократно) — при точечной мутации amend краснеет. Урок общий: в пробах по большому файлу пинить уникальный контекст и проверять, что изменилась ИМЕННО та строка. +30 тестов, ops-сюита 608. **Срез 30 (e622ae7)** — чтение сдач `GET /api/operations/daily-submissions/` и `/{id}/` под **status.view** (в источнике читают под mark_update; здесь довод: снимок = тот же состав и те же факты, что уже открывает status.view, а право ЗАПИСИ чтения открывать не должно — обе стороны закреплены персонами). Список: только ДЕЙСТВУЮЩИЕ версии по умолчанию, история флагом `history`; область сужает всегда, чужое подразделение → 403, не пустая страница; порядок `-business_date, division_id, -version, id` (проба на ТРЁХ элементах + сквозной проход по страницам). **Снимок отдаёт только деталь** и именно ЗАПРОШЕННУЮ версию (голова не подставляется); в списке `defer("snapshot")` + ассерт ПО SQL, что колонка не выбирается (сериализатор без неё этого не доказывает). Два теста поверхности срезов 27/29 переписаны — GET перестал быть 405 (смена поверхности, не регресс). Все 10 проб краснеют; проба гейта потребовала точечной мутации (блок `permission_map` встречается трижды). +20 тестов, ops-сюита 627. **Срез 31 (e63e035)** — светофор ОДНОГО подразделения (`traffic_light.py`): RED = нет действующей сдачи, YELLOW = разошлось, GREEN = сходится. **Расхождение по ПОБЕДИТЕЛЯМ дня, не по сырым фактам** (расход держится снимком) — половина тестов про то, что осталось ЗЕЛЁНЫМ: пересоздание идентичного факта, статус под старшим приоритетом, переименование, факт вне дня, вакантный слот. Разведено с `_diff_key` сдачи (там сырые интервалы для выбора события). Отличия от источника: живой знаменатель = тот же селектор слотов, что у снимка/расхода; приоритеты из `StatusCatalog` (справочник), не из литеральной карты; **NEUTRAL/UNKNOWN не объявлены** — появятся со сводом, который их вернёт; неизвестный код в снимке падает громко. Новый `DailySubmissionSelector.current_for`. ⚠️ Проба «уволенные в живом знаменателе» была зелёной — уволенного отсекает СЕЛЕКТОР, а фильтр в светофоре владеет ВАКАНСИЯМИ; добавлен тест пустого слота → краснеет. Фикстура `seed_types` не годится светофору (один приоритет всем, нет IN_SERVICE) — своя фикстура с реальными приоритетами. +19 тестов, ops-сюита 646. **Срез 32 (de5f4fe)** — свод светофора по дереву: худший цвет наверх, late = ИЛИ по поддереву, UNKNOWN выше RED. 4 запроса на всё поддерево (новый `current_for_many`) + один скан дерева, переиспользуемый `subtree_ids`; проба сравнивает ДВА размера дерева. **Сведено расхождение источника**: там пустой несдавший узел = RED точечно и NEUTRAL в каскаде; здесь правило одно — RED значит «есть кого сдавать и не сдали», поэтому точечный светофор среза 31 ДОУЧЕН возвращать NEUTRAL, равенство путей закреплено тестом. Асимметрия оставлена только у сломанного справочника (точечный падает, свод изолирует в UNKNOWN — разный способ доставки ошибки, не разный цвет); ловится ровно ValueError. Свёртка держит петлю в `parent` отметкой «в работе» (у Division нет запрета циклов). 8 из 9 проб краснеют; девятая (`current_for_many` без is_current) потребовала теста «вытесненная версия не отменяет несданности». +16 тестов, ops-сюита 663. **Срез 33 (064e459)** — маршрут светофора `GET /api/operations/traffic-light/tree/` под status.view. Вьюха не пересчитывает цвет; добавляет имя/родителя + гарды (порядок НЕСУЩИЙ: область → существование). Корень необязателен, выводится из области: корни = узлы, чей родитель вне области (МИНИМАЛЬНОЕ множество — ответ тот же, цена разная); ветвление строго `is None` (глобальный грант ≠ пустая область). Отличия от источника: будущая дата НЕ запрещена (сдача «на завтра» штатна); гард «дата до начала данных» не нужен — на доисторической дате сдач нет, цвет красный сам собой; drift в ответе нет. ⚠️ Две пробы потребовали доработки тестов: «корень не выводится из области» вакуумна (дубли схлопывает словарь merged) → переписана на СТОИМОСТЬ; «пустая область = всё дерево» недостижима за грубым гейтом → покрыта monkeypatch резолвера, иначе опечатка `if not visible` жила бы незамеченной. +23 теста, ops-сюита 686. **Срез 34 (81e3832)** — точечный светофор `GET /api/operations/traffic-light/{id}/`: цвет СВОЕГО уровня + поимённый drift (свод отвечает «куда смотреть», узел — «кого проверять»). Цвет узла в дереве и точечный МОГУТ отличаться — разные вопросы, закреплено тестом с обоими ответами. Код вне справочника → **422 UNRESOLVABLE_STATUS_TYPE** (не 500 и не цвет UNKNOWN); в дереве та же поломка остаётся цветом — асимметрия покрыта тестом. ⚠️ **ENUM_NAME_OVERRIDES в settings/base.py**: поле «status» коллидировало со старыми сериализаторами, spectacular давал машинное `Status3a8Enum`, меняющееся от правок соседей → сгенерированный клиент ломался бы; теперь `TrafficLightStatusEnum`, база схемы снова 3 warning. Все 8 проб краснеют. +20 тестов, ops-сюита 706. **Срез 35 (d0f4427)** — расход по СДАННОМУ дню: `expense_from_snapshot` (чистая) + `submitted_expense` (действующая версия). Живых таблиц не читает — ассерт ПО SQL (только ops_daily_submissions) + тесты «числа до/после правки живых данных совпадают». Полей МЕНЬШЕ живой строки намеренно: снимок не хранит штат/вакансии/«+N», подмешивать их живыми = выдать полусегодняшние числа за сданные. Паспорт версии едет с числами. Отдельный тест: на нетронутом дне сданные и живые числа СОВПАДАЮТ (иначе светофор жёлтый на ровном месте). Фикстура типов доучена давать каждому типу СВОЮ колонку (seed_types кладёт все в "X" → «занял свою колонку» неотличимо от «занял чужую»). ⚠️ Проба «снять Σ колонок == Список» зелёная — инвариант держится построением; дублирующий гард убран ([[feedback-redundant-guards-vacuous-probe]]). +19 тестов, ops-сюита 725. **Срез 36 (542f23c)** — маршрут сданного расхода: GET /api/operations/strength-report/submitted/ под status.view, отдельным ДЕЙСТВИЕМ (не флагом живого расхода: разные вопросы и разный набор полей, один контракт описывал бы две формы). division_id ОБЯЗАТЕЛЕН — сдают поштучно, снимка поддерева нет. Два «не найдено» разведены: ENTITY_NOT_FOUND (нет подразделения) vs DAY_NOT_SUBMITTED (день не сдан); порядок гардов область→существование; код вне справочника → 422 UNRESOLVABLE_STATUS_TYPE. `columns` = ПОРЯДОК колонок на обоих маршрутах расхода, числа в `counts` (порядок сверяется тестом с живым расходом). ⚠️ Проба «снять many=False» зелёная — list-эвристика spectacular включается по ИМЕНИ действия (`submitted`, не `list`); обёртка оставлена ради единообразия, тест это признаёт. 9/10 проб краснеют. +25 тестов, ops-сюита 750, схема 3 warn (база). **Срез 37 (2cc1194)** — справочник контроля сдачи: OpsSubmissionControlSettings (ops_submission_control_settings, миграция 0008) + SubmissionControlSettingsSelector; контрольный час из константы кода стал НАСТРОЙКОЙ, параметр control_hour остался старшим (при нём справочник не читается — ассерт по SQL). Синглтон держит БД (unique + CHECK singleton_key=1: одной уникальности мало, она разрешает «строку на ключ»). Сверх источника — CHECK содержимого required_division_ids через RunSQL (Q-язык не выражает предикаты по элементам массива): NULL/неположительный id = подразделение-призрак → вечная блокировка завтра; ПОРЯДОК конъюнктов несущий (`0 < ALL` на массиве с NULL даёт NULL, CHECK пропускает). Отличия: массив ЦЕЛЫХ (не UUID); default_notify_recipient НЕ портирован (уведомлений в разделе нет — настройка без читателя). Строка сеется миграцией, селектор самолечится get_or_create; DEFAULT_CONTROL_HOUR переехал к модели. late сданного задним числом не переписывается. ⚠️ Проба «отдаёт поле, а не копию» была ЗЕЛЁНОЙ — get() читает заново, list(...) второй владелец; снят, тест переписан на «правка видна следующему читателю» (краснеет от кеша). 8/9 проб краснеют. +17 тестов, ops-сюита 767, миграция применена на стенде. **СРЕЗЫ 38-47 (по 5828409) — блокировка на завтра и СВОДКА закрыты целиком.** 38 (5f4116a) tomorrow_block вывод: отстающие = «необходимые управления» без ТЕКУЩЕЙ сдачи, порядок ЧИСЛОВОЙ (не str как у UUID-источника), ранний выход на пустом списке СНЯТ (ORM не ходит в базу на `__in=[]` — был вторым владельцем). 39 (e8088b4) OpsTomorrowBlockOverride (миграция 0009) + override_tomorrow_block: уровень ДАТА, CHECK непустоты причины/актора, 409 из уникальности, отказы DomainError (не ValueError+__cause__ как в источнике), entity_id = int pk (источник выводил uuid5); вложенный atomic снят — декоратор уже даёт savepoint. 40 (7df4cf3) tomorrow_gate: 422 TOMORROW_BLOCKED на ЖИВОМ расходе только для БУДУЩИХ дат, порядок гардов область(403)→правило(422), мусор настройки (удалённые/отключённые) вычёркивается ДО решения. 41 (2233f8c) POST /api/operations/tomorrow-block/override/ под daily_report.override_block, горизонт +7 дней, дата только будущая. 42 (eece878) GET /api/operations/tomorrow-block/ (status.view): blocked/overridden/laggards с именами; чистка и решение переехали в общий resolve_block() → ОДИН рассказ на отказ и показ; умолчание даты — ЗАВТРА. 43 (c55301b) admin.py раздела: только справочник контроля сдачи (синглтон, add условно/delete никогда) + тест-гвард «бизнес-модели НЕ регистрируются». 44 (edfafc0) assemble_summary: сводка = ТА ЖЕ OpsDailySubmission родителя + ключ снимка `sources` (пины версий детей); снимок СТРОГО своего уровня; ждёт только детей с людьми в ПОДДЕРЕВЕ (новый StaffUnitSelector.occupied_division_ids); 422 SUMMARY_CHILDREN_NOT_SUBMITTED; событие учитывает пины. 45 (59b6e86) summary_freshness — ВЫВОДИТСЯ, три раздельные оси superseded/missing/unpinned, None у обычной сдачи; выборка живых сужена до запиненных (расширение источника было без читателя). 46 (4d2fef1) rebuild_summary — поправка сводки: v+1 AMENDED, свежие пины, прежняя байт в байт, обычная сдача сводкой не становится (400). 47 (5828409) /api/operations/daily-summaries/: POST (generate), POST rebuild/ (correct), GET freshness/ (status.view). Журнал раздела пополнился TOMORROW_BLOCK_OVERRIDDEN (сущность tomorrow_block_override), DAILY_SUMMARY_ASSEMBLED, DAILY_SUMMARY_REBUILT. ops-сюита 912 зелёных, миграции применены на стенде, схема spectacular: 3 warn + 3 ошибки (пред-существующий UserRoleViewSet). **СРЕЗЫ 48-53 (по d752400) — ВЫГРУЗКИ расхода закрыты.** 48 (0cb5e68) expense_document.py — чистый билдер «снимок → ExpenseDocumentData» (строка, ячейки с ПОИМЁННЫМ составом, итог); счёт берётся у expense_from_snapshot (тот же владелец, что у экрана — отличие от источника с его вторым derive_report), состав СВЕРЯЕТСЯ со счётчиком (AssertionError останавливает выпуск). 49 (d733a09) expense_layout.py (один владелец шапки/подписей/строки члена; ПОРЯДОК КОЛОНОК ИЗ СПРАВОЧНИКА, не прибитый 17-колоночный донорский; незнакомый код печатается кодом) + expense_csv.py (только числа, `;`, utf-8-sig BOM, CRLF — проверяется по БАЙТАМ). 50 (15e8763) expense_xlsx.py — числа ЧИСЛАМИ, ячейка с составом текстом, cap 20 имён + хвост «… ещё N», придано «+N», формул в файле нет, лист = ISO-дата, openpyxl лениво. 51 (4c91876) GET /api/operations/strength-report/export/ (status.view) — expense_release.py: список/колонки/состав ИЗ СНИМКА, штат/вакансии/приданные ЖИВЫЕ (снимок их не хранит). ⚠️ ПАРАМЕТР НЕЛЬЗЯ ЗВАТЬ `format` — DRF занимает его под согласование содержимого и отвечает 404 ДО вьюхи; у нас `file_format`. 52 (dd3a349) combine_documents + build_summary_expense_document — сводный документ строится из ЗАПИНЕННЫХ версий детей (протухшая сводка не печатается свежей), итог = СУММА строк. 53 (d752400) GET /api/operations/daily-summaries/export/. ops-сюита 980. Уроки проб этой пачки: сравнение двух ответов между собой вакуумно (одинаково пустыми они сравняются) — ассертить конкретные числа; «область раньше существования» различима ТОЛЬКО на id, который И вне области, И не существует. **СРЕЗЫ 54-55 (по 00bdf64) — личная копия сданного дня («щит»).** 54 (47bada3) personal_export.py + personal_export_service.py: паспорт версии (в т.ч. «ДЕЙСТВУЮЩАЯ ли») + ведомость по паре (человек, статус); человек без статусов = строка с ПУСТЫМИ ячейками (умолчание «в строю» не подставляется — обещан снимок, не вывод), строка на сотрудника вне списка печатается, порядок = порядок снимка; незнакомый код/непарсящаяся дата печатаются дословно; гард схемы снимка ДО генерации и ДО журнала по ТОЧНОМУ типу (isinstance пропустил бы True); SUBMISSION_EXPORTED = событие ЧТЕНИЯ в журнале мутаций (осознанное исключение — копию предъявляют в споре); время сдачи приводится к локальной зоне СТРОКОЙ (openpyxl не пишет tz-aware). 55 (00bdf64) GET /api/operations/daily-submissions/{id}/export/ под status.view — выгружается ЗАПРОШЕННАЯ версия; имя файла кириллическое → `filename*=UTF-8''` с процентным кодированием (голый filename искажается). ops-сюита 1005. **СРЕЗ 56 (c7a3468) — .docx-расход (печатная форма).** expense_docx.py: третий формат ОДНОГО билдера (.csv числа для счёта, .xlsx лист для работы, .docx страница под подпись). Несущее отличие от .xlsx — ДВА КЕГЛЯ в ячейке: число 12pt + состав 8pt отдельными АБЗАЦАМИ (перенос внутри одного run унаследовал бы кегль числа, и разницы с .xlsx не осталось бы). Times New Roman ЯВНО на каждом run (дефолт шаблона python-docx — Calibri) + `w:cs` через rFonts; A4 landscape задаётся И ориентацией, И габаритами (смена ориентации размеры сама не свопает). ⚠️ **«+N» ТОЛЬКО у приданных** — отличие от источника, где «+N» вешалось на статусную колонку ATTACHED: прикомандированный внутри списка и приданный сверх него складываются по-разному. Сетка «Table Grid» (в ячейках многострочные списки). MEMBER_SIZE_PT добавлен в expense_layout. ⚠️ Габариты страницы в тесте сверять В МИЛЛИМЕТРАХ: формат хранит их в твипах, round-trip даёт 10692130 против Mm(297)=10692000. Формат врезан в FORMATS обоих маршрутов выгрузки; `details.allowed` стал ["csv","docx","xlsx"]. +16 тестов, ops-сюита 1021, 8/8 проб красные, схема не сдвинулась. **СРЕЗЫ 57-58 — ПРИНУЖДЕНИЕ К ПОПРАВКЕ (инвариант «ретро-правка накрытой сданной даты обязана триггерить amendment»).** 57 (3b5ef0a) amendment_enforcement.py + `DailySubmissionSelector.covering`: затронутые дни = ОБЪЕДИНЕНИЕ полуинтервалов (габарит поправил бы промежуток чужой санкцией); обнаружение по ПРИНАДЛЕЖНОСТИ СНИМКУ (JSONB containment, один запрос), не по живому подразделению — перевод по штату между сдачей и правкой увёл бы поиск к чужому дню; только is_current. Санкция = причина САМОЙ правки, reason = служебный AUTO_AMENDMENT_REASON. Отличие от источника: прямой вызов вместо `register_amendment_handler` — раздел один пакет, цикла нет, слот был бы церемонией. 58 (0554890) врезка во ВСЕ пять путей status_service. ⚠️ **Решение принято автономно (Bratan не спрашивался): причина требуется УСЛОВНО** — только когда сданное задето, иначе 422 AMENDMENT_REASON_REQUIRED; санкция не выдумывается, но и обязательного поля на каждой правке нет. Затронутые дни у каждого пути СВОИ: создание — новый интервал, правка — старый И новый (и только при смене ДАТ), досрочное закрытие — только освобождённые [факт, прежний конец), продление — только добавленные, отмена — весь интервал. У отмены отдельного поля нет (её reason и есть объяснение). На PATCH добавлено `amendment_reason`, НЕ считается изменением (тело из одной причины → 400). +30 тестов, ops-сюита 1051. **СРЕЗЫ 59-61 — шов достроен на ВСЕ пути записи.** 59 (d32e637) пачка: `covering_many` (OR containment, один запрос), ОДНА поправка на день независимо от числа строк; периметр = все сотрудники пачки, интервалы ПОСТРОЧНЫЕ (габарит накрыл бы промежуток); потолок запросов 10→11. ⚠️ две вакуумные пробы: «габарит» зеленел т.к. промежуточный день не был сдан; «периметр первой строки» — т.к. знаменатель подразделения содержит всех его людей (нужна пачка на ДВА подразделения). 60 (80253ce) увольнение: **у СИСТЕМНЫХ путей причина ВЫВОДИТСЯ из события, а не спрашивается** — событие известно целиком (человек+дата), спрашивать некого; санкция несёт дату; периметр = отрезанный хвост, не весь интервал. 61 (c852bd6) прикомандирование + **починка дефекта, внесённого срезом 58**: возврат закрывал обе ноги через cancel_status → день получал v2 И v3 за одно решение. Правило: **владелец поправки — ОПЕРАЦИЯ, а не строка**; оркестратор забирает поправку себе, низкоуровневый путь зовётся `cancel_status(..., amend=False)`. `_end_leg_tomorrow` возвращает (строка, освобождённый интервал). Единственное место БЕЗ шва — правка метаданных (комментарий/основание), расхода не меняет. ops-сюита 1064. **СРЕЗЫ 62-63 — разрешение заглушки «уточняется» (последняя неперенесённая операция статуса).** 62 (7a9a653) `resolve_placeholder`: заглушка = отсутствие факта, её НЕ правят, а закрывают и кладут рядом реальный статус (правка превратила бы «мы не знали» в «мы знали и ошиблись»); ОДНА транзакция + ОДНО событие STATUS_CLARIFICATION_RESOLVED, лента по ЗАГЛУШКЕ. **Отличие от источника: заглушку задаёт СПРАВОЧНИК** — новое поле `StatusType.is_placeholder` (миграция 0010, применена и на стенде), а не литерал PENDING_CLARIFICATION в сервисе. Причина обязательна ВСЕГДА (не условно). Интервал разрешения свой, заглушка исключается из периметра конфликтов. ⚠️ проба обхода мягкого конфликта требует ИДУЩЕГО соседа (мягкое с PLANNED понижается до warning — та же ловушка, что в срезе 8). 63 (31a6212) POST /api/operations/statuses/{id}/resolve/ под status.manage, ответ = СОЗДАННАЯ строка. ⚠️ **действие, не внесённое в `permission_map`, отвергается ДАЖЕ wildcard-админу** (гейт fail-closed) — при добавлении action карту прав править вместе с ним. ops-сюита 1086. Осталось непортированным из источника: lagging_check/уведомления (нужен целый apps.notifications + watermark + advisory locks), catch_up (реестр эффектов пуст), golden-master .docx. Дальше НЕ портировано: линия уведомлений/lagging-check (5.7 — уведомлений в разделе нет вовсе), фронтовая привязка новых маршрутов (/security-ops/* пока на моке). Срез 3 (50543df): справочник ops_status_types + legacy_code-мост ко всем 12 старым кодам + GET /api/operations/status-types/ под status.view. Срез 2 (fa612c6): админ-API RBAC — /api/operations/{roles,permissions,user-roles,temporary-duty}/ за гейтом admin.roles, записи через RoleAdminService; отличия от источника: сериализаторы запросов (400 вместо KeyError→500), created_by только из аутентификации, expire несуществующего → 404. 41 тест в разделе. Следующие куски: statuses (ops_status_types/overrides/secondments), submissions (daily/expense/traffic-light — то, что фронт мокает), затем перевод старых вьюх на require_permission. **API-РЕЖИМ ВКЛЮЧАЕМ (a2c5fc2)** — NEXT_PUBLIC_OPS_DATA_SOURCE=api теперь реально работает: Django отдаёт живой GET /api/operations/my-permissions/ (common/api/operations_views.py; superuser→["*"], иначе Role.get_permissions из общей RBAC — erda ROLE_3 даёт 10 staffing/vacancy/status-кодов, ops-кодов в БД нет), rewrites /api/operations|ops/* добавлены в next.config.js (пересборка обязательна — rewrites запекаются). В api-сборке мок-данные не появляются, /api/ops/* доходит до Django и падает честным «HTTP 404 Not Found» (envelope-парсер переживает HTML-404), WS тихо в reconnecting. Дефолт стенда — мок-сборка (возвращена). Дальше по желанию: реальные /api/ops/*-эндпоинты отдельными срезами. **ВЕСЬ NEW ПОРТИРОВАН (a5a636e)** — последними закрыты combat-группы: /security-ops/duties/combat (§24 целиком: entities/combat-duty + mocks/ops/combat-handlers.ts со стором ops-mock-combat и 11 обработчиками — валидации репозитория SPA дословно, DOUBLE_ASSIGNMENT §24.17 на подаче и замене, MISSING_HANDOVER перед завершением, фактический состав отдельно §24.23; features/ops-combat/combat-groups-section.tsx — карточки всех этапов, клиент решений не принимает; ссылка из плана дежурств + сайдбар «Боевые группы»). Прожит полный цикл создать→…→COMPLETED + 10 негативов по кодам + UI-клик «Принять». Непортированных фич NEW не осталось. **ВТОРАЯ ОЧЕРЕДЬ ЗАВЕРШЕНА (536ae45)** — последним портирован Daily Grid: /security-ops/daily-expense (чистая клавиатурная грамматика NAVIGATE/EDIT/PERIOD_EDIT в entities/daily-grid, роверный фокус + type-ahead-комбобокс, дельты полуинтервалом, двухфазный ритуал «Сохранить правки»→«Сдать день», панель сдачи с окном/версиями/amendment/drift; мок bulk АТОМАРЕН с details.rows[] — hard 422/soft 409 STATUS_OVERLAP_WARNING; без виртуализации намеренно). Прожит полный клавиатурный цикл + мягкий конфликт + baseline-сдвиг + drift. Итог 2-й очереди: рейтинги (b5f6d45), аналитика (ce0d221), отчёты (caa57d3), WS/changelog (6682db7), обратная связь (2c19df8), Daily Grid (536ae45). НЕ портировано из NEW: combat-группы (вне очереди по решению Bratan). **Вторая очередь-6 готова (2c19df8)** — Обратная связь §28: /security-ops/feedback{,/[id]} (реестр: серверная сводка/поиск/фильтры/страницы, форма с черновиком, вложения-МЕТАДАННЫЕ, техинфо по согласию; карточка: переписка PUBLIC_REPLY/INTERNAL_NOTE, разбор по карте переходов справочника, закрытие только терминальным статусом с ответом автору, лента=timeline+audit диффом в одной точке). Чужой ЧЕРНОВИК — живое закрытие при wildcard (404 по прямой ссылке); содержимое вложения не сохраняется по построению; ассерты по стору прожиты. **Вторая очередь-5 готова (6682db7)** — WS/changelog: lib/ops-ws.ts (порт транспорта SPA: статусы/backoff+джиттер/epoch/дедуп-кольцо; WS=сигнал, истина=REST; фабрика сокета инжектируется — в мок-режиме «сокет» наблюдает стор рейтинг-уведомлений и выталкивает НОВЫЕ записи кадрами {type,payload}; SEED: baseline с первого опроса, застающего ленивый стор, — гонка с baseline-на-открытии чинилась по живой пробе), OpsNotificationBell в layout /security-ops (бейдж=кадры с закрытия панели, кадр→invalidate rating-notifications), /security-ops/changelog (версия среза PORT_VERSION + журнал реальных FIX'ов, проп-шов fixes для будущей обратной связи). Прожито: онлайн, оценка→кадр≤2.4с→бейдж 1+ровно один рефетч. **Вторая очередь-4 готова (caa57d3)** — Отчёты службы: /security-ops/service-reports{,/history,/[id]} (§22.18-22.28: асинхронная state machine PENDING→PROCESSING→COMPLETED/FAILED с продвижением на чтении, immutable-артефакт с замороженными версиями и expiresAt, server-side masking §22.24 — обычный CSV без колонок Примечание/Обоснование ВООБЩЕ, идемпотентность, retry→reused/new-revision→revision по серии, действия строк считает сервер). Раздел REPORT_LIMITS в «Настройках» (предел периода по типу + срок хранения 14 дн), источник строк — стор дежурств. Прожито: полный цикл + маскирование ассертом по файлу + PERIOD_TOO_LONG + revision 2 + reused:true. **Вторая очередь-3 готова (ce0d221)** — Аналитика службы: /security-ops/analytics (дашборд §22.4-22.12: пресеты из registry, KPI со state от серверных порогов, drill-down с проверкой snapshotId, «Требует внимания» §22.11 своим детектором, нагрузка §22.9) + /security-ops/analytics/operations (§22.13/22.15 иерархия до поста; воронка §22.14 честно недоступна — журнала переходов в host-сторе ОМ нет). Источники — узкие проекции duties/security-events сторов (duties экспортирует readDutyShiftsStore/readDutyTypesRegistry); в «Настройках» новые разделы ANALYTICS_LIMITS/LOAD_POLICY/ATTENTION_POLICY + ридеры; сквозняк правки предела проверен живьём (v1→v2 меняет отказ). snapshotId детерминирован от max(updatedAt) смен. **Вторая очередь-2 готова (b5f6d45)** — Оперативный рейтинг целиком: 7 маршрутов /security-ops/ratings{,/workspace,/evaluations,/employees/[id],/export,/audit,/analytics}; entities/operational-rating (типы+чистая логика §19), mocks/ops/ratings-handlers.ts (агрегат считает сервер, закрытые оценки наружу не едут, идемпотентность, 409 REVISION_MISMATCH с diff — НЕ overridable, свой EvaluationConflictNotice), методика = раздел RATING_POLICY в settings-store (readRatingPolicy — правка порогов на /settings меняет расчёт). Прожито живьём: цикл оценка→агрегат→исправление→экспорт CSV→журнал, 4 негативные пробы, ассерт закрытости по всему JSON и CSV. ⚠️ актор мока = demo-admin (wildcard); стейл sessionStorage без RATING_POLICY даёт честный POLICY_UNDEFINED — лечится новой вкладкой. **Вторая очередь-1 готова (3abc61d)** — Аудит (пишется мок-мутациями), Справочники (usage сервером, 409/422 на удалении), Настройки (живые потребители: duties/objects мок-слои читают общий стор политик; сквозняк policy-v2 проверен). FIX: settingPath(":код") энкодил ':' → %3A, PATCH-маршрут не матчился. Из 2-й очереди не осталось ничего: все пункты портированы. **ПЕРВАЯ ОЧЕРЕДЬ ЗАВЕРШЕНА (acc6675)** — все 6 фаз прототипа портированы и прожиты живьём: Ф0 фундамент, Ф1 Объекты, Ф2 Реестр ОМ, Ф3 Карточка ОМ (полный цикл), Ф4 Командный центр, Ф5 Дежурства, Ф6 Календарь (FullCalendar, два источника). Вторая очередь (не портировано): рейтинги/оценивание, аналитика, отчёты службы, справочники, настройки, аудит, WS-уведомления/changelog, обратная связь ОМ, расход Daily Grid, combat-группы. **Ф5 Дежурства — готова (857aa8d)**: месячный план (lifecycle: draft→check(отпечаток)→approve→замок→reopen, серверная action policy с причинами), создание смены с 409 DUTY_CONFLICT_DETECTED (отдых) через ConflictDialog, карточка смены (линейный цикл + отмена + stale-паспорт); FIX вложенных Radix-диалогов (onInteractOutside-гарды при открытом конфликте). Combat-группы вне очереди. **Ф4 Командный центр — готов (a8232e0)**: KPI по реестру ОМ + лента готовности + создание; реактивность проверена живьём. **Ф3 Карточка ОМ — готова (3051794)**: полный цикл 9 стадий с переходной семантикой донора (422-коды, 409 SOFT_CONFLICT_DETECTED → ConflictDialog при minRating без данных рейтинга), журнал штаба, замена, закрытие с итогами направлений, read-only закрытое дело; сквозной живой прогон + 4 негативные пробы. **Ф2 Реестр ОМ — готова (0b2400f)**: /security-ops/events (поиск/фильтр в URL, создание с bindable-objects и привязкой версии паспорта по бизнес-дате, карточка-каркас — этапы в Ф3); entities/security-event несёт ПОЛНЫЙ домен ОМ (типы всех 6 этапов, stage-meta, passport-binding) — Ф3 строится на нём. Демо-сотрудники засеяны (10 чел + 12 слотов, seed-скрипты в scratchpad). **Ф1 Объекты — готова (aec5a31)**: /security-ops/objects (реестр+KPI+URL-фильтры), [id] (паспорт: форма секторов/постов + публикация версий), deep link версии; мок-стор в sessionStorage (ключ ops-mock-objects) — иначе `<a>`-навигация теряла мутации. M4-диффы закоммичены общим коммитом f2acd11 по решению Bratan. **Фаза 0 (фундамент) — готова, коммит 16a2662**: lib/ops-{api,errors,env}.ts, hooks/use-ops-{mutation,permissions}.ts, features/ops-conflict-override, components/ui/toaster.tsx (Toaster в OLD не был смонтирован нигде — toast() был no-op), mocks/ops/* (MSW только на /security-ops/*, `onUnhandledRequest:'bypass'`), app/security-ops/{layout,command-center}. Проверено живым: перехват my-permissions, регресс /ops и /statuses. ⚠️ Вне ops пред-существующе красно и без переезда: 7 падений в apps/employees и 2 ошибки сборки в apps/statuses (проверено на чистом дереве) — гейт переезда это ops-сюита, не весь проект.

**Локальный стенд (43a7515)**: Django Personnel-Records на :8100 — `cd Backend/PersonnelStatus/Personnel-Records && DJANGO_SETTINGS_MODULE=organization_management.config.settings.sqlite .venv/bin/python manage.py runserver 0.0.0.0:8100 --noreload` (venv `.venv`, БД db.sqlite3 — обе untracked). Учётки: admin/admin123 (супер), erda/erda123 (ROLE_3 + scope «Управление объектами» — единственная роль, которой открыт directorate-эндпоинт страниц Статусы/Персонал), observer/demo123 (ROLE_1). Сиды в scratchpad прошлой сессии; при пересоздании БД: makemigrations уже закоммичен, роли/права/учётки сеять заново. Next-prod стартует с BACKEND_URL=http://localhost:8100 (в launch.json; .env.local несёт мёртвый 10.115.70.56, НЕ убран). ⚠️ rewrites запекаются в build → пересборка тоже требует BACKEND_URL в env. Кэш прав LocMem per-process: сид прав из shell не виден работающему runserver — перезапускать.

**venv Personnel-Records в этом worktree СОЗДАН** (`.venv`, python 3.12, рабочий: `PR_DB_NAME=... .venv/bin/python -m pytest organization_management/apps/operations -q`; ruff в нём нет — гейт раздела это pytest). История вопроса: изначально `.venv` отсутствовал. Соседние окружения НЕ подходят: `Backend/VAPS/.venv` без corsheaders, `govtech/pr_gov/Personnel-Records/.venv` на Django 6.0.3 без django_filters (176 из 390 ops-тестов краснеют импортом `apps/audit/views.py`). Рабочий рецепт — свой venv: `uv venv <dir> --python 3.12 && uv pip install --python <dir>/bin/python -r requirements.txt -r requirements/base.txt psycopg2-binary pytest pytest-django` (Django 5.1.15 пинится requirements.txt). Тесты идут на Postgres контейнера `vaps-db-5434`; при параллельной сессии разводить `PR_DB_NAME` ([[project-test-db-collision-parallel-sessions]]).

Пред-существующий красный (НЕ регресс, проверено на чистом HEAD в одноразовом worktree): `apps/statuses` — 2 ошибки СБОРА (`No module named 'infrastructure.test_statuses_infrastructure'`), из-за них `pytest organization_management` целиком не стартует, нужен `--ignore`; `apps/employees` — те же 6 failed + 1 error, что и всегда.

Несущие факты:
- Изоляция двух MSW: sidebar ходит по `<a>` (полная перезагрузка) — host-MSW и SPA-MSW не сосуществуют в документе; гейт по pathname в mocks/ops/browser.ts.
- `getAccessToken` в lib/api.ts не экспортирован — в ops-api.ts свой.
- ESLint в OLD не установлен (`next lint` мёртв) — гейт = tsc внутри `npm run build` + `npm run check:josparlau`.
- sidebar.tsx несёт чужие незакоммиченные M4-правки — свою добавку (секция «Охранные мероприятия (натив)») НЕ коммитить вместе с ними без решения Bratan ([[feedback-parallel-story-commit-sweep]]).
- Мок-режим по умолчанию (`NEXT_PUBLIC_OPS_DATA_SOURCE!==\"api\"`); в мок-режиме права запрашиваются и без host-логина.

Связано: [[project-smart-josparlau-frontend-state]] (история SPA-стройки и host-переноса M1–M4).
