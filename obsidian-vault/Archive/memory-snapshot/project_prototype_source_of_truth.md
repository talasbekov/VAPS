---
name: project_prototype_source_of_truth
description: "Прототип Smart Josparlau живёт в Claude Design; локальные копии на диске — устаревшие снимки, и трогать их запрещено решением Bratan 19.08.2026"
metadata: 
  node_type: memory
  type: project
  originSessionId: 4d711a7b-a88a-4142-92de-5a6d1dea6bb6
---

**Источник правды по прототипу — проект Claude Design, а не файл на диске.**

`project_id = a4364827-4c67-45cd-8b23-ab917113856c` («Smart Josparlau»),
рядом `73608d4f-cf62-41a0-a954-53ab820a98c3` («VAPS Design System») — тот
самый, чьё имя носит папка `_ds/vaps-design-system-73608d4f-…`.

На диске лежат ДВЕ копии, и обе отстают:

* `Прототип/VAPS Prototype.dc.html` — **трекается в git** с 18.07.2026
  (`22fa60d5`), июльская навигация (13 пунктов, есть «План дежурств»,
  «Потребность», «Проведение» — в актуальном их нет);
* `Smart Josparlau (Прототип HTML)/Smart Josparlau.dc.html` — untracked,
  только в ГЛАВНОМ чекауте (в worktree его нет вовсе), 996 719 Б / 8169 строк.
  Совпадает БАЙТ В БАЙТ со снимком `design_handoff_reestr_gvo/` внутри
  облачного проекта, то есть это не корневой файл, а старый хендофф.

Актуальный корневой файл — 1 078 233 Б / **8870 строк**, на 701 строку больше.

🔴 **Решение Bratan 19.08.2026: дерево проекта НЕ трогать.** `Прототип/` не
заменять, файлы прототипа в репозиторий не раскладывать — «дерево не трогай,
пусть остаётся как в локальном проекте». Обе копии на диске остаются
устаревшими НАВСЕГДА, и это осознанно.

**Канал обновления — архив, который обновляет сам Bratan:**
`/home/erda/Музыка/VAPS/Smart Josparlau.zip` (507 КБ). Внутри
`handoff_smart_josparlau/design/Smart Josparlau.dc.html` — актуальный корневой
файл. Распаковывать В СКРЕТЧПАД (`unzip -d $SCRATCHPAD/proto`), не в проект, и
дальше читать обычным grep — это бесплатно и точнее любых окон.

В архиве есть то, чего нет в старых копиях: **`handoff_smart_josparlau/README.md`**
— формальная передача с картой маршрутов (`view` → экран), разбором «что важно
не потерять» по каждому экрану, ЯВНОЙ ролевой моделью из четырёх точек и
списком серверных сущностей. Читать ЕГО первым, а не разметку.

**MCP — для сверки, не для выкачивания.** `read_file` отдаёт содержимое В
ДИАЛОГ, `copy_files` копирует проект→проект НА СЕРВЕРЕ; пути «сервер → диск» у
MCP нет вовсе. Полный файл = ~10 окон по 256 КБ ≈ 250–300 тыс. токенов, почти
вся сессия. Дешёвый приём сверки без архива: якорь грепом в локальной копии +
сдвиг (на 19.08 ≈ +403 строки на уровне `this.ratingJournal`), окно в 10–15
строк. Трёх вызовов хватило, чтобы найти новый экран и ролевую модель.
См. [[project_prototype_walkthrough_progress]].
