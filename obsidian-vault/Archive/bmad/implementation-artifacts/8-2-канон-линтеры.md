---
baseline_commit: 75519a00d149a77d459c5df325dd5b392b120316
---

# Story 8.2: Канон-линтеры

Status: done

> **Контекст запуска:** вторая стори E8 (frontend-first, MSW впереди бэка). Каркас 8.1 закоммичен
> и отревьюирован; baseline диффа: `75519a0` (пост-ревью 8.1: Vite 7.3.6 / TS 5.9.3 / eslint 9.39 flat + compat).
> Эта стори — чистый тулинг: НИ ОДНОГО нового UI-компонента, НИ ОДНОЙ фичи. Только механическое
> энфорсмент-зеркало канона ARCH-FE (010/013/014/015) + самопроверка, что энфорсмент реально красный.
> Комментарий в `frontend/eslint.config.js` («Полный канон-набор … — стори 8.2») закрывается здесь.
> **Решения Bratan 2026-07-06 (Q1–Q3 = дефолты спеки):** «CI красный» = `npm run gate`, wiring корневого
> гейта остаётся deferred; prettier без format-check в гейте (зеркало ruff); бан-список полный.

## Story

As a разработчик,
I want eslint-boundaries (матрица app/features/shared), no-restricted-imports (полный канон-список банов), react-hooks: error, prettier + prettier-plugin-tailwindcss, deps-gate (запрещённые пакеты в lock) и канон-самотест красноты,
so that канон ARCH-FE enforced механически — нарушение ловится гейтом, а не ревью.

## Acceptance Criteria

1. **Given** файл в `src/features/a/` импортирует из `src/features/b/`, **When** `eslint`, **Then** ошибка `boundaries/element-types` (красный). **And** легальные направления зелёные: `features → shared`, `app → features|shared`, внутрифичевые импорты.
2. **Given** `import ... from 'zustand'` (или любой пакет канон-списка банов) в `src/`, **When** `eslint`, **Then** ошибка `no-restricted-imports` (красный) — включая `import type`.
3. **Given** `npm i zustand` БЕЗ единого импорта, **When** `npm run gate`, **Then** deps-gate красный с именем пакета (скан `package-lock.json`, ловит и транзитивные вхождения).
4. **Given** хук в условии (`if (x) useState()`), **When** `eslint`, **Then** `react-hooks/rules-of-hooks` — **error**, не warn; `exhaustive-deps` тоже error.
5. **Given** `src/**/index.ts|tsx` (barrel-реэкспорт), **When** `npm run gate`, **Then** канон-чек красный (ARCH-FE-013 MUST NOT).
6. **Given** `npm run gate` на чистом клоне (`npm ci`), **Then** ВСЯ цепочка зелёная: deps-gate → tsc → eslint → канон-самотест → vite build → size-gate; существующий код 8.1 канону не противоречит. **And** канон-самотест доказывает красноту фикстурами (AC 1/2/4/5) и зелёность легального импорта — вакуумный pass невозможен (урок 8.1).
7. **Given** prettier 3.x + `.prettierrc` с `prettier-plugin-tailwindcss`, **Then** `npx prettier --check src/` зелёный (существующие файлы единожды отформатированы); format-check в gate НЕ входит (зеркало ruff-прецедента бэка: гейт = check, не format).

## Tasks / Subtasks

- [x] Task 1: Зависимости (AC: 1, 2, 4, 7)
  - [x] `npm i -D eslint-plugin-boundaries@^7.0.0 eslint-plugin-react-hooks@^7.1.1 prettier@^3.9.4 prettier-plugin-tailwindcss@^0.8.0 eslint-config-prettier@^10.1.8` (версии проверены против registry 2026-07-06; boundaries 7.0 и react-hooks 7.x поддерживают eslint 9 flat)
- [x] Task 2: eslint.config.js — канон-набор (AC: 1, 2, 4)
  - [x] Блок boundaries: `settings['boundaries/elements']` = `[{type:'app',pattern:'src/app/**'},{type:'features',pattern:'src/features/*',capture:['feature']},{type:'shared',pattern:'src/shared/**'}]`; правило `boundaries/element-types` (default: disallow): app→[app,features,shared]; features→[shared] + та же фича (`capture` match); shared→[shared]
  - [x] Блок no-restricted-imports для `src/**`: канон-список банов (см. Dev Notes, ПОЛНЫЙ — не сокращённый из текста эпика); использовать `@typescript-eslint/no-restricted-imports` (ловит `import type`)
  - [x] Path-scoped впрок (ARCH-FE-015): для `src/features/**` дополнительно бан `importNames: ['useMutation']` из `@tanstack/react-query` (пакет ещё не установлен — правило спит до 8.4/8.5)
  - [x] react-hooks: flat-конфиг плагина + оба правила `error` (свериться с README v7 за точным именем экспорта flat-конфига)
  - [x] `eslint-config-prettier` — ПОСЛЕДНИМ элементом (гасит стилистические конфликты)
  - [x] НЕ потерять существующие блоки: compat на `src/**` и js/mjs-блок node-скриптов (ревью 8.1)
- [x] Task 3: prettier (AC: 7)
  - [x] `.prettierrc.json` (`plugins: ["prettier-plugin-tailwindcss"]` + минимум опций — дефолты prettier) + `.prettierignore` (dist, node_modules, package-lock.json)
  - [x] Единожды `npx prettier --write src/ scripts/ *.ts *.js *.json` — привести существующее (диф мелкий: ~6 файлов 8.1)
- [x] Task 4: deps-gate (AC: 3)
  - [x] `scripts/deps-gate.mjs`: скан ключей `packages` в `package-lock.json` на `node_modules/<banned>`; список банов — единый источник с eslint-списком (константа в скрипте, eslint-конфиг импортирует ИЛИ два списка с cross-check в канон-самотесте — решить при имплементации, зафиксировать в Dev Record)
- [x] Task 5: канон-самотест (AC: 5, 6)
  - [x] `scripts/lint-canon.test.mjs`: создаёт временные фикстуры (`src/features/__canon_a__/probe.tsx` и пр.) → прогоняет eslint (Node API или child `npx eslint --no-ignore`) → ассертит КОНКРЕТНЫЕ ruleId по AC 1/2/4 + зелёность легального `features→shared` → `finally` удаляет фикстуры
  - [x] Barrel-чек там же: рекурсивный поиск `src/**/index.ts|tsx` → найдено → красный с путём (AC 5)
  - [x] Урок 8.1: пути через `fileURLToPath` (кириллица в пути репо ломает `URL.pathname`)
- [x] Task 6: gate-цепочка (AC: 6)
  - [x] `package.json` scripts: `gate` = `node scripts/deps-gate.mjs && tsc -b && eslint . && node scripts/lint-canon.test.mjs && vite build && node scripts/size-gate.mjs` (быстрые проверки первыми); `format` = `prettier --write .`
  - [x] Чистый прогон: `npm ci && npm run gate` зелёный

### Review Findings

Code-review 2026-07-07 (bmad-code-review, Fable 5 ×3 слоя Blind/Edge/Auditor — same-model caveat). Auditor: 7/7 AC SATISFIED, лок-файл чист (registry.npmjs.org, версии по спеке), границы стори соблюдены.

- [x] [Review][Decision] Бан-список — перечисление против категорийной претензии — комментарии заявляют «стейт вне канона» / «runtime CSS-in-JS», но valtio/recoil/effector/nanostores и goober/@stitches/styled-jsx не забанены. Код соответствует спеке (список = канон architecture L237/241/249 дословно); расширение — правка канона, решает Bratan: расширить сейчас / зафиксировать в deferred / оставить перечисление как есть.
- [x] [Review][Patch] Файлы вне app/features/shared выпадают из boundaries-матрицы (обход размещением в src/lib и т.п.) — включить `boundaries/no-unknown-files` + `boundaries/no-unknown` error; легальных unknown-файлов нет (main/App уже в src/app) [frontend/eslint.config.js:48]
- [x] [Review][Patch] Самотест не доказывает разрешённые направления (app→, same-feature) и не исполняет общий src-бан-блок (все бан-фикстуры в features) — +зелёные фикстуры app→features|shared и same-feature, +красная бан-фикстура вне features, +красная no-unknown-фикстура [frontend/scripts/lint-canon.test.mjs:96]
- [x] [Review][Patch] Легаси-ключи в message-шаблоне boundaries — рендерится пустое «( → )»; заменить на действующие v7-ключи [frontend/eslint.config.js:54]
- [x] [Review][Patch] deps-gate обходится npm-алиасом (`npm i x@npm:zustand` → ключ node_modules/x, поле name=zustand не проверяется) — матчить и поле `name` записи lock [frontend/scripts/deps-gate.mjs:31]
- [x] [Review][Patch] Осиротевшие фикстуры (kill −9 до finally) валят tsc/eslint ДО самотеста; параллельные прогоны коллидируют на фиксированных путях — eslint-ignores + tsconfig-exclude на `__canon_*` (самотест линтит их через ignore:false) + PID-суффикс путей фикстур [frontend/scripts/lint-canon.test.mjs:21]
- [x] [Review][Patch] `.jsx/.js/.mjs/.cjs` в src невидимы для всех канон-блоков (eslint не матчит, barrel-regex только tsx?) — файловый скан в самотесте: src TS-only [frontend/scripts/lint-canon.test.mjs:59]
- [x] [Review][Patch] Dev Record: «в v7 правило boundaries/dependencies (не element-types)» — неточность: element-types в v7 существует с deprecated:true; уточнить формулировку [спека, Debug Log]
- [x] [Review][Defer] Динамический `import('@tanstack/react-query')` обходит бан useMutation (no-restricted-imports статичен; deps-gate не страхует — пакет канонный) [frontend/eslint.config.js:106] — deferred, актуализируется при установке пакета в 8.4/8.5

Dismissed (4): prettier --check не в гейте (решение Q2 Bratan — зеркало ruff); «пустые src/features|shared остаются» (ложная посылка — каталоги с .gitkeep существуют с 8.1); .prettierignore без node_modules (дефолт prettier 3.x); react-hooks recommended шире двух правил (направление канона, info).

## Dev Notes

### Архитектурные гварды (обязательны, источник — architecture.md)

- **ARCH-FE-013** (L240/760): feature-folders `app/ features/ shared/`; `features/A→features/B` запрещено; `shared→features` запрещено; `app→всё`; barrel-index.ts реэкспорты — MUST NOT; enforcement = eslint-plugin-boundaries («зеркало AST-тестов бэка»). Пробел архитектуры, ЗАКРЫВАЕМЫЙ этой спекой: `features→app` и `shared→app` тоже запрещены (единственная односторонняя привилегия — у app; анти-пример «shared→app» в Decision Register L760).
- **ARCH-FE-010** (L237/757): стейт = TanStack Query + URL params + useState/useReducer + 2 Context. Бан: zustand/jotai/redux/mobx. Enforcement ДВУХСЛОЙНЫЙ: «no-restricted-imports + CI: npm ls … && exit 1» — потому deps-gate обязателен, eslint один AC-3 не закрывает.
- **ARCH-FE-011** (L238/758): бан orval/openapi-generator (кодоген клиентов); только openapi-typescript (придёт в 8.3).
- **ARCH-FE-014 финал-2** (L241/331/761, ревизия 2026-07-04): styled-components/emotion (runtime CSS-in-JS) — бан (клиенты: 4 ГБ RAM, без GPU — L58/246); prettier-plugin-tailwindcss — часть канона.
- **ARCH-FE-015** (L242/762): «сырой useMutation в features» — бан через no-restricted-imports (paths). Вводим впрок (правило спит: пакета нет до 8.4/8.5).
- **react-hooks: error** (L244) — блок «анти-смерть на 4 ГБ»; оба правила error.
- **Чёрный список UI** (L249): MUI, Vuetify, AG Grid, Handsontable, полный AntD, Quasar — в бан-список импортов И deps-gate.
- **compat остаётся в гейте** (L256) — блок compat из 8.1 не трогать/не терять.

### Канон-список банов (полный, единый для eslint и deps-gate)

`zustand`, `jotai`, `redux`, `react-redux`, `@reduxjs/toolkit`, `mobx`, `mobx-react`, `mobx-react-lite`, `orval`, `@openapitools/openapi-generator-cli`, `styled-components`, `@emotion/react`, `@emotion/styled`, `@emotion/css`, `@tanstack/react-router` (ARCH-FE-012: только React Router), `@mui/material`, `@mui/system`, `antd`, `ag-grid-community`, `ag-grid-enterprise`, `handsontable`, `quasar`, `vuetify`. Для scoped-семейств (`@emotion/*`, `@mui/*`) в eslint использовать `patterns`, в deps-gate — префикс-матч. Текст эпика (L1012) называет 4 имени — это СОКРАЩЕНИЕ; полный канон из architecture L237/238/241/249 (подтверждено research 2026-07-06).

### Границы стори (не расползаться)

- **НЕТ**: Tailwind и eslint-plugin-tailwindcss (оба — 8.7, когда появится вендоринг ДС донора: plugin требует установленный tailwindcss; в тексте стори 8.2 его и нет); Vitest/RTL/MSW (тестовая пирамида приходит с shared/api-сториями); линт literal-путей routes.ts (ARCH-FE-012: «ревью-правило, ужесточить линтом при нарушениях» — 8.7+); wiring корневого `make gate`/CI (остаётся в deferred-work из ревью 8.1 — см. Q1); React Compiler (заявлен L229, но не установлен — не сейчас); подслои `shared/api↔ui↔lib` (архитектура правил не задаёт — появятся с наполнением shared).
- **НЕ трогать**: `scripts/size-gate.mjs` (закалён ревью 8.1), `vite.config.ts`, tsconfig-и, `Backend/**`, корневой `.gitignore` (негейт `!frontend/**/lib|build/` уже стоит).

### Фактура (ground truth после ревью 8.1, коммит 75519a0)

- `frontend/eslint.config.js` сейчас: `tseslint.config({ignores:['dist']}, {files:['**/*.{ts,tsx}'], extends:[js.recommended, ...tseslint.recommended], ecmaVersion 2022, globals.browser}, {files:['src/**/*.{ts,tsx}'], ...compat.configs['flat/recommended']}, {files:['**/*.{js,mjs}'], js.recommended + globals.node})`. Канон-блоки ДОБАВЛЯЮТСЯ в эту структуру.
- Установлено: eslint 9.39, typescript-eslint 8.46, eslint-plugin-compat 6.0.2, globals 16, TS 5.9.3, Vite 7.3.6. Node 24 (`.nvmrc`).
- `src/features/` и `src/shared/` — ПУСТЫЕ (`.gitkeep`): boundaries-матрица будет доказана ТОЛЬКО фикстурами канон-самотеста — потому он обязателен, иначе конфиг непроверяем до 8.4+.
- Гейт сейчас: `tsc -b && eslint . && vite build && node scripts/size-gate.mjs`; js/mjs-блок линта уже покрывает НОВЫЕ скрипты автоматически.

### Ловушки

1. **boundaries в flat-конфиге**: настройки живут в `settings` конфиг-объекта (`'boundaries/elements'`, `'boundaries/include': ['src/**/*']`), правило — `boundaries/element-types` с `default: 'disallow'` и allow-матрицей. Матчер той-же-фичи — через `capture: ['feature']` и `[['features', {feature: '${from.feature}'}]]`-паттерн (README v7). Не забыть `plugins: {boundaries}` в блоке.
2. **Вакуумный pass канон-самотеста** (урок ревью 8.1 — там им страдал size-gate): самотест ОБЯЗАН упасть, если eslint вернул 0 ошибок на заведомо красной фикстуре; ассертить именно ruleId, не «есть хоть какая-то ошибка» (иначе синтакс-ошибка фикстуры маскирует пробел конфига). Негативный контроль (легальный импорт зелёный) обязателен — иначе конфиг «всё запрещено» тоже пройдёт.
3. **Фикстуры vs tsc/build**: временные файлы живут только внутри работы самотеста (`finally` удаляет даже при падении ассертов); `tsc -b` и `vite build` в gate-цепочке идут в другие моменты времени — пересечения нет. Паттерн `__canon_*` добавить в `.gitignore` фронта (страховка от осиротевших фикстур при kill -9).
4. **`@typescript-eslint/no-restricted-imports`, не базовое** — базовое пропускает `import type {X} from 'zustand'`; тип-импорт запрещённого стейт-менеджера — тоже нарушение канона.
5. **eslint-config-prettier ПОСЛЕДНИМ** — иначе стилистические правила из recommended-набора конфликтуют с prettier-форматированием (классический дребезг).
6. **react-hooks v7**: точное имя flat-экспорта менялось между v5/v6/v7 (`configs['recommended-latest']` → `configs.flat.recommended`) — свериться с установленной версией, не копировать из старых туториалов. Правила скоупить на `src/**/*.{ts,tsx}` (node-скриптам react-hooks не нужен).
7. **Кириллица в пути репо** (`/home/erda/Музыка/VAPS`): в новых скриптах — `fileURLToPath(new URL(...))`, НЕ `new URL(...).pathname` (урок закреплён комментарием в size-gate.mjs).
8. **deps-gate по lock, не по node_modules**: `package-lock.json` — единственный источник правды, работает на чистом клоне до `npm ci`; ключи вида `node_modules/zustand` и `node_modules/a/node_modules/zustand` (транзитивный) ловятся одним startsWith/includes-матчем по сегменту.
9. **prettier --write однократный**: дифф форматирования смешается с канон-диффом — прогнать ДО остальных правок отдельным аккуратным шагом (или отдельным коммит-стейджем в Dev Record зафиксировать), чтобы ревью могло отделить механику от канона.

### Project Structure Notes

- Файлы create: `frontend/scripts/deps-gate.mjs`, `frontend/scripts/lint-canon.test.mjs`, `frontend/.prettierrc.json`, `frontend/.prettierignore`.
- Файлы modify: `frontend/eslint.config.js`, `frontend/package.json` (+`package-lock.json` генерируемый), `frontend/.gitignore` (+1 строка `__canon_*`), `src/**` — только след prettier --write (без семантики).
- Целевое дерево не меняется (ARCH-FE-013 уже стоит с 8.1); стори добавляет только тулинг-слой.
- Соответствие BMAD-правилам размера: одна ответственность (линт-канон), 5 содержательных файлов, бэкенд не затронут, миграций нет.

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story 8.2 (L1010-1016)] — текст стори и AC
- [Source: _bmad-output/planning-artifacts/architecture.md#Канон фронтенд-стека (L233-249)] — ARCH-FE-010…015 + чёрный список UI
- [Source: _bmad-output/planning-artifacts/architecture.md#Decision Register (L757-762)] — enforcement-колонки, анти-примеры
- [Source: _bmad-output/planning-artifacts/architecture.md#L479] — сводная механика enforcement (eslint boundaries/no-restricted-imports/react-hooks/compat + CI npm ls)
- [Source: _bmad-output/implementation-artifacts/8-1-scaffold-vite-react-ts-с-контурной-донастройкой.md#Review Findings + Post-Review Fixes] — уроки: вакуумный pass, fileURLToPath, js/mjs-линт-блок, deferred wiring корневого гейта
- [Source: _bmad-output/implementation-artifacts/deferred-work.md#Deferred from code review 8.1 (2026-07-06)] — make gate wiring НЕ в этой стори (Q1)
- Версии registry (2026-07-06): eslint-plugin-boundaries 7.0.0 (peer eslint>=6), eslint-plugin-react-hooks 7.1.1, prettier 3.9.4, prettier-plugin-tailwindcss 0.8.0 (peer только prettier ^3 — tailwindcss НЕ требуется, сортирует по дефолт-порядку до прихода конфига донора в 8.7), eslint-config-prettier 10.1.8

## Dev Agent Record

### Agent Model Used

Claude Fable 5 (claude-fable-5), bmad-dev-story, 2026-07-06.

### Debug Log References

- Red-фаза (до конфига): кросс-фичевый импорт + `import type` из zustand проходили eslint молча (EXIT=0) — необходимость канона доказана.
- ⚠️ Главная находка: boundaries v7 МОЛЧА зелёный без `settings['import/resolver'].node.extensions: ['.ts','.tsx']` — дефолтный резолвер не знает TS-расширений, цель импорта `isUnknown: true`, правило не срабатывает (диагноз через `ESLINT_PLUGIN_BOUNDARIES_DEBUG=1`). Именно такой класс молчаливых провалов ловит канон-самотест.
- Ловушка №1 спеки описывала v6-синтаксис: в v7 правило `boundaries/dependencies` (преемник `element-types`, который в v7 существует с deprecated:true), опция `policies` (не `rules`), объектные селекторы `{from: {element: {types}}}`, шаблоны `{{from.feature}}` (не `${...}`), матчер той-же-фичи — поле `captured` (сверено по typings `@boundaries/elements`). Legacy-синтаксис не падает, а деградирует с warning — вторая причина обязательности самотеста.
- Матрица проверена вживую 5/5 до самотеста: cross RED, shared→features RED, features→shared GREEN, same-feature GREEN, app→shared GREEN.
- deps-gate red-фаза: реальный `npm i zustand` → EXIT=1 с именем пакета; lock/package.json откатаны, `npm prune` — AC-3 дословно.
- react-hooks v7: flat-экспорт `configs.flat.recommended` (есть также `recommended-latest`); оба классических правила принудительно `error`.
- Самотест ужесточён после первого зелёного прогона: ассерт `severity === 2` (правило в warn прошло бы мимо канона «error, не warn»).

### Completion Notes List

- Task 4, развилка «единый источник»: выбран отдельный модуль `scripts/banned-packages.mjs` — его импортируют И eslint.config.js (BANNED_IMPORT_PATTERNS), И deps-gate.mjs (BANNED_PACKAGES/BANNED_SCOPES); cross-check не нужен, источник один. Scoped-семейства (@reduxjs/@emotion/@openapitools/@mui) банятся целиком, @tanstack — только react-router (react-query — канон).
- `.prettierrc.json`: `semi: false, singleQuote: true` — подгон под идиому Vite-шаблона, дифф однократного `--write` минимален (3 файла, только переносы строк). Format-check в гейт НЕ входит (решение Q2), `npm run format` добавлен.
- Гейт-цепочка: deps-gate первым (миллисекунды), самотест после `eslint .` (тёплый конфиг), бандл не изменился (59.4/300 КБ gzip — все канон-пакеты dev-only).
- ARCH-FE-015 впрок работает: фикстура `useMutation` красная уже сейчас (eslint статичен, установка пакета не нужна).
- `__canon_*` в frontend/.gitignore — страховка от осиротевших фикстур при аварийном обрыве самотеста.
- Чистый прогон `npm ci && npm run gate` зелёный (AC-6). AC-1 формулировала ruleId как `boundaries/element-types` (v6-имя) — фактический ruleId в v7 `boundaries/dependencies`, семантика та же.

### File List

- `frontend/scripts/banned-packages.mjs` — new (единый источник канон-банов: 16 имён + 4 scope)
- `frontend/scripts/deps-gate.mjs` — new (скан package-lock, вкл. транзитивные; гвард пустого lock)
- `frontend/scripts/lint-canon.test.mjs` — new (5 красных фикстур + негативный контроль + barrel-скан, severity-ассерт)
- `frontend/.prettierrc.json` — new (semi:false, singleQuote, plugin-tailwindcss)
- `frontend/.prettierignore` — new (dist, package-lock.json)
- `frontend/eslint.config.js` — modified (boundaries v7 dependencies/policies + import/resolver TS-ext, no-restricted-imports ×2 блока, react-hooks error, config-prettier последним)
- `frontend/package.json` — modified (5 devDeps, scripts: gate-цепочка + format)
- `frontend/package-lock.json` — modified (генерируемый)
- `frontend/.gitignore` — modified (+`__canon_*`)
- `frontend/vite.config.ts`, `frontend/scripts/size-gate.mjs` — modified (только prettier-переносы, без семантики)

### Post-Review Fixes (2026-07-07)

Проход 1 bmad-code-review: 1 decision (решён Bratan: расширить) + 7 patch применены, 1 defer → deferred-work.md, 4 dismiss. Все патчи верифицированы красными пробами:

- **Бан-список расширен сверх исходного канона** (решение Bratan): +valtio/recoil/effector/effector-react/nanostores (стейт), +goober/styled-jsx (CSS-in-JS), +scope @stitches/@nanostores; ревизия отражена в architecture.md (таблица ARCH-FE + нота о едином источнике `banned-packages.mjs`). Итог: 23 имени + 6 scope.
- **boundaries/no-unknown-files + no-unknown-dependencies: error** — файл вне app/features/shared больше не обходит матрицу (недепрекейченное имя `no-unknown-dependencies`, зеркало кейса element-types→dependencies).
- **Самотест расширен до полной матрицы**: 8 красных фикстур (+ бан вне features — исполняет общий src-блок; + escape-файл вне слоёв → no-unknown-files; + импорт unknown из фичи → no-unknown-dependencies) и 3 негативных контроля (features→shared, same-feature, app→features+shared). Регресс `captured`-матчера или потеря блока конфига теперь красные.
- **v7-шаблон message**: `{{from.type}} → {{to.type}}` (легаси `{{file.type}}/{{dependency.type}}` рендерились пусто); проверено вживую: «features → features».
- **deps-gate ловит npm-алиас**: матч и по полю `name` записи lock (`npm i canon-ok@npm:zustand` → красный «zustand (алиас node_modules/canon-ok)», проверено doctored-lock).
- **Сироты/параллель**: PID-суффикс путей фикстур + pre-clean осиротевших `__canon_*` в самотесте + eslint global-ignores `src/**/__canon_*/**` + tsconfig.app.json exclude (осознанное отступление от «не трогать tsconfig-и», прецедент ревью 8.1; самотест линтит фикстуры через `ignore:false`).
- **TS-only src**: файловый скан `.js/.jsx/.mjs/.cjs` в самотесте (такие файлы невидимы канон-блокам eslint) + самопроверка сканера фикстурой probe.js.
- Формулировка Debug Log про element-types уточнена (deprecated, не отсутствует).

Файлы сверх исходного File List: `frontend/tsconfig.app.json` (exclude `__canon_*`), `_bmad-output/planning-artifacts/architecture.md` (ревизия бан-списков). Верификация: `npm run gate` зелёный (59.4/300 КБ gzip, без депрекейт-варнингов), `npx prettier --check .` зелёный, red-пробы алиаса/valtio/сообщения — красные как положено.
