---
title: Архитектура бэкенда
module: personnel-records
updated: 2026-08-21
tags: [backend, architecture]
---

# Архитектура бэкенда (Personnel-Records)

Django-проект `organization_management` в `Backend/PersonnelStatus/Personnel-Records`. DRF + drf-spectacular (схема `/api/schema/`, Swagger `/docs`), JWT (`/api/token/`), Channels (WS), Celery (регламентные задачи; на локальном стенде брокер недоступен — см. [[Known-Issues#Celery-задачи статусов не доезжают на стенде]]). БД — PostgreSQL (контейнер `vaps-db-5434`). Парный фронт — [[../Frontend/Архитектура|PersonalRecordFront]] (:3106).

## Django-приложения

| App | Ответственность | API-префикс |
| --- | --- | --- |
| `common` | User (кастомная модель), Role/Permission/UserRole — RBAC; плоские коды прав (`event.view`), пагинация, троттлинг | `/api/common/` |
| `employees` | Сотрудники, маскирование sensitive-полей (ИИН), валидаторы | (через core/staff_unit) |
| `divisions` | Подразделения — MPTT-дерево | `/api/divisions/` |
| `staff_unit` | Штатные единицы; связь Employee→StaffUnit→Division; ручки директорий | `/api/staff_unit/` |
| `statuses` | Статусы сотрудников: сервисный слой инвариантов, сигналы, селекторы, задачи | `/api/statuses/` |
| `secondments` | Прикомандирования | `/api/secondments/` |
| `dictionaries` | Справочники (редактирование — только Admin) | `/api/dictionaries/` |
| `documents` | Вложения, DocumentSequence | (внутри core) |
| `reports` | Отчёты | `/api/reports/` |
| `notifications` | Уведомления + WS-consumers | `/api/notifications/` |
| `audit` | Журнал аудита (слоёный: domain/application/infrastructure), middleware request-id | `/api/audit/` |
| `core` | Перенесённый контракт донора `/api/core/*` (переезд завершён — см. [[Decisions]]) | `/api/core/` |
| `operations` | Домен «Расход дня»: bulk-статусы, сдача дня (`DailySubmission`), амендменты, светофор (`traffic-light`), блокировки/override, документы расхода (DOCX/XLSX/PDF/CSV, golden master), catch-up/clock, увольнение, RBAC-прокси (roles/permissions), `my-permissions`/`my-employee` | `/api/operations/` |
| `ops` | Домен раздела «Охранные мероприятия»: security_events (жизненный цикл ОМ), passport (объекты/паспорта), duties (типы/смены), combat (боевые группы/маршруты), daily (суточный свод, адаптеры расхода), gvo (каталог ОЛ + патчи сводок), законы (`legal-documents`), ratings, analytics, reports, feedback, dictionaries, settings, audit-logs | `/api/ops/` |

Два «operations» не путать: `operations` — расход дня и платформенные сервисы (историческое имя), `ops` — раздел ОМ. У обоих свои `audit-logs`.

## Ключевые инварианты (держатся сервисным слоем и БД)

- **Один активный статус на сотрудника** — `create_status` закрывает предыдущий (lte); `EmployeeStatus.save()` зовёт `full_clean()`.
- **Статус по умолчанию каждому работающему** — сигнал on_commit + команда `ensure_employee_statuses` (порядок: применить запланированные → закрыть истёкшие).
- **Append-only аудит** — мутации статусов/сдач/увольнений журналируются; coverage-тест.
- **DB-уровень для инвариантов** — CheckConstraint для choice-полей без дефолта и числовых floor; conditional-CHECK с `isnull=False` (NULL-пробел); EXCLUDE/GiST на пересечение статусов.
- **Сдача дня** — снапшот-срез; ретро-правки только амендментом (новая версия, гонка = 409); блокировка «на завтра» с override.
- **Протокол 409/override** — `OVERRIDABLE_CODES`, `DomainError` + единый exception handler, коды в `error_codes.py` (сверять с raise-сайтами, не с `docs/registries/error-codes.yaml`).
- **Архитектурные гварды** — operations не импортирует core.models; Django Admin — только справочники; ARCH-SEC-030 сканирует и докстринги.

## Тестирование

- Гейт: `PR_TEST_DB_NAME=<уникальное> ./.venv/bin/python -m pytest -q` из Personnel-Records; **без** форса `DJANGO_SETTINGS_MODULE` (штатный `pytest.ini` → `settings.test`; форс краснит тесты изоляции БД).
- Тестовая БД разведена по чекауту (`test_personnel_records_<sha1(BASE_DIR)[:8]>`).
- Полный прогон 21.08.2026 — **3109 passed / 0 failed** (для зелени нужны `pypdf`+`hypothesis` в `.venv`); `operations` отдельно — 2688.
- Данные сеются напрямую (`bulk_create`), factory_boy не используется. `ruff` в `.venv` не установлен — гейт линта см. [[../Infrastructure/Status]].

## Стенд

Django `:8100` (`DJANGO_SETTINGS_MODULE=...settings.local_postgres`, БД `personnel_records` в контейнере `vaps-db-5434` — **не пересоздавать**, живые данные стройки) + Next `:3106`; логин `admin/admin123`. Ямы подъёма и ловушки соседних чекаутов — [[Status]].

## Наполнение стенда демо-данными (Plane №198)

Единая точка входа — `manage.py seed_demo_all`; порядок шагов несущий, каждый
следующий ссылается на заведённое предыдущим:

| Шаг | Команда | Что заводит |
|---|---|---|
| 1 | `seed_org_structure` | 3 департамента / 18 управлений / 27 отделов / 6 сквозных (48 узлов) под СУЩЕСТВУЮЩИМ корнем |
| 2 | `seed_positions_ranks` | лестница из 9 должностей и 10 званий; строки стенда усыновляются по имени |
| 3 | `seed_staffing` | 426 штатных единиц: начальник и заместитель везде, плюс 10 исполнителей в отделах и сквозных управлениях |
| 4 | `seed_employees` | 426 человек на слотах; всё выводится из номера слота, случайности нет |
| 5 | `seed_employee_photos` | аватарки из `docs/PersonnelStatus/attendee_photos` по кругу, с уменьшением до 512×512 |
| 6-8 | `seed_status_types`, `seed_operations`, `seed_legal_documents` | раздел ОМ; снимаются флагом `--skip-ops` |

- Каждая команда идемпотентна и гоняется отдельно: оркестратор своих строк в
  базу не пишет вовсе.
- Границы: трогается только «своё» — подразделения с кодом `SEED-`, люди с
  табельным `SD…`. Четырнадцать старых сотрудников стенда и их слоты не
  затрагиваются, потому что на них настроены пробы смоука.
- `seed_demo_all --wipe` сносит кадры в обратном порядке и вслух говорит, что
  данные раздела ОМ остались: сноса у тех команд нет.
- Справочники портала: живыми остались `Position` и `Rank`, остальные семь
  архивированы (`apps/dictionaries/archived.py`) — см. [[Decisions]].
