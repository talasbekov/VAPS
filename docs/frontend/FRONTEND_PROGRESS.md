# FRONTEND_PROGRESS

## Реализовано

### Этапы 0–1 — Done (baseline, mock-runtime foundation, MSW bootstrap, persona/demo toolbar)

### Этап 2 (первый сквозной срез ОМ) — **Done**
Все 6 стадий базового жизненного цикла ОМ реализованы и проверены и автотестами, и вручную в браузере от начала до конца одного сценария:

**Бюллетень** → **Рекогносцировка** (чек-лист 6 пунктов + посты/секторы) → **Потребность** (строки расчёта, утверждение с авто-агрегацией в запросы группам) → **Запрос сил** (ручное выделение, статусы NOT_SENT/PARTIALLY/ALLOCATED) → **Расстановка** (назначение/снятие сотрудников на посты, hard-rule двойного назначения, read-only кадровый ростер через `GET /api/ops/personnel/`) → **Согласование** (утвердить / вернуть на доработку с причиной, откат стадии).

- `SecurityEvent` — полная модель (bulletin/recon/demand/forces/placement/approval поля).
- Repository: 15 доменных операций (list/get/create/updateBulletin/updateRecon/completeRecon/approveDemand/updateForceAllocation/completeForces/assignPlacement/unassignPlacement/completePlacement/approvePlacement/returnPlacement), все с permission-check + server-like валидацией + атомарными мутациями.
- `SecurityEventDetailPage` ветвится по `event.stage` — единый паттерн для всех 6 стадий, честные read-only сводки для не-текущих стадий (не мёртвый контент).
- **Найдено и исправлено 2 реальных бага** при браузерной проверке (не гипотетических): (1) `DemoClock.advanceMs()` терял таймзону, нормализуя в UTC — переписан на epoch+offset; (2) `ensureSeeded()` не ловил дрейф ФОРМЫ существующего слайса при добавлении новых полей — добавлено `SCHEMA_VERSION`-версионирование с безопасным полным reset. Оба закрыты тестами (`demo-clock.test.ts`, `demo-runtime.test.ts`).
- Сознательно НЕ реализовано (см. `FRONTEND_DECISIONS.md` A15-A18): межсобытийные конфликты (Epic 16.3), точный подсчёт need vs assigned (демо-ростер мал), материалы/фото рекогносцировки (нет blob-хранилища).

### Этап 3 (остальной жизненный цикл ОМ) — **Done**
Оставшиеся 3 стадии реализованы поверх Этапа 2, доведены до `CLOSED` (терминал):

**Согласование** (утверждение → авто-переход в «Ознакомление», без промежуточного клика; возврат → откат в «Расстановка») → **Ознакомление** (каждое назначение подтверждается индивидуально; переход в «Проведение» разблокирован только когда ВСЕ назначения подтверждены) → **Проведение** (журнал штаба: инструктаж/распоряжение/инцидент/замена — добавление без редактирования/удаления, хронологический append-only) → **Закрыто** (обязательные итоги по КАЖДОМУ направлению из `reconSectorPosts.sector`, терминальная стадия — read-only сводка).

- `SECURITY_EVENT_STAGES` расширен до 9 значений; `SCHEMA_VERSION` поднят до 5 (backfill demo-данных старой формы через safe-reset).
- Repository: +4 операции (`acknowledgePlacement`, `completeAcknowledgement`, `addJournalEntry`, `closeSecurityEvent`) — итого 19, все permission-check + server-like валидация + атомарные мутации.
- `StageTracker` переведён с хардкод `grid-cols-6` на динамическую сетку по `SECURITY_EVENT_STAGES.length` (иначе визуально ломался на 9 стадиях).
- `ApprovalPanel`: убрана мёртвая ветка `approvalStatus === 'APPROVED'` — недостижима с автопереходом (approve/return теперь ВСЕГДА уводят со стадии APPROVAL, `PENDING` — единственное видимое состояние).
- **Найден и исправлен реальный баг** при браузерной проверке: `securityEventAcknowledgePath` и `securityEventAcknowledgementCompletePath` использовали общий сегмент `acknowledgement/`, из-за чего path-to-regexp у MSW матчил `POST .../acknowledgement/complete/` через РАНЕЕ зарегистрированный роут `.../acknowledgement/:assignmentId/` (assignmentId="complete") → 404 от не того handler'а. Фикс: индивидуальное подтверждение переименовано в `.../acknowledge/:assignmentId/` (разные сегменты для разных операций).
- Сознательно НЕ реализовано: опрос по факту и оценки участников (Epic 18.3, hidden-score) — см. `FRONTEND_DECISIONS.md`; архив ОМ (отдельный список закрытых, вне текущего Reestr) — Not started.

## Проверено (автоматически)
- `npm run gate`: **58 test files, 848 tests**, все зелёные (включая деф. и `--mode mock` сборки).
- `vite build` (production, дефолт): **202.7 KB gzip** из 300 KB бюджета.
- `features/security-events/mocks/repository.test.ts`: 34 теста покрывают все 19 операций (permission/validation/business-rule/persistence).

## Проверено вручную в браузере (dev:mock, 2026-07-24)
Полный цикл на persona `admin` (ОМ-2026-3, из стадии «Согласование»): утверждение расстановки → авто-переход «Ознакомление» → отметка ознакомления 1/1 → «Начать проведение» → «Проведение» (добавлена запись журнала типа «Инструктаж») → «Закрытие мероприятия» (итог по направлению A) → «Закрыто» (терминальная сводка, `closedAt` проставлен). Реестр ОМ отражает `Закрыто` для события сразу после закрытия. Каждый переход подтверждён снимком accessibility-дерева.

### Этап 4 (личный состав, §20 мастер-промпта) — **Done (в объёме «Сотрудники»)**
`EmployeesStub` заменён на реальный `features/personnel`: список сотрудников (поиск + фильтр по подразделению) и карточка сотрудника — оба read-only, поверх РЕАЛЬНОЙ (`existing`, не pending-contract) donor-схемы `/api/core/employees|divisions|positions|ranks/` (ARCH-FE-011: типы из `schema.d.ts`, не рукописные).

- §20.2 удалено относительно прототипа: полный ИИН (маскируется до последних 4 цифр), интерактивный Select статуса, кнопка «Редактировать», кадровые операции/ставки/вакансии, локально рассчитанный рейтинг, «Дата найма» как сортировка.
- §20.4: кадровое (identity/employment, read-only из донора) и оперативное (Smart Josparlau: дежурства/ОМ/рейтинг) состояния НЕ объединены в одну сущность — карточка сотрудника честно показывает секцию «Not started» для оперативных данных вместо придуманных цифр (запрет §35).
- Донорские `/api/core/*` эндпоинты не были замочены в `dev:mock` ни для одной существующей фичи (daily-grid/traffic-light/expense используют собственные per-test MSW-серверы, не общий demo-runtime) — добавлены MSW handlers в `features/personnel/mocks/handlers.ts`, зарегистрированы в `compose-handlers.ts`.
- Permission — переиспользован существующий `status.view` (уже гейтил маршрут `/employees` до этой стори), новый код не вводился.

### Этап 5 (служба, §21 мастер-промпта) — **Done (в объёме «Объекты и паспорта»)**
Новая mock-only-demo фича `features/objects` (тот же паттерн, что security-events: repository/handlers/fixtures/runMutation) — реестр объектов (поиск) и редактируемый паспорт (секторы + постоянные посты, permission `ops.object.manage`).

- §21.5 разделение состояний: `objectState`/`passportState` — РАЗНЫЕ поля (не один бейдж), актуальность/данные-подтверждены — Not started (нет policy-версии в demo-срезе).
- §21.7 KPI реестра (серверные агрегаты «Паспорта актуальны», «Красный статус» и т.д.) — Not started: считать по текущей странице таблицы запрещено §21.7, а серверного агрегата в demo-срезе нет — честнее не показывать.
- `objects_admin` persona (`ops.object.view/manage`, `ops.passport.publish` — уже были в каталоге с Этапа 1) получила реальный `homeRoute`.
- Сознательно НЕ реализовано в этом срезе: план дежурств, история дежурств, единый календарь смен, схемы/документы/чек-листы объекта, публикация паспорта (`ops.passport.publish` заведён, но экрана-действия нет) — см. FRONTEND_DECISIONS A29-A31.

### Этап 6 (контроль и аналитика) — **Done (в объёме «Аудит»)**
`AuditStub` заменён на реальный `features/audit`: read-only журнал действий поверх РЕАЛЬНОЙ (`existing`) donor-схемы `/api/audit/logs/` (ARCH-FE-011), с поиском по действию/сущности/пользователю (клиентская фильтрация, demo-масштаб — как personnel).

- Тот же паттерн, что personnel (Этап 4): донорский эндпоинт существовал, но не был замочен в `dev:mock` — добавлен MSW handler.
- Аналитика службы/ОМ/рейтинга, уведомления (реальный экран, не только NotificationBell), обратная связь, справочники, настройки — Not started, см. FRONTEND_DECISIONS A32.

### Этап 7 (hardening, §34) — **Done (в объёме route audit + gate)**
- **Route audit**: новый `smart-josparlau-routing.qa.test.tsx` — 14 тестов, карта гейтов для ВСЕХ Smart Josparlau-маршрутов (commandCenter/securityEvents/securityEventDetail/employees/employeeDetail/objects/objectDetail): без нужного кода → «Доступ запрещён», ровно с нужным кодом → доступ открыт. Раньше эти маршруты были покрыты ТОЛЬКО ручной browser-QA, ни одного автотеста на гейт не было.
- **Production build/size gate**: зелёный на каждом Этапе 2-7 (последний замер — 207.1 KB gzip из 300 KB, `npm run gate` — 862 теста).
- **Traceability**: `FRONTEND_TRACEABILITY_MATRIX.md` обновлена на каждом Этапе, 17/~70 экранов честно Verified, остальное — явный Not started (не скрыто).
- **Playwright E2E** (после разрешения пользователя продолжить): новый `playwright.mock.config.ts` + `e2e-mock/` (тот же принцип, что `playwright.live.config.ts` — отдельный конфиг/порт 4176/скрипт `test:e2e:mock`, штатный `playwright.config.ts` не тронут ни строкой). Две спеки: (1) создать ОМ → сохранить бюллетень → reload; (2) Согласование → Ознакомление → Проведение → Закрыто (полный хвост жизненного цикла) — обе в РЕАЛЬНОМ chromium против `build:mock` (IndexedDB, не in-memory adapter юнит-тестов). Прогнаны дважды подряд (детерминизм), плюс полный прежний `npm run test:e2e` (62 теста) — не задет.
- **Написание E2E нашло 2 реальных дефекта** (не гипотетических): (1) 8 полей форм в `SecurityEventDetailPage.tsx` использовали `<label>` без `htmlFor`/`id` — реальный accessibility-дефект, исправлен; (2) `DemoToolbar` физически перекрывает кнопки на страницах с длинным контентом — потребовал `hideDemoToolbar()` хелпер (dev-only, не влияет на продакшн). Подробности — FRONTEND_TEST_MATRIX.md.
- ⚠️ **Не сделано в этом hardening-проходе** (сознательный scope cut, не молчаливое упущение):
  - E2E покрывает только стадию BULLETIN — остальные 8 стадий жизненного цикла, personnel, objects, audit проверены только вручную;
  - accessibility-аудит (axe/screen reader) — не проводился отдельно, только опора на существующие `aria-label`/семантику компонентов;
  - tablet/mobile responsive-проверка — не проводилась ни для одного нового экрана;
  - Firefox-совместимость — не проверялась (вся сессия — Chromium через preview-инструменты);
  - `build:mock` бандл (~316.9 KB gzip) превысил 300 KB — см. обновлённый A9, не гейтится отдельно, риск задокументирован, не устранён.

- **Аналитика службы** (после разрешения продолжить): `app/ServiceAnalyticsPage.tsx` — распределение ОМ по этапам + объектов по состоянию паспорта, ЧЕСТНО вычислено из уже существующих read model (`useSecurityEventsList`, `useObjectsList`), явно подписано «по видимым записям» (не выдаёт себя за серверный агрегат). Живёт в `app/`, НЕ в `features/analytics` — композиция данных ДВУХ фич нарушила бы ARCH-FE-013 (features→features запрещён); `app/` может импортировать любые фичи, это законная точка композиции (см. FRONTEND_DECISIONS A33). `analyst` persona получила реальный `homeRoute`.
- **План дежурств** (§21/§24, по запросу «Duties»): новая mock-only-demo `features/duties` — Duty Type Registry (§24.3, НЕ хардкод в UI), суточные дежурства на собственных/охраняемых объектах (`assignmentMode=INDIVIDUAL` подмножество §24.4), процесс PLANNED→ACKNOWLEDGED(ознакомление)→ACTIVE(заступление)→COMPLETED(завершение). Один `useDutyShifts()` источник, два представления «По объектам»/«По сотрудникам» (§21.4 — НЕ два отдельных запроса). Сознательно НЕ реализовано: боевые группы на Трассе (§24.1 явно «нельзя моделировать как обычный объект» — отдельный многоуровневый процесс потребность→подача начальником управления→рассмотрение→утверждение), история/revisions, месячное планирование (сейчас только текущая дата) — см. FRONTEND_DECISIONS A35-A36.

### Этап 8 (справочники, §30 мастер-промпта) — **Done (в объёме 3 справочников)**
Новая mock-only-demo фича `features/dictionaries` (тот же паттерн repository/handlers/fixtures, что security-events/objects/duties) — реестр справочников (`/dictionaries`) + значения одного справочника (`/dictionaries/:code`).

- 3 справочника без существующего владельца: «Причины возврата на доработку», «Требования постов», «Сезонные/погодные поправки» — должности/звания (donor `/api/core/positions|ranks/`) и виды дежурств (`features/duties` Duty Type Registry) НЕ дублируются (см. A40).
- Операции: `listDefinitions` (счётчики active/total), `listEntries`, `createEntry` (валидация: непустой code/label, уникальность code без учёта регистра внутри справочника), `setEntryActive` (soft-деактивация; при `referencedCount>0` — 409 `RepositoryConflictError` с текстом причины дословно из `mutation.error.message`, см. A41).
- `referencedCount` — значение demo-фикстуры, НЕ живой cross-feature подсчёт (ARCH-FE-013 запрещает импорт чужих фичей) — честно задокументировано (A42).
- Права: `ops.dictionary.view` добавлен всем существующим non-admin persona (частичная реализация «все авторизованные видят», см. A43), `ops.dictionary.manage` — только `objects_admin`+`admin`.
- Route audit (`smart-josparlau-routing.qa.test.tsx`) расширен на `/dictionaries` и `/dictionaries/:code`; unit-тесты `mocks/repository.test.ts` (permission/validation/business-rule/persistence, включая 409-конфликт деактивации используемого значения) — первый repository-тест в проекте на новую фичу, написанный СРАЗУ (не отложен, в отличие от duties на Этапе 7).
- Проверено вручную в браузере (dev:mock, persona admin): реестр справочников с корректными счётчиками → детальная страница → 409 на деактивации используемого значения (текст показан дословно) → успешная деактивация неиспользуемого → блокировка дубликата кода при создании → успешное создание нового значения, персистентное через reload.
- Сознательно НЕ реализовано: остальные 4 справочника из §30 (должности/звания — уже есть donor-владелец; типы статусов, группы — Not started, нет явного текущего потребителя данных в этом mock-срезе).

### Этап 9 (единый календарь смен, §25 мастер-промпта, по запросу «Единый календарь смен») — **Done (в объёме «Календарь по дням»)**
`app/CalendarPage.tsx` (route `/calendar`, композиция `app/`-уровня — тот же принцип, что `ServiceAnalyticsPage`/A33): read-only список дежурств (`features/duties`) и расстановок ОМ (`features/security-events`) за ОДИН выбранный календарный день, с навигацией по дате (← Пред. день / date input / След. день →, URL-параметр `?date=`), ссылкой «Открыть источник» на `/duties` или `/security-events/:id`.

- §25 целиком описывает объединение 9 backend-контуров (Employee Status/Duty/Security Event/Route/Facility/Secondment/Conflict/Workload/Acknowledgement) в ОДНУ проекцию по стабильному `employeeId`, с режимами «Мой календарь»/«Сотрудник»/«Подразделение»/«Боевые группы», конфликтами, отдыхом, нагрузкой — ничего из этого честно не реализуемо в demo-срезе (нет соответствующих repositories ни у одной фичи проекта).
- **До написания кода** сверены фикстуры duties/security-events: сотрудники НЕ имеют общего стабильного идентификатора (2 из 4 имён в duties не встречаются в security-events roster) — объединение по ФИО дало бы ложные совпадения, поэтому каждая запись календаря показана как есть, от своего источника, без попытки собрать «карточку сотрудника» (см. FRONTEND_DECISIONS A44-A46).
- **Найден и исправлен реальный баг** при первой ручной проверке non-admin persona: `ops.calendar.view` (гейт роута) не отменяет права владельцев источников (`ops.duty.view`/`ops.security_event.view`, проверяются в `mocks/repository.ts` каждой фичи) — без `enabled`-гейта на запросах persona с ТОЛЬКО одним из двух прав получала 403 → бесконечный retry react-query → вечная «Загрузка…». Исправлено: `useDutyShifts()`/`useSecurityEventsList()` получили необязательный `{ enabled }`, `CalendarPage` гейтит каждый источник отдельно и честно показывает, какой источник скрыт и почему (A47).
- `ops.calendar.view` добавлен `event_planner`/`recon_officer`/`objects_admin`/`analyst` (роли, уже читающие хотя бы один из композируемых источников или владеющие аналитикой) + `admin`.
- Route audit (`smart-josparlau-routing.qa.test.tsx`) расширен на `/calendar`.
- Проверено вручную в браузере (dev:mock): admin — 4 дежурства на 2026-07-20, расстановка ОМ на 2026-07-22/07-25 с рабочей ссылкой на карточку ОМ; `objects_admin` (только `ops.duty.view`) — честный частичный список + баннер «расстановки ОМ скрыты»; `analyst` (ни одного из двух прав) — честный пустой список, не завис.
- Сознательно НЕ реализовано: «Мой календарь», представления «Сотрудник»/«Подразделение», боевые группы на Трассах, конфликты, отдых после дежурства, нагрузка (план/факт часов), статусы (отпуск/больничный/командировка/прикомандирование) — см. FRONTEND_DECISIONS A44-A46.

### Этап 10 (остальные справочники §30 — «типы статусов»/«группы», по запросу «Остальные справочники §30») — **Done (в объёме 2 новых справочников)**
`features/dictionaries` расширена на 2 справочника из §30, для которых не было явного кандидата в домене (кадровый `EmployeeStatus` исключён из Smart Josparlau, принадлежит донору) — трактовка подтверждена пользователем ДО реализации.

- `JOURNAL_ENTRY_TYPES` — самостоятельный каталог типов записей журнала штаба (стадия «Проведение» ОМ, 4 значения совпадают с существующим `JournalEntryType` в `features/security-events`, но справочник технически НЕ подключён к форме журнала — ARCH-FE-013 запрещает features→features импорт, тот же принцип, что A40/A42). Обычная страница-справочник, без отличий от RETURN_REASONS/SEASONAL_CORRECTIONS.
- `POST_REQUIREMENT_GROUPS` — категории, РЕАЛЬНО категоризирующие записи существующего `POST_REQUIREMENTS` через новое поле `groupCode` на `DictionaryEntry` (единственный cross-reference внутри самой фичи `dictionaries`, не межфичевый импорт). `DictionaryDetailPage` показывает колонку «Группа» и select в форме создания только для `POST_REQUIREMENTS`; `createEntry()` валидирует `groupCode` — должен ссылаться на АКТИВНУЮ запись `POST_REQUIREMENT_GROUPS`, иначе 400 по полю `groupCode`.
- `SCHEMA_VERSION` (compose-seed.ts) поднят 5→6 — форма `DictionaryEntry` получила новое обязательное поле `groupCode`; без бампа старый IndexedDB-снапшот не пересевался, `/dictionaries` показывал только 3 из 5 справочников (найдено и исправлено при ручной проверке ДО коммита, см. FRONTEND_DECISIONS A49).
- +3 unit-теста repository (валидация groupCode: неактивная/несуществующая группа отклоняется, активная сохраняется, groupCode вне POST_REQUIREMENTS игнорируется) — 23 теста в фиче (было 20).
- Проверено вручную в браузере (dev:mock, persona admin): реестр из 5 справочников с корректными счётчиками → JOURNAL_ENTRY_TYPES без колонки «Группа» → POST_REQUIREMENTS с колонкой «Группа» и рабочим select → создание нового требования с группой «Документы», персистентно через reload.
- Сознательно НЕ реализовано: должности/звания как отдельный справочник (уже реальные donor-справочники через personnel, A40) — §30 список из 7 пунктов покрыт полностью в рамках проекта (5 через `features/dictionaries`, 2 через существующих владельцев).

### Этап 11 (расширение Playwright E2E, по запросу «Playwright E2E») — **Done**
5 новых спек в `e2e-mock/` (было 2, стало 7): `personnel-registry.spec.ts` (поиск/фильтр → карточка), `objects-passport.spec.ts` (реестр → паспорт → добавление сектора/поста → persist-through-reload), `dictionaries.spec.ts` (409-конфликт деактивации используемого значения, деактивация неиспользуемого, создание нового), `calendar.spec.ts` (дефолтный день → навигация на конкретную дату → переход к источнику), `security-event-placement.spec.ts` (стадия PLACEMENT: назначение/снятие сотрудника на пост, hard-rule двойного назначения блокирует уже занятого сотрудника даже на другом посту).

- Демо-сценарий детерминирован (`DemoClock` стартует с фиксированного `DEFAULT_SCENARIO_START_ISO`) — даты в спеках календаря и placement (фикстура «Международный экономический форум», стадия PLACEMENT) взяты как константы из `mocks/fixtures.ts`, не вычислены в рантайме.
- Не найдено новых дефектов продукта (в отличие от Этапов 7/9) — 3 итерации по тестовой инфраструктуре: `hideDemoToolbar()` для паспорта объекта (тот же паттерн Этап 7), `exact: true` для устранения strict-mode коллизии `<span>`/`<option>`, `selectOption` не поддерживает RegExp для label (заменено на `value`).
- `npm run test:e2e:mock` прогнан дважды подряд (детерминизм) — 7/7 оба раза; `npm run gate` (892/892, 213.2 KB gzip) и `npm run test:e2e` (62/62, продакшн-сборка) не задеты ни строкой.
- Подробности — `FRONTEND_TEST_MATRIX.md` Этап 11.

### Этап 12 (e2e-mock: RECON→DEMAND→FORCES→PLACEMENT, продолжение «Playwright E2E») — **Done**
Последний непокрытый участок жизненного цикла ОМ: `security-event-recon-to-placement.spec.ts` — довести чек-лист рекогносцировки до 6/6 → сохранить расчёт → завершить этап (RECON→DEMAND) → добавить строку потребности и утвердить (DEMAND→FORCES, авто-переход БЕЗ промежуточного состояния — `approveDemand` меняет stage в одной мутации) → выделить силы полностью и завершить этап (FORCES→PLACEMENT).

- Не найдено дефектов продукта — только уточнение теста: изначальное ожидание видимого «заблокированного» DEMAND-состояния между approve и переходом на FORCES было неверным (см. `FRONTEND_TEST_MATRIX.md` Этап 12 для разбора).
- `npm run test:e2e:mock` прогнан дважды подряд — 8/8 оба раза (было 7/7 на Этапе 11); `npm run gate` (892/892, 213.2 KB gzip) и `npm run test:e2e` (62/62) не задеты.
- E2E теперь покрывает ВЕСЬ жизненный цикл ОМ (все 9 стадий, разнесённые по 4 спекам): bulletin, RECON→DEMAND→FORCES→PLACEMENT, PLACEMENT (assign/unassign), APPROVAL→ACKNOWLEDGEMENT→CONDUCT→CLOSED.

### Этап 13 (e2e-mock: audit/analytics/duties, по запросу «ок продолжай») — **Done**
Последние 3 экрана без E2E: `audit.spec.ts` (поиск по действию/пользователю сужает журнал независимо), `analytics.spec.ts` (агрегаты «ОМ по этапам»/«Объекты по паспорту» честно совпадают с demo-seed: 5 ОМ по 1 на каждой из 5 представленных стадий, 3 объекта по 1 на GREEN/YELLOW/RED), `duties.spec.ts` (переключатель «По объектам»/«По сотрудникам» перегруппировывает один datasource, полный цикл дежурства PLANNED→ACKNOWLEDGED→ACTIVE→COMPLETED).

- Не найдено дефектов продукта — 1 уточнение теста: групповой заголовок в `DutyPlanPage.tsx` — обычный `<div>`, не heading role, `getByRole('heading', …)` не подходил.
- `npm run test:e2e:mock` прогнан дважды подряд — 11/11 оба раза (было 8/8 на Этапе 12); `npm run gate` (892/892, 213.2 KB gzip) и `npm run test:e2e` (62/62) не задеты.
- **`e2e-mock/` теперь покрывает все 11 экранов, у которых есть реализация** — весь жизненный цикл ОМ, personnel, objects, dictionaries, calendar, audit, analytics, duties. Подробности — `FRONTEND_TEST_MATRIX.md` Этап 13.

## Осталось (проект в целом)
- Playwright E2E покрывает ВСЕ реализованные экраны (11 спек). Никаких «реализовано, но не покрыто E2E» пробелов не осталось — оставшиеся пункты ниже про НЕреализованный функционал.
- Боевые группы на Трассе, месячное планирование дежурств, история/revisions (§24) — Not started.
- Нагрузка личного состава, оперативный рейтинг, уведомления (полноценный экран), обратная связь, остальные справочники (типы статусов, группы) — Not started.
- Accessibility-аудит (кроме найденной и исправленной label-проблемы), tablet/mobile, Firefox — не начаты.
- Единый календарь смен реализован только в объёме «по дням» (§25.12) — «Мой календарь»/«Сотрудник»/«Подразделение»/боевые группы/конфликты/нагрузка (§25.8-25.18), схемы/документы объекта (§21.2), оперативный профиль сотрудника (§20.5) — Not started, см. FRONTEND_DECISIONS A28/A44-A46.
- Оценки участников/рейтинг (Epic 18.3/19, D3) — Not started, сознательный scope cut с самого начала (см. A24).

## NEXT ACTION
Playwright E2E больше не имеет «реализовано, но не покрыто» пробелов — 11 спек покрывают все реализованные экраны. Следующая сессия — по функциональному объёму: оперативный профиль сотрудника (§20.5), полноценный экран уведомлений, боевые группы на Трассе (§24.1), или accessibility-аудит/tablet-mobile (не начаты вовсе). §30 справочники закрыты полностью. Решение — за пользователем; текущий срез — стабильная, полностью зелёная база (gate 892/892, size 213.2 KB, e2e-mock 11/11, e2e прод 62/62).
