# FRONTEND_DECISIONS

Разрешённые противоречия, допущения, открытые вопросы. Обновляется по мере реализации.

## Разрешено (источник: docs/RECONCILIATION.md v2)

| # | Вопрос | Решение | Источник |
|---|---|---|---|
| D1 | Модель конфликтов при назначении | Гибрид через `is_hard_block`: hard-block (422, без override) для отпуск/больничный/командировка/рапорт; soft (409, override с `override_reason`) для прочих | RECONCILIATION PR-1 |
| D2 | BEFORE_DUTY | Отдельная авто-проекция перед началом дежурства, длительность — configurable (demo dictionary), НЕ в прототипе изначально | RECONCILIATION PR-2 |
| D3 | Оценки участников (rating) | Обязательны, двусторонние, 10-балльная шкала, дефолт **8** (не 7 — см. явный запрет п.35 мастер-промпта), оценки и оценщики скрыты | RECONCILIATION PR-3 |
| D4 | Коэффициенты лимита поста (погода/сезон) | Отдельный справочник-множитель к лимиту поста + фактическое время из опроса | RECONCILIATION PR-4 |
| D5 | Кадровые данные (Employee/Division/Rank/Position) | READ-ONLY snapshot из внешнего источника (VAPS HR core). Smart Josparlau НЕ создаёт `EmployeeStatus`, кадровые mutations, светофор сдачи расхода — это чужой bounded context | RECONCILIATION R5, TECHNICAL_AUDIT |
| D6 | ОМД/ОРГД | НЕ постоянные роли — временные полномочия (`TemporaryPermissionGrant`) на личном кабинете | RECONCILIATION R3 |
| D7 | hard-status-conflict (старая редакция) | Заменяется read-only фактом из `PersonnelAvailabilitySnapshot` | master-prompt §8.9 |

## Архитектурные решения этой реализации (Этап 1)

| # | Решение | Причина |
|---|---|---|
| A1 | Единый `apiClient` (существующий `shared/api/client.ts`) обслуживает и старые (`/api/core/…`, `/api/operations/…`), и новые Smart Josparlau пути. MSW перехватывает на уровне Service Worker/node — ветвления `isMock` в продуктовом коде нет | master-prompt §7, §8 |
| A2 | Новые доменные операции — namespace `/api/ops/…` (Smart Josparlau backend будущего), НЕ `/mock/...` или `/demo/...` | §8.3 запрет на demo-роуты |
| A3 | Persistence: IndexedDB adapter в браузере, in-memory adapter в тестах, `localStorage` только для persona/scenario/toolbar-состояния | §8.6 |
| A4 | Multi-tab: `BroadcastChannel('smart-josparlau-demo')` шлёт `{revision}` — другая вкладка инвалидирует protected queries. Реализовано в Этапе 1 в объёме "не даёт молча перезаписать более новую ревизию", без полного conflict-resolution UI (не требуется DoD) | §8.6 |
| A5 | Demo persona/scenario/fault-profile — `mock-only-demo` runtime, отдельный от продуктового `AuthContext`; persona переключает credential через существующий `login()`, НЕ создаёт вторую auth-модель | §8.3, запрет п.35 "создавать production role codes из подписей demo-переключателя" |
| A6 | `Backend/VAPS/schema.yaml`/`schema.d.ts` не трогаются; новые доменные типы — pending-контракты внутри `features/<feature>/api/pending-contracts.ts` | §7.5 |
| A7 | Два build-режима: `npm run build`/`npm run gate` (дефолт, `mode=production`, БЕЗ MSW — bundle не меняется для существующих epics 1-13) и `npm run build:mock`/`dev:mock` (`--mode mock`, читает `.env.mock`). Gate этого файла (`size-gate.mjs` суммирует ВСЕ JS в `dist/`, включая lazy-чанки) считает production-сборку — mock-бандл её не задевает | размер, §8.1 |
| A8 | `import.meta.env.MODE === 'mock'` — ОБЯЗАТЕЛЬНАЯ первая проверка (build-time литерал) перед стартом MSW/toolbar в `main.tsx`/`App.tsx`: только литерал даёт Rollup статически выкинуть `mocks/browser` chunk (MSW ~92 KB gzip) из production-сборки. Рантайм-проверка через `getFrontendEnv().dataSource` одна, без литерала, оставила бы MSW в каждой сборке | size-gate замер: без литерала prod-бандл рос на ~92 KB |
| A9 | ⚠️ Риск бюджета (обновлено, Этап 7): `build:mock` теперь ~316.9 KB gzip — превысил 300 KB после Этапов 2-6 (feature handlers/fixtures трёх новых фич). `size-gate.mjs` в `npm run gate` считает ТОЛЬКО production-сборку (без MSW, A7) — производственный гейт зелёный (207.1 KB), это НЕ регрессия гейта. Демо-бандл нигде не гейтится отдельным скриптом — риск материализовался, но не блокирует. Если mock-режим станет реальным deployment-таргетом (не только dev:mock для локальной демонстрации) — нужны manualChunks/per-feature lazy для mock-handlers | измерено на Этапе 1 (277.2 KB), обновлено на Этапе 7 (316.9 KB) |
| A12 | Командный центр (Этап 2) реализует ТОЛЬКО «Готовность охранных мероприятий» (реально подкреплено `security-events`). Карточки численности личного состава/карта готовности подразделений/график нагрузки из прототипа НЕ нарисованы — для них нет read model в этом срезе (кадровый агрегат = Epic 19/20, вне scope Smart Josparlau по D5/read-only границе). Придумывать цифры запрещено §35 | нет источника данных, не должность лениться |
| A14 | `SCHEMA_VERSION` (compose-seed.ts) бампается при КАЖДОМ изменении формы существующего feature-слайса (не только при добавлении новой feature). `ensureSeeded()` при несовпадении версии делает безопасный полный reset снапшота с `console.warn`. Найдено вручную в браузере: добавление `reconChecklist`/`reconSectorPosts` в `SecurityEvent` уронило `ReconForm` на `TypeError` для существующих IndexedDB-снапшотов (readSlice ловит ТОЛЬКО полностью отсутствующий слайс, не устаревшую форму ВНУТРИ него) | ручная проверка dev:mock, 2026-07-23; тест demo-runtime.test.ts |
| A13 | Stage-tracker на карточке ОМ показывает все 6 стадий Этапа 2 визуально, но только `BULLETIN` кликабелен/имеет форму — остальные 5 задизейблены с приглушённым стилем (не мёртвая кнопка: явно неактивны, не притворяются рабочими) | запрет §35 «прятать незавершённую функцию за мёртвой кнопкой» |
| A11 | `ensureSeeded()` (app/mocks/demo-runtime.ts) докатывает НЕДОСТАЮЩИЕ feature-слайсы при каждом запуске, не только сидирует пустое хранилище — иначе браузер с IndexedDB от предыдущего Этапа падает на `readSlice` («слайс не засеян») при каждом обращении к новой feature НАВСЕГДА, пока пользователь не нажмёт «Сбросить demo-данные» (нашёл вручную при первой браузерной проверке `security-events`, 2026-07-23) | ручная проверка dev:mock |
| A10 | ⚠️ Известный шумный пробел (НЕ регрессия Этапа 1): существующий `NotificationBell` (шапка `AppLayout`) поллит `/api/notifications/` и открывает WS `/ws/notifications/` безусловно для любого авторизованного — в mock-режиме оба не замочены (уведомления вне Smart Josparlau scope) → MSW честно кричит в консоль по контракту §8.1 «browser console должна показывать operation/method/path необработанного запроса». Это ожидаемо, не блокирует Этап 2+; если станет мешать демо — минимальный `mock-only-demo` handler для этих двух путей, БЕЗ превращения уведомлений в продуктовую Smart Josparlau feature | ручная проверка dev:mock, 2026-07-23 |

| A15 | Конфликты назначений в «Расстановке» — реализован ТОЛЬКО hard-rule «один сотрудник — один пост ВНУТРИ одного ОМ» (без override). Межсобытийные конфликты, отдых, усталость, полный `PersonnelAvailabilitySnapshot` (Epic 16.3) — Not started: нет реального read model доступности сотрудника вне контекста этого ОМ | scope-решение, не придумывать данные §35 |
| A16 | `completePlacement()` считает пост укомплектованным при ХОТЯ БЫ ОДНОМ назначении, а не точном совпадении с `reconSectorPosts.need`. Точный подсчёт с 8-человечным демо-ростером (personnelRoster.ts) был бы нечестной демонстрацией на постах need=12 | нет смысла фейковый большой ростер ради одной цифры |
| A17 | `PersonnelSummarySnapshot` (кандидаты для расстановки) — read-only, отдаётся через `GET /api/ops/personnel/` (external-personnel-contract-pending), НЕ прямым импортом `mocks/personnelRoster.ts` из pages/ — иначе страница знала бы, что источник mock, и сломалась бы при переключении на реальный backend (нарушение §7 "UI не должен определять источник данных") | §7 канонический поток Page→hook→apiClient→handler |
| A18 | «Вернуть на доработку» (returnPlacement) откатывает `stage` APPROVAL→PLACEMENT, а НЕ оставляет событие в APPROVAL с флагом — `StageContent` рендерит нужный экран автоматически по `event.stage` без доп. кода на странице | согласовано с существующим паттерном стадийного рендера |

## Архитектурные решения этой реализации (Этап 3)

| # | Решение | Причина |
|---|---|---|
| A19 | `approvePlacement()` авто-переводит `stage` APPROVAL→ACKNOWLEDGEMENT в ОДНОЙ мутации (не отдельным кликом «открыть ознакомление») — прототип показывает это как непрерывный переход, а не ручной шаг | Epic 16.4→16.6 непрерывность, повтор паттерна A18 |
| A20 | Индивидуальный путь подтверждения переименован в `.../acknowledge/:assignmentId/` (было `.../acknowledgement/:assignmentId/`) — при общем сегменте `acknowledgement/` MSW матчил `POST .../acknowledgement/complete/` через РАНЕЕ зарегистрированный `:assignmentId`-роут (assignmentId="complete"), отдавая 404 от чужого handler'а | найдено вручную в браузере, 2026-07-24 |
| A21 | `completeAcknowledgement()` требует, чтобы ВСЕ `placementAssignments` имели `acknowledgedAt !== null` — частичное ознакомление не пускает в «Проведение» (кнопка честно задизейблена, не скрыта) | нет UX-обоснования пускать без полного ознакомления |
| A22 | `JournalEntry` — append-only: `addJournalEntry()` только добавляет, нет edit/delete операций — журнал штаба как источник истины по инцидентам не должен переписываться задним числом | доменный смысл «журнала» |
| A23 | `closeSecurityEvent()` требует итог по КАЖДОМУ уникальному `reconSectorPosts.sector` (направлению) — если хотя бы одно направление не покрыто `directionSummaries`, закрытие отклоняется business-rule ошибкой | Epic 18.1 FR-30, явное требование мастер-промпта |
| A24 | Оценки участников (Epic 18.3, hidden-score, D3) НЕ реализованы в Этапе 3 — сознательный scope cut, не молчаливое упущение: нет времени сделать честно (скрытие оценщика от оцениваемого требует отдельной модели видимости) | явно зафиксировано, не путать с «забыли» |

## Архитектурные решения этой реализации (Этап 4)

| # | Решение | Причина |
|---|---|---|
| A25 | `features/personnel` использует РЕАЛЬНУЮ (`existing`) donor-схему `/api/core/employees\|divisions\|positions\|ranks/` из `schema.d.ts` (не pending-contract) — эти эндпоинты существовали ДО Smart Josparlau (Epic 1-13), только не были замочены в `dev:mock` compose-handlers | ARCH-FE-011 запрет рукописных типов при наличии схемы |
| A26 | Дублирование personnel-фикстур с `security-events/mocks/personnelRoster.ts` — намеренное: разные bounded context (кадровый справочник vs узкий read-only снапшот кандидатов на пост), фичи не шарят mocks/ друг с другом (ARCH-FE-013) | §20.3 разделение источников истины |
| A27 | ИИН в таблице «Сотрудники» маскируется до последних 4 цифр (`••••••••1234`), а не убирается полностью — компромисс между §20.2 «удали полные ИИН» и полезностью для идентификации однофамильцев | явное указание §20.2, не придумано |
| A28 | Оперативный профиль сотрудника (§20.5: availability/nextAssignment/workloadSummary/ratingSummary) — Not started, карточка сотрудника явно показывает секцию «не подключено» вместо fake-данных | §35 запрет придумывать показатели без сервера |

## Архитектурные решения этой реализации (Этап 5)

| # | Решение | Причина |
|---|---|---|
| A29 | `features/objects` — mock-only-demo (как security-events), а НЕ donor/existing: объекты/секторы/посты — собственная сущность Smart Josparlau (§21.3 Objects Repository), не кадровый донор | §21.3 разделение источников истины |
| A30 | KPI реестра объектов (§21.7: «Паспорта актуальны», «Красный статус» и т.д.) — Not started, не считаются по текущей странице таблицы (прямой запрет §21.7) и не подкреплены серверным агрегатом в demo-срезе | §21.7 явный запрет клиентского подсчёта |
| A31 | План дежурств/История/КалендарьСмен (§21.4: единая проекция, используемая в 3 местах) — Not started целиком: требует отдельный Duties Repository, которого в этом срезе нет; частичная реализация (только «по объектам», без «по сотрудникам»/«истории») создала бы иллюзию единого источника без него | §21.4 явно требует ОДИН набор данных, не 3 независимых |

## Архитектурные решения этой реализации (Этап 6)

| # | Решение | Причина |
|---|---|---|
| A32 | `features/audit` использует ТУ ЖЕ схему `AuditLog` доступа, что и personnel/objects — существующая (`existing`) donor-схема `/api/audit/logs/`, замочена одним handler'ом (`limit=100`, без реальной пагинации — demo-масштаб мал) | ARCH-FE-011, тот же паттерн, что A25 |

## Архитектурные решения этой реализации (Этап 7, после разрешения продолжить)

| # | Решение | Причина |
|---|---|---|
| A33 | `ServiceAnalyticsPage` живёт в `app/`, НЕ в `features/analytics` — экран композирует данные `features/security-events` И `features/objects` одновременно, а ARCH-FE-013 запрещает features→features (та же матрица, что не даёт personnel читать objects напрямую). `app/` может импортировать любую фичу — законная точка композиции, не побег от границ | eslint-plugin-boundaries матрица (ARCH-FE-013), поймано ДО коммита (первая попытка была `features/analytics`, esline не успел проверить — self-caught) |
| A34 | Агрегаты на `ServiceAnalyticsPage` явно подписаны «по видимым записям» — НЕ выдаются за серверный агрегат (§21.7/§35 запрет). Вычислены из уже загруженного `useSecurityEventsList`/`useObjectsList`, без новых mock-эндпоинтов | §35, тот же принцип, что A30 |

## Архитектурные решения этой реализации (Duties, по запросу пользователя)

| # | Решение | Причина |
|---|---|---|
| A35 | `features/duties` реализует ТОЛЬКО `assignmentMode=INDIVIDUAL` подмножество §24.4 (суточное дежурство на собственном/охраняемом объекте, один сотрудник — одна смена). «Боевые группы на Трассе» (`assignmentMode=COMBAT_GROUP`, `targetType=ROUTE_SET`) НЕ реализованы — §24.1 прямо требует отдельный многоуровневый процесс (потребность→подача начальником управления→рассмотрение→утверждение→...), регистрировать эти типы дежурств в Duty Type Registry без рабочего процесса за ними было бы мёртвой кнопкой (§35) | явный текст §24.1 «нельзя моделировать как обычный объект» |
| A36 | Duty Type Registry (§24.3) — реальный справочник в seed (не хардкод строк в JSX), но живёт вместе со сменами в ОДНОМ demo-слайсе, не отдельным API — тот же mock-only-demo статус, что весь runtime; переусложнять отдельным сервисом ради двух записей не оправдано | масштаб демо-среза |
| A37 | «По объектам»/«По сотрудникам» — ОДИН `useDutyShifts()` запрос, группировка client-side по `target.safeLabel`/`employeeName` — НЕ два раздельных источника данных (§21.4 прямое требование «представления одного набора данных, а не отдельные источники истины») | §21.4 |
| A38 | Упрощённый lifecycle смены: PLANNED→ACKNOWLEDGED→ACTIVE→COMPLETED, БЕЗ «формирование потребности→подача состава→рассмотрение→утверждение смены» из §24.1 — эти шаги осмысленны только для `COMBAT_GROUP`/`POST_ROSTER` составов (несколько сотрудников на смену), для `INDIVIDUAL` (один заранее назначенный сотрудник) они были бы пустыми экранами без реального workflow за ними | §24.1 процесс описан для группового комплектования, INDIVIDUAL — вырожденный случай |
| A39 | Месячное планирование (создание/редактирование `dutyPlanId` на период) — Not started: сеятся уже готовые смены на текущую бизнес-дату, нет UI создания плана | scope cut, честно задокументирован |
| A40 | `features/dictionaries` (§30, по запросу «Справочники/настройки») покрывает ТОЛЬКО 3 самостоятельных справочника без существующего владельца: «Причины возврата на доработку», «Требования постов», «Сезонные/погодные поправки». Должности/звания — реальные donor-справочники `/api/core/positions\|ranks/` (`features/personnel`), виды дежурств — `features/duties` (Duty Type Registry, A36) — НЕ дублируются здесь, ARCH-FE-013 всё равно не дал бы шарить их фикстуры между фичами | §30 полный список из 7 пунктов слишком широк для одного среза; выбраны те 3, у которых нет ни одного текущего владельца |
| A41 | §30 «не допускай удаление значения, используемого связанными сущностями» — деактивация (soft, НЕ hard-delete) с `referencedCount>0` отклоняется 409 (`RepositoryConflictError`, канон 409=конфликт), с текстом причины дословно из `mutation.error.message` — не generic-баннером, как `ObjectPassportPage` (§30 явно требует «понятную зависимость», не просто факт отказа) | §30, отличие от прецедента A-паттерна ObjectPassportPage |
| A42 | `referencedCount` — заранее заданное значение demo-фикстуры, НЕ живой перекрёстный подсчёт по `security-events`/`objects` (потребовал бы cross-feature импорта, запрещённого ARCH-FE-013). Честно задокументировано в `model/types.ts`, не выдаётся за серверный агрегат (§35) | ARCH-FE-013, тот же принцип, что A26/A35 | **ОТМЕНЕНО Этапом 52 (A99).** Обоснование устарело: приём «узкая проекция чужого слайса» появился позже (A58/A62/A75 и далее) и позволяет читать соседний слайс без cross-feature импорта. Хуже того, у единственного справочника с настоящими ссылками (`JOURNAL_ENTRY_TYPES`) фиктивное число утверждало «связей нет» — то есть ошибалось в сторону разрешения. Поле удалено с записи ВОВСЕ.
| A43 | §30 «все авторизованные могут видеть разрешённые справочники» реализовано частично: `ops.dictionary.view` добавлен ВСЕМ существующим non-admin persona (event_planner/recon_officer/broker/placement_approver/analyst/objects_admin) — честная демонстрация принципа в рамках существующего ролевого каталога, не полная «доступно буквально всем авторизованным» инфраструктура (в проекте нет единого «authenticated»-флага вне persona-based RBAC). `ops.dictionary.manage` — только `objects_admin` (существующая persona «Ведение объектов», уже владеет паспортами/дежурствами — тематически ближайший владелец) и `admin` (wildcard) | нет отдельной «Настройки»-persona в каталоге; расширение существующей objects_admin — тот же паттерн, что добавление `ops.duty.*` в неё же при реализации duties |

## Архитектурные решения этой реализации (Календарь смен, §25, по запросу «Единый календарь смен»)

| # | Решение | Причина |
|---|---|---|
| A44 | `CalendarPage` НЕ объединяет дежурства и расстановки ОМ по сотруднику — каждая запись показана как есть, от своего источника. Причина: `DutyShift.employeeName` (features/duties) и `PlacementAssignment.employeeId/employeeName` (features/security-events, через собственный `personnelRoster.ts`) — РАЗНЫЕ bounded context с независимыми demo-фикстурами (тот же принцип, что A26). Проверено: из 4 имён в duties-фикстурах только 2 («Ахметов Б.», «Ерланов Д.») совпадают со списком security-events roster, 2 остальных («Сагинова А.», «Оразов К.») — нет. Join по совпадению ФИО дал бы ложные/неполные совпадения — нечестно (§35 запрет придумывать связи, которых нет у backend) | обнаружено при проектировании — сверка фикстур ДО написания кода, не после |
| A45 | Реализован ТОЛЬКО «Календарь по дням» (§25.12) в урезанном виде: список назначений (дежурства §24 + расстановки ОМ §16-18) за ОДИН выбранный календарный день, без слоёв «Готовность»/«Ограничение»/«Конфликт»/«Нагрузка» (§25.4), без Calendar Repository, без Conflict/Workload/Employee Status repositories — их нет в demo-срезе и подделать честно нельзя. «Мой календарь», «Сотрудник», «Подразделение», «Боевые группы» (§25.8) — Not started целиком | §25 описывает объединение 9 контуров backend (Employee Status/Duty/Security Event/Route/Facility/Secondment/Conflict/Workload/Acknowledgement) в ОДНУ проекцию по стабильному employeeId — этой инфраструктуры нет ни у одной существующей mock-only-demo фичи проекта |
| A46 | Дефолтная дата календаря берётся из уже загруженных данных (`businessDate` первого дежурства/ОМ), а НЕ из `new Date()` — demo-runtime repositories сами живут по `DemoClock` (§8.8, комментарий `demo-clock.ts:1-3` явно запрещает `new Date()`/`Date.now()` в коде, читающем/пишущем demo-состояние), и локальные wall-clock часы страницы могли бы разойтись с датой сидированных данных. Навигация по дням (`shiftDate()`) работает на компонентах локальной даты (`getFullYear`/`getMonth`/`getDate`), не через `new Date(dateString)` (ISO-parse даёт UTC-полночь — тот же класс бага, что фиксирует memory `feedback_vaps_date_guard_needs_negative_tz`) | §8.8 демо-часы, известный класс TZ-багов проекта |
| A47 | `useDutyShifts()`/`useSecurityEventsList()` получили НЕОБЯЗАТЕЛЬНЫЙ второй параметр `{ enabled?: boolean }` (default `true`, обратная совместимость сохранена для всех существующих вызовов). `CalendarPage` гейтит каждый запрос отдельно по `hasPermission('ops.duty.view')`/`hasPermission('ops.security_event.view')` — `ops.calendar.view` (гейт роута) НЕ отменяет права владельцев источников, они по-прежнему проверяются в `mocks/repository.ts` каждой фичи (403 при отказе). **Найден и исправлен реальный баг** при ручной браузерной проверке persona «Ведение объектов» (есть `ops.duty.view`, нет `ops.security_event.view`): без `enabled`-гейта запрос к security-events получал 403, react-query уходил в бесконечный retry, страница висела на «Загрузка календаря…» без выхода. Persona «Аналитик» (нет ни одного из двух прав) — честный пустой список с явным объяснением, не путать с «данных нет» | найдено вручную в браузере при первой проверке non-admin persona, до коммита |

| A48 | §30 «типы статусов»/«группы» — 2 из оставшихся 4 пунктов справочников — не имеют явного кандидата в текущем домене Smart Josparlau: кадровый `EmployeeStatus` явно исключён из проекта (принадлежит донору, см. `EmployeeStatus не используется как продуктовая сущность` в мастер-промпте, п. 4535). По уточнению пользователя реализованы как: (1) `JOURNAL_ENTRY_TYPES` — самостоятельный каталог типов записей журнала штаба (стадия «Проведение», значения совпадают с существующим `JournalEntryType` в `features/security-events`, НО справочник технически НЕ подключён к форме журнала — ARCH-FE-013 запрещает features→features импорт, тот же принцип, что A40/A42); (2) `POST_REQUIREMENT_GROUPS` — категории, реально категоризирующие записи `POST_REQUIREMENTS` через новое поле `groupCode` на `DictionaryEntry` (единственный случай в фиче, где один справочник ссылается на другой — оба владеет `features/dictionaries`, cross-reference ВНУТРИ одной фичи, не межфичевый импорт). `createEntry()` валидирует `groupCode` только для `dictionaryCode==='POST_REQUIREMENTS'` — должен ссылаться на АКТИВНУЮ запись `POST_REQUIREMENT_GROUPS`, иначе `RepositoryValidationError` | нет честного кандидата в домене без явного уточнения — пользователь подтвердил трактовку до реализации (см. диалог сессии), не выдумано в одностороннем порядке |
| A49 | `SCHEMA_VERSION` (compose-seed.ts) поднят 5→6 — форма `DictionaryEntry` (features/dictionaries) получила НОВОЕ обязательное поле `groupCode`, добавились 2 новых справочника. Без бампа старый IndexedDB-снапшот браузера не переcеивался, и `/dictionaries` показывал только 3 из 5 справочников (проверено вручную: без бампа — 3 карточки, после бампа+reload — 5). Тот же класс бага, что фиксирует `FRONTEND_PROGRESS.md` Этап 2 (`ensureSeeded()` не ловит дрейф формы существующего слайса) | найдено вручную в браузере при первой проверке после реализации, до коммита |

| A50 | §20.5 «Оперативный профиль сотрудника» — по запросу пользователя реализована СТРУКТУРА (вкладки §20.15: Сводка/Доступность/Назначения/Подготовка и допуски/Нагрузка/Рейтинг/Документы на `EmployeeDetailPage`), но НЕ данные: каждая вкладка честно объясняет ПОЧЕМУ раздел не подключён (в стиле §20.21-25 «Источник не подключён»), вместо одной общей заглушки-абзаца (было в A28). Проверено: `features/duties` (`employeeName`, без id) и `features/security-events` roster (`emp-1..emp-8`) НЕ используют тот же ID-пространство, что `features/personnel` (`employee-1..employee-N`) — тот же вывод, что уже был сделан для календаря «по сотруднику» (A44-A46): собирать `EmployeeOperationalProfile` по ФИО было бы ложным совпадением, честнее показать явное «не подключено» на каждой вкладке отдельно с указанием причины | пользователь явно попросил «оперативный профиль сотрудника» — реализована честная структура, не выдуманы данные (§35) |

| A51 | «Боевые группы на Трассе» (§24.5-24.10, по запросу «боевые группы на Трассе») — реализовано СОКРАЩЁННОЕ подмножество §24.1: подача состава (leader+members+reserve) начальником управления → рассмотрение (принять/вернуть с причиной, дословно). НЕ реализовано: формирование потребности на период (Трассы/составы заведены фикстурой заранее), ознакомление КАЖДОГО члена группы отдельно, заступление/несение/замены/сдача/факт (полный §24.13 lifecycle), Conflict Repository (пересечение с ОМ/другим дежурством — только внутрифичевый hard-rule DOUBLE_ASSIGNMENT между combat-submission'ами на ту же дату), представление подачи от имени другого лица, revision/optimistic-concurrency (`expectedRevision`), режимы покрытия SEQUENTIAL/PARALLEL — хранятся, но НЕ проверяются frontend (backend бы считал состав). Route/reserve/routeSet заводятся фикстурой при seed, не создаются в UI (нет экрана формирования потребности на период, §24.1 первый шаг) | явный текст §24.1 «нельзя моделировать как обычный объект», пользователь явно запросил именно эту фичу после A35 |

| A52 | «Полный §24 боевых групп», по запросу пользователя после A51 — реализован СЛЕДУЮЩИЙ слой lifecycle ПОСЛЕ принятия состава (§24.19-24.23): `CombatDutyExecution` (`PENDING_ACKNOWLEDGEMENT→READY→ACTIVE→COMPLETED`), 3 новые операции — `acknowledgeCombatDuty` (индивидуальное ознакомление КАЖДОГО leader+members, БЕЗ резерва — §24.19, переход в READY когда покрыт весь обязательный список), `checkInCombatDuty` (заступление — только из READY, §24.20 упрощено до одной фиксации `actualStart` без чек-листа/опоздавших/отсутствующих), `completeCombatDuty` (факт — §24.23, `actualMemberNames` задаётся ОТДЕЛЬНО от планового `memberEmployeeNames`, может быть подмножеством — «план ≠ факт», буквальное требование §24.23). Новые permissions `ops.combat_group.acknowledge/checkin/complete` — все у `combat_department_chief` (начальник управления отвечает за исполнение своей группы целиком, тот же принцип, что и `submit`, не передаётся центральному оператору). НЕ реализовано в этом срезе (полный список §24 остаётся открытым, см. FRONTEND_TRACEABILITY_MATRIX): формирование потребности на период (§24.1 первый шаг, Трассы/составы всё ещё фикстура), замены `DutyReplacement` (§24.21), передача смены `HANDOVER` (§24.22 — упрощённый lifecycle без промежуточного состояния передачи), Conflict Repository за пределами внутрифичевого DOUBLE_ASSIGNMENT (§24.17 — пересечение с ОМ), revision/`expectedRevision` (§24.13), представление от имени другого лица (§24.5), несколько подгрупп на SEQUENTIAL/PARALLEL Трассах в режиме текущего дежурства (§24.20 «одна подгруппа покрывает первую Трассу…» — coverageMode хранится, но заступление/факт остаются на уровне ВСЕЙ группы, не подгрупп), rest policy проекции (§24.18). `SCHEMA_VERSION` (compose-seed.ts) 7→8 — `CombatDutyRosterSubmission` получила обязательное поле `execution`; демо-фикстура «Трасса №2 (принятый состав)» — ACCEPTED-группа с открытым execution для ручной/e2e-проверки без обязательной подачи+рассмотрения заново, состав (Кенжебаев А./Тастанова Г.) намеренно НЕ пересекается с составами, которые e2e-спеки combat-duty-groups.spec.ts подают руками (иначе DOUBLE_ASSIGNMENT заблокировал бы их подачу на ту же businessDate — найдено и исправлено ДО коммита при первом прогоне e2e-mock) | пользователь выбрал «Полный §24 боевых групп» из 3 предложенных направлений (уведомления/§24/accessibility); полный объём §24 огромен (месячное планирование, handover, rest policy, per-route подгруппы) — реализован следующий связный вертикальный срез (ознакомление→заступление→факт), честно не весь пункт разом, остальное явно перечислено выше и в TRACEABILITY |

| A53 | «Продолжение §24» (выбор из 3 предложенных: уведомления/§24/accessibility-аудит, по запросу «Продолжение §24») — реализованы замены §24.21 `DutyReplacementRecord`: одна атомарная команда `requestReplacement` (без отдельного approval-шага для замены внутри своего управления — сокращённое подмножество спека «представление другого управления требует отдельного согласования», не реализовано). Доступна только пока `execution.stateCode` ∈ {PENDING_ACKNOWLEDGEMENT, READY} (INVALID_STATE_TRANSITION на ACTIVE/COMPLETED — замена ПОСЛЕ заступления не моделируется, см. §24.20 «замены» упомянуты как часть текущего несения службы, тот же вывод, что упрощение checkInCombatDuty в A52). Правила: NOT_IN_ROSTER (заменяемый не в составе), ALREADY_IN_ROSTER (заменяющий уже в составе), DOUBLE_ASSIGNMENT (заменяющий уже ПРИНЯТ в другую группу на ту же дату — тот же §24.17 hard-rule, что submitCombatGroup/A51), REASON_REQUIRED. Эффект замены: подменяет имя в `groupLeaderEmployeeName`/`memberEmployeeNames`, убирает заменённого из `acknowledgedMemberNames` (новый участник должен подтвердить ознакомление САМ — READY откатывается в PENDING_ACKNOWLEDGEMENT, если заменённый уже был ознакомлен), пишет запись в `replacements: DutyReplacementRecord[]` (история, «предыдущая revision сохраняется» — упрощено до плоского массива без формального revision-номера). Новое право `ops.combat_group.replace` — у `combat_department_chief` (тот же принцип ответственности за свой состав, что submit/acknowledge/checkin/complete). `SCHEMA_VERSION` 8→9 (аддитивное обязательное поле `replacements` на `CombatDutyRosterSubmission`). НЕ реализовано (полный список §24 остаётся открытым): формирование потребности на период (§24.1 первый шаг), передача смены `HANDOVER` (§24.22), Conflict Repository за пределами DOUBLE_ASSIGNMENT (§24.17 — пересечение с ОМ), revision/`expectedRevision` формальный (§24.13 — есть только плоская история замен, не оптимистичная конкурентность), представление от имени другого лица (§24.5), подгруппы на SEQUENTIAL/PARALLEL Трассах (§24.20), rest policy (§24.18), кросс-департаментное согласование замены. `mocks/repository.test.ts` +11 тестов (permission, REASON_REQUIRED, INVALID_STATE_TRANSITION на ACTIVE, NOT_IN_ROSTER, ALREADY_IN_ROSTER, DOUBLE_ASSIGNMENT, успешная замена участника/старшего, откат READY→PENDING_ACKNOWLEDGEMENT, персистентность, RepositoryNotFoundError). `e2e-mock/combat-duty-replacement.spec.ts` — новая спека (15-я), использует ту же демо-фикстуру «Трасса №2 (принятый состав)», что combat-duty-execution.spec.ts | пользователь выбрал «Продолжение §24» из 4 предложенных направлений после Этапа 16; из списка A52 замены — самый связный следующий вертикальный срез (несёт прямую пользу до формирования потребности на период, которое требует отдельного UI создания Трасс/составов) |

| A54 | «Продолжение §24» (выбор из 2 предложенных: передача смены §24.22 / формирование потребности §24.1, по запросу «Продолжение §24» повторно) — реализовано формирование потребности на смену: `createCombatDutyShift` заводит НОВУЮ `CombatDutyShift` (submission: null, сразу «Требует подачи») по форме (дата, вид дежурства из Duty Type Registry, режим покрытия, требуемая численность `requiredEmployees`, набор Трасс из существующего реестра `routes`). Закрывает единственный оставшийся пробел, где весь §24-конвейер (потребность→подача→рассмотрение→ознакомление→заступление→факт→замена) был недостижим целиком из UI — Трассы/составы были ТОЛЬКО фикстурой. Право — `ops.duty.manage` (уже есть у `objects_admin`, новое право заводить не понадобилось — тот же владелец, что общий план дежурств). Валидация: INVALID_BUSINESS_DATE, EMPTY_ROUTE_SET, INVALID_REQUIREMENT (requiredEmployees<1), UNKNOWN_DUTY_TYPE, UNKNOWN_ROUTE, TOO_MANY_ROUTES (вид `COMBAT_GROUP_SINGLE_ROUTE` не поддерживает больше одной Трассы, см. `CombatDutyTypeDefinition.supportsMultipleRoutes`). НЕ реализовано (см. A51/A52/A53): отдельная публикация графика комплектования (§24.1 второй шаг — в этом срезе создание сразу видно всем как готовая запись), `requiredGroups`/`requiredPosts` (§24.4, только `requiredEmployees`), создание НОВЫХ Трасс/реестра (`routes` остаётся фикстурой — форма только выбирает из существующих). `requiredEmployees: number \| null` — `null` для демо-фикстур, заведённых ДО этого среза (честно не пересчитано задним числом), число — для новых. `mocks/repository.test.ts` +10 тестов. `e2e-mock/combat-duty-requirement.spec.ts` — новая спека (16-я): создать смену на будущую дату → появляется «Требует подачи» с честной `requiredEmployees` → состав подаётся на неё как на любую другую. `npm run gate` (941/941, 218.8 KB gzip); `npm run test:e2e:mock` (16/16, дважды подряд); `npm run test:e2e` (62/62, не задет). Ручная browser-QA (dev:mock, persona с `ops.duty.manage`) подтвердила создание смены на 2026-08-05 с 2 Трассами | пользователь снова выбрал «Продолжение §24»; из оставшихся кусков (потребность на период / передача смены §24.22) потребность выбрана как более связная — передача смены (§24.22) предполагает РОТАЦИЮ смен внутри дня, чего текущая модель (одна смена = один день) не поддерживает без более крупного структурного изменения |

| A55 | §24.22 «Передача и завершение смены» — предполагает РОТАЦИЮ экипажей (сдающий+принимающий), которую текущая модель (один экипаж на businessDate) не поддерживает. Пользователю ПРЯМО задан вопрос о сокращённом объёме — подтверждено: реализован только checkpoint сдачи (`CombatDutyExecution.handover`: незакрытые происшествия, замечания, кто сдаёт — БЕЗ принимающего экипажа). `submitCombatDutyHandover` доступна только из ACTIVE, сдающий обязан быть в leader+members (NOT_IN_ROSTER), обязательно указать кто (CONFIRMER_REQUIRED). `completeCombatDuty` теперь требует `handover !== null` (MISSING_HANDOVER) — кнопка «Завершить дежурство» задизейблена с `title` до оформления сдачи. Право переиспользует `ops.combat_group.complete` — новое право не заводилось (тот же актор оформляет и сдачу, и завершение). `SCHEMA_VERSION` 9→10 (аддитивное обязательное поле `handover`). `mocks/repository.test.ts` +8 тестов. `e2e-mock/combat-duty-execution.spec.ts` — существующая спека расширена шагом сдачи смены (не новый файл). НЕ реализовано: принимающий экипаж/подтверждение с его стороны (модель не поддерживает), состояние постов/Трасс, переданные задачи, средства/имущество по permission (§24.22 полный список пунктов) — всё это требовало бы выдуманной второй стороны или новых сущностей за пределами текущего честного среза | пользователь подтвердил сокращённый объём после явного вопроса (не начато вслепую, риск непропорционален без подтверждения) |

| A56 | §9.11 «Замена выбывшего сотрудника» (CONDUCT stage, по запросу «продолжай разрабатывать») — реализована ТОЛЬКО ручная замена: мастер-промпт сам помечает алгоритм авто-подбора кандидата как `REPLACEMENT-SUGGESTION-001 = business-policy-pending` (не утверждён заказчиком) — авто-подбор НЕ реализован. `replaceAssignment` — одна атомарная мутация: снимает старое назначение, создаёт новое на ТОТ ЖЕ пост, пишет journal entry типа REPLACEMENT (переиспользует `features/security-events` журнал, не новую фичу). Право `ops.conduct.manage` (то же, что журнал/закрытие) — новое право не заводилось. Валидация: REASON_REQUIRED (пустая причина), сотрудник не найден, INVALID_STAGE_TRANSITION (не CONDUCT), assignmentId не найден, DOUBLE_ASSIGNMENT (заменяющий уже на другом посту). По пути найден и исправлен реальный форматирующий баг ДО коммита: ФИО в ростере оканчиваются на «.» (инициалы) — шаблон `${name}. Причина:` давал двойную точку («Д.. Причина»), заменено на разделитель «—». `mocks/repository.test.ts` +8 тестов — первое unit-покрытие CONDUCT-стадии в этом файле вообще (addJournalEntry/closeSecurityEvent остаются непокрытыми юнит-тестами, предсуществующий пробел). `e2e-mock/security-event-approval-to-closure.spec.ts` расширена шагом замены (не новый файл). НЕ реализовано: авто-подбор кандидата (business-policy-pending), полный набор сохраняемых полей §9.11 (revision, инициатор/санкционировавший ОТДЕЛЬНО от actorUserId, исходные конфликты, результат повторной проверки — described в мастер-промпте, но требует полей, которых в упрощённой модели нет) | пользователь дал общее «продолжай разрабатывать»; выбрана эта фича среди Not started пунктов как готовая к безопасной реализации — переиспользует существующие assignPlacement/unassignPlacement паттерны и journal, минимальный риск |
| A57 | Печатная форма расстановки (§9.15, `/print/placement`) — **demo-предпросмотр, а не официальный документ**. §9.15 прямо называет официальным artifact'ом `.docx`, который бэкенд строит из утверждённой версии; фронт не может его подменить, поэтому лист несёт печатаемый (не только экранный) маркер «ДЕМОНСТРАЦИОННЫЙ ПРЕДПРОСМОТР» — класс `.print-demo` СОЗНАТЕЛЬНО не скрыт под `@media print`, в отличие от `.print-screen-hint`. Реквизиты §9.15 `reportJobId`/`policyVersion`/`placementVersionId`/`sourceRevisions` НЕ печатаются — их в demo-модели нет, а выдумывать реквизиты документа запрещено (§35); печатаются только реально известные: ответственный, этап, `updatedAt` как снимок. §19.24 («в печатную расстановку не включай rating/score/комментарии/оценщиков») выполнен СИЛЬНЕЕ требования: `result`/`comment` постов рекогносцировки не просто не рендерятся, а НЕ РАЗБИРАЮТСЯ парсером — поле физически не может протечь через будущую правку вёрстки (закреплено красной пробой в `placementPrint.test.ts`). Страница живёт в `features/print-forms` (там block-scoped eslint печатного канона: только `print-*`-классы, импорт `shared/ui`/`lucide-react` красный) и читает данные ЗАПРОСОМ по URL API, а НЕ импортом из `features/security-events` — кросс-фичевый импорт красный по ARCH-FE-013; отсюда рукописная узкая поверхность ответа и дубль `STAGE_LABEL` (обоснованы в шапке `placementPrint.ts`). Строки документа: пост без назначений печатается как «не назначен» (дырка в расстановке — факт документа), пост с N назначениями даёт N строк с потребностью только в первой, назначение на удалённый пост уходит в хвост «вне рекогносцировки» (молча терять назначенного человека нельзя). Итоги «Потребность»/«Назначено» — два независимых числа, к «укомплектовано X из Y» не сводятся. | пользователь дал общее «продолжай разрабатывать фронтенд часть»; выбрана эта фича среди Not started как единственная крупная, под которую УЖЕ есть и реальные данные (recon-посты + placement-назначения), и готовый печатный канон 8.8 — ни выдуманных цифр, ни новой инфраструктуры |
| A58 | Ссылка на печатную форму — В ТОЙ ЖЕ вкладке, БЕЗ `target="_blank"`, вопреки прецеденту `ExpenseReportPage.tsx:285-288`. Причина не стилистическая: credential живёт в `sessionStorage`, а Chromium ≥88 трактует безымянный `target="_blank"` как implicit `noopener` — новая вкладка не является auxiliary browsing context, `sessionStorage` в неё НЕ клонируется, и печатная форма открывается на экране входа. Это НЕ гипотеза: первая версия ссылки была с `_blank`, и новый e2e (`e2e-mock/placement-print.spec.ts`) реально отрендерил в попапе «Вход» — дефект пойман тестом, а не рассуждением. Донорская форма расхода несёт ТУ ЖЕ латентную проблему, но её e2e её маскирует (`e2e/expense-print.spec.ts:200` сеет credential через `context.addInitScript`, т.е. каждая новая вкладка получает его заново — в проде так не бывает). Чинить донорскую ссылку в этом скоупе не стал (E10, отдельное решение) — вынесено отдельной задачей. | найдено при первом же прогоне новой e2e-спеки |

| A59 | «Архив дела» (`/security-events/:id/archive`, прототип `Smart Josparlau.dc.html:1459-1490`, мастер-промпт L5643 «архив открывается только в read-only режиме») — read-only дело ЗАКРЫТОГО ОМ. Архив ничего не досчитывает и не хранит своего: он раскладывает уже накопленные стадиями факты (бюллетень, чек-лист и посты рекогносцировки, потребность и запросы группам, расстановка на момент закрытия, замены §9.11, журнал штаба, итоги закрытия). Read-only — СВОЙСТВО МОДУЛЯ, а не пожелание: страница не импортирует ни одного мутирующего хука и не рендерит ни одного `<button>`/`<input>`/`<textarea>`/`<select>`; закреплено ассертом и в jsdom-тесте, и в e2e (в e2e локатор сужен до `<main>` — кнопки каркаса AppLayout к делу не относятся, иначе ассерт был бы про каркас). До закрытия дело НЕ открывается: и ссылки в шапке нет, и прямой заход по URL даёт названную причину, а не пустую страницу («пусто» читалось бы как «данные потеряли»). Из 6 карточек прототипа две реализованы честным ТЕКСТОМ вместо содержимого: «Все версии расстановки» — в demo-модели одна действующая расстановка, `placementVersionId` (мастер-промпт L5647) отсутствует, список из одной выдуманной «версии 1» был бы ложью; «Оценки, справки, аналитика» — скрытые оценки D3 (A24) не реализованы, и раздел получает `count: null`, а НЕ `0` (ноль читался бы как «оценивали и никого не оценили», §35). Прототипная фраза «Паспорт объекта сохранён на момент мероприятия» не перенесена: snapshot паспорта в модели не хранится. Осиротевшее назначение (пост удалён из расчёта после назначения) выводится отдельной группой с `need: null` — тот же инвариант, что в печатной форме (A57), покрыт красной пробой. `missingClosureDirections` показывает направления расчёта без итога закрытия: закрытие требует итоги всех направлений (Epic 18.1 FR-30), но дело читают годы спустя — расхождение показываем, а не заглаживаем. `JOURNAL_TYPE_LABEL` переехал из `SecurityEventDetailPage.tsx` в `lib/stageMeta.ts` — второй словарь-литерал разъехался бы с первым. Новых прав/фикстур/эндпоинтов НЕТ: право `ops.security_event.view` то же, что у карточки (архив не открывает ничего сверх уже читаемого), CLOSED-события в demo-seed нет намеренно — e2e доводит сеяное APPROVAL-событие до закрытия своим же сценарием (новая CLOSED-фикстура сломала бы точные счётчики `analytics.spec.ts`). | пользователь дал общее «продолжай разрабатывать фронтенд часть»; направление выбрано самостоятельно из Not started-матрицы по тому же критерию, что Этап 26: есть реальные данные (все стадии уже пишут факты в модель) и нулевая новая инфраструктура |

| A60 | Версии паспорта объекта (§8.5 `publishPassportVersion`, §8.10) — реализована публикация ДЕЙСТВУЮЩЕЙ редакции паспорта как неизменяемого снимка + read-only просмотр версии по собственному deep link `/objects/:id/passports/:versionId` (мастер-промпт L5562/L6038). Секторы в запросе НЕ передаются: снимок делает repository с того, что реально лежит в объекте — иначе клиент мог бы опубликовать не то, что показывает форма. Правила публикации: дата обязательна и строго `YYYY-MM-DD`; §8.10 «не более чем одна действующая опубликованная версия на дату» — повтор на ту же дату отклоняется; паспорт без единого поста публиковать нечего (документ ни о чём). Право — существующее `ops.object.manage` (тот же владелец, что правит паспорт), новое не заводилось; чтение версии — `ops.object.view`, версия не открывает ничего сверх объекта. Страница версии read-only по построению (ни мутирующего хука, ни `button`/`input`), и она НЕ переключает пользователя на действующую редакцию (мастер-промпт L5651): показывает снимок и говорит об этом текстом. `SCHEMA_VERSION` 10→11 (`SecurityObject.passportVersions` — обязательное поле). Демо-сид: у «Дворца Независимости» версия v1 есть изначально (иначе read-only просмотр недостижим, пока кто-нибудь не опубликует руками), у остальных объектов история пуста. **Честная оговорка про глубокую копию снимка**: красная проба показала, что unit-тест неизменяемости СЕГОДНЯ зелёный и без неё — `updatePassport` заменяет `sectors` целиком новым массивом из запроса, поэтому общая ссылка ничего не портит. Копия оставлена как ВТОРОЙ гард (защита от будущей правки, меняющей секторы на месте) и помечена в коде как непроверяемый тестом инвариант, а не выдана за доказанный. НЕ реализовано: выбор действующей версии на дату операции (`passportVersionId` в дежурствах/расстановке — мастер-промпт L3767/L3791, требует протянуть ссылку через две другие фичи), архивация/отзыв версии, сравнение версий, snapshot паспорта внутри ОМ (архив дела честно не заявляет его, см. A59). | пользователь дал общее «продолжай разрабатывать фронтенд часть»; направление выбрано самостоятельно: `publishPassportVersion` — операция, ПРЯМО названная в §8.5, с инвариантами, прямо названными в §8.10, поверх уже существующих данных паспорта — ни выдуманных сущностей, ни новых прав |

| A61 | §9.6 «Объект, паспорт и посты» (Этап 29) — ОМ привязывается к КОНКРЕТНОЙ опубликованной версии паспорта. Решения: (1) объект в форме создания ОМ стал ВЫБОРОМ из реестра вместо свободного текста — без `objectId` привязывать версию не к чему; имя объекта снимает СЕРВЕР, клиент его больше не присылает; (2) снимок `PassportBinding` ХРАНИТСЯ в ОМ, а «какая версия действует сейчас»/`stale` — производные, считаются на каждом чтении отдельным endpoint'ом `…/passport/`: хранимый флаг устаревания молча разъехался бы, потому что публикация новой версии ОМ не трогает (§9.6 «публикация новой версии паспорта не переписывает действующую расстановку»); (3) цепочка §9.6 `objectId/passportVersionId/sectorId/postId` замкнута через `ReconSectorPost.sourceSectorId/sourcePostId` — назначение → строка расчёта → снимок привязки → объект; строки из паспорта РАЗРЕШЕНО править (§9.6 «event-specific изменение поста не редактирует паспорт автоматически»), метка сообщает источник, а не запрещает правку; (4) импорт ДОБАВЛЯЕТ строки и дедуплицируется по `sourcePostId` — затирать ручной расчёт операцией «подтянуть из паспорта» значило бы терять работу человека; (5) `need: 1` у импортированной строки — паспорт описывает пост, а не численность на мероприятие (§35: выдуманное число хуже минимального). **Кросс-фичевая граница**: версии паспорта принадлежат `features/objects`, импорт оттуда красный по ARCH-FE-013 — сделан СЕРВЕРНЫЙ join: mock-repository читает соседний слайс `objects` из того же снапшота по имени через рукописную узкую проекцию (`lib/passportBinding.ts` + `mocks/objectsSlice.ts`), тот же приём, что в печатной форме (A58); запись в чужой слайс запрещена и покрыта тестом. Для demo-сида пришлось сделать порядок builder'ов ЗНАЧИМЫМ (`SeedContext.builtSlices`, `objects` строится первым) — иначе ОМ нечем сослаться на объект по id, а склейка по имени запрещена (A44-A46); порядок держит `app/mocks/compose-seed.test.ts`, не комментарий. **Не реализовано**: перепривязка ОМ на новую версию по кнопке (только предупреждение — §9.6 называет предупреждение, перепривязку не называет), `passportVersionId` в дежурствах (`features/duties` — своя модель Трасс, отдельный срез), предложение обновить паспорт после закрытия ОМ (§9.6 последний абзац) | пользователь выбрал направление явно («бери passportVersionId в расстановке»); §9.6 — единственный раздел мастер-промпта, где эти инварианты перечислены списком, реализованы ровно они |

| A62 | §9.6 для ДЕЖУРСТВ (Этап 30) — `DutyShift.passportBinding` (objectId/versionId/versionNumber/effectiveFrom/**sectorId/postId**/boundAt). Привязка дежурства ШИРЕ, чем у ОМ (A61): ОМ привязывается к объекту целиком, а расчёт постов строит рекогносцировка; дежурство же заступает на КОНКРЕТНЫЙ пост — поэтому снимок несёт сектор и пост. Отсюда же отказ переиспользовать `features/security-events/lib/passportBinding.ts`: кросс-фичевый импорт красный по ARCH-FE-013, а поднимать общий кусок в `shared/` значило бы вынести доменное знание туда, где доменных модулей нет вовсе — сделана вторая рукописная узкая проекция (`features/duties/lib/passportBinding.ts` + `mocks/objectsSlice.ts`), тот же приём, что A58/A61. Производное «какая версия действует сейчас»/`stale` НЕ хранится: приходит блоком `passportStatuses` рядом с `results` в ответе списка — отдельный endpoint на каждую строку таблицы плана был бы N+1, а поле внутри `DutyShift` смешало бы хранимое с вычисленным. Отсутствие привязки объясняется ТРЕМЯ разными текстами (объекта нет в реестре / версии на дату нет / в версии нет постов), а не одним прочерком: для планировщика это три разных действия. `bindDutyPost` возвращает `null`, если поста нет в этой версии — подставить «какой-нибудь» пост хуже, чем показать отсутствие привязки. **Demo-сид склеивает дежурство с объектом по НАЗВАНИЮ** (`findObjectByName`) — это НЕ нарушение A44 (запрет join по ФИО между bounded context): склейка живёт только в сиде, где обе стороны — фиксированные строки под нашим контролем (id генерируются во время композиции, сослаться на них статически нечем), а в рантайме связь только по `objectId`; «Штаб управления» намеренно не заводится в реестре объектов, чтобы исход «объект вне реестра» оставался достижимым. `SCHEMA_VERSION` 12→13. **Не реализовано**: перепривязка дежурства на новую версию по кнопке (§9.6 называет предупреждение, перепривязку не называет — то же решение, что в A61), выбор сектора/поста человеком при планировании (сид берёт первый пост версии — операции «создать индивидуальное дежурство» в UI нет вовсе), привязка боевых групп на Трассах (`CombatDutyShift` — Трассы, а не объекты; §24.11 требует их различать) | пользователь выбрал направление явно («теперь passportVersionId в дежурствах») — единственный оставшийся пункт §9.6, помеченный как не сделанный в A61 |

| A63 | Месячный план дежурств (§21.27-21.30, §21.34-21.35, Этап 31) — вкладка «Месяц» на `/duties` поверх ТОГО ЖЕ `DutyShift`, но через ОТДЕЛЬНЫЙ серверный ресурс `GET /api/ops/duty-monthly-plan/?month=YYYY-MM`. Почему не фильтр по уже загруженным сменам: §21.29 требует показывать СЕРВЕРНЫЕ значения KPI и прямо запрещает «вычислять итог по отрисованной части календаря», §21.34 — «frontend не определяет severity самостоятельно»; поэтому и сетка, и KPI, и конфликты приходят готовыми из `lib/monthlyPlan.ts`, вызываемого mock-repository («сервером»), а страница только рисует. Свойство закреплено красной пробой: тест отдаёт KPI, ЗАВЕДОМО расходящийся с сеткой (сетка — 1 смена, ответ — 42), и на экране обязано быть серверное число. **Конфликты (§21.34)**: реализованы два выводимых из модели класса — пересечение (два дежурства сотрудника в один день, всегда HARD, тот же принцип, что hard-rule двойного назначения в расстановке и §24.17) и нарушение обязательного отдыха; severity ВТОРОГО берётся из `DutyTypeDefinition.restPolicy` (HARD_BLOCK→HARD, SOFT_OVERRIDE→SOFT), длительность — из `restAfterMinutes` (§21.35 «не хардкодь 24 часа»). §21.35 называет политику ГЛОБАЛЬНОЙ серверной настройкой — здесь она атрибут вида дежурства, рядом с минутами, к которым относится: фронт в любом случае читает действующее значение и severity не выводит. Вид дежурства вне реестра отдыха НЕ навязывает (нечего читать — 24 часа не выдумываем). Конфликты ищутся по ВСЕМУ набору смен и лишь потом обрезаются месяцем: пара «последний день месяца → первый день следующего» иначе молча терялась бы (покрыто тестом стыка месяцев). **Чего в этом срезе НЕТ и почему**: lifecycle плана DRAFT→VALIDATED→APPROVED и кнопки §21.28 («Сформировать черновик», «Утвердить план», «Экспортировать») — у месячного плана нет собственной сущности, месяц это ПРОЕКЦИЯ существующих смен; кнопка без операции запрещена §35. Показатели §21.30 «укомплектовано 4/4» и «были замены» не вычисляются вовсе и приходят отдельным блоком `unavailableMetrics` С ПРИЧИНОЙ (у суточного дежурства нет требуемой численности — `requiredEmployees` только у боевых групп; история замен только у боевых групп §24.21): молчаливый ноль читался бы как «посчитали, вышло 0». Представление «По сотрудникам» (матрица доступности §21.30) не сделано — требует слоёв отдыха/ОМ/кадровой недоступности, из которых в модели есть только отдых. Warning-severity (третий уровень §21.34) не заводится: единственный кандидат — устаревшая версия паспорта — уже показан построчно в плане (§9.6, A62), второй владелец одного факта не нужен. **Демо-сид**: к четырём сменам на бизнес-дату добавлены шесть на соседних днях (по представителю каждого класса конфликта). Все они ПОСЛЕ бизнес-даты намеренно — дефолтный день «Календаря смен» и дефолтный месяц этой вкладки берутся из первой по дате загруженной смены (реального «сегодня» у фронта нет, demo-runtime живёт по DemoClock), смена раньше бизнес-даты увела бы дефолт календаря в прошлое; настоящий backend прислал бы бизнес-дату явно. Объект «Дом Министерств» и ФИО существующих смен в новых фикстурах не используются — e2e-локаторы вида `locator('tr', { hasText: … })` обязаны остаться однозначными. `SCHEMA_VERSION` 13→14 (`DutyTypeDefinition` получил обязательные `restAfterMinutes`/`restPolicy`). Новых прав НЕТ: чтение плана — тот же `ops.duty.view`. | пользователь дал общее «продолжай разработку фронтенд части»; направление выбрано самостоятельно: A31 закрывал месячное планирование фразой «нет Duties Repository» — с Этапа 5 репозиторий появился, и месяц строится поверх одного набора данных, как §21.4 и требует |

## Архитектурные решения этой реализации (Этап 49 — Настройки §29)

| # | Решение | Причина |
|---|---|---|
| A81 | §29 «Настройки» — ОТДЕЛЬНЫЙ маршрут `/settings` со своим правом, а не вкладка `/audit`: промпт прямо требует разделить read-only operational audit и role-restricted administration, хотя прототип сводит их одним экраном | §29 дословно |
| A82 | Администрируется ПОЛИТИКА НАБЛЮДЕНИЙ §22.11 (допуски, пороги), потому что у неё есть живой потребитель — блок «Требует внимания» аналитики службы. `reset demo data`/feature flags НЕ заведены: §8.3 называет их `mock-only-demo` runtime, которому запрещено появляться в продуктовом API; `status types`/`dictionaries` уже администрирует §30 (второй экран = второй источник истины); `rating rules` — рейтингов в сборке нет (A24), настройка без предмета мертва | §8.3, §35, A24 |
| A83 | Владение разделено: методика (мера, текст, маршрут перехода) остаётся у аналитики, ЧИСЛА (допуски, пороги, версия политики) — у настроек. Аналитика читает их узкой проекцией чужого слайса `mocks/settingsSlice.ts` — пятый случай приёма (A58, A62, отчётный реестр, источник смен), `features→features` по-прежнему запрещён | ARCH-FE-013 |
| A84 | Нет слайса настроек — блок наблюдений ЧЕСТНО недоступен с причиной, а не пуст: пустой список читался бы как «замечаний нет» — утверждение, которого сервер не делал | §35, тот же принцип, что отсутствие источника смен |
| A85 | Повтор ТОГО ЖЕ значения — 422 `SETTING_VALUE_UNCHANGED`, а не тихий успех: иначе журнал наполнялся бы записями «53 → 53», а `policyVersion` рос бы без смены методики, и снимок аналитики отличался бы от предыдущего без причины | §22.11 (снимок несёт версию политики) |
| A86 | Порядок порогов проверяется МЕЖПОЛЕВО (422 `SETTING_THRESHOLD_ORDER_INVALID`): значение законно само по себе и незаконно рядом с соседним. Диапазон каждой записи приходит с сервера вместе со значением — форма не догадывается о границах | §29 «валидируются» |
| A87 | След ведёт САМА фича (журнал изменений в своём слайсе), а не `/api/audit/logs/`: тот в этой сборке read-only витрина фиксированных данных, дописывать в чужой агрегат нельзя. Тот же вывод, что у ленты обращения §28 и журнала переходов ОМ. Журнал виден и тому, кто менять не вправе — контроль администратора отделён от администрирования | ARCH-FE-013, §29 «попадать в audit» |
| A88 | `canManage` приходит С СЕРВЕРА (ответ списка) и перепроверяется на PATCH; persona `analyst` получила `ops.settings.view` без `ops.settings.manage` — иначе состояние «просмотр без изменения» недостижимо (у администратора wildcard) | §29 role-restricted; приём «право без persona-без-него не демонстрируется» |
| A89 | Журнал изменений живёт по СВОЕМУ префиксу `/api/ops/setting-changes/`, а не `/api/ops/settings/change-log/`: второй вариант сматчился бы маршрутом `settings/:settingCode/` (`settingCode="change-log"`) — ровно тот дефект, что уже ловили на `acknowledgement/complete` | инцидент Этапа 3 |
| A90 | §29 `conflict rules` — **режим обязательного отдыха переехал из ВИДА дежурства в глобальную политику** (Этап 50). §21.35 разделяет владение прямо: «используй `restAfterMinutes` вида дежурства» (СРОК) и «применяй серверную политику `REST_AFTER_DUTY_POLICY`, читай действующее значение `HARD_BLOCK`/`SOFT_OVERRIDE`» (РЕЖИМ). До этого среза режим был атрибутом `DutyTypeDefinition`: разные виды блокировали по-разному, то есть действующей глобальной политики, которую §21.35 велит читать, НЕ СУЩЕСТВОВАЛО и администрировать её было негде. Поле `restPolicy` удалено из типа ВОВСЕ (недостижимость по типу надёжнее договорённости — тот же приём, что отсутствующий `VALIDATED` в A70). Потребителей три, и все читают ОДНУ политику: `detectConflicts` (план/форма/карточка), список дежурств и аналитика службы — иначе один конфликт был бы жёстким на плане и мягким в аналитике. |
| A91 | Разделы политики **версионируются порознь**: `policyVersion` (наблюдения §22.11) и `conflictPolicyVersion` (правила §21.34). Общая версия означала бы, что правка порога наблюдений меняет методику конфликтов, — снимок и план утверждали бы о себе неправду. Версия правил стоит В КАЖДОМ конфликте (`MonthlyDutyPlanConflict.policyVersion`), а не только в ответе: конфликт живёт дальше ответа (диалог обхода, протокол, месячный план), и там нечем сверить редакцию. `null` — политика не прочитана, применён строгий дефолт. |
| A92 | Дефолт при отсутствии политики — **строгий** (`HARD_BLOCK`, буквально §21.35), а сеяное значение — мягкое (`SOFT_OVERRIDE`). Это не противоречие: дефолт отвечает на вопрос «что делать, когда действующего значения нет вовсе» (молча ослабить запрет из-за недоступной политики = пропустить назначение, которое политика могла бы отвергнуть), а сид демонстрирует путь 409+override, иначе недостижимый ни на одном экране. Непонятый код режима тоже не превращается в «ограничений нет». |
| A93 | **Редактируемость — свойство ПРАВИЛА, а не прав смотрящего.** Правило пересечения (§21.34 «hard-conflict нельзя обойти») показано в разделе запертым: `editable: false` + `lockedReason`, PATCH отвечает 422 `SETTING_RULE_LOCKED` даже wildcard-персоне. Замок проверяется ПЕРВЫМ — и в выдаче (`buildAction`), и в операции: проверь право раньше, и причина отказа зависела бы от того, кто спросил (тот же приём, что замок закрытого обращения A80). Расхождение этих двух порядков было РЕАЛЬНЫМ дефектом, найденным собственным тестом. Право раздела отделено: `ops.settings.manage_conflict_rules` ≠ `ops.settings.manage` — правка правил меняет ИСХОД чужой операции, а не окраску экрана; persona `analyst` получила второе без первого. |
| A94 | Запись настройки стала дискриминированным союзом `NUMBER \| CHOICE`, журнал хранит ГОТОВЫЕ подписи (`oldValue`/`newValue: string`): «SOFT_OVERRIDE → HARD_BLOCK» требовало бы от читателя знания кодов, а сборка подписи на экране держала бы словарь режимов в двух местах. Форматирует владелец вариантов — сервер. `detectorCode` переименован в `groupCode` (группа = детектор §22.11 ЛИБО правило §21.34); проекция аналитики правится вместе с ним — она читает слайс через `unknown` и типы её НЕ защищают. |
| A95 | Версии разделов политики хранятся КАРТОЙ `sectionVersions: Record<SettingSectionCode, string>` (Этап 51), а не именованными полями. Третий раздел показал, что поля плодятся, а операция правки обрастает ветвлением «какое из них двигать»; с картой это одна строка, и новый раздел её не трогает. Экран печатает версию В ШАПКЕ СВОЕГО раздела — общий блок версий при трёх разделах уже не отвечает, какая из них к чему относится. |
| A96 | §21.7 «срок актуальности приходит от policy»: политика свежести паспорта переехала из слайса `objects` в раздел «Настройки» (Этап 51). Интервал лежал в данных с Этапа 37, но у него не было АДМИНИСТРАТОРА, а порог «скоро проверка» вообще оставался константой `DUE_SOON_FRACTION = 0.2` в коде — вторым захардкоженным числом периода рядом с настраиваемым первым, ровно тем, что §21.7 запрещает. Порог задан ДОЛЕЙ интервала: растянув интервал, администратор иначе молча остался бы с прежним окном предупреждения. Поля `freshnessPolicy` в слайсе `objects` больше НЕТ (недостижимость по типу, как `restPolicy` в A90). |
| A97 | Третье право раздела — `ops.settings.manage_passport_policy` у persona «Ведение объектов»: политикой объектов владеет тот, кто ведёт объекты. Права по разделам теперь демонстрируются ЗЕРКАЛЬНО: `analyst` правит наблюдения и не трогает паспорта, `objects_admin` — наоборот, и обе стороны закреплены тестами (одностороннее «отдельное право» доказывало бы только половину). |
| A98 | **Контракт demo-снапшота вынесен отдельным тестом в `app/`** (`settings-projections.contract.test.ts`). Граница «владелец слайса → узкая проекция потребителя» нетипизирована по построению (ARCH-FE-013 запрещает импорт между фичами), поэтому переименование поля у владельца компилируется зелёным, а потребитель молча падает на дефолт; юнит-тесты обеих сторон остаются зелёными — у каждого своя фикстура. Так Этап 51 сломал ТРИ проекции разом (`policyVersion`/`conflictPolicyVersion` → карта), и поймал это только живой e2e. Тест собирает всех потребителей против ТОГО САМОГО сида, который уезжает в снапшот. Живёт в `app/` — единственном слое, которому позволено видеть несколько фич сразу (там же `compose-seed.ts`); в фиче он краснил бы ARCH-FE-013. |

## Открытые вопросы (не блокируют frontend, из §37)

- Длительность `BEFORE_DUTY` — configurable demo dictionary (значение по умолчанию зафиксировать в Этапе 5).
- Погодные коэффициенты — demo dictionary, значения не финальны.
- Состав «боевого расчёта подразделений» сверх известных сценариев — traceability open question.
- Роли вне ОМ — pending dictionary.
- Реальная ЭЦП, интеграция КУ — future integration, вне текущей задачи.

## NEXT ACTION
См. `FRONTEND_PROGRESS.md`.
| A64 | §21.31/§21.33/§21.34 «Создание индивидуального дежурства» (Этап 32, направление выбрано самостоятельно — разрыв, названный в NEXT ACTION Этапов 30 и 31: смены были только из сида). **Главное решение — обход soft-конфликта идёт КАНОНИЧЕСКИМ путём платформы, а не своим**: код `DUTY_CONFLICT_DETECTED` уже лежит в `docs/registries/error-codes.yaml` (`overridable: true`, `category: conflict_soft`, source «VAPS_7.8.2 §6 (дежурства)») и включает цепочку `useApiMutation.conflict` → общий `shared/ui/ConflictDialog` → повтор с `override`/`override_reason`; свой код (`DUTY_CONFLICT_SOFT`) завёл бы ВТОРОЙ параллельный протокол обхода при живом первом. Следствие: переменные мутации `useCreateDutyShift` — САМО ТЕЛО запроса, а не `{ body }` как у остальных мутаций фичи (`confirmOverride` дописывает два ключа в КОРЕНЬ переменных; при обёртке они уехали бы рядом с телом — закреплено красной пробой). §21.34 severity выводит `detectConflicts` — ТОТ ЖЕ модуль, что строит месячный план: иначе форма и план разошлись бы в определении конфликта; берётся РАЗНИЦА «после − до», уже существовавший в данных конфликт новую смену не блокирует. HARD → 422 (не обходится даже с `ops.duty.override_rest`), SOFT → 409 с `details.conflicts[]`. Обоснование хранится в `DutyShift.overrideReason` и видно строкой в плане; при отсутствии конфликта НЕ записывается (обоснование без конфликта читалось бы как «здесь что-то обходили»). §21.31 server policy — «красный паспорт» блокирует только под видом с новым атрибутом `requiresCurrentPassport`, три причины недоступности различаются текстами и не схлопываются. §21.33 реализован в честном подмножестве: из перечисленных промптом факторов модель даёт РОВНО ОДИН (уже запланированные дежурства), остальные приходят блоком `unavailableAttributes` с причиной (§35), а не нулём. НЕ реализовано: «время»/«продолжительность» отдельными полями смены (модель «одна смена = один день», A55; продолжительность и правила отдыха ПОКАЗЫВАЮТСЯ из вида дежурства, §21.35), «необходимое количество сотрудников» (у индивидуального режима равно одному), редактирование/отмена заведённой смены, карточка дежурства §21.32 | форма не имеет права выводить ни политику, ни severity (§21.29/§21.34) — поэтому и список объектов, и причина блокировки, и конфликт приходят готовыми с «сервера»; переиспользование канонического 409-протокола вместо своего — прямое следствие ARCH-FE-015 «диалог ОДИН, в shared/ui» |
| A65 | §21.32 «Карточка дежурства» (Этап 33, прямое продолжение Этапа 32). §21.32 перечисляет ОДИННАДЦАТЬ блоков; из модели суточного дежурства выводимы ПЯТЬ (пост и исполнитель, конфликты, ознакомление, фактическое участие, паспорт) — они нарисованы, остальные шесть («Ответственный по объекту», «Резерв», «Замены», «Инциденты», «Итоги», «Оценивание», «Журнал») плюс `revision` и время из шапки приходят С СЕРВЕРА списком `unavailableBlocks` С ПРИЧИНОЙ по каждому: тот же приём, что архив дела закрытого ОМ (A59), потому что пустой блок читается как «данных нет», а молчание — как «учтено». Ответ собирается ОДНИМ ресурсом `GET /api/ops/duty-shifts/:id/`, а не четырьмя запросами со страницы: карточка обязана показать согласованный срез, конфликт по другому снимку смен, чем показанная смена, хуже его отсутствия. Конфликты фильтруются по сотруднику И по дню смены — у нарушения отдыха `businessDate` это день ВТОРОГО дежурства, поэтому первая смена пары конфликта не показывает, и это не потеря: конфликт принадлежит той смене, которая его создала (закреплено тестом обеих сторон пары). `dutyType` может прийти `null` (вид выведен из реестра) — карточка называет это явно и НЕ подставляет «сутки» (§21.35). Маршрут `/duties/:id` за тем же `ops.duty.view`, что план (карточка не открывает ничего сверх строки плана — разворачивает её), действия внутри гардятся `ops.duty.manage` отдельно. Мутации перехода инвалидируют и `['duties','shift-detail']` — иначе карточка осталась бы на прежнем состоянии после собственного действия. `SCHEMA_VERSION` не менялся: новых полей нет | разрыв между «названо в §21.32» и «есть в модели» — главное, что срез обязан показать честно; §35 запрещает и выдумывать данные, и молча опускать блок |
| A66 | §21.31, правка и отмена заведённой смены (Этап 34). **Дата и вид дежурства НЕизменяемы** — оба задают действующую версию паспорта и правила отдыха, смена с другой датой это другая смена; перенос делается отменой + созданием, где конфликты пересчитываются с нуля и видны человеку. **Смена сотрудника снимает ознакомление** (ACKNOWLEDGED → PLANNED, `acknowledgedAt` → null): отметка приписывала бы согласие человеку, который его не давал — тот же принцип, что §24.21 у боевых групп. **Отмена, а не удаление**: `CANCELLED` — тупиковое состояние с обязательной причиной, смена остаётся в плане; удаление стёрло бы след планирования, а он и есть предмет §21. Отменённая смена при этом ВЫБЫВАЕТ из `detectConflicts` (иначе отмена «лечила» бы план на экране, продолжая блокировать планирование — худший исход) и из KPI, но не молча: заведены отдельный KPI «Отменено», `cancelledCount` в ячейке сетки и отдельный бакет в аналитике службы — вычет без показа сделал бы разницу между запланированным и оставшимся невидимой (§35). Правка/отмена только до заступления (PLANNED/ACKNOWLEDGED): после него подмена сотрудника или поста — подлог; гейт в репозитории, UI лишь не рисует кнопки. Гейт конфликтов §21.34 стал ОБЩИМ для создания и правки — `assertNoNewConflicts`, где «до» считается по набору БЕЗ смены-кандидата: при правке она уже в слайсе, и без исключения по id смена конфликтовала бы сама с собой и не сохранялась НИКОГДА (красная проба покраснила четыре теста разом). Обоснование обхода снимается, если правка конфликт устранила — оно относится к конфликту, а не к смене навсегда. `SCHEMA_VERSION` 15→16. Новых прав нет | заведённая смена не должна быть неисправимой; при этом «исправить» не может означать «подменить незаметно» — отсюда узкий список изменяемых полей и обязательный след у отмены |
| A67 | §21.30 «По сотрудникам — матрица доступности» (Этап 35, направление выбрано самостоятельно). Реализовано ЧЕСТНОЕ ПОДМНОЖЕСТВО: из шести слоёв промпта модель даёт четыре (дежурство, обязательный отдых, конфликт, неполные данные) — они рисуются; занятость ОМ и кадровая недоступность приходят блоком `unavailableLayers` с ПРИЧИНОЙ (§35), причём причина у ОМ содержательная («другое ID-пространство сотрудников, склейка по ФИО дала бы ложные совпадения», A44-A46/A50), а не «нет данных». Прежняя отметка «§21.30 Not started, требует слоёв ОМ и кадровой недоступности» была верна для ПОЛНОГО объёма и неверна как запрет на подмножество: это другая проекция ТЕХ ЖЕ смен, §21.4 прямо называет представления «представлениями одного набора данных». Матрица приходит в ТОМ ЖЕ ответе месячного плана (`employeeRows`), а не отдельным запросом — два представления одного месяца обязаны считаться по одному снимку, иначе разойдутся; закреплено тестом «переключение не делает второго запроса». Семантика клетки: `DUTY` доминирует над `REST` (в день второй смены подряд сотрудник прежде всего дежурит, а нарушение отдыха уже названо конфликтом — второй способ показать тот же факт завёл бы второго владельца); слои СКЛАДЫВАЮТСЯ в текстовой подписи, а не вытесняют друг друга; длина отдыха — `ceil(restAfterMinutes / сутки)` у вида ДЕЖУРСТВА (§21.35), вид вне реестра отдыха не навязывает; отдых считается по всему набору смен и обрезается месяцем на выдаче (иначе хвост от 30 июня терялся бы в июле); строку получает только сотрудник с занятостью в месяце (полного списка личного состава фича не знает, A26/A35). Два дефекта, найденных прогонами: коллизия имён кнопок (переключатель матрицы дублировал подписи вкладок страницы — переименован в нотацию §21.30 «Объекты × дни»/«Сотрудники × дни») и `scrollable-region-focusable` (прокручиваемую таблицу нельзя было пролистать с клавиатуры, WCAG 2.1.1 — починено в ОБЕИХ матрицах, старая не падала лишь по случайности замеров). `SCHEMA_VERSION` не менялся, новых прав нет | §35 запрещает и выдумывать слои, и молча их опускать; отсюда «четыре рисуем, два называем с причиной» вместо «отложить весь §21.30» |
| A68 | §21.30 «Список дежурств» и «История» (Этап 36). Из одиннадцати колонок промпта модель даёт семь — они таблица; четыре (интервал, ответственный, открытые посты, подтверждение плана) приходят `unavailableColumns` с причиной (§35). **«История» = ДВА обязательных признака**: `stateCode === 'COMPLETED'` И `businessDate < businessDate(сервера)`. Одного мало ни в ту, ни в другую сторону: завершённая сегодня смена ещё не история, а завершённого факта у будущей смены не бывает — оба случая закреплены тестами и красными пробами (каждая проба снимает один признак). Формулировка промпта «с подтверждённым ПЛАНОМ и фактом» выполнена наполовину сознательно: lifecycle плана §21.28 не реализован, «подтверждённого плана» как состояния не существует — это НЕ подменено молчанием, а названо среди невыводимых колонок. Граница прошедшего берётся у DemoClock и ПРИХОДИТ В ОТВЕТЕ (`businessDate`), чтобы экран назвал её вслух, а не отфильтровал по часам машины (§8.8). Счётчики конфликтов серверные (§21.34) — иначе список разошёлся бы с месячным планом и карточкой, где они уже считаются. Сортировка зависит от режима: план от ближайшего вперёд, история от свежего назад. `SCHEMA_VERSION` не менялся, новых прав нет. Честная деталь демо: история пуста по построению (Этап 31 положил все сеяные смены на бизнес-дату или позже) — e2e проверяет именно это состояние, а не обходит его | §21.30 перечисляет колонки, которых у суточного дежурства не бывает в принципе; «показать семь и назвать четыре» честнее, чем и отложить весь список, и нарисовать пустые ячейки |
| A69 | §21.7 «KPI реестра объектов» (Этап 37). **Отменяет A30**: тот отклонял §21.7 фразой «не подкреплены серверным агрегатом в demo-срезе» — блокер устарел ровно так же, как у §21.30, mock-репозиторий и есть сервер. Главное требование §21.7 — «не используй фиксированный frontend-период („Паспорт старше 90 дней“), срок приходит от policy» — выполнено СТРУКТУРНО: интервал хранится в слайсе (`PassportFreshnessPolicy`), `verificationDueAt`/`freshnessState` считаются на сервере и приходят готовыми, ответ несёт `freshnessPolicyVersion`, экран показывает, чем посчитан срок. Интервал сида НАМЕРЕННО 120, а не 90 — совпадение с примером промпта скрыло бы захардкоженный период, если бы он где-то остался (есть тест «90 дней не порог» и красная проба на него). Порог «скоро проверка» — ДОЛЯ интервала (0.2), а не своё число дней: вторая константа периода рядом с настраиваемой была бы тем же нарушением. Второе требование — «не считай KPI по текущей странице таблицы» — тоже структурно: агрегаты приходят из ответа, посчитанные по всему реестру, и тест подсовывает KPI, заведомо расходящийся с числом строк. `NO_PUBLISHED_VERSION` — отдельное состояние, а не «просрочен»: непубликовавшийся паспорт не устарел, у него другой дефект. Клик по KPI пишет фильтр В URL (переживает reload, передаётся ссылкой), повторный клик снимает; KPI при фильтре не пересчитываются. Demo-сид вырос 3→5 объектов, чтобы все четыре состояния актуальности были достижимы; рябь — счётчики в `analytics.spec.ts`. `UnavailableMetric` объявлен в objects СВОЙ: импорт из duties красный по ARCH-FE-013, а поднимать три строки в shared/ значило бы вынести туда доменное понятие (A58/A62). Дефект, найденный e2e: `PASSPORT_LABEL.GREEN` и `FRESHNESS_LABEL.FRESH` оба звались «Актуален» — два одинаковых слова в соседних колонках, ровно то, что §21.5 запрещает; актуальность переименована в «Срок соблюдён». `SCHEMA_VERSION` 16→17 | «серверного агрегата нет» перестало быть правдой, как только mock-repository начал их строить; §21.7 при этом задаёт два ПРОВЕРЯЕМЫХ запрета, и оба закреплены пробами, а не обещанием |
| A70 | §21.27 lifecycle месячного плана и §21.28 шапка плана (Этап 38). **Отменяет A63** в части «у плана нет собственной сущности, рисовать кнопки не за чем»: заведена `MonthlyPlanRecord` (ключ — месяц). **Состояний ДВА, `VALIDATED` состоянием НЕ стало** — §21.27 прямо разрешает это («если registry содержит только DRAFT и APPROVED, не добавляй промежуточный статус; VALIDATED может быть результатом проверки»), поэтому проверка живёт как `lastValidation` (когда, сколько жёстких/мягких, пройдена ли), а в типе `MonthlyPlanStateCode` варианта `VALIDATED` нет вовсе — недостижимость по типу надёжнее договорённости. Проверка привязана к `planFingerprint` (id+`updatedAt` активных смен месяца): любая правка состава обесценивает её, и утверждение снова требует проверки — иначе утверждали бы по протухшему результату. **§21.27 «изменения через новую revision» реализовано замком, а не правкой поверх**: утверждённый месяц закрыт для создания/правки/отмены смен (`PLAN_APPROVED_LOCKED`), а `REOPEN` поднимает редакцию и возвращает план в DRAFT; история только дополняется. Замок СОЗНАТЕЛЬНО не распространён на ознакомление/заступление/завершение — это факт несения службы, а не изменение плана (красная проба). **§21.28 «доступность кнопки приходит от action policy API»**: все шесть действий считает сервер (`buildPlanActions`), экран не имеет ни одной ветки «если утверждён — выключить»; тест подсовывает ответ, где у утверждённого плана «Утвердить» ДОСТУПНО, и экран обязан его показать активным. Утверждение и переоткрытие — за отдельным правом `ops.duty.approve_plan`, не за `ops.duty.manage`. Невыводимые поля шапки (scope пользователя, состояние доступности) и эффекты утверждения (проекции занятости, уведомления) приходят с причиной (§35). «Экспортировать» недоступен по НЕРЕАЛИЗОВАННОСТИ, а не по правам — подмена одной причины другой ввела бы в заблуждение. `SCHEMA_VERSION` 17→18 | §21.27 сам ограничивает свой lifecycle двумя состояниями, а §21.28 требует серверной action policy: и то и другое — прямые запреты, выполнимые структурно, а не декоративно |
| A71 | §20.27/§20.28/§20.33 «Персональные данные», раскрытие sensitive identity (Этап 39). **Реальный дефект, который этим закрыт**: ИИН маскировался в ВЁРСТКЕ (`maskIin` в списке), а полное значение приезжало с донорского `/api/core/employees/` и оседало в query cache у любого, кто открыл список — маскирование в вёрстке маскированием не является. Донорский контракт менять нельзя (ARCH-FE-011, типы из схемы), поэтому Smart Josparlau получил СВОЮ безопасную проекцию `GET /api/ops/personnel-directory/` (§20.29 «list endpoint должен возвращать безопасную проекцию»): в ответе нет поля `iin` ВООБЩЕ, только `iinMasked`, посчитанный сервером, и `canRevealIin` — доступность действия тоже считает сервер (тот же принцип, что action policy §21.28). Донорские `/api/core/*` остаются замоченными, но экраны портала их больше не читают, и хуков `useEmployees`/`useEmployee` больше нет — хук существовал бы, значит рано или поздно был бы использован. **Раскрытие — отдельная операция** `POST …/identity-disclosure/` с четырьмя из пяти условий §20.27: своё право `personnel.identity.reveal` (отдельно от просмотра), обязательная ЦЕЛЬ (не короче 10 символов), КОНЕЧНЫЙ срок полномочия (15 минут, считает сервер) и аудит. Пятое — organization scope — НЕ реализовано и названо вслух и в коде, и на экране: RBAC demo-режима плоский (§8.9/D9), подменять его молчаливым «разрешено всем в пределах права» нельзя. **Журнал раскрытий (§20.33) НЕ содержит значения** ни целиком, ни хвостом: он отвечает «кто, когда и зачем смотрел», а не «что увидел» — §20.33 запрещает класть чувствительное в audit payload без отдельной policy, и такой policy проект не заводит. Запись пишется ВНУТРИ мутации, после всех проверок (§20.33 «не записывай success audit до успешного ответа»), и читается по СВОЕМУ праву `personnel.identity.audit` — раскрывающий журнала не видит, контролёр не раскрывает (persona `analyst` демонстрирует это живьём). Маска фиксированной ширины: длина маски не выдаёт длину значения. `SCHEMA_VERSION` 18→19 (новый слайс `personnel`) | §20.27 перечисляет условия раскрытия и места, где значение НЕ должно остаться, поимённо — это набор проверяемых инвариантов, а не пожелание; выполнимы все, кроме организационного scope, которого в demo-RBAC нет |
| A72 | Отчётный реестр службы: асинхронная генерация, immutable-артефакт, server-side masking (§22.18-22.25, §20.32, Этап 40). Новая фича `features/service-reports`, свой слайс, маршрут `/service-reports` (НЕ `/reports` — там донорский «Расход дня» E10). **Прямое продолжение A71**: та же природа sensitive-данных, но на выгрузке. §22.24 «нельзя формировать чувствительный XLSX из уже загруженного в браузер полного массива» выполнено структурно — и выборка строк, и маскирование, и сборка файла живут в repository; экран получает МЕТАДАННЫЕ, а содержимое видит только в ответе операции скачивания. **Маскирование убирает КОЛОНКИ, а не значения**: пустая ячейка читалась бы как «примечания не было». Обычный экспорт исключает `note` (персональные комментарии) и `overrideReason` (скрытые ограничения), sensitive export — отдельная операция за отдельным правом `ops.report.export_sensitive`, которое право `ops.report.generate` НЕ включает, и проверяется оно ПОВТОРНО на скачивании (право могли отозвать после генерации). **§22.21 состояния серверные**: работа создаётся PENDING, артефакт приходит ровно с COMPLETED, до этого ни метаданных, ни кнопки. Фонового исполнителя в demo нет (DemoClock не тикает), поэтому ступень продвигается сервером НА ЧТЕНИИ реестра — упрощён исполнитель, а не модель состояний; экран опрашивает и останавливает опрос на терминальном состоянии. Идемпотентность по `idempotencyKey` из параметров: повторный клик не создаёт вторую работу. **§22.22 срок хранения — из политики В ДАННЫХ** (21 день, намеренно не «привычные» 30), хардкода «файлы доступны 30 дней» нет; глубина периода — тоже политика (92 дня, не 90). **§22.23 постоянной ссылки на файл не существует вовсе**: скачивание отдаёт ПОТОК, поэтому ссылке неоткуда утечь в HTML, list endpoint, Tooltip, `aria-label`, telemetry, localStorage или URL. Форматы, которых проект не формирует (XLSX/PDF/DOCX), названы с причиной, а не нарисованы кнопками — прямой запрет §22.23 на «фальшивые действия». `SCHEMA_VERSION` 19→20 | §22.24 и §22.23 — набор проверяемых запретов, а не пожеланий: каждый из них выражается тестом на САМ ответ сервера, а не на вёрстку |
| A73 | §22.25 «История отчётов» отдельным экраном `/service-reports/history` (Этап 41, прямое продолжение собственного NEXT ACTION Этапа 40). **Редакция стала настоящей, а не украшением**: артефакт нумеруется по СЕРИИ (тип + период + режим выгрузки), номер считается по максимуму существующих, а не по их количеству — исчезнувший по сроку хранения артефакт не освобождает свой номер, иначе два разных файла звались бы «редакция 2». Режим выгрузки входит в ключ серии намеренно: обычный и чувствительный отчёты содержат разные колонки, это разные документы, а не редакции одного. **Два действия §22.25 различаются содержательно, а не названием**: `Повторить` возвращает уже готовый пригодный артефакт, если он есть (промпт прямо это допускает: «создаёт новый job … если repository не возвращает существующий пригодный артефакт»), `Сформировать новую revision` собирает ВСЕГДА — иначе новая редакция не появилась бы никогда. Параметры повтора берёт СЕРВЕР из исходной работы: «с теми же параметрами» должно означать те же самые, а не те, что клиент считает прежними; автор новой работы — тот, кто её запустил, а не автор исходной. **Доступность каждого действия и причина отказа приходят с сервера** (`buildJobActions`) — в компоненте нет ни одной ветки «если работа упала, выключить кнопку»; тест подсовывает ответ, где у УПАВШЕЙ работы скачивание доступно, а просмотр ошибки нет, и экран обязан послушаться ответа (тот же приём, что action policy месячного плана, A70). **Видимость — исправленный пробел §22.25 «показывай только разрешённые jobs и artifacts»**: работа со скрытыми полями невидима без права `ops.report.export_sensitive` — её параметры и автор сами по себе говорят, кого и за какой период выгружали целиком; фильтрация серверная, а повтор чужой невидимой работы отвечает «не найдено», а не «нет прав» (отказ по правам подтвердил бы, что такая выгрузка существует). Фильтры (состояние, «только мои») применяет сервер и хранит URL; продвижение работ при этом идёт для ВСЕХ, а не только для показанных, иначе скрытая фильтром работа застряла бы в очереди навсегда. **Сбой сборки стал состоянием, а не исключением** (§22.21): ошибка проекции чужого слайса теперь роняет одну работу в FAILED с безопасным сообщением, а не чтение всего реестра — до этого одна испорченная строка источника лишала человека и готовых отчётов, и причины. Колонка `scope` из §22.25 не нарисована пустой, а названа с причиной (плоский RBAC demo-режима, §35). `SCHEMA_VERSION` не менялся, новых прав нет: история не открывает ничего сверх того, что уже разрешено запускающему отчёт, но маршрут проверяет право ЗАНОВО (§22.27) | §22.25 перечисляет колонки и действия поимённо — это набор проверяемых утверждений; «редакция» и «повтор» без серверной модели серии были бы подписями на кнопках |
| A74 | §22.27 карточка работы `/service-reports/:reportJobId` (Этап 42, прямое продолжение собственного NEXT ACTION Этапа 41). **Собственная пометка «это вариация истории» снова оказалась НЕВЕРНОЙ — второй раз подряд**: срез потребовал новой серверной операции (§22.28 «получить состояние job» + «получить метаданные artifact» ОДНИМ ответом: двумя запросами карточка показывала бы состояние из одного, а файл из другого, и между ними работа успевает продвинуться), нового права и вскрыл реальную утечку уже написанного. **Главное содержание — §22.26 «просмотра параметров чужого отчёта» как ОТДЕЛЬНОЕ право** `ops.report.view_foreign_parameters`: право запускать отчёты даёт видеть, ЧТО коллега выгружал; период и режим его выгрузки — следующий шаг. Своя работа доступна всегда, разрешение нужно ровно на чужую. Вырезание — СЕРВЕРНОЕ (§22.27 «при запрещённом доступе закрытые данные отсутствуют в DOM»): `parameters`, `artifact.parameterSnapshot` И `idempotencyKey` приходят `null`. **Ключ идемпотентности — не перестраховка: он ПО ПОСТРОЕНИЮ содержит период** (`PERSONNEL_EXPENSE:2026-07-01:2026-07-31:N:1`, собирается формой запуска), и оставить его значило бы вернуть период соседним полем ответа — тот же класс, что снимок параметров в артефакте. **Скачивание чужого артефакта закрыто тем же правом**, и причина названа честно: период написан в ПЕРВОЙ СТРОКЕ содержимого (`buildReportContent`), поэтому скрыть его на экране и отдать файлом — не защита, а её видимость; проверка стоит на СЕРВЕРЕ, а не только в политике действий (выключенная кнопка не мешает позвать операцию). Отказ по скачиванию проверяется ПЕРВЫМ, до срока хранения: сказать «срок истёк» тому, кому вообще нельзя видеть эту выгрузку, — подмена причины (тот же порядок, что sensitive-право при создании, A72). Право на чужие параметры НЕ заменяет `export_sensitive`: sensitive-работа остаётся невидимой (404) и тому, у кого оно есть. **Побочно исправлена реальная утечка Этапа 41**: реестр печатал период ЛЮБОЙ видимой работы, включая чужие. Карточка продвигает работу своим чтением — иначе открытая по прямой ссылке PENDING-работа висела бы вечно, а реестр ту же работу доводил бы до конца. Действия считает сервер (`buildJobActions` получил `parametersVisible`), в компоненте ни одной ветки; «Открыть параметры» и «Посмотреть ошибку» на карточке не рисуются КНОПКАМИ вовсе — карточка и есть развёрнутая строка, и кнопка, ничего не открывающая, была бы действием без последствий. `/reports/artifacts/:artifactId` из §22.27 сознательно не заведён (у работы ровно один артефакт, его метаданные показаны здесь, постоянной ссылки на файл не существует — §22.23), причина названа блоком §35 на экране. `SCHEMA_VERSION` НЕ менялся — новых полей в слайсе нет, изменилась только ПРОЕКЦИЯ. |
| A75 | §22.3-22.12 аналитика службы переписана на СЕРВЕРНЫЙ снимок (Этап 43). **Отменяет прежнюю реализацию экрана целиком, и это не рефакторинг, а исправление**: `app/ServiceAnalyticsPage.tsx` складывал распределения В БРАУЗЕРЕ из постраничных списков трёх фич (`pageSize: 100`) и подписывал их «по видимым записям». Подпись была ЧЕСТНОЙ, но §22.3 запрещает сам приём — «итог по видимой части таблицы» стоит там в одном списке с численностью, просрочкой и светофором. Новая feature `service-analytics` считает всё в repository и отдаёт `MetricValue` со `state`/`displayValue`; **в компоненте нет ни одной ветки, смотрящей на `value`** — карта цветов идёт от СОСТОЯНИЯ, и тест подсовывает ответ, где значение НОЛЬ, а состояние `CRITICAL`. Экран переехал из `app/` в `features/`: композировать три фичи больше не нужно (читается ОДИН эндпоинт), а значит и повода жить в `app/` не осталось. **§22.4 снимок целиком**: `snapshotId`, business date, timezone, период, scope, `generatedAt`, `sourceUpdatedAt`, `freshnessState`, `completenessState`, версии расчёта и политики. `snapshotId` ДЕТЕРМИНИРОВАН входом (версия данных + период + scope): §22.12 требует таскать его в drill-down, а сверить можно только воспроизводимый — случайный различал бы два одинаковых снимка, а часы делали бы его невоспроизводимым. **Отсутствие источника даёт `value: null` + `UNKNOWN`, а НЕ ноль** — «0 конфликтов» и «конфликты неизвестны» человек читает по-разному (§22.4 отдельно запрещает заменять `UNKNOWN` зелёным); ради этого проекция чужого слайса возвращает `null`, а не пустой массив — в отчётном реестре тот же случай решён ИНАЧЕ осознанно (там пустой отчёт — правда). **§22.5 периоды в ДАННЫХ**: пресеты и предел произвольного периода (62 дня, снова не круглые) приходят из registry, дефолт решает сервер — ни «последних 7 дней», ни порога перегрузки в коде экрана нет. **§22.12 drill-down**: строки НЕ едут с KPI (отдельный запрос, пин красной пробой, добавляющей их в ответ), `rowId` — стабильный идентификатор смены, курсорная страница НАМЕРЕННО мала (4), иначе вторая страница не наступила бы на demo-объёме и pagination жил бы непроверенным. Три права вместо одного (§22.26): `view` / `drilldown` / `personal_detail`; ФИО вырезает сервер. Число показателя равно ДЛИНЕ выборки, а не считается отдельно: два места разошлись бы при первой правке. Показатели §22.7, которых demo-срез не даёт (численность, доступность, перегрузка, участие в ОМ), приходят `unavailableMetrics` с причиной — причём численность исключает САМ §22.7 как кадровый KPI. `SCHEMA_VERSION` 20→21 (новый слайс `serviceAnalytics`). |
| A76 | §22.11 блок «Требует внимания» — СВОИ серверные детекторы, а не перекрашенные KPI (Этап 44). Соблазн был собрать блок из уже посчитанных показателей («конфликтов 3 → внимание»), и это была бы перестановка чисел экрана, выданная за серверное наблюдение. Поэтому: отдельный ресурс `/api/ops/service-analytics-attention/`, отдельная политика в слайсе (`attentionDetectors`), отдельная версия `ATTENTION_POLICY_VERSION` — намеренно НЕ равная `POLICY_VERSION` порогов показателей, и ни один детектор не читает `MetricValue`. Отличие видно на каждой мере: «превышение порога» считает ДОЛЮ конфликтных смен (три конфликта на четыре смены и три на сорок — разные наблюдения, KPI в обоих случаях покажет «3»); «незавершённые процессы» и «данные не подтверждены» требуют ВЫДЕРЖКИ допуска (KPI считает с первого дня); «требуется проверка» смотрит на отсутствие отметки перед заступлением — такого показателя в §22.7 нет вовсе. **Словарь §22.11 — гейт, а не соглашение**: `ATTENTION_TITLES` содержит ровно пять разрешённых формулировок, заголовок берётся ОТТУДА по коду (свободного текста в заголовке нет — именно там обвинительный вывод появился бы первым), а `FORBIDDEN_ATTENTION_PHRASES` проверяется тестом по КАЖДОМУ произведённому тексту. Описания — шаблоны В ДАННЫХ с плейсхолдерами: текст составляет сервер, экран печатает дословно и НЕ сортирует (порядок — свойство ответа). **`count: null` у наблюдения об источнике**: ноль читался бы как «ноль случаев», то есть как отсутствие наблюдения. **Пустой список ≠ мёртвый детектор**: нет источника или нет политики → `detectionState: 'UNAVAILABLE'` + причина; молчание сказало бы «замечаний нет» от имени сервера, который ничего не проверял. Переход §22.27 рисуется по `targetPermission` элемента (persona `analyst` видит наблюдение, но не получает ссылку в раздел дежурств) — это не замена проверке на маршруте, а отказ рисовать заведомо закрытую ссылку. `SCHEMA_VERSION` 21→22. Побочно: блок `ATTENTION_ITEMS` убран из `UNAVAILABLE_HEADER_BLOCKS` — он больше не «то, чего нет». |
| A77 | §22.13/§22.15 аналитика ОМ реализована, §22.14 воронка — НЕТ, и это решение, а не пропуск (Этап 45). §22.14 первой строкой требует строить воронку по СЕРВЕРНЫМ transition events; событий перехода в модели ОМ нет вовсе — `SecurityEvent` держит только текущую стадию. Собрать конверсию, «достигших этапа» и среднее время из распределения по стадиям значило бы выдать снимок «кто где сейчас» за историю переходов, поэтому воронка приходит блоком §35 с причиной; той же причиной закрыты «возвраты на доработку (количество)» и «просроченные действия» (сроков этапов модель не хранит), а «открытые инциденты» — потому что у записи журнала нет состояния открыт/закрыт. **Колонки принадлежат УРОВНЮ и приходят с сервера**: §22.15 запрещает смешивать «запрошено», «выделено», «назначено» и «фактически участвовало», а значит решать, что с чем сопоставимо, не может вёрстка; на уровне поста колонок «запрошено»/«выделено» нет вовсе — пустая колонка читалась бы как ноль. **Всё адресуется стабильными ID**: id направления ВСЕГДА содержит id ОМ (одинаково названный сектор в двух мероприятиях — два разных направления), строка расчёта из паспорта и ручная строка того же сектора НЕ сливаются (у ручной подпись прямо говорит «без связи с паспортом»), а ОМ без объекта в реестре лежат в своей корзине «Объект вне реестра» — свести их по `objectName` было бы ровно той склейкой по имени, что запрещена A44-A46. **Lifecycle Registry — в данных** (`opsLifecycleRegistry` в слайсе): подписи стадий принадлежат чужой feature, импорт красный по ARCH-FE-013, а дубль-константа разошлась бы молча; код вне реестра приезжает списком `unknownLifecycleCodes` и НЕ раскладывается по чужим корзинам. Право своё — `ops.analytics.operations` (§22.26), проверено в обе стороны: право на аналитику службы не открывает ОМ и наоборот. Экран живёт в той же feature `service-analytics`: снимок §22.4 и §35-словарь общие, вторая feature потребовала бы дубля контракта. |
| A78 | §22.14 воронка ОМ построена — но сначала заведён APPEND-ONLY ЖУРНАЛ ПЕРЕХОДОВ, потому что §22.14 запрещает строить её по текущему массиву карточек (Этап 46). **Журнал пишется ДИФФОМ в единственной точке**: `commitEvents` в repository ОМ — теперь единственное место, где слайс уходит в снапшот, и оно сравнивает стадии «до/после». Ручная запись означала бы девять мест, где её можно забыть, а забытый переход не сломал бы ни одного теста стадий и молча исказил бы конверсию; при таком устройстве новая операция попадает в журнал сама, ничего не зная о нём (закреплено тестом «операция, меняющая стадию, оставляет запись, ничего не зная о журнале»). Возврат на доработку пишется `kind: 'RETURN'`, а не вычитается из переходов. **Шесть показателей §22.14 держатся раздельно**: «достигших этапа» считает РАЗНЫЕ ОМ (вернувшееся и снова дошедшее мероприятие достигло этапа один раз, а переходов сделало два), «находящихся на этапе» — единственный показатель, который читает текущие карточки, и назван так, чтобы его нельзя было спутать с «достигшими». Складывать их нельзя даже по единицам (ОМ, переходы, часы) — экран показывает ОДИН за раз через явный переключатель, как требует §22.14. **Среднее и медиана считаются отдельно и только по ЗАКРЫТЫМ интервалам**: мероприятие, стоящее на этапе сейчас, в среднее не попадает — иначе среднее падало бы тем сильнее, чем дольше ОМ висит; сеяные интервалы намеренно разной длины, чтобы среднее не совпало с медианой. **`excludedEventIds` пуст, но с ЯВНОЙ причиной**: состояний «отменено»/«приостановлено» в модели нет, и это сказано вслух, а не изображено пустым фильтром; «количество отменённых ОМ» осталось в §35. `SCHEMA_VERSION` 22→23. |

| A79 | §28 «Обратная связь» — реестр обращений и создание (Этап 47, направление выбрано самостоятельно). **Пункт был помечен Not started БЕЗ ПРИЧИНЫ** — и перечитывание показало, что единственное, что могло бы его блокировать (вложения), в §28 названо `attachment metadata`, то есть blob-хранилища не требует вовсе. Реализовано: своя feature `features/feedback`, свой слайс, маршрут `/feedback`, четыре права (`ops.feedback.view` / `create` / `view_all` / `view_confidential`). **Пять решений, которые держат этот срез.** (1) **Черновик не открывается никаким правом чтения**: §28 держит «Черновик» отдельным статусом, то есть обращением, которое автор ещё не отправил; показать его читателю значило бы отдать недописанный текст без ведома написавшего, и `ops.feedback.view_all` этого не меняет — проверено и на wildcard-персоне. (2) **Конфиденциальность закрывает СОДЕРЖАНИЕ, а не строку**: тема, тип, приоритет, статус и модуль остаются видны, иначе реестр перестаёт быть реестром (обращение нельзя ни найти, ни отличить от дубликата); вырезаны описание, ожидаемый результат, шаги, контакт, вложения, техинформация и `relatedRoute` — вырезает СЕРВЕР, и вместе с ними вырезано **производное `descriptionPreview`** (оставить его значило бы вернуть первые 120 символов закрытого текста соседним полем — тот же класс, что `idempotencyKey` в A74). (3) **Область поиска ограничена видимыми полями**: поиск по вырезанному описанию выдаёт его содержимое, ничего не показав — человек набирает слово и по факту совпадения узнаёт, что оно там есть; описание участвует в поиске ровно тогда, когда оно же приедет в ответе. (4) **Согласие на техническую информацию решает СЕРВЕР**: присланная без согласия техинформация не сохраняется — тело запроса согласия не заменяет, иначе «include technical info» был бы галочкой, ни на что не влияющей; отсутствие — `null`, а не пустой объект («не собирали» ≠ «собрали и не нашли»). (5) **Вложение — ровно три поля**: запись собирается поимённо, месту для содержимого неоткуда взяться; форма не читает `File` ни `FileReader`, ни `arrayBuffer()`. Сводка §28 считается сервером по всему ВИДИМОМУ набору (до фильтров и до страниц) — сводка по отрисованной странице была бы итогом по видимой части таблицы, ровно тем, что §22.3 запрещает. Подписи и порядок справочника — В ДАННЫХ (`FEEDBACK_REGISTRY`), коды — в типах: экран не хранит ни одной русской подписи справочника, и тест подсовывает непривычную подпись. Размер страницы 4 — намеренно мал, иначе вторая страница на demo-данных не наступила бы никогда. Отметка времени техинформации берётся из `serverTime` последнего ответа реестра, а не `new Date()` (§8.8). Не реализовано и названо блоком §35 с причиной по каждому пункту: содержимое вложений, рабочий приоритет (§28 отличает его от заявленного — назначает разбирающий), переходы статусов после «Новое», ответственный. | §28, Этап 47 |

| A80 | §28 detail — карточка обращения (Этап 48, прямое продолжение своего NEXT ACTION; заранее «вариацией реестра» не помечалась и ею не оказалась: потребовала двух новых прав, четырёх операций и своей ленты). **Шесть решений.** (1) **Внутренняя заметка — отдельный ВИД комментария, а не флаг «приватный»**: у них разные читатели, разные права на запись и разная судьба в ленте; флагом их различать значило бы держать оба текста в одной коллекции и полагаться на то, что фильтр не забудут. Право `ops.feedback.internal_note` отделено от `ops.feedback.triage`: заметка пишется о человеке, обратившемся за помощью, и право её вести — не то же, что право менять статус (демонстрируется persona `analyst`, у которой есть разбор и нет заметок). (2) **Событие внутренней заметки скрывается ЦЕЛИКОМ, а не «без текста»**: строка «добавлена внутренняя заметка» без содержания всё равно сообщает автору, что о нём что-то написали и когда, — лента превратилась бы в счётчик работы разбора. Автор видит СВОЙ разговор, а не чужую работу над ним. (3) **Лента пишется ДИФФОМ в единственной точке** (`commitChange` + `diffEvents`) — приём журнала переходов ОМ (A78) переиспользован осознанно: четыре операции меняют поля, ничего не зная о ленте, и забытое событие невозможно. Неизменённое поле события не порождает — повтор того же значения не поступок. (4) **timeline и audit — ОДНА лента**: два журнала об одних и тех же событиях рано или поздно разойдутся, поэтому событие несёт и вид для человека, и `fieldCode`/`oldValue`/`newValue` для аудита §28. (5) **Закрытие — отдельная операция с ОБЯЗАТЕЛЬНЫМ ответом автору**, а не «ещё один переход статуса»: разрешить терминальный статус в разборе значило бы закрыть обращение молча. Разбор такой переход отклоняет (`FEEDBACK_USE_CLOSE`). Признание дубликатом требует указать оригинал, и оригинал обязан быть ВИДИМ закрывающему — сослаться на обращение, которого он не видит, значит утверждать о содержимом вслепую; ссылка на оригинал скрывается уже от СМОТРЯЩЕГО, если тот его не видит, иначе дубликат стал бы обходным путём к чужому обращению. (6) **Замок закрытого обращения проверяется ПЕРВЫМ и одинаково для всех действий** — иначе причина отказа зависела бы от прав смотрящего, и один и тот же закрытый разговор объяснялся бы по-разному. Карта переходов и терминальные статусы — В ДАННЫХ (`statusTransitions`/`terminalStatuses`): порядок разбора принадлежит службе, а не экрану; компонент не выводит допустимые статусы из текущего и не знает, какие из них закрывают обращение. Рабочий приоритет и ответственный — поля ОТКРЫТЫЕ: это решение службы об обращении, а не его содержание, и признак конфиденциальности их не закрывает. §35-блок карточки: содержимое вложений, срок реакции (SLA-политики нет), связанная сущность. | §28, Этап 48 |


| A99 | **Связи значения справочника считает СЕРВЕР по общему снимку — поле `referencedCount` удалено с записи (Этап 52, §30).** Оно было константой фикстуры и не пересчитывалось ничем; проверка «не допускай удаление используемого значения» держалась на выдуманном числе. Причём ошибалось оно в опасную сторону: у `JOURNAL_ENTRY_TYPES` — единственного справочника, чьи КОДЫ реально хранит потребитель (`journalEntries[].type` в слайсе ОМ), — фикстура утверждала «связей нет», и используемые значения деактивировались беспрепятственно; зато у неотслеживаемых справочников стояли «3», «5», «2», выглядевшие как результат подсчёта. Поле удалено ИЗ ТИПА, а не занулено (приём Этапа 50 с `restPolicy`): недостижимость по типу надёжнее договорённости не читать. Считается на чтении, проекция — не слайс (A74). | §30, Этап 52 |

| A100 | **Три состояния связей вместо числа: `TRACKED` / `NOT_TRACKED` / `UNKNOWN`.** Ноль отдаётся ТОЛЬКО когда счёт действительно произведён. Для справочников, чей потребитель хранит свободный текст, а не код (паспорт объекта пишет требования поста строкой, возврат расстановки — комментарием), ноль был бы утверждением «удалять безопасно», то есть ложью (§35) — вместо него отдаётся ПРИЧИНА, почему счёт невозможен. Отсутствие чужого слайса даёт `UNKNOWN`, а не ноль: «посчитать не удалось» и «связей нет» — разные утверждения (тот же принцип, что в снимке аналитики, A75). Реестр отслеживаемости — В ДАННЫХ (`USAGE_TRACKING` в `lib/usage.ts`), и справочник, которого в нём нет, считается неотслеживаемым по умолчанию: забыть строку безопаснее, чем молча объявить связи посчитанными. | §30/§35, Этап 52 |

| A101 | **Удаление и деактивация различаются СТРОГОСТЬЮ, а не только эффектом.** §30 говорит именно об удалении, поэтому операция заведена своим путём (`DELETE /api/ops/dictionaries/entries/{id}/`), а не флагом у `set-active`. Деактивация обратима — ей достаточно «живых связей не найдено»; удаление необратимо — оно требует ДОКАЗАННОГО отсутствия связей, поэтому `NOT_TRACKED` и `UNKNOWN` его запрещают своими кодами (`DICTIONARY_USAGE_NOT_TRACKED` / `DICTIONARY_USAGE_UNKNOWN`), а не общим 409. Признанное следствие, закреплённое тестом: требование поста неотслеживаемо, значит удалить его нельзя, а значит и его группа остаётся неудаляемой — рабочим путём для обоих остаётся деактивация. Это записано явно, чтобы не всплыть у пользователя как загадка. | §30, Этап 52 |

| A102 | **Отказ несёт ПОНЯТНУЮ ЗАВИСИМОСТЬ, а не счёт** (§30 требует именно этого). `RepositoryConflictError` носит объект связей, handler кладёт его в `details` конверта — источник назван подписью и до трёх носителей (`Записи журнала штаба (охранные мероприятия) — 1 (ОМ-2026-3)`). Подписи источников — готовые строки, а не коды (тот же приём, что журнал §29, A94). Склонение отказа задаёт ВЫЗЫВАЮЩАЯ сторона (`'деактивация запрещена'` / `'удаление запрещено'`), а не общий шаблон: подстановка существительного в фиксированную форму дала «деактивация запрещено» — дефект поймал живой e2e, unit-тесты сверяли только вхождение подстроки. | §30, Этап 52 |

| A103 | **Живой перекрёстный счёт заставил расширить инвалидацию кэша.** Связи считаются между справочниками (значение «Требований постов» — связь своей группы), поэтому мутация в одном справочнике меняет отображаемые связи ДРУГОГО. Узкая инвалидация по `['dictionaries','entries',code]` показывала бы соседу устаревшую зависимость, поэтому все три мутации раздела инвалидируют `['dictionaries']` целиком. Класс тот же, что «узкая проекция ломается молча» — компилятор такую границу не охраняет. | §7.13, Этап 52 |

| A104 | **Предел принадлежит не тому, кого он связывает.** §22.5-числа (глубина произвольного периода аналитики, период отчёта, срок хранения файла) переехали из слайсов `serviceAnalytics`/`serviceReports` в раздел «Настройки». Число в собственном слайсе потребителя — тот же хардкод, что и константа в его коде, только на шаг дальше: править его было негде, а §22.5 прямо перечисляет эти величины в списке «не хардкодь». Права на разделы (`ops.settings.manage_analytics_limits`, `ops.settings.manage_reporting_limits`) СОЗНАТЕЛЬНО не выданы persona `analyst`, хотя ей принадлежит методика её собственных наблюдений (A81): аналитик правит то, как считает, но не рамки, в которых ему разрешено запрашивать и выгружать. Срок хранения — ещё и окно доступности sensitive-выгрузки. | §22.5/§29, Этап 53 |

| A105 | **Отсутствие политики — запрет, а не «без ограничения».** Непрочитанный предел даёт `null`, и каждый потребитель обязан решить его сам, а не подставить «привычное» число: аналитика отвергает произвольный период кодом `PERIOD_LIMIT_UNAVAILABLE` (именованные пресеты продолжают работать — их глубина задана самим пресетом), отчёт с типом без записи политики не формируется (`PERIOD_LIMIT_UNAVAILABLE`), а без срока хранения не собирается вовсе (`RETENTION_UNAVAILABLE`) — файл без `expiresAt` жил бы вечно. Дефолт «90 дней» вернул бы ровно тот хардкод, ради переноса которого политика и заведена (тот же принцип, что `null` вместо нуля в снимке аналитики, A75). | §22.5/§35, Этап 53 |

| A106 | **Предел периода отчёта задан НА ТИП** (`groupCode` записи = код типа отчёта), а не одним числом на раздел: типов станет больше, и общее значение объявило бы про новый тип то, чего о нём никто не решал. Тип, для которого записи политики нет, приезжает с `maxPeriodDays: null` и §35-причиной. Согласованность реестра типов с политикой проверяет контрактный тест в `app/` — он перебирает РЕАЛЬНЫЕ `REPORT_TYPES`, поэтому заведённый без политики тип краснеет в гейте, а не на экране. | §22.19/§22.5, Этап 53 |

| A107 | **Срок хранения замораживается в момент СБОРКИ файла.** Артефакт хранит `expiresAt` и `retentionPolicyVersion`, по которой срок назначен; правка политики потом не пересчитывает уже выданные файлы. Пересчёт на чтении означал бы, что сокращение политики задним числом отнимает выгрузку, которую человек уже получил, а продление — воскрешает истёкшую. При этом НОВАЯ работа считается по действующей редакции, и политика, исчезнувшая между запуском и сборкой, роняет саму РАБОТУ (`FAILED`/`RETENTION_UNAVAILABLE`), а не чтение реестра (тот же приём, что сбой сборки, A73). Этот путь недостижим через запуск (там отказ приходит раньше) и потому закреплён отдельным тестом — иначе гард остался бы непроверенным кодом. | §22.22, Этап 53 |

| A108 | **Рейтинг заведён с той стороны, где данные ЗАКРЫТЫ.** §19 — эпик из 29 подразделов; первым срезом взято ЧТЕНИЕ агрегатов, а не формы оценивания. Причина не в объёме: §19.21 требует, чтобы закрытость обеспечивалась API, а не скрытием колонок, и это свойство дешевле заложить в первый же ответ, чем вырезать потом из готовых экранов. `EventEvaluation` (оценка, оценщик, комментарий) живёт только в слайсе и не покидает сервер ни одним полем — закреплено тестом по ВСЕМУ JSON ответа и живой проверкой тел ответов в e2e. Права под sensitive-просмотр НЕ заведены: право без операции ничего не охраняет, а его наличие читалось бы как «функция есть, просто не выдали». | §19.21/§19.22, Этап 54 |

| A109 | **Отсутствие рейтинга — четыре РАЗНЫХ состояния, и ни одно из них не ноль** (§19.2 «не считать отсутствие рейтинга нулевым», §19.19 «не показывай 0,0»): `FEATURE_DISABLED` (функция выключена §19.3), `POLICY_UNDEFINED` (методики нет), `INSUFFICIENT_DATA` (оценок меньше минимума), `READY`. Порядок проверок значим и закреплён тестом: выключенная функция отвечает РАНЬШЕ отсутствующей политики, а та — раньше счёта оценок, иначе человек шёл бы искать недостающие оценки там, где выключен весь раздел. Количество учтённых оценок остаётся видимым и при «недостаточно»: «недостаточно» без числа не отвечает, сколько ещё нужно. | §19.2/§19.3/§19.19, Этап 54 |

| A110 | **Методика рейтинга — раздел «Настроек», а его РЕДАКЦИЯ и есть версия методики.** §19.19 требует, чтобы период, минимальное количество оценок и правила включения приходили от policy, а §19.1 прямо называет захардкоженные «90 дней» ошибкой прототипа. Поэтому период (105 суток — намеренно не 90) и минимум (4) уехали в `RATING_POLICY`, а префикс редакции — имя методики (`OPERATIONAL-RATING-2026.07.1`), потому что эту строку читает человек в сводке. Неполная политика (есть период, нет минимума) даёт `null`, а не половину методики: посчитать по половине и подписать версией значило бы соврать о том, как получено число. Право `ops.settings.manage_rating_policy` отделено от права ЧИТАТЬ рейтинг — правка порога меняет значение, приписанное конкретным людям. | §19.19/§29, Этап 54 |

| A111 | **Порядок строк задаёт сервер и задаёт его по ПОДПИСИ.** §22.16 запрещает таблицу лидеров, место сотрудника и место подразделения; сортировка по значению агрегата и есть таблица лидеров, как её ни озаглавь. Тест сверяет не только «отсортировано по подписи», но и что этот порядок НЕ совпадает с порядком по рейтингу — иначе проверка была бы вакуумной на случайно согласованных данных. Отсутствие запрещённых колонок проверяется СТРУКТУРНО (перечень `columnheader`), а не поиском слов по странице: слово «оценок» законно стоит в подписи «Учтено оценок» и в §35-причинах. | §22.16, Этап 54 |

| A112 | **Точки динамики §19.20 — ЗАПИСАННЫЕ агрегаты закрытых периодов, а не производное от оценок.** Считать ряд из `evaluations` при каждом запросе значило бы применять к прошлому СЕГОДНЯШНЮЮ методику — ровно то, что §19.20 запрещает («не пересчитывай старые точки во frontend»). Поэтому `dynamicsPoints` живут в слайсе отдельным полем (`SCHEMA_VERSION` 31→32, миграции нет: достроить точку из текущих оценок нельзя), у каждой своя `policyVersion`, и ни одна не подписана ТЕКУЩЕЙ редакцией раздела «Настроек» — действующая методика вступила в силу на ОТКРЫТОМ периоде и ни одного периода ещё не закрывала. Экран говорит это прямо (`currentPolicyHasClosedPeriods: false`), а не оставляет читателя думать, что линия построена по действующим правилам. Контрактный тест в `app/` пинит и семейство редакций (общий префикс `OPERATIONAL-RATING-`), и то, что текущая среди них не появилась. |
| A113 | **Линия рвётся по ДВУМ причинам, и обе — требования §19.20, а не оформление.** `segmentDynamics()` начинает новый отрезок при смене `policyVersion` («не соединяй несопоставимые периоды как одну однородную линию») и при `aggregateRating === null` (соединить соседей через период без агрегата значило бы нарисовать значение, которого не было, а положить точку на ноль запрещено §19.19). Точка-пропуск в отрезки не попадает вовсе — её место на графике отдельная отметка, не вершина. Обе причины РАЗВЕДЕНЫ по разным периодам в фикстуре экранного теста: совпади они, тест не отличил бы один разрыв от другого и не заметил бы потери первого (проверено красной пробой на каждом правиле порознь). Границу (`boundaries`) считает СЕРВЕР — для экрана это факт из ответа, а не догадка по соседним точкам. |
| A114 | **График — не единственное представление ряда.** Рядом с SVG стоит таблица тех же точек, а значение и версия методики каждой точки лежат в `<title>` (это и есть tooltip §19.20). Значение, доступное только по наведению мыши, недоступно с клавиатуры и в screen reader — а §19.20 требует tooltip именно как носитель версии policy. Первый SVG-график в проекте: сторонняя chart-библиотека не заводилась — она несла бы собственные умолчания (сглаживание, соединение через пропуски, авто-шкала от нуля), каждое из которых противоречит §19.19/§19.20. Из §35-блока «что не показывается» пункт про отсутствующую динамику заменён на честный `RATING_DYNAMICS_FORECAST`: тренд, скользящее среднее и достроенные значения не рисуются — это было бы вычисление на клиенте поверх агрегатов. |

| A115 | **Отчёт §22.16 охраняет право РАЗДЕЛА АНАЛИТИКИ (`ops.analytics.view`), а не право сводки.** Отсюда и отдельный маршрут `/ratings/analytics`: секцией на `/ratings` отчёт открывался бы тому, у кого есть `ops.rating.view_aggregate`, но нет аналитики — а §22.26 перечисляет просмотр аналитики отдельным пунктом. Новое право НЕ заводилось: существующее уже роздано и уже гейтит два соседних раздела. В тестах разделение демонстрируется ДВУМЯ personas — держатель сводки без аналитики и аналитик без сводки: право, которого никто не лишён, не демонстрируется вовсе. |
| A116 | **Подавление §22.17 — отсутствие расчёта, а не скрытие результата.** Группа меньше порога не считается: `aggregateRating` не появляется в ответе API, поэтому его нечего «раскрыть» в DevTools. Порог (`RATING.SUPPRESSION_MIN_GROUP.PARAMETER = 3`) живёт в разделе «Настроек» отдельной проекцией `readRatingSuppressionMinGroup` — НЕ полем `RatingPolicy`: методика расчёта и правило приватности отсутствуют порознь, и слив их в один объект гасил бы сводку из-за настройки, которую она не читает. Незаданный порог отменяет ВЕСЬ отчёт (`SUPPRESSION_UNDEFINED`), а не публикует его с умолчанием — выбрать порог приватности в коде значило бы решить вопрос за политику. Пустая группа — ТРЕТЬЕ состояние (`NO_AGGREGATE`), не подавление: причины разные, и человеку они говорят разное. |
| A117 | **Общего среднего в отчёте нет — и это защита, а не упущение.** §22.17 «не пытайся восстановить скрытое значение из других показателей»: общее среднее вместе с опубликованными средними и размерами остальных групп даёт подавленное значение арифметикой в одну строку. §22.16 общего среднего и не требует — в его списке распределение, покрытие и количества. Закреплено тестом по ВСЕМУ JSON (искомое 7,7 подавленной группы и 8,02 общего среднего), а не проверкой имён полей. Полосы распределения полуоткрытые (`[from, to)`): «7–8» и «8–9» с включёнными концами оставили бы 8,6 без полосы, а 8,0 — сразу в двух; сумма полос сверяется с числом участников. |

| A118 | **Оценивание и просмотр агрегата — РАЗНЫЕ права, и оценщику агрегат не выдан.** §19.22 перечисляет «выставление оценки» отдельным пунктом; право `ops.rating.evaluate` заведено именно теперь, потому что за ним наконец стоит операция. Организатор ОМ получил его БЕЗ `ops.rating.view_aggregate`: оценщик обязан выставить оценку, не видя, как она сложится в рейтинг человека, — иначе оценивание превращается в подгонку под число. На этом же разделении держится демонстрация §19.14 «Сводка мероприятия показывается только при наличии permission»: у организатора вкладки нет, у администратора-эталона есть, и в сиде специально заведено ЕГО задание — без него вкладку некому было бы открыть живьём. | §19.14/§19.22, Этап 57 |

| A119 | **Задание формирует сервер, и оценщик в ответ не едет ни одним полем.** §19.7 запрещает фронту определять список target, генерировать идентификаторы и создавать задание после refresh — поэтому `workItems` лежат в слайсе (`SCHEMA_VERSION` 32→33), а проекция наружу собирается ПОЛЕМ ЗА ПОЛЕМ, а не «всё, кроме оценщика»: исключающий список молча отдал бы новое закрытое поле. Тело отправки не несёт ни оценщика, ни target, ни мероприятия, ни направления — всё это свойства ЗАДАНИЯ, и переслать их значило бы дать их подменить (§19.18 «нельзя изменить оценщика/target/event»). Очередь отбирается по оценщику НА СЕРВЕРЕ, поэтому §19.14 «не показывай оценки, полученные от других лиц» выполняется отсутствием данных, а не фильтром экрана. | §19.7/§19.14, Этап 57 |

| A120 | **Правило формы одно, а проверок две.** §19.9 требует буквально: форма не отправляется до прохождения frontend validation, backend выполняет ТУ ЖЕ проверку. Две копии разошлись бы молча — первой сдалась бы серверная, её никто не видит глазами; поэтому `validateSubmission` живёт в `lib/` и вызывается обеими сторонами, а репозиторий зовёт её на СВОИХ данных (своя редакция задания, свой перечень оснований). Порядок проверок закреплён тестом: шкала → основание → пояснение → комментарий, и требование комментария не отменяется основанием (§19.10 «основание не заменяет обязательный комментарий»). Отказ едет своим КОДОМ, и экран ставит сообщение рядом с полем по коду, а не разбирая текст. Отказ, адресованный СКРЫТОМУ полю (пояснение к «Другому»), печатается общим сообщением формы — иначе нажатие «Отправить» осталось бы без следа. | §19.9/§19.10, Этап 57 |

| A121 | **Счётчики шапки — про МОЮ работу, сводка вкладки — про чужую, и они названы порознь.** §19.14 перечисляет в шапке «количество work items / завершено / осталось»; здесь они подписаны «Моих заданий», «Отправлено мной», «Осталось мне», потому что очередь отобрана по оценщику, а общий счёт по мероприятию охраняется другим правом. Сводка мероприятия несёт ТОЛЬКО прогресс (участники, задания, отправлено, разложение по направлениям) — ни одного значения оценки, оценщика или комментария: прогресс оценивания и его содержание — разные вещи (§19.21). Участники в ней считаются по различимым target, а не по числу заданий: одного человека могут оценивать двое. | §19.14/§19.21, Этап 57 |

| A122 | **Системная восьмёрка §19.8 не имеет оценщика — и это поле модели, а не оформление.** `EventEvaluation.evaluatorUserId` стал `string | null`: подставленный технический пользователь был бы приписыванием («не приписывай её конкретному человеку как оценщику»). Задания она не порождает: задание — это работа, а системная восьмёрка её ОТСУТСТВИЕ. Кнопки «Применить 8 всем» нет, и отказ назван вслух в §35-блоке — §19.8 перечисляет её поимённо. Групповая оценка (§19.13) объявлена направлением, но заданий этого вида нет: состав группы на момент мероприятия и фактический состав нигде не записаны, а без них групповая оценка была бы оценкой неизвестно кого. | §19.8/§19.13, Этап 57 |

| A123 | **Исправление — новая запись, а исходная остаётся и помечается.** §19.18 «оценку нельзя бесследно редактировать или удалять»: замещающая оценка создаётся отдельной строкой, у исходной меняется РОВНО одна ссылка (`supersededById`), а связь и причина ложатся третьей записью `EvaluationCorrection`. Замещающая наследуется от исходной через `...original` — оценщик, target, мероприятие и направление не переносятся «аккуратным перечислением», а гарантированы структурно (§19.18 запрещает их менять). Из агрегата вытесненную запись исключает СЕРВЕР, и тест это фиксирует: после исправления число учтённых оценок НЕ растёт, а значение меняется. | §19.18, Этап 58 |

| A124 | **Исправление и просмотр цепочки — два права, и оба заведены порознь** (§19.22 перечисляет их отдельными пунктами). Видеть, что запись правили и почему, — контрольная функция; править — распорядительная. Организатору ОМ выданы оба, офицеру рекогносцировки — ни одного при наличии `ops.rating.evaluate`: без persona, лишённой прав, ни отсутствие кнопки, ни закрытая история не были бы достижимы (у администратора wildcard). Исправление ограничено СОБСТВЕННОЙ записью: §19.21 требует для чужой ещё organization scope, event scope и срок полномочия — их нет, и отказ назван вслух в §35-блоке, а не заменён правом «править всё». | §19.18/§19.21/§19.22, Этап 58 |

| A125 | **Карточка — отдельный запрос, потому что §19.18 требует ПЕРЕЗАГРУЗИТЬ редакцию (шаг 3).** Строка из списка заданий для этого не годится: она прочитана раньше и могла устареть, а мутация несёт `revision`. Побочно это исполняет §19.17 «не загружай sensitive detail заранее в общий list endpoint»: закрытые поля едут только тогда, когда карточку открыли, и это проверяется счётчиком запросов, а не намерением. Diff (шаг 8) стоит МЕЖДУ формой и мутацией: «Показать изменения» не отправляет ничего, отправляет только «Подтвердить». Пустой diff запрещает подтверждение — исправлять нечего, и это ответ, а не пустой экран. | §19.17/§19.18, Этап 58 |

| A126 | **Реестр §19.15 живёт в aggregate-only представлении, и это не урезанная версия, а единственная честная.** §19.16 описывает для держателя права на агрегат ровно такой экран: score, комментарий, основание и оценщик скрыты, показан разрешённый агрегат, на месте закрытых величин — подпись «Детали оценки закрыты». Sensitive-ветка НЕ реализована и права под неё не заведены: §19.21 требует к ней organization scope, event scope и срок полномочия одновременно (то же основание, что у отказа исправлять чужую запись, A124). Закрытые поля не спрятаны — их нет в ответе, и это закреплено ассертом по всему JSON, включая проверку, что значения конкретных сеяных оценок в нём не встречаются. | §19.15/§19.16/§19.21, Этап 59 |

| A127 | **URL — единственный источник состояния отбора.** §19.15 требует хранить фильтры в search params и восстанавливать их после возврата из detail; локальной копии фильтров на экране нет вовсе, поэтому «рассинхрон URL и формы» структурно невозможен. Ссылка в карточку несёт текущий запрос параметром `back`, и возврат ведёт на него же — восстановление не зависит от истории браузера и переживает прямую ссылку. Любая правка фильтра сбрасывает страницу: третья страница прежнего отбора к новому отношения не имеет и показала бы пустоту как «ничего не найдено». | §19.15, Этап 59 |

| A128 | **Перечень значений фильтров даёт СЕРВЕР, а не текущая страница.** §19.15 «autocomplete не должен раскрывать сотрудников вне scope» — собрав список из полученных строк, экран показал бы только тех, кто уже виден, и это выглядело бы как полный перечень. Тест на это устроен так, что подмена заметна: отбор оставляет одну запись, а список сотрудников остаётся полным. Отбор при этом выполняет сервер — фильтровать в браузере значило бы сначала привезти туда строки вне разрешённого периода и подразделения. | §19.15, Этап 59 |

| A129 | **Безопасный контекст — роль и способ появления записи, а не человек.** §19.16 запрещает показывать «кто оценил» и требует, чтобы контекст не позволял вычислить личность оценщика. Системная оценка названа отдельно (§19.8: оценщика у неё нет, роль «старший» приписала бы ей человека), исправленная — «Исправление уполномоченным пользователем». Признак исправления берётся из correction chain, а не из сравнения старого и нового значения (§19.11), поэтому в реестре видны ОБЕ записи пары — вытесненная и замещающая. | §19.16/§19.11, Этап 59 |

| A130 | **Конфликт версии оценки ведётся СВОЕЙ панелью, а не общим `ConflictDialog`.** §19.25 запрещает это прямо, и запрет содержательный: диалог назначений предлагает «повторить с обоснованием», то есть РАЗРЕШИТЬ конфликт override'ом, — а конфликт редакции разрешать нечем, запись уже изменилась. Поэтому код `EVALUATION_REVISION_MISMATCH` намеренно не входит в `OVERRIDABLE_CODES` (иначе диалог открылся бы сам), ответ едет 409 и несёт актуальную редакцию с текущими значениями, а экран показывает diff «сейчас / вы вводите» РЯДОМ с формой. Введённый текст не стирается — §19.25 «комментарий сохраняется локально для ручного восстановления»; панель ничего не сбрасывает, и это проверено повторным входом в правку. | §19.25, Этап 60 |

| A131 | **«Уже исправлена» — отдельный конфликт от «редакция устарела».** §19.25 перечисляет их разными пунктами, и они действительно про разное: редакция могла совпасть, а запись — уже быть вытесненной; тогда человек правит не ту строку, и сказать это надо прямо, а не общим «обновите страницу». | §19.25, Этап 60 |

| A132 | **Ключ идемпотентности живёт ОДНУ ФОРМУ и переживает её повторные отправки.** §19.26 требует, чтобы повтор не создавал второй оценки, второго audit и не пересчитывал агрегат как новую операцию. Новый ключ на каждую попытку сделал бы защиту бессмысленной ровно в том случае, ради которого она заведена, — поэтому ключ рождается вместе с формой (`useState(newIdempotencyKey)`), и это закреплено тестом: первая попытка теряет ответ, вторая уходит с ТЕМ ЖЕ ключом. Сервер хранит по ключу идентификаторы результата, а не снимок ответа: снимок нёс бы закрытый комментарий целиком (проверено ассертом по всему слайсу ключей). Ключ случайный — собранный из полей формы, он вёз бы значение оценки и комментарий в каждом заголовке и журнале. | §19.26, Этап 60 |

| A133 | **Неизвестный исход называется неизвестным.** §19.26 запрещает при таймауте показывать «Оценка сохранена», менять состояние на «Отправлено», создавать локальный `evaluationId`, обновлять агрегат и автоматически повторять мутацию. Ничего этого не происходит: сетевой сбой рисует отдельную панель «Исход неизвестен» с предложением проверить состояние и с прямым указанием, что повтор из этой формы безопасен (тот же ключ). Авто-ретраев нет и не было — `retry: false` стоит у мутаций с 8.5. | §19.26, Этап 60 |

| A134 | **Журнал оценивания живёт в СВОЁМ разделе со своим правом, а не в общем `/audit`.** §19.22 перечисляет «просмотр rating audit» отдельным пунктом, а §19.27 прямо предупреждает: «обычный пользователь не должен раскрывать закрытые оценки через общий экран аудита». Общий журнал читают люди без права на рейтинг — сложив события туда, мы раздали бы их вместе с ним. Право выдано аналитику (роль контролёра: видит действия, но не оценивает и не исправляет) и НЕ выдано организатору ОМ: контроль над собственными действиями не является их частью. | §19.22/§19.27, Этап 61 |

| A135 | **Запись журнала по построению не может нести значение оценки.** §19.27 отдаёт old/new score, комментарии и оценщика отдельной audit privacy permission, а §19.21 требует к ней тот же scope и срок полномочия, что и к sensitive-просмотру, — их нет, поэтому и полей нет: `auditEntry()` принимает только ссылки (`securityEventId`, `eventRunId`, `assignmentId`, `evaluationId`, `correctionId`, `requestId`, `revision`) и код события. Это дешевле, чем помнить про фильтрацию на каждом вызове, и проверено ассертом по всему JSON. `requestId` — ключ идемпотентности §19.26: одна и та же операция опознаётся в журнале и в защите от повтора одинаково. | §19.27/§19.21, Этап 61 |

| A136 | **Отказ пишется СВОЕЙ транзакцией — иначе он исчезает вместе с откатом.** §19.27 требует аудировать попытку снижения без комментария, отклонённое исправление и запрещённую попытку просмотра. Все три — случаи, когда состояние НЕ меняется: запись, сделанная внутри отклонённой мутации, откатилась бы вместе с ней, и журнал помнил бы только удачные действия, то есть не отвечал бы на главный вопрос контроля. Успех, наоборот, пишется в ТОЙ ЖЕ транзакции, что и коммит (§19.27 «success audit создаётся только после commit»): отдельная запись успеха могла бы пережить неудавшийся коммит. | §19.27, Этап 61 |

| A137 | **«Изменение относительно начального» — отдельное событие, а не вывод из значения.** §19.27 перечисляет его самостоятельным пунктом: отправка канонической восьмёрки и снижение до четвёрки — разные факты, и общая запись «оценка отправлена» их не различает. Событие пишется рядом с основным, а не вместо него, поэтому по журналу видно и что отправка была, и что значение отличалось. | §19.27, Этап 61 |

| A138 | **Уведомления раздела живут в СВОЁМ слайсе, а не в общей ленте `shared/notifications`.** Та читает контракт донорского бэкенда (GET-only, свои типы `Notification`), и класть в неё события pending-контракта значило бы выдать их за существующие на сервере. Отдельного права под чтение не заведено: уведомление адресовано человеку, отбор по адресату СЕРВЕРОМ и есть право его прочитать — чужие в ответ не попадают, поэтому «право видеть чужие» нечему охранять. | §19.28, Этап 62 |

| A139 | **В записи уведомления нет текста — только код.** §19.28 перечисляет допустимые формулировки и запрещает нести ФИО оценщика, персональный score, закрытый комментарий и причину снижения. Готовая строка однажды была бы собрана из закрытых полей и не замечена; код подставляется в ФИКСИРОВАННУЮ формулировку экрана, в которой подстановок нет вовсе. Проверено ассертом по всему JSON (нет ни значений, ни самой строки) и красной пробой «формулировка собирается вольно». | §19.28, Этап 62 |

| A140 | **Уведомление создаётся в ТОЙ ЖЕ транзакции, что и коммит** (§19.28 «уведомления создаются только после commit») — тот же приём, что у success-audit §19.27: отдельная запись могла бы пережить неудавшийся коммит и сообщить о том, чего не произошло. Отклонённая отправка уведомления не создаёт, и это проверено отдельно, а не подразумевается. Deep link ведёт на МАРШРУТ, который перепроверяет права: получатель без права видит отказ, а не содержимое. | §19.28, Этап 62 |

| A141 | **Два уведомления из списка §19.28 не заведены — с причиной.** «Итоговая сводка оперативного рейтинга обновлена» не имеет адресатов: перечня людей с правом на агрегат в сборке нет (права резолвятся для запрашивающего, а не перечисляются), а разослать «всем» значило бы придумать список. Уведомления оцениваемому сотруднику недоставимы: связи persona↔сотрудник в demo-режиме нет (§8.9), и отправить их оценщику вместо участника значило бы адресовать чужое сообщение. Оба названы на самом экране, а не опущены молча. | §19.28/§35, Этап 62 |
| A142 | **Индивидуальная (sensitive) выгрузка §19.29 НЕ выдаётся — и права под неё не заведено.** §19.29 требует под неё отдельного permission и audit; §19.21 требует к тем же данным organization scope, event scope и срок полномочия, которых в сборке нет. Завести право было бы хуже, чем отказать: у эталонного администратора wildcard, и «отдельное право» немедленно оказалось бы у того, кому никакого scope не выдавали. Режим отклоняется СЕРВЕРОМ по коду `SENSITIVE_EXPORT_UNAVAILABLE` (проверено на wildcard-персоне), причина названа на экране, отказ ложится в журнал. | §19.29/§19.21/§35, Этап 63 |
| A143 | **XLSX и PDF §19.29 не предлагаются: генератора нет.** Тот же вывод, что §22.23 у отчётного реестра — формат без реализации обещает файл. Собирать XLSX в браузере из полученных строк значило бы перестать быть серверной операцией с правом и audit; выдать окно печати за PDF-выгрузку — назвать файлом то, что файлом не является. Доступен CSV, оба отсутствующих формата названы в `unavailableFormats` с причиной. | §19.29/§22.23/§35, Этап 63 |
| A144 | **Файл собирается ИЗ СВОДКИ, а не фильтрацией закрытых полей.** §19.29 перечисляет запрещённое списком (score, оценщики, комментарии, sensitive documents, персональные основания, скрытые correction details) — источником сборки служит `OperationalRatingSummary`, где таких полей нет вовсе. Фильтр можно забыть применить; отсутствующее поле забыть нельзя. Проверено ассертом по ВСЕМУ ответу выдачи вместе с содержимым и красной пробой (комментарий подставлен в строку файла). Отсутствие агрегата печатается состоянием и пустой клеткой — не нулём (§19.19), и в файле это важнее, чем на экране: таблица живёт дольше. | §19.29/§19.19, Этап 63 |
| A145 | **Ссылки на файл в работе нет, а скачивание — отдельная операция.** §19.29 «Файл считается готовым только после ответа repository» и «Не добавляй фиктивную ссылку на файл»: `artifactId` появляется ровно на переходе в `READY`, выдача перепроверяет право, владельца и СОСТОЯНИЕ работы заново. Экран не выводит право на выдачу из наличия метаданных — проверено ответом, которого сервер не создаёт (артефакт при отменённой работе). | §19.29, Этап 63 |

| A146 | **`post.min_rating` живёт у поста РАСЧЁТА мероприятия, а не в паспорте объекта.** §19.24 называет требование свойством `post` — в расстановке это `ReconSectorPost`; паспорт объекта требования не несёт, и импорт из паспорта честно приходит без него. Флаг `ratingConflicts` слайса рейтинга ВКЛЮЧЁН только теперь: до §19.24 проверки не существовало, и включённый флаг обещал бы то, чего нет (§35). Идентификатор человека в ростере расстановки и в оцениваемом наборе теперь ОБЩИЙ для тех, кто в demo — один человек: без общего ключа сводка не находилась бы ни для кого, а мягкое предупреждение было бы недостижимо живьём. | §19.24/§19.3, Этап 64 |
| A147 | **Несоответствие рейтинга — 409 `SOFT_CONFLICT_DETECTED` через ОБЩИЙ протокол обхода, отсутствие данных — своё предупреждение `RATING_DATA_MISSING`.** §19.24 «рейтинг не блокирует назначение автоматически… используй существующий workflow override» — свой код не заведён намеренно: он не входил бы в `OVERRIDABLE_CODES`, и путь `useApiMutation.conflict` → общий `ConflictDialog` → повтор с `override/override_reason` В КОРНЕ тела не включился бы (код `RATING_DATA_MISSING` заранее закреплён в `docs/registries/error-codes.yaml` как расширение BR-RATING-CONFLICT-002). Жёсткое правило первее мягкого: у отказа один владелец, и «сохранить можно с обоснованием» при double-assignment было бы ложью (закреплено тестом без override). Обоснование сохраняется ТОЛЬКО при реально возникшем предупреждении. | §19.24/§19.3, Этап 64 |
| A148 | **Сводка рейтинга назначенных — отдельный ресурс, значения вырезает СЕРВЕР по `ops.rating.view_aggregate`, а соответствие остаётся видимым.** Право на вход — право расстановки (строки с постом и соответствием — часть её самой); своё право не заведено: операция та же, что чтение агрегата, второе право раздавало бы тот же доступ дважды (§19.22). Без права на агрегат `null` едут ВСЕ поля данных, включая СОСТОЯНИЕ («оценок недостаточно» — тоже сведение о рейтинге человека). Источник сводки — записанные точки динамики §19.20 (последний закрытый период), а не пересчёт из оценок: второй расчёт разошёлся бы с репозиторием рейтинга молча, и проекция не читает поле `evaluations` вовсе. В печатную расстановку ни `minRating`, ни `ratingOverrideReason` не попадают — их нет в модели документа, закреплено ассертом по всему JSON. | §19.24/§19.21/§19.20, Этап 64 |
| A149 | **Канонические формулировки §19.31 стоят ДОСЛОВНО и не вытесняют точную причину.** «У вас нет участников для оценивания по этому мероприятию.» (пустая очередь заданий), «По выбранным условиям оценки не найдены.» (пустой отбор реестра, с уточнением «результат отбора, а не отсутствие оценивания»), «Оценок пока недостаточно…» / «Итоговый рейтинг пока не сформирован.» (карточка агрегата: предложение говорит, ЧТО отсутствует, строка «Состояние» — ПОЧЕМУ, §19.30 различает причины точно). Выключенной функции канон «пока не сформирован» не выдаётся — он обещал бы расчёт, которого при выключенном флаге не будет (§19.3 держит свой текст). | §19.31/§19.30, Этап 65 |
| A150 | **Замок §19.23 стоит на ПОЗДНЕЙ ОЦЕНКЕ, а не на исправлении.** Отправка новой оценки по закрытому мероприятию отвергается `EVALUATION_ARCHIVE_LOCKED` (поздняя оценка оформлялась бы «разрешённым дополнением», которого в контракте нет), отказ ложится в журнал своей транзакцией. Исправление после закрытия ПРОХОДИТ — сценарий §19.34 требует ровно этого: исходная запись не переписывается, создаётся связанное замещение, архивный снимок не меняется по построению. Мероприятие ВНЕ реестра ОМ (исторические задания demo-сида) замка не получает: закрытым может быть только то, что существует, — отказ по отсутствию заблокировал бы всё оценивание сида. Стадию читает узкая проекция чужого слайса (`ratings/mocks/securityEventsSlice.ts`), единственный вопрос которой — «закрыто ли»; показ «незаполненные не блокируют закрытие» на экране закрытия ОМ — отдельный срез. | §19.23/§19.34, Этап 66 |
| A151 | **«Оценивается фактический участник» — серверный гард, а не свойство вёрстки.** Ревизия перечня §19.33 нашла: задание с `participated: false` показывалось (§19.9 требует показывать факт участия), но оценку ПРИНИМАЛО. Теперь отправка отвергается 422 `PARTICIPATION_NOT_CONFIRMED` с канонической формулировкой §19.30 «Фактическое участие сотрудника не подтверждено.» — до правил формы. «Заменённый до заступления не получает итоговую оценку» демонстрировать не на ком (снимка замен в данных мероприятий сида нет) — отказ назван на экране (`REPLACED_BEFORE_START` в §35-блоке), правило действует косвенно через тот же гард. Связь оценки с ЕЁ заданием закреплена тестом из БД: ссылку на созданную запись получает ровно одно задание. | §19.33/§19.30/§19.35, Этап 67 |
| A152 | **Экраны оценивания добавлены в axe-аудит (§19.32) — их там не было ВОВСЕ.** Все семь маршрутов (`/ratings`, workspace, evaluations, карточка агрегата с сеяным id, audit, export, analytics) сканируются в общем цикле critical/serious. Побочная находка самого аудита: заголовок сводки «Рейтинг назначенных» (Этап 64) шёл `h3` после `h2` карточки ОМ — heading-order исправлен на `h2`. Урок прежний: новый раздел обязан попадать в перечень axe-цикла в ТОМ ЖЕ этапе, где появляется маршрут. | §19.32, Этап 68 |
