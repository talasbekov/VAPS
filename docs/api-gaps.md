# Пробелы API: экраны без бэкенда

Снято на живом стенде: целевой бэк `Backend/PersonnelStatus/Personnel-Records`
(Django, :8100), фронт `Backend/PersonnelStatus/PersonalRecordFront` (Next, :3106),
ветка `claude/smart-josparlau-e55`.

Источники истины:

* список маршрутов целевого бэка — резолвер Django (`get_resolver()`, 391 запись);
* список маршрутов донора — `Backend/VAPS/schema.yaml` (39 записей).

**Ни один путь с префиксом `/api/ops/` не существует ни в целевом бэке, ни в
доноре.** На стенде эти запросы отдаёт браузерный мок-слой MSW
(`mocks/ops/browser.ts`, включён по умолчанию: `lib/ops-env.ts` считает режим
мок-ом, пока не выставлен `NEXT_PUBLIC_OPS_DATA_SOURCE=api`). Из-за этого экраны
раздела выглядят рабочими — именно это и маскирует дыру.

## Как устроена заглушка

Один переиспользуемый компонент, одна точка врезки:

| Файл | Роль |
| --- | --- |
| `PersonalRecordFront/lib/api-gaps.ts` | реестр «маршрут → недостающие пути», поиск по самому длинному префиксу |
| `PersonalRecordFront/components/api-gap-notice.tsx` | единственная разметка пометки |
| `PersonalRecordFront/components/dashboard-layout.tsx` | врезка в `<main>` — общий корень контента хостовых экранов, `/security-ops/*` и `/ops` |

Пометка не скрывает содержимое и не ломает навигацию: она рисуется над контентом
экрана и прямо говорит, что данные ниже — мок.

## Экран → недостающий путь → что показывает заглушка

Текст пометки: **«Не подключено — ‹раздел›: на бэке нет ‹путь›»**, ниже —
перечисление остальных недостающих путей и приписка «Всё, что показано ниже,
отдаёт браузерный мок-слой MSW, а не сервер».

| Экран | Недостающие пути | Что показывает заглушка |
| --- | --- | --- |
| ~~`/feedback/` (хост)~~ | **переписан целиком (срез J)**: легаси-чат (`FeedbackChat`, опрос несуществующего `/api/dictionaries/feedback/`) удалён; адрес РЕНДЕРИТ новый модуль §28 поверх живого `/api/ops/feedback-requests/` — реализация одна, у страницы два входа (`/feedback` и `/security-ops/feedback`), ссылки внутри модуля строятся от текущего пути, пользователь остаётся в своём разделе | врезки нет (live: feedback) / «Демоданные — Обратная связь раздела ОМ» (mock) |
| ~~`/ops/*` (встроенная SPA)~~ | **выведена из навигации**: SPA дублировала переписанные нативные страницы `/security-ops/*` (живой `/api/ops/*`); группа `/ops/*` снята из сайдбара, адреса редиректят на нативные экраны и живые хостовые (`/employees`, `/organization`, `/reports`, `/feedback`). Копия `josparlau/` и sync-инфраструктура демонтированы; Vite-источник `frontend/` остаётся | врезки нет: экранов SPA в навигации больше нет |
| ~~`/security-ops/command-center/`, `/security-ops/events/`, `…/events/[id]/`~~ | **закрыт срезом B1**: `/api/ops/security-events/` (реестр с фильтрами, карточка, создание с привязкой версии паспорта, все девять стадий жизненного цикла) + `/api/ops/personnel/` (кадровый снимок из живых Employee). Экраны проведены `NEXT_PUBLIC_OPS_LIVE_DOMAINS=security-events` | врезки нет (live) / «Демоданные — Охранные мероприятия» (mock) |
| ~~`/security-ops/objects/`, `/security-ops/objects/[id]/`~~ | **закрыт срезом A2**: конверт списка (kpi/freshness/политика), `PATCH …/passport/`, `POST …/passport/versions/` — всё живое. Экран проведён пер-доменным переключателем `NEXT_PUBLIC_OPS_LIVE_DOMAINS=objects`; в mock-режиме врезка честно говорит «бэк готов, экран на MSW по конфигурации» | врезки нет (live) / «Демоданные — Объекты и паспорта» (mock) |
| ~~`/security-ops/duties/`, `/security-ops/duties/[id]/`~~ | **закрыт срезом C1**: виды+политика, месячный план (черновик/проверка/утверждение/новая редакция, отпечаток состава, конфликты и action-policy по RBAC), смены с циклом PLANNED→…→COMPLETED и отменой, объекты и кандидаты формы. Проведён `NEXT_PUBLIC_OPS_LIVE_DOMAINS=duties` | врезки нет (live) / «Демоданные — План дежурств» (mock) |
| ~~`/security-ops/duties/combat/`~~ | **закрыт срезом C2**: реестры видов и Трасс, кандидаты из живых кадров, смены с процессом §24.1 целиком (потребность → подача → рассмотрение → ознакомление → заступление → сдача смены → факт; замена до заступления; DOUBLE_ASSIGNMENT на дату). Проведён `NEXT_PUBLIC_OPS_LIVE_DOMAINS=combat` | врезки нет (live) / «Демоданные — Боевые группы» (mock) |
| ~~`/security-ops/calendar/`~~ | **закрыт срезами C1+C2**: оба источника (duty-shifts и combat-duty-shifts) живые | врезки нет (live: duties,combat) / «Демоданные — Календарь смен» (mock) |
| ~~`/security-ops/daily-expense/`~~ | **закрыт срезом L-адаптеров**: `/api/ops/daily/*` — ТОНКИЕ адаптеры над живым `/api/operations/` (bulk-статусы с атомарным построчным отказом, сдача дня с окном/версиями/поправкой — те же сервисы, свой только адрес и форма контракта: строковые id, все версии дня, читаемая подпись сдавшего). Своего бэка нет намеренно (группа L плана «не строить — дубликат»). Проведён `NEXT_PUBLIC_OPS_LIVE_DOMAINS=daily` | врезки нет (live: daily) / «Демоданные — Расход дня (ОМ)» (mock) |
| ~~`/security-ops/ratings/*` (все семь экранов)~~ | **закрыт срезом G**: сводка/динамика/карточка (агрегат считает сервер, вытесненные исключены), рабочее пространство с отправкой и исправлением (идемпотентность, конфликт редакций 409, замок закрытого ОМ), реестр без закрытых полей, аналитика §22.16 с подавлением малых групп, свой журнал §19.27, уведомления, экспорт CSV с журналом выдачи. Проведён `NEXT_PUBLIC_OPS_LIVE_DOMAINS=ratings` | врезки нет (live: ratings) / «Демоданные — Оперативный рейтинг» (mock) |
| ~~`/security-ops/analytics/`, `…/operations/`~~ | **закрыт срезом H**: снимок KPI с детерминированным snapshotId (выборка = число), drill-down со своим правом и отказом SNAPSHOT_OUTDATED, блок «Требует внимания» по политике из «Настроек», нагрузка §22.9 (план/факт порознь, LOAD_POLICY), уровни §22.15 и воронка §22.14 по НОВОМУ журналу переходов ОМ (пишется операциями стадий с этого среза). Проведён `NEXT_PUBLIC_OPS_LIVE_DOMAINS=analytics` | врезки нет (live: analytics) |
| ~~`/security-ops/service-reports/`, `…/history/`, `…/[reportJobId]/`~~ | **закрыт срезом I**: каталог типов с пределами из REPORT_LIMITS, асинхронная генерация (PENDING→PROCESSING→COMPLETED\|FAILED на чтении), immutable-артефакт с ревизиями серии и замороженным сроком хранения, server-side masking (обычный экспорт без sensitive-колонок ВООБЩЕ), retry с переиспользованием пригодного артефакта / new-revision, скачивание с повторной проверкой права/sensitive/владельца/срока. Проведён `NEXT_PUBLIC_OPS_LIVE_DOMAINS=service-reports` | врезки нет (live: service-reports) |
| ~~`/security-ops/dictionaries/`, `…/[code]/`~~ | **закрыт срезом D1**: реестры с честными связями (TRACKED поимённо по живым журналам ОМ и groupCode; NOT_TRACKED — с причиной), заведение/деактивация/удаление с доказательством отсутствия связей | врезки нет (live: dictionaries) |
| ~~`/security-ops/settings/`~~ | **закрыт срезом D1**: настройки — владелец политик, правка пишется НАСКВОЗЬ в синглтоны свежести/конфликтов с версией раздела; журнал изменений с готовыми подписями; право правки решает сервер по-записно | врезки нет (live: settings) |
| ~~`/security-ops/audit/`~~ | **закрыт срезом D1**: read-only поверх ЖИВОГО журнала раздела (те же строки, что пишут срезы A2-C2) | врезки нет (live: audit) |
| ~~`/security-ops/feedback/`, `/security-ops/feedback/[feedbackId]/`~~ | **закрыт срезом J**: реестр со сводкой по всему видимому набору и страницами (черновик не открывается никаким правом, содержание чужого конфиденциального вырезает сервер вместе с превью, поиск — только по видимым полям), создание с согласием на техинформацию и вложениями-метаданными, отправка черновика, карточка с единой лентой timeline+audit (внутренняя заметка и её событие скрываются целиком), разбор одной операцией по карте переходов, закрытие с обязательным ответом автору. Проведён `NEXT_PUBLIC_OPS_LIVE_DOMAINS=feedback` | врезки нет (live: feedback) / «Демоданные — Обратная связь раздела ОМ» (mock) |
| `/security-ops/gvo/`, `/security-ops/gvo/[id]/` | `/api/ops/gvo-summaries/` | **бэка нет вовсе.** Список мероприятий — ЖИВОЙ (реестр ОМ), база сводки выводится из бюллетеня на клиенте; в моке хранится только патч ручных правок по коду ОМ. Пер-доменного переключателя у раздела нет: переключать нечего, врезка стоит всегда |
| `/security-ops/persons/` | `/api/ops/protected-persons/` | **бэка нет вовсе.** Профили лиц отдаёт мок. Связь «лицо → мероприятия/объекты» в карточке настоящая: собирается по сводкам ГВО поверх живого реестра ОМ (совпадение ФИО), при отсутствии совпадений экран говорит об этом прямо |
| `/security-ops/laws/` | `/api/ops/legal-documents/` | **бэка нет вовсе.** Перечень документов отдаёт мок; ФАЙЛОВ документов система не хранит ни на одном источнике — вместо кнопок «Открыть»/«Скачать PDF» стоит надпись «Файл документа в системе не хранится» (поле `fileUrl`, у всех фикстур `null`) |
| `/security-ops/changelog/` | — | пометки нет намеренно: журнал изменений порта собран из статического текста, бэкенд ему не нужен |

Хостовые экраны `/dashboard/`, `/organization/`, `/employees/`, `/statuses/`,
`/reports/` пометки не получают: они ходят в живой бэк и данные отдают настоящие.

### Полный список путей `/api/ops/*`, объявленных во фронте

41 константа (`entities/*/model/*.ts`, `entities/*/index.ts`); ни одна не
резолвится ни целевым бэком, ни донором:

```
/api/ops/audit-logs/                      /api/ops/objects/
/api/ops/combat-duty-shifts/              /api/ops/operational-rating-dynamics/
/api/ops/combat-duty-types/               /api/ops/operational-rating-employee/
/api/ops/combat-roster-candidates/        /api/ops/operational-ratings/
/api/ops/combat-routes/                   /api/ops/operations-analytics/
/api/ops/daily/daily-submissions/         /api/ops/personnel/
/api/ops/daily/divisions/                 /api/ops/rating-analytics/
/api/ops/daily/employees/                 /api/ops/rating-audit/
/api/ops/daily/statuses-bulk/             /api/ops/rating-export-artifacts/
/api/ops/dictionaries/                    /api/ops/rating-exports/
/api/ops/duty-candidates/                 /api/ops/rating-notifications/
/api/ops/duty-monthly-plan/               /api/ops/security-events/
/api/ops/duty-plan-objects/               /api/ops/service-analytics/
/api/ops/duty-shifts/                     /api/ops/service-analytics-attention/
/api/ops/duty-types/                      /api/ops/service-analytics-drilldown/
/api/ops/evaluation-registry/             /api/ops/service-analytics-presets/
/api/ops/evaluation-work-items/           /api/ops/service-report-artifacts/
/api/ops/evaluation-workspace/            /api/ops/service-report-jobs/
/api/ops/feedback-requests/               /api/ops/service-report-types/
/api/ops/load-analytics/                  /api/ops/setting-changes/
                                          /api/ops/settings/
```

Вне семейства `/api/ops/` недостающих путей не осталось: `/api/dictionaries/feedback/` умер вместе с легаси-чатом `/feedback/` (срез J).

## Кандидаты на следующие срезы переезда

Эти пути **есть в доноре** `Backend/VAPS/schema.yaml`, но ещё не переехали в
целевой бэк. Это не «заглушка навсегда», а очередь переезда. Вычеркнутые
строки переехали — срезы 157–159; во всех трёх перенесён КОНТРАКТ, а не
модель, и все три только для чтения.

| Путь донора | Состояние в целевом бэке |
| --- | --- |
| ~~`/api/core/staffing-slots/`~~ | **переехал, срез 157** — поверх `staff_unit.StaffUnit`, только чтение |
| ~~`/api/core/staffing-slots/{id}/`~~ | **переехал, срез 157** (тот же ReadOnly-роутер) |
| `/api/core/staffing-slots/{id}/assign-employee/` | нет — экшен пишущий, правка штата живёт на старой стороне |
| `/api/core/staffing-slots/{id}/release/` | нет — экшен пишущий, там же |
| ~~`/api/core/vacancies/`~~ | **переехал, срез 158** — свободные слоты (без сотрудника), строка та же, что у staffing-slots; параметр `division_id` есть, `date` не заведён (у старой схемы нет временных границ) |
| `/api/core/employees/{id}/archive/` | нет |
| `/api/core/employees/{id}/restore/` | нет |
| `/api/core/divisions/{id}/leaf-descendants/` | нет |
| ~~`/api/documents/attachments/`~~ | **переехал, срез 159** — списочный GET поверх `operations.OpsAttachment`, область по выпуску-владельцу. ВНИМАНИЕ: у донора по этому адресу только POST (загрузка); переехал ряд полей его проекции Attachment, сам список заведён заново. Загрузка НЕ переехала |
| `/api/documents/attachments/{id}/download/` | нет (у цели — `/api/operations/attachments/{id}/download/`, другой префикс; `id` в списке среза 159 — именно тот, что принимает этот маршрут) |
| `/api/operations/expense-reports/` | переименовано у цели в `/api/operations/strength-report/` — сверить контракт перед переездом |
| `/api/operations/expense-reports/period/` | переименовано у цели в `/api/operations/strength-report/period/` |
| `/api/operations/expense-reports/override-tomorrow-block/` | у цели `/api/operations/tomorrow-block/override/` — другой путь, тот же смысл |

Остальные 26 путей донора у цели уже есть (`/api/core/*`, `/api/audit/logs/`,
`/api/notifications/`, `/api/operations/{daily-submissions,my-permissions,
permissions,roles,statuses/bulk,temporary-duty,traffic-light/tree,user-roles}`).

## Найденные и починенные дефекты (для контекста)

1. **`/organization/`** — схема штатных единиц. `getStaffUnits` в `lib/api.ts` не
   понимал конверт DRF-пагинации `{count,next,previous,results}` и плоскую строку
   списка (`position`/`employee` в корне вместо массива `employees`), молча
   возвращал `[]`, и экран писал «Данные не загружены из API» при HTTP 200.
   Починен адаптер формы на фронте — контракт бэка не трогали.
2. **`/feedback/`** — бесконечный опрос 404. `FeedbackChat` глушил ошибку в
   `console.error` и продолжал опрос раз в 3 секунды; экран показывал «Пока нет
   сообщений», выдавая отсутствующий бэк за пустой список. Опрос теперь
   останавливается на первой ошибке, ошибка выводится на экран.

### Обход стенда Playwright 11.08.2026 (коммит `00a7c37f`)

Полный клик-обход по всем кнопкам, 80 страниц. Четыре дефекта — все 404:

3. **`/statuses`** — маршрут `/api/secondments/` был выключен в корневом
   urlconf с самого импорта донора: вьюхи ходили в поля кастомного пользователя
   (`user.role`, `user.division`), которых у целевой модели нет. Область
   портирована на цепочку `User → Employee → StaffUnit → Division`, сериализатор
   отдаёт `employee_detail`/`division_detail` в форме, которую ждёт фронт;
   роут включён.
4. **Карточка отчёта** — `/api/ops/service-report-jobs/{id}/detail/`. Фронт
   зовёт **только** `/detail/`, ручки не было. Заведён алиас `retrieve` по
   контракту мока — контракт фронта не трогали.
5. **Уведомления рейтинга** — `deep_link` вёл на `/ratings/workspace` (адрес
   выведенной SPA-встройки). Генератор и сид переведены на нативный
   `/security-ops/ratings/workspace`; стендовые строки поправлены данными.
6. **Сайдбар «Настройки»** — `/settings` был 404. Страница рендерит модуль
   настроек политик по приёму `/feedback`: одна реализация, два входа.

**Не дефекты** (валидация/домен, а не дыра): `DAY_NOT_SUBMITTED` на экспорте
несданного дня; 400/422 на пустых формах диалогов.

7. **Hydration mismatch на `/dashboard`** (коммит `8179e037`) — три `new Date()`
   прямо в JSX (шапка «Последнее обновление», бейдж «Синхронизация»,
   «Последняя проверка») расходились SSR/клиент на границе секунды: 2 из 8
   жёстких перезагрузок падали. Время ставится в `useEffect`, до маунта — «…».

**`CLIENT_FETCH_ERROR` next-auth — не чинится:** `net::ERR_ABORTED` на
`/api/auth/session`, когда жёсткая навигация обрывает in-flight фетчи страницы
логина. Библиотечный шум next-auth v4, сессия поднимается штатно.

### Живость раздела ОМ держалась на untracked-файле (коммит `907f9c99`, 11.08.2026)

8. **Раздел `/security-ops/*` молча уезжал в мок на любом чекауте без
   `.env.local`.** Пер-доменный переключатель в `lib/ops-env.ts` был
   инвертирован: домен считался живым, только если его имя перечислено в
   `NEXT_PUBLIC_OPS_LIVE_DOMAINS`, а список жил в untracked `.env.local`.
   Чистый клон, tarball и внешний агент получали мок — и не могли этого
   заметить: фикстуры несут те же названия ОМ, что и сид БД (отличие —
   лишняя строка `ОМ-2026-3`). Теперь домен живой по умолчанию, на мок
   возвращает явный `NEXT_PUBLIC_OPS_MOCK_DOMAINS`.

   `isOpsMockMode()` намеренно не тронут — он про транспорт (host-MSW и
   подмена WebSocket), а не про данные. Живой сокет висел бы на
   `${location.host}/ws/notifications/`, то есть на Next-хосте, который WS не
   проксирует; колокольчик остаётся на мок-сокете. На моке осознанно остался
   и `*/api/operations/my-permissions/` (`identityHandlers`), хотя ручка на
   живом бэке есть.

**Ловушка проверки:** `onUnhandledRequest: "bypass"` означает, что промах пути
уходит в сеть без ошибки — «данные показались» не доказывает живость.
Проверять мутацией БД. И не поднимать вторую копию `next dev` в том же
чекауте для сравнения режимов: серверы делят `.next/`, `NEXT_PUBLIC_*`
инлайнятся при компиляции, и проба переписывает поведение основного стенда.

### Права раздела ОМ рисовал мок — RBAC не работал (11.08.2026)

9. **`identityHandlers` снят: `*/api/operations/my-permissions/` теперь живой.**
   Мок отдавал `{"permissions":["*"]}` ЛЮБОМУ посетителю, поэтому раздел
   `/security-ops/*` на каждой учётке выглядел администраторским, а гейты
   страниц (`!isLoading && !hasPermission(...)`) не срабатывали никогда.

10. **Коды прав фронта не существовали на бэке.** Страницы спрашивали
    `ops.object.view` / `ops.duty.view` / `ops.security_event.view`, справочник
    RBAC знает `object.view` / `duty.view` / `event.view` (префикса `ops.` нет
    ни в одном коде). Под мок-wildcard-ом расхождение было невидимо: `*`
    проходит любую проверку. Со снятым моком раздел закрылся бы у ВСЕХ, кроме
    ADMIN. Коды приведены к бэку.

11. **Запрос прав отключался без host-логина.** `enabled: isOpsMockMode() ||
    user !== null`: `/security-ops/*` не закрыт middleware (его нет в
    `matcher`), а выключенный запрос навсегда оставляет `isLoading=true` —
    гейты не срабатывают и раздел открывается анониму. Запрос уходит всегда;
    без токена бэк отвечает 403 → прав нет → гейт закрыт (проверено).

12. **Календарь смен звал реестр ОМ без права.** `useSecurityEvents` не
    принимал `enabled`, и персона без `event.view` получала 403 на каждый
    рендер (баннер «Мероприятия скрыты» при этом рисовался верно).

**Проверка на трёх учётках** (стенд :8100/:3106, ручка отвечает с бэка —
видно в access-логе Django):

| Учётка | `my-permissions` | Объекты | Дежурства | Реестр ОМ | Командный центр | Календарь |
|---|---|---|---|---|---|---|
| `admin` (ADMIN) | `["*"]` | открыт | открыт | открыт | открыт | полный |
| `observer` (`OPS_READER`) | `["duty.view","object.view"]` | открыт | открыт | **закрыт** | **закрыт** | только дежурства + баннер |
| `erda` (DIVISION_OPERATOR) | 7 кодов, ни одного ОМ-чтения | **закрыт** | **закрыт** | **закрыт** | **закрыт** | пуст |
| аноним | 403 | **закрыт** | — | — | — | — |

**Роль `OPS_READER` внесена в сид** (`seed_operations`, единственное
отступление от дословного порта раскладки — помечено на месте). Раскладка
намеренно неполная — `object.view` + `duty.view`, БЕЗ `event.view`: это
единственная сеяная персона, на которой видно, что гейты раздела вообще
работают. У ADMIN «*» проходит любую проверку, у прочих ролей не проходит ни
одна — обе крайности одинаково зелены и при сломанном RBAC (так и было под
мок-wildcard-ом). Закреплено тестом
`test_the_ops_reader_holds_two_registers_and_not_the_third` (красная проба:
добавление `event.view` в раскладку роняет тест).

**ОТКРЫТОЕ (решение за владельцем продукта):** портированные роли по-прежнему
не несут чтения раздела ОМ — держатель `object.view`/`duty.view` теперь один
(`OPS_READER`), `event.view` не достался никому напрямую. Кому в проде
полагается видеть реестр ОМ, план дежурств и объекты — продуктовое решение, не
догадка разработчика.

### Два предупреждения сюиты — одно из них был латентный дефект (12.08.2026)

13. **Список отчётов пагинировался без порядка.** `Report.Meta` не задавал
    `ordering`, а `/api/reports/` отдаётся пагинатором DRF: раскладку по
    страницам выбирал планировщик, поэтому одна и та же запись могла прийти
    дважды или не прийти вовсе. Django предупреждал ровно об этом
    (`UnorderedObjectListWarning`), но предупреждение тонуло в сводке.
    Порядок задан МОДЕЛЬЮ — `['-created_at', '-id']`; `-id` обязателен
    вторым ключом: `created_at` это `auto_now_add`, и записи одной транзакции
    делят штамп до микросекунды. Пин литеральный (`_meta.ordering`) плюс
    проба tie-breaker-а на записях с принудительно равным `created_at` —
    поведенческая проба на паре записей вакуумна, планировщик и без ordering
    обычно отдаёт их в порядке вставки. Красная проба: `ordering = []`
    роняет и новый тест, и `test_list_visibility_scopes`.

14. **`CheckConstraint.check` → `condition`** (Django 6.0 убирает `check`) в
    `dictionaries/models.py` (`ck_position_code_not_blank`,
    `ck_rank_code_not_blank`) и в применённой миграции
    `0003_position_rank_code`. Переименование состояние не меняет —
    `makemigrations --check` не видит расхождений, лишней миграции нет.

Сюита после правок: **2995 passed, предупреждений НЕТ** (было 2994 и пять
предупреждений).

### Фоновый рефетч затирал открытую форму (коммит `53658a6d`, 14.08.2026)

Модалка правки статуса (`EditStatusDialog`) засевала поля с сервера в эффекте
с данными штатки в зависимостях. Данные там нужны — диалог открывается раньше
их загрузки, — но эффект прогонялся заново на КАЖДОМ обновлении запроса
`staff-units-by-directorate`: кнопка «Обновить», `invalidateQueries` после
чужого действия, массовая правка. Выбранные пользователем статус и даты молча
возвращались к серверному снимку.

Ловушка воспроизведения: React Query отдаёт ПРЕЖНЮЮ ссылку, если данные не
изменились (structural sharing), поэтому «просто нажать Обновить» дефект не
показывает — красная проба обязана менять данные на сервере. Проба: открыть
модалку, выбрать другой статус, завести сотруднику статус через API, нажать
«Обновить». С гардом форма держит введённое (таблица под модалкой при этом
обновляется — рефетч реально дошёл), без гарда перескакивает на серверный.

Лечение — засев один раз на открытие (`seededForRef` по `employeeId`, сброс
при закрытии). То же в `EditProfileDialog`: объект `user` пересоздаётся на
каждом обновлении сессии NextAuth, в том числе фоновом по возвращении фокуса
на вкладку.

### Тот же дефект в карточке ОМ — через ключ монтирования (коммит `7c560dc6`)

`app/security-ops/events/[id]/page.tsx` монтировал форму этапа с ключом
`${event.stage}-${event.updatedAt}`: любое изменение данных ОМ пересобирало
компонент и сбрасывало локальное состояние формы. Своя же мутация в соседней
панели (журнал штаба в «Проведении», импорт постов в «Рекогносцировке») или
чужая правка возвращали набранное к серверному снимку. Ключ теперь — этап.

Ловушка при проверке: повторный импорт постов идемпотентен («Все посты этой
версии паспорта уже в расчёте»), `updatedAt` не меняется — форма выживает и
на дырявом коде. Чтобы проба была не вакуумной, серверное состояние должно
реально измениться: сохранить рекогносцировку с нулём постов, потом
импортировать.

Побочное следствие: импорт на этой пересборке и держался — ответ мутации
теперь переносится в форму явно (`onEvent` у стадийных мутаций).

### Три модуля прототипа перенесены без бэка (14-15.08.2026)

Коммиты `28eb2588` (Реестр ГВО), `4a602ecb` (Охраняемые лица), `144aac67`
(Законы об ОМ). Все три экрана прототипа Smart Josparlau не имеют серверного
хранилища, и это первый случай в разделе, когда врезка `api-gaps` ставится
БЕЗ пер-доменного переключателя: `isDomainLive()` по умолчанию отдаёт «живой»,
и домен без бэка, заведённый по общему шаблону, молча ушёл бы в сеть за 404.

Что стоит знать при подключении бэка:

* **Сводка ГВО — проекция, а не запись.** Персистится только патч ручных
  правок по коду ОМ; база каждый раз выводится из бюллетеня. Статус
  «Заполнена» = патч непуст, пустой патч не хранится вовсе — иначе «Вернуть
  исходные» не вернул бы черновик. Удаление лица или группы это тоже правка:
  статус остаётся «Заполнена», пока раздел не сброшен явно.
* **Связь «охраняемое лицо → ОМ» держится на ФИО в сводке ГВО.** Ссылки на
  лицо у `SecurityEvent` нет; сводка — единственное место, где лицо и
  мероприятие названы рядом. Переименование лица в сводке рвёт связь в
  каталоге молча.
* **Ловушка мок-слоя.** Паттерны handlers обязаны начинаться с `*`
  (``http.get(`*${PATH}`)``): в dev клиент бьёт по абсолютному `BACKEND_URL`
  (:8100), относительный путь резолвится от origin документа (:3106), а при
  `onUnhandledRequest: "bypass"` промах не падает ошибкой — запрос молча
  уходит в сеть. Признак: GET «работает» (пустой список выглядит нормально),
  а мутация тихо не доезжает.

Пробы (нужен `SMOKE_LIVE=1` и поднятый стенд): `e2e/gvo-sections.spec.ts`,
`e2e/protected-persons.spec.ts`, `e2e/legal-documents.spec.ts`.

### Смоук-обход падал по таймауту на /employees и /statuses (коммит `6c8780e3`)

Падение по таймауту не оставляет отчёта вовсе — а обход к тому моменту уже всё
нашёл. Три причины: (1) раскрытый Radix Select внутри модалки не закрывался, и
весь остаток модалки отчитывался «не кликается» — 20 ложных обвинений по 5 с;
(2) элемент, унесённый предыдущим кликом обхода (переключатель «Карточки»
прячет таблицу), считался пропавшим без второго захода с чистого листа —
31 строка; (3) потолок обхода считал ЭЛЕМЕНТЫ, хотя цена элемента не
постоянна: клик, открывший модалку, тянет за собой до восьми кликов внутри.

Появился бюджет времени на страницу (`SMOKE_PAGE_BUDGET`, 170 с), лимит теста
считается ОТ него. Усечение печатается находкой («пройдено N из M»), а не
молчит. Итог: обе страницы проходятся ЦЕЛИКОМ за 2.2–2.4 мин, «не кликается» и
«элемент исчез» исчезли — все они были обвинением обхода, а не UI.

### «Сведения об ОМ» в бюллетене: четыре факта без источника (коммит `f72059d8`, 17.08.2026)

Справочный блок прототипа перенесён в этап «Бюллетень». Из тринадцати фактов
прототипа система знает девять; четыре не хранит ВООБЩЕ, и ячеек под них на
экране нет — вместо них строка о том, где эти сведения ведутся:

| Факт прототипа | Почему его нет |
| --- | --- |
| Охраняемые лица | у `SecurityEvent` нет ссылки на охраняемое лицо; связь «лицо → ОМ» держится на ФИО в сводке ГВО (мок) |
| Старший ГВО | того же происхождения: поле есть только в сводке ГВО (`sbChief`), у мероприятия — нет |
| Количество участников | численности у мероприятия нет ни в модели, ни в контракте (в самом прототипе это поле тоже «Не указано») |
| Количество охраняемых лиц | считается по списку лиц, которого нет |

Адрес мероприятие тоже не хранит — он берётся из КАРТОЧКИ ОБЪЕКТА отдельным
запросом под правом `object.view` (та же схема, что «Сведения об объекте» в
рекогносцировке). Появится у объекта адрес — блок покажет его сам; появится
у мероприятия своё поле места — понадобится решение, что из двух главнее.

Разбор дат переехал из сводки ГВО в `lib/ru-date.ts` (строкой и в UTC:
`new Date("2026-09-01")` в минусовых зонах даёт 31 августа). Продолжительность
ВЫВОДИТСЯ из пары дат включительно и третьим полем не хранится.

### «Ответственный за ОМ» был идентификатором учётки (коммит `b475a538`, 17.08.2026)

Обнаружено при переносе блока «Сведения об ОМ». `create_event` писал
`owner_name=resolve_actor_id(request)`, то есть в поле ложился id
пользователя: на стенде «Ответственный за ОМ: 1». То же значение уже стояло в
шапке карточки и в значениях фильтра реестра «ответственный» (`owners` сервер
собирает из этих же строк) — блок лишь назвал дефект вслух.

Подпись теперь общая с разделом обращений — `security_events.
actor_display_name`: ФИО из живой кадровой записи, иначе username учётки,
иначе сам идентификатор. Аудит по-прежнему хранит id: подпись для экрана,
идентификатор для журнала.

Данные переписаны миграцией `operations/0030`: подпись выводится только там,
где в поле ЧИСЛО и учётка с таким id ещё существует. На стенде 44 строки «1»
стали «Абенов С.» (учётка admin оказалась привязана к этой кадровой записи),
две сеяные строки «admin» остались как были. Обратной операции нет: id по
подписи не восстановить.

Ловушка на будущее: `Employee.user` заполнена не у всех учёток (сид её не
делает), поэтому username в этом поле — штатный исход, а не признак сбоя.

### Пробы раздела молча уходили в skip на растущем реестре (коммит `c20f379c`)

Три спеки искали фикстуру перебором ПЕРВОЙ страницы реестра
(`?page_size=50`). Каждый прогон сюиты заводит новые мероприятия; на
пятьдесят седьмом фикстуры стадий «Проведение», «Расстановка» и свёрнутых
уехали за границу страницы, и `test.skip('на стенде нет ОМ на стадии …')`
выключил 4 пробы из 19 — сюита осталась зелёной, сторожить перестала.

Стадию теперь фильтрует сервер (`?stage=`), состояние после операции читается
по id мероприятия. Тот же приём нужен любой новой пробе: искать фикстуру
запросом, а не перебором страницы, и проверять результат по адресу сущности.

### Командный центр: численность пришла из расхода (коммит `77f8ad38`, 17.08.2026)

Прошлый срез записал показатели прототипа «Личный состав» и «В строю» в «нет
источника». Источник есть и он живой — `/api/operations/strength-report/`
(право `status.view`, дата по Clock сервера, область видимости сужает выборку
сама). Плитки читают Σ `staff_total`, Σ `list_total` и колонку `IN_SERVICE`;
отсутствие права названо на плитке, ноль вместо числа не рисуется.

«Дефицит по ОМ» считается на клиенте из `forceRequests` уже загруженного
реестра — своего показателя у бэка нет, и заводить его ради одной плитки не
понадобилось: реестр всё равно грузится целиком для остальных счётчиков.

Чего у прототипа нет источника на этом экране:

| Блок прототипа | Почему не перенесён |
| --- | --- |
| Карта готовности подразделений | сдача дня по подразделениям — это светофор (`/api/operations/traffic-light/tree`), у него свой экран и своё дерево; вторая витрина разошлась бы с ним |
| График нагрузки за 14 дней | нагрузку считает аналитика §22.9 по методике из «Настроек»; нарисованная здесь вторая методика противоречила бы ей |
| «Требует решения» (запросы без ответа, не сдали расход) | «запрос без ответа» и «просрочен на N часов» требуют времени ОТПРАВКИ запроса сил — у `ForceRequest` его нет; «не сдали расход» живёт в светофоре |

Ловушка на будущее: колонки расхода задаёт СЕРВЕР (`report.columns`). Нет
колонки — плитка честно показывает прочерк; ноль там читался бы как «в строю
никого».

### Аналитика ОМ доведена до вида прототипа (17.08.2026)

Экран `/security-ops/analytics/operations` до этого показывал плоский список
состояний, воронку и таблицу «Детализация». Прототип на этом месте держит
семнадцать блоков, и прошлая оценка «источника нет у большей части» оказалась
завышенной: `/api/ops/operations-analytics/` (срез H, `78a9f915`) отдаёт
достаточно, чтобы собрать шапку показателей, структуру, воронку в двух
режимах, обеспеченность и таблицу объектов.

Правило переноса то же, что у командного центра: экран НЕ заводит своего
источника. Всё, что он делает поверх ответа, — складывает колонки строк ТОГО
ЖЕ снимка и делит одно серверное число на другое. Второй запрос дал бы второй
снимок, и шапка разошлась бы с таблицей под ней.

Что перенесено и откуда:

| Блок прототипа | Живой источник |
| --- | --- |
| Показатели шапки (8 плиток) | суммы колонок `EVENTS`/`REQUESTED`/`ALLOCATED`/`ASSIGNED`/`ACKNOWLEDGED`/`CONFLICTS`/`INCIDENTS` + `lifecycleDistribution` |
| «Структура мероприятий» (донат) | `lifecycleDistribution` — по СОСТОЯНИЯМ, а не по типам |
| «Прохождение этапов подготовки» | `funnel` (6 показателей), режимы «Воронка» и «Таблица» |
| «Обеспеченность личным составом» | цепочка `DEMAND_NEED → REQUESTED → ALLOCATED → NEED → ASSIGNED → ACKNOWLEDGED` |
| «Аналитика объектов» / «Мероприятия на объекте» / «Направления» / «Посты» | `rows` + `columns` уровня, подпись таблицы меняется вместе с уровнем |
| Карточка ОМ | `eventCard.facts`, включая факты с `unavailableReason` |

Решения, которые стоит помнить:

* терминальные состояния (`CONDUCT`, `CLOSED`) названы кодами, а список
  «этапов подготовки» НЕ перечисляется: он принадлежит реестру lifecycle
  сервера, и второй список здесь разъезжался бы с ним при первом же
  изменении реестра. Нет кода в ответе — плитка показывает прочерк с
  причиной, а не ноль;
* конверсия воронки считается от ПЕРВОГО этапа, а не от предыдущего: у
  прототипа столбец называется «Конверсия», и вторая формула под тем же
  словом дала бы другое число;
* донат ниже уровня объекта не рисуется — распределение одного ОМ это его
  состояние, оно уже стоит фактом в карточке;
* разряды форматируются вручную неразрывным пробелом: `toLocaleString`
  зависит от ICU, а он у Node и браузера разный — числа разъезжались бы на
  гидрации. Отметка «данные на ЧЧ:ММ» — только после монтирования (тот же
  дефект чинили на `/dashboard`);
* поиск и сортировка правят уже полученный снимок и обратимы: порядок
  сервера возвращается ТРЕТЬИМ щелчком по заголовку, иначе «как отдал
  сервер» становится недостижимым состоянием.

Чего у прототипа нет источника на этом экране (список продублирован на самом
экране, в блоке «Чего в этой аналитике нет»):

| Блок прототипа | Почему не перенесён |
| --- | --- |
| Период и фильтры (статус, тип, департамент, риск, ответственный) | ручка считает снимок на дату раздела и параметров периода не принимает; фильтровать посчитанное на экране — показать долю под подписью целого |
| Динамика по дням/неделям/месяцам | время перехода журнал хранит, но рядами по датам сервер его не отдаёт; ряд из выгруженного реестра — вторая методика рядом с воронкой |
| Структура по типам ОМ | типа у мероприятия модель не хранит вовсе |
| Участие департаментов | `group` в потребности и запросе сил — свободная строка расчёта («ГР-1»), а не подразделение оргструктуры; назначения расстановки несут пост и сотрудника, но не его подразделение |
| Дефицит по мероприятиям | аналитика отдаёт СУММЫ запрошенного и выделенного, а командный центр считает дефицит по КАЖДОМУ запросу (перевыдача не гасит нехватку соседнего). Разность сумм дала бы другое число под тем же словом |
| Укомплектованность постов по всем ОМ | посты живут внутри мероприятия: своды по постам сервер отдаёт с уровня ОМ и ниже, сводного по всем — нет |
| Столбцы объектов «постов», «сотрудников», «часов», «на 100 часов», «средняя подготовка» | часов у назначений нет вовсе; средний срок подготовки требует плановых сроков этапов |
| Конфликты по видам и журнал ручных разрешений | у мероприятия один счётчик конфликтов, вида и решения он не несёт |
| Хронология ОМ | по отдельному мероприятию переходы приходят сводно (этапы и часы), а не строками с датами и исполнителями |
| Замены после утверждения и их причины | замену расстановки модель не отличает от правки: назначения перезаписываются, истории и поля причины нет |
| План/факт, человеко-часы, нагрузка в ОМ | назначение расстановки — строка без часов; нагрузку по сменам считает аналитика службы по своей политике |
| «Требует внимания» | детекторы §22.11 живут в аналитике службы со своими порогами из «Настроек» |
| Экспорт (PDF/XLSX/изображение) | выгрузки у аналитики ОМ нет; служебные отчёты собираются на своём экране |

Четыре показателя (`OVERDUE_ACTIONS`, `CANCELLED_COUNT`, `OPEN_INCIDENTS`,
`ACTUAL_PARTICIPATION`) называет сам сервер в `unavailableMeasures` — экран
печатает их ЕГО словами, а не пересказом, и проба сверяет это построчно.

Проба: `e2e/operations-analytics.spec.ts` (4 сценария, живой стенд). Красные
пробы: константа вместо «в подготовке», константа вместо конверсии, потеря
порядка сервера на третьем щелчке, отказ по незагруженным правам — каждая
роняет свой сценарий.

#### Порядок колонки объявлен `aria-sort`, а проба перестала выводить его из строк

Такт сортировки экран сообщал ТОЛЬКО стрелкой `↓`/`↑` с `aria-hidden`, то есть
программе чтения с экрана отсортированная колонка ничем не отличалась от
прочих. Теперь `<th>` несёт штатный `aria-sort` (`none`/`descending`/
`ascending`), и проба пинит именно его.

Это не косметика, а починка самой пробы. Прежняя проверяла такт по ПЕРВОЙ
СТРОКЕ таблицы, а строчная проверка между щелчками совпадает и на промежуточном
кадре: одиночное падение 17.08 показало три щелчка при итоговом состоянии
«убывание» — то есть один щелчок доехал дважды, и прежняя формулировка это
проглотила бы, случись оно в другом месте цикла. Красная проба: `aria-sort`
прибит к `none` — сценарий падает на первом же такте.

Механику при этом проверил отдельным прогоном: 12 кругов «щелчок → снимок
указателя и первой строки» — все 12 дают `none → ↓ → ↑ → none`. Дефекта в
трёхтактной сортировке нет.

#### Открытое наблюдение: одиночный кадр с чужим снимком

В первом же прогоне 17.08 плитка «В подготовке» показала `0` там, где сервер в
том же прогоне отдал `63`. Число не выдумано: `0` — законное значение
«в подготовке» для СУЖЕННОГО снимка (`level=OBJECT&object_id=2` даёт ровно его),
и на экране без параметров ему взяться неоткуда. Похоже на кадр с чужим
снимком, но воспроизвести не удалось: 6 холодных прогонов (с пересборкой
маршрута), 8 повторов сценария подряд, 60 кадров с шагом 40 мс от навигации до
успокоения — везде `63`, промежуточного нуля нет ни на одном кадре.

Оставляю как открытое наблюдение, а не как «починено»: гасить его ретраем
значило бы прикрыть ровно тот кадр, ради которого проба и написана. Если
повторится — смотреть в сторону ключа кэша `useOperationsAnalytics` (уровень и
все четыре id входят в него) и порядка монтирования при медленной первой
отрисовке.

Ловушка на будущее: фикстуру для проб порядка надо искать по НАИБОЛЬШЕМУ
объекту, а не по первой строке снимка — первая строка стенда это объект с
одним мероприятием, и проба порядка на нём вырождается.
